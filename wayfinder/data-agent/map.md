# wayfinder:map — deepseek-harness-data-agent

> 本地 markdown tracker（wayfinder skill 默认；未显式提供 GitHub issue tracker）。子 ticket 在 `tickets/`，研究笔记在 `research/`。本 map 是**索引**，非存储——决策详情在其 ticket / 研究笔记。

## Destination

把 `deepseek-harness-da`（`deepseek-ai/deepseek-harness` 的 fork——插件化 agent harness on vendored Cordis）改造成 **deepseek-harness-data-agent**（一个 data agent；正式名待定）。以 `reverse-bi`（上游 `track2data`，AI 原生游戏取数平台）为能力源，**通过插件化、additive-only、逐步迁移**其核心数据能力（四阶段 pipeline + retrieval + query + guard + eval + 语义层）到 harness 上；筛除 code-agent 特性（disable-only 保上游升级路径）；新增生产需求（DashScope + Qoder LLM 接入、内网穿透 + per-game 访问隔离）。reverse-bi 为只读源、重新实现不改。

## Notes

- **域**：agent harness → data agent（NL→SQL/取数）改造；reverse-bi 为能力源。
- **每会话应查 skills**：`dsh-plugin-development`（插件开发模型）、`grilling` + `domain-modeling`（决策）、`research`（调研）、`prototype`（原型）。
- **常设原则**：
  - **additive-only**：da 改动只叠加（preset overlay + data 插件 + persona），不改/不删 core → 保上游升级路径（core 不动 = Q4）。(c) npm-消费纯产品仓库留作后续低风险选项。
  - **reverse-bi 只读源**：重新实现，不修改 reverse-bi。
  - **intranet-security-first**：内网穿透暴露面 = 安全加固设计；信任边界单一在 RBI 门；业务用户问题不得触达 bash 等禁止命令（工具门禁）。
  - **PAT auth**：Qoder 集成走 Qoder 自身账号 PAT（`QODER_PERSONAL_ACCESS_TOKEN`，经 `credentials` seam 存 `~/.dsh/.credentials.yaml` file 层、doc 0600、**不**进 process.env——intranet-security-first；P3 用 `ctx.credentials.resolve` + SDK `accessToken(value)`，非 `accessTokenFromEnv()`）。〔T1 resolved 2026-08-19〕
  - **语义层一等公民**：NL2SQL 成败在语义层（MDL/指标层/Text2DSL），作 data-agent 插件内一等公民。
- **研究笔记**（`research/`，11 篇）：reverse-bi purpose/capability/data-behaviors；harness plugin-model/package-removal/agent-loop；synthesis；access-isolation；vectorization；frontier-fork-precedent；qoder-sdk-ts；qoder-model-migration。

## Decisions so far

<!-- 一行一 closed 决策的 gist；详情在 research/ 笔记或后续 ticket -->

- **拓扑 (Q4)**：选择性 fork、additive-only——da 即 data agent（名待定），保留 git fork，da 只叠加不改 core；(c) npm-消费纯产品仓库留作后续低风险选项。〔research/frontier-fork-precedent.md〕
- **去除 (Q2 修正)**：disable-only——code-agent 在 data-agent preset 不挂载（不物理删，保升级路径）。
- **迁移范围 (Q3)**：核心能力集（四阶段 pipeline + retrieval + query + guard + eval）；裁 flywheels/accel/frontend/超结构；查数优先。
- **ODPS 解耦 (Q5)**：引擎可插拔（`QueryEngine` 协议 + 每引擎 `conventions.yaml`；MaxCompute 为首引擎）。
- **访问隔离**：方案 1（复用 RBI `scope_id` + 每作用域凭证，admin 作 harness app）+ 2 约束（门覆盖 `X-RBI-Scope`；不退休 override）+ intranet-security-first。〔research/access-isolation-options.md〕
- **rbi-agent core/ (②)**：退役基础设施（用 harness agent-loop/session/mcp-client/llm），保留 per-turn 隔离纪律（`ToolResultCache`/`TurnBudget`/`tool_health`）为插件；reverse-bi 只读源。
- **rbi-agent data_agent/ (③)**：移植为一份 preset（四阶段全部工具/persona/段 + 压缩，保目录稳定）+ phase-gate 插件（`guard` + `turn-stopping` + `post-execute` + `request` waterfall）；不自定义 agent-loop、不坍缩阶段。〔research/harness-agent-loop.md〕
- **per-phase 门控 (Q7)**：harness 无原生支持 → 必须加 phase-gate hook。〔research/harness-agent-loop.md〕
- **LLM 接入 (P0)**：`llm-dashscope`（DashScope 百炼 OpenAI 兼容，da 直接 LLM）+ `subagent-qoder`（Qoder 作 harness subagent 插件，`query()` 委派，`SDKMessage`→harness 流式适配保 tool/reasoning，PAT auth，`resolveModel`/BYOK 控制 Qoder 用哪个模型）；"用 Qoder 内置模型当主 LLM"无干净路径。〔research/qoder-sdk-ts.md, qoder-model-migration.md〕
- **rbi-mcp 轻量工具 (④)**：进程内置为 harness tool 插件（`defineTool`）。
- **rbi-mcp 查询引擎 (⑤a)**：混合——`ctx.query` seam + `tool-query` + Guard 进程内（da 掌控可插拔点），MaxCompute Provider 外置 sidecar（rbi-mcp），保 ADR-0028 D3、ODPS 可插拔。
- **rbi-mcp 检索/向量化 (⑤b) (P5 prototype, resolved 2026-08-20)**：mirror rbi-retrieval/semantic——`ctx.embedder`+`ctx.retrieval` 两 seam（hybrid BM25+vec+RRF(k=60) 内部组合、reranker peer-protocol RRF 后注入非顶层 seam）；**D2 (c) guided agentic hybrid**（pipeline-internal 默认 + additive retrieve-tool escape-hatch，keep/regress evals 驱动）；默认 `FakeHashEmbedder`（零依赖 stub）+ 重模型外置 OpenAI 兼容插件（`InfinityEmbedder`）+ in-proc bge-m3 可选 + `InferenceError`→BM25-only 降级；**TS-aware 默认纯 JS in-mem cosine**（zero-dep）、sqlite-vec（better-sqlite3 loadable ext, npm 0.1.9）升级档、Qdrant/Milvus 外置；包 `packages/{embedder,retrieval}/...` 镜像 P4/credentials。〔research/vector-rbi-mirror.md, vector-embedder-intranet.md, retrieval-consumer-model.md, vectorization-frontier.md, vector-seam-structure.md, vector-backend-ts.md〕 prototypes/p5-retrieval-vectorization/
- **rbi-mcp loaders/语义层 (⑤c)**：语义层（埋点 + 表）进程内核心能力；ODPS schema 读取解耦到查询引擎（⑤a）。
- **rbi-mcp audit (⑤d)**：进程内——audit 作 guard/session-event + `tool-audit` + `ctx.storage`（SQLite）。
- **rbi-mcp admin (⑤e)**：进程内 harness app（管理 per-game scope/credential/access-link + 系统配置）。
- **goal/todo/plan (Q8)**：保留（不禁用），四阶段 Pipeline 作默认编排；后期实验对比 Pipeline vs goal/todo/plan（不同模型可能不同搭配）——对比作 ticket。
- **变换执行 (Q9)**：code-runtime 跑 pandas 变换 + bash 跑 shell；内网穿透暴露面加工具门禁（业务用户不得触达 bash）。
- **python/ (Q10)**：前期保留（additive），按 Python 消费者需求后 disable-only 裁。
- **tracker**：本地 markdown（`.agents/wayfinder/`）。
- **MaxCompute 凭证缓存 (R2 research, resolved)**：目标态正经接 tier-0（`install_credential_resolver` 已接线 + `TestAcceptanceGate` 验证）；override 过渡保险不删（退休判据=生产验收绿；2026-08-05 五天停服红线）；不新建第二份 override-factory；query-maxcompute sidecar 设计（per-call `ctx.credentials.resolve` + `scope_id` 显式 + 凭证经 stdio env + sidecar 自有 per-scope缓存 + 监听 `credentials/updated`）。〔research/r2-maxcompute-cred-cache.md〕
- **多轮 eval hook (R3 research, resolved)**：harness 暴露 response hook 但响应正文在 session 事件流（非 agent 事件层）；主路径 Python JSON-RPC SDK `Session.run()` 包 `AgentResponder` 复用 rbi-eval；多轮同 Session 多次 run()；pass_k k 独立 session_id；确定性 `dsh-llm-replay`；agentic 判分用 `RunResult.events` tool/call+tool/result。〔research/r3-multiturn-eval-hook.md〕
  - ⚠️ **R3 的 Python 偏好已被 G2 更正（2026-08-20）**：R3 漏看 `packages/sdk/client`（TS 规范客户端，`run()`→`RunResult{finalResponse,events}` 带 tool/call+tool/result 全轨迹），误以为 TS 侧只有 ACP（剥 tool）→ "agentic 判分需 Python" 不成立；eval 落 TS。正源〔research/g2-eval-ts-review.md, tickets/phase-4/G2-eval-ts-vs-python.md〕。
- **DashScope LLM seam (R1 research, resolved)**：`llm-dashscope` 可干净镜像 `llm-deepseek`——百炼 OpenAI 兼容端点 wire 与 DeepSeek 同构，流解析层（sse.ts/translate.ts）原样可复用，仅序列化+身份层改（`enable_thinking`/`thinking_budget`/`tool_stream`/baseURL/env `DASHSCOPE_API_KEY`/provider dashscope/catalog qwen）。〔research/r1-dashscope-seam.md〕
- **query-engine trio (P4 prototype, resolved)**：A1-split——`ctx.query.execute` owns engine-wrapper 门（cost/timeout/retry/orphan，镜像 `pipeline.py:run_query_async`+`core/guards/*`），会话门（G1/G5/budget/near-dup/halt/cache）留 `tool-query`（镜像 `execution.py`），sidecar dumb raw executor+per-scope 缓存；C1 tool-query 吃 SQL（NL→SQL 归 P6）；B seam=`execute/attach/cancel/get_progress`+3-state、`estimate_cost` CostGuard 内部、不暴露 `getEngine`；D `packages/query/{query,query-maxcompute,query-tool}`；E `credentials/updated→invalidate_scope`（reconnect 兜底）；F2 sidecar 经 mcp-client（spawn-env 注入）；prototype surface 一条 tension（spawn-env cred 热更与 invalidate 不相容，待定）。〔research/p4-guard-chain-placement.md, p4-build-defaults.md, prototypes/p4-query-engine/〕
  - ⚠️ **R1 结论已被 P2 live 探针证伪（2026-08-19）**：实为 native AGA 原生协议（非 OpenAI 兼容镜像），sse/translate **不可**复用（无 `[DONE]`、incremental_output delta、payload `output.choices[].message`），无 thinking_budget/tool_stream/include_usage，requestId 在错误体非头。正源〔research/p2-dashscope-wire.md〕。
- **T1 Qoder PAT (task, resolved 2026-08-19)**：Qoder PAT 存 `~/.dsh/.credentials.yaml` 为 `QODER_PERSONAL_ACCESS_TOKEN`（seam file 层、doc 0600；**不**入 `.env`/process.env——intranet-security-first，PAT 不被 bash 等工具子进程继承）。P3 须 `ctx.credentials.resolve(credentialRef('QODER_PERSONAL_ACCESS_TOKEN'))` 每操作解析 + Qoder SDK `accessToken(value)` 传值（**不**用 `accessTokenFromEnv()`）；前提账号有 Credits。〔tickets/phase-1/T1-qoder-pat.md〕
- **dsh-data-agent 脚手架 (P1 resolved)**：patch-only bundle 叠 base——disable code-agent 面（tool-str-replace-editor/ralph + `tools.mode:native`）；data 插件行注释 TODO（P4-P11 填）；persona 留 P7；bash/code-runtime 保留（Q9，gating 归 P10）；新建 packages/data group 壳（tsconfig.base wildcard + host ref）；不碰 app-boot/core，out-of-tree `dsh plugin` 建可运行 profile。〔tickets/phase-0/P1-data-agent-scaffold.md, packages/bundle/data-agent/〕
- **G3 per-user Qoder PAT provisioning (grilling, resolved 2026-08-19)**：业务用户自带 per-user Qoder PAT（per-individual-user，非 per-scope）。存 credentials seam 的 deferred keychain/KMS provider（agent at-rest 不可读、自助填、跨浏览器）；Qoder 侧强制权限+Credits；身份=web UI per-user 登录（复用 RBI `Tenant`）+P10 mTLS；fallback 分期（早期 T1 全局个人 PAT、稳定 per-user 必填）；P3 caller-parameterized `resolve(ref,{userId})`+fallback 层为留接口；per-user Qoder 用量进 P8 审计。per-user 存储基建=P12（keychain provider+per-user 寻址，per-user 维度与 R2 per-scope 正交）。〔tickets/phase-1/G3-per-user-qoder-pat.md〕
- **llm-dashscope (P2 prototype, resolved 2026-08-19)**：native AGA 原生协议 adapter（阿里内网 AGA 网关，非 R1 的公网 OpenAI 兼容）；6 live 探针兑现 wire（无 [DONE]、incremental_output delta、tools 在 parameters.tools、思考靠选模型、requestId 在错误体、usage 原生字段）；sse/translate/serialize/types 从零写（不复制 llm-deepseek）；64/64 单元 spec + host typecheck + live e2e 4/4（real AGA）全绿；R1 论线作废。〔research/p2-dashscope-wire.md, tickets/phase-1/P2-llm-dashscope.md〕
- **credentials keychain + per-user 寻址 (P12 prototype, resolved 2026-08-19)**：grill 定向部署事实=前期单机（用户 Mac）→ 后端收窄 macOS Keychain（`security` CLI spawn，非 keytar——免 native 构建污染构建链）；红线拆清：P12/G3 字面要 at-rest 不可读（PAT 入加密 keychain 不再可 grep），runtime-exfil（agent 跑 `security find-generic-password -w`）+ 多 host KMS/Vault + 真实包/branding 延后 P12b。seam 加可选 `address?: {userId?, scopeId?}` 到四方法 + `notifyUpdated` + `credentials/updated(ref, address?)`，**条件 arity**（全局保单参 + 2 元 args 零涟漪、per-user 才扩参）保后向兼容（LocalCredentialProvider 少参 override 仍合法=flat/全局 G3 fallback 落点）；即 credentials-local README 预告的 "richer addressing" 门。prototype `prototypes/p12-credentials-keychain/` live 兑现：per-user CRUD+alice⊥bob⊥global 隔离+G3 分期 fallback+address 事件+at-rest（keychain DB grep PAT absent）。解锁 G3 per-user 切片（P3 caller-param `resolve(ref,{userId})`、P9 自助 `set(ref,value,{userId})`）。〔tickets/phase-2/P12-credentials-keychain.md, prototypes/p12-credentials-keychain/〕

- **凭证热更 (R6 research, resolved 2026-08-19)**：cred 变更不重启 sidecar 即生效——选 **(b) per-call `set_credentials` + P1**（da 自持 raw SDK `Client`+stdio transport 连 query sidecar，**不用 mcp-client plugin**，sidecar 工具非 model-facing per A1-split → 控制信道缺口消解、additive-only）；E 精炼（cred→`set_credentials` 推新值+丢连接 / 非-cred 配置→`invalidate_scope` / reconnect→崩溃兜底）；R2 §5.2c spawn-env 对 cred 热更证伪（per-call resolve 硬规则保留；creds 不进 spawn-env 反贴合 PAT not in process.env）。(a)/(c) 不取（前者丢所有 scope + 需 core restart API；后者 plugin 构造固定 + HTTP 攻击面 contra intranet-security-first）。〔research/r6-cred-hot-reload.md, tickets/phase-2/R6-cred-hot-reload.md〕
  - ⚠️ **adversarial 复核**（2026-08-19，`research/r6-cred-hot-reload-review.md`）：7 claim 全 VERIFIED、(b)+P1 成立（`Protocol.request()` `shared/protocol.js:611` 纯 JSON-RPC send、非 `tools/list`-gated 实证 P1 支点）；surface HOLE-A（initialize 握手）/B（P1 跳过 reconnect=可靠性债，最重）/C（在途原子性）/D（notification）——P1 vs P2 可靠性权衡 + 崩溃恢复形态开 **G4 grilling 票**定夺。
- **query sidecar 控制信道+可靠性 (G4 grilling, resolved 2026-08-20)**：P1（da 自持 raw SDK `Client`+stdio transport，新 `packages/query/query-maxcompute/` 不碰 core；按 raw name 程序化调全部 sidecar 工具无一进 `ctx.tools`→无 model-facing 注册→控制信道缺口消解、`set_credentials`/`invalidate_scope` 天然非 model-callable）+ (ii) lazy on-next-call re-spawn（`connectingPromise` 单 owner 锁防 double-spawn；**在途 query 崩溃后 SDK `_onclose` reject ConnectionClosed 不 hang**；首条 query ~1.5-3.5s 延迟对 da 用户发起非连续模式可接受；~40-70 行）+ HOLE-A 强制 `await client.connect(transport)`（SDK 自动 initialize+协议校验+`notifications/initialized`、失败 `close()`+抛；初始启动失败 fail-fast、lazy re-spawn 失败 crash-loop bounded）+ HOLE-C drop（`set_credentials`=推新凭据+丢该 scope 连接缓存，镜像 reverse-bi `invalidate_credential` 纯 pop、在途持旧跑到完下次取新、弃「原子」、cancel 留显式用户工具；leaked-cred 轮换在途继续用泄露凭据至完成=已接受）+ HOLE-D no-op polling（inbound handler 留 no-op，sidecar 零 notification per P4；outbound cancel 经 `request()` 的 `signal` 内建、TimeoutGuard 传 signal 即可 ~0 行；progress 走 `get_progress` 轮询；YAGNI sidecar 改 progress 推送届时加 `onprogress`）。P2（`callTool`+allowlist core 改 ~60-100 行 3 处）倒置不取；mcp-client reconnect 6 能力对 da 非连续 query 过建（仅 2 项高价值简化入 Q1）。query-maxcompute=Service Provider（fiber-scoped），HMR/dispose→`client.close()`→在途 reject+sidecar kill（ODPS 孤儿归 OrphanReaper A1-split 门）。**解锁 P4b 生产硬化**。〔research/g4-sidecar-reliability.md, tickets/phase-2/G4-query-sidecar-control-reliability.md〕

- **query-maxcompute 生产硬化 (P4b prototype, resolved 2026-08-20)**：G4 P1 落地为真 `packages/query/{query,query-maxcompute}/`——`query`（极简 Service Definition：abstract `QueryEngine extends Service` + `ctx.query` + 4 件 execute/attach/cancel/getProgress + 3-state `QueryOutcome`；`estimate_cost` CostGuard 内部不上 seam）+ `query-maxcompute`（Provider，da 自持 raw SDK `Client`+`StdioClientTransport`+`[Service.init]` eager connect+`ensureConnected`/`connectingPromise` 单 owner 锁 lazy re-spawn+crash-loop 计数器+`onclose=dead`/`onerror=log`+raw-name `client.request('tools/call')` 调全 sidecar 工具**无一进 ctx.tools**→控制信道缺口消解、set_credentials/invalidate_scope 天然非 model-callable+per-call 幂等 `set_credentials`(drop HOLE-C)+outbound cancel via `signal`(HOLE-D no-op)+dispose `client.close`+kill+orphan-deferred；spawn-env `scrubbedParentEnv()` 不带 cred → PAT not in process.env）。stand-in node sidecar 手搓 MCP+fake ODPS（真 pyodps 延后）；4/4 scenario 全绿（cred 热更/崩溃恢复+dispose/cancel/控制工具非 model-callable）证 P1 接线非真 ODPS。query-trio 剩余生产（tool-query+engine-wrapper guard chain）见 Not-yet-specified。〔packages/query/{query,query-maxcompute}/, tickets/phase-2/P4b-query-maxcompute-hardening.md〕

- **semantic-layer substrate (P6 prototype, resolved 2026-08-19)**：substrate 一等公民——TS **zod 镜像 RBI pydantic**（`EventDefinition`(埋点)/`TableDefinition`(表) + `extra=allow`→`.passthrough`、`model_validator`→`.refine`/`.superRefine`、`canonicalize_type`→`.transform`、`Literal`→enum）+ 逐字镜像 RBI YAML 格式 → 与 RBI 531 curated 表/事件/terminology **交叉兼容**；per-scope YAML + atomic write + ADR-0011 cache-invalidate + **BasicIndex**(dep-free 查找) + terminology + accumulated_definitions。ODPS schema 解耦到 query-engine 经 sibling **`ctx.schema` seam**（`discover`/`describe`/`sample`，MaxCompute sidecar 同 `ctx.query.execute` 共实现；`probe_pk_uniqueness` 走 `ctx.query.execute`；P4 `ctx.query` 保窄）——镜像 RBI 独立 `DataSourceConnector` 抽象（`odps_metadata_service` 已 source-agnostic）。**write-tiers**：Tier-1 suggest→pending→approve 守事实来源（agent 自修改回路；approve 侧 P9-gated）+ Tier-2 per-scope 留痕（连 intranet-security-first）。prototype `prototypes/p6-semantic-layer/` 4 scenario 全绿（真实 RBI YAML round-trip deep-equal + DIM `.superRefine` 拒 malformed + sync 不触 pyodps + suggest→pending→approve + ADR-0011 rebuild）。**deferred**: search_data_sources/sql_evaluator/sql_critic → **P13 NL→SQL 引擎 ticket**（Text2DSL 雾清：RBI 无 canonical Text2DSL IR——rule-based IntentSignature + LLM-inline-SQL + 自报 dsl_json，本质 prompt+feedback+guards；research 证完整引擎 (C) 单期不可行：RBI 自身 L1 ~9%、`plan_query` LATENT、sqlglot 无 MaxCompute 方言 RBI 用 hive 代理）。〔research/p6-nl2sql-feasibility.md, prototypes/p6-semantic-layer/, tickets/phase-2/P6-semantic-layer.md, tickets/phase-3/P13-nl2sql-engine.md〕

- **subagent-qoder (P3 prototype, resolved 2026-08-20)**：terminal-only 外部 one-shot provider（照搬 `subagent-claude-code` 先例）——drain `query()` 取终态 `result`（Qoder `SDKResultMessage` Claude 形，.d.ts 钉死）、tool/reasoning 留 product-local 不进父（外部 one-shot not trace-enumerable）；map 原"保 tool/reasoning"改释为"正确提取终态不被 naive drain 丢"（可见性延后，仅 P8/forensic 确认需要时另开 core seam 变更票）；PAT 经 credentials seam `resolve(credentialRef('QODER_PERSONAL_ACCESS_TOKEN'))`（MVP 无 address→T1 全局）→`accessToken(value)`→`options.auth`（非 `accessTokenFromEnv`）；模型=`options.model` from config（平台，消耗 Credits），`resolveModel`/BYOK 留接口延后；Qoder 默认 `WorkerTransport`（混淆 worker runtime、postinstall 下载、pnpm approve-builds 门忽略——mock 测试不需、live e2e 延后需 approve/`QODERCLI_PATH`），`Query.close()` teardown；`packages/subagent/subagent-qoder/`（additive，镜像 claude-code + 复用 `settleRunResult`/`subprocessRunHandle`），21/21 单元 spec + P3 包 typecheck-clean（host 仅剩 pre-existing P12 `credentials.spec.ts:77` exactOptional 错）；@qoder-ai 作 peerDep+devDep（repo 不 bundle 非 permissive obfuscated worker，绕开 `gen-third-party-notices` supply-chain 门——作 devDep 披露为 not-distributed；opt-in 部署自装 @qoder-ai + 自负 supply-chain）。〔research/qoder-sdk-dts.md, tickets/phase-1/P3-subagent-qoder.md, packages/subagent/subagent-qoder/〕
- **audit (P8 prototype, resolved 2026-08-20)**：RBI 忠实精简——3 面持久化（tool-audit 全 tool 调用 tagged + session-event + guard 决策 + Tier-2 写）+ 关系型 own-`node:sqlite`（`audit_event` 不可变 + `audit_override` append-only 点路径 + `audit_tag` junction + WAL + 索引 tenant/scope/user/ts）+ `ctx.audit` service；**不含** ADR-0005 分类/flywheel（map Out-of-scope；分类是 query-round trust 非 per-user 合规）。G3 per-user Qoder feed（tag=`qoder_call` + Credits `total_cost_usd`/`total_credits?`/`usage`/`modelUsage` from `SDKResultSuccess`，`research/qoder-sdk-dts.md`；**P3 已落地 2026-08-20** → 生产包 hook 真实 subagent-qoder tool post-execute，P8 审 call outcome 非 internal stream——P3 标的 core-seam 变更票仅当 forensic 需 stream 才开）。单 DB + `user_id`/`scope_id` first-class 列 + 所有权 guard（RBI P1-2 IDOR fix 扩 `user_id`；privileged 旁路=P9 admin）。**map ⑤d「ctx.storage」澄清**=`ctx.audit`：ctx.storage 实 KV-only 无索引/跨表（`storage-domain` 印证），RBI 关系型 3 表须 own sqlite（sibling ctx seam，additive declare-merge 不改 core）。prototype `prototypes/p8-audit/` 6 场景全绿（zod 镜像 + 3 表 + IDOR-safe null + Credits 捕获 + Tier-2 hash-not-body + cross-scope per-user 索引查询 + override immutability）；surfaced 2 生产包张力（override-of-identity / override-vs-聚合）→ **P8b**（blocked by P9）。〔research/p8-audit-scope.md, prototypes/p8-audit/, tickets/phase-2/P8-audit.md〕
- **four-phase preset + phase-gate (P7 prototype, resolved 2026-08-20)**：rbi `DataAgentPipeline`（`pipeline.py` 四阶段 phase-gated）重新表达到 harness seam——additive preset overlay + phase-gate 插件（**不自定义 agent-loop、不坍缩阶段** map ③③）。8 决策（grilling）：persona option C（base 留 preset `section()` + `_PHASE_INSTRUCTIONS` 由 phase-gate 在 `system-prompt/assemble` 按 `current_phase` 动态注入，避免 `complete:true`）；goal/todo/plan **不挂** planning group 但包不禁用（honors Q8「保留不禁用」=不 disable 包；G1 另开含 planning group 的 preset 做实验对比）；phase 转换=`agent/turn-stopping` serial（**非** model-exit——rbi `sql_syntax_gate` 模型不知 gate 结果，model-exit 是语义位移）；gate 落地=GENERATION `sql_syntax_gate` 在 turn-stopping 查 **phase 最终文本**（非 post-execute 单工具）+ EXECUTION 由 `ctx.query.execute` 3-state `QueryOutcome` 直接驱动（done→advance/failed→fallback GENERATION）+ fallback/delivery 在 `tools/post-execute`；budgets 采用 rbi `PipelineConfig` 初始默认（8/60/20/2/4 等，R8 重定标；llm 计 `llm/stream`/exec 计 `tools/post-execute`/fallback 计 `turn-stopping`）；model 全 dashscope + per-phase reasoning effort（`agent/request`），Qoder 作可选委派非主 LLM（map out-of-scope）；stub-isolate 聚焦编排验证。harness 无原生 per-phase 白名单（Q7）→ `ctx.tools.guard()` 硬门控保工具目录稳定（cache 友好）。prototype `prototypes/p7-four-phase-preset/` 8 场景全绿（推进/guard 拒越界/ctx.query 3-state fallback/budget cancel/sql_syntax_gate retry/persona option C/honest_decline/forced_load）；surfaced 6 finding（→P7b：forced_load 程序化 `ctx.tools.execute` 路径/SQL 同源/stall watchdog 独立 timer/question-scoped 计数器须 question-start seam 非 turn-start/llm 计费点/step max_steps per-step hook）。生产（`packages/phase-gate/` TS + `agent.cordis.yml` + 解注释 bundle）→ **P7b**（blocked by P13——GENERATION critic 接线）；G1（Pipeline vs goal/todo/plan）随 P7 解锁。〔research/p7-four-phase-fit-to-da.md, prototypes/p7-four-phase-preset/, tickets/phase-3/P7-four-phase-preset.md〕

- **admin+访问隔离 (P9 prototype, resolved 2026-08-20)**：单一 additive 插件 `@deepseek-ai/dsh-admin`（gate+admin+登录共享 `ctx.storageDomain` store + `Service`/`ctx.effect` 生命周期，100% additive 不碰 core，data-agent bundle `id:admin` 占位已预留 + storage 家族 3 行 insert；不拆双 app——共享 domain handle/登录态/路由表契约）+ scope **服务端解析非客户端可供给**（「门覆盖 `X-RBI-Scope`」stdio 重解：`AccessLink.linkToken→scopeId`，da per-call 程序参传 sidecar，无 header 可覆盖，比 RBI header-override 更强、单信任边界，丢 RBI HTTP-gate 特有 header/`is_http_request`/ASGI/共享 Bearer）+ 忠实重实现 RBI 模型（`Tenant`/`ScopeRecord`(=game, migration#35)/`OdpsConfig`**单例**共享 access_id/key+region 选 project/endpoint〔research note §1.2 erratum〕）+ net-new `AccessLink`(token→scope 颁发/吊销/TTL fail-closed) + `SystemConfig` 单例 + fail-closed authz(`scope_allowed_for_tenant`/`can_access`) + per-user PAT 自助经 P12 keychain(`{userId}`⊥`{scopeId}`, admin 不经手) + per-user 登录 net-new(bcrypt+session)；信任边界单一在门、TLS 门前隧道(P10)终结。prototype `prototypes/p9-admin-access-isolation/` 25/25 全绿。解锁 P10 门形态 + P4b per-scope 凭证寻址(i)/(ii)（P9 荐 (i)）；毕业 Not-yet-specified「per-user 登录+端点→user→scope 绑定」。〔research/access-isolation-options.md, prototypes/p9-admin-access-isolation/, tickets/phase-2/P9-admin-access-isolation.md〕
- **audit 生产包 (P8b prototype, resolved 2026-08-20)**：P8 audit 生产态——真 `packages/data/audit/`（`Audit extends Service` ctx.audit + own `node:sqlite` 3 表 audit_event/audit_override/audit_tag，WAL+STRICT+ownership guard，替代 prototype harness-stub）+ 真实 Cordis `ctx.on('tools/post-execute' | 'session/event')`（observe-only；签名纠 prototype 假设——无 decision 参、deny=isError、post-execute 对 denied 也触发）；①a verdict-only patch（拒身份字段 throw + appendCorrection append 新行+attribution_correction tag）+ ②c stats immutable + correctedStats O(n) 重聚合；**N1 Credits 捕获经 additive P3 改**（SubagentResult +costs? + consumeQoderQuery close 前捕 SDKResultSuccess cost + tool-subagent result.value.costs surface——P3 原本 discard cost 字段 + Query.close 在 post-execute 前使 fallback fetch 不可行）；userId null T1 fallback（P9 未建；session_id 从 agent.session.id）；guard_deny 经 explicit record()（post-execute 无 decision 参不能自动区分，P10 hook）；data-agent bundle cordis.patch.yml audit insert uncomment + dep 声明。typecheck-clean + audit 11/11 spec + P3 26/26+117/117。〔packages/data/audit/, tickets/phase-2/P8b-audit-prod-hardening.md, P3 additive 跨 subagent/subagent-qoder/tool-subagent〕
- **内网穿透安全 (P10 prototype, resolved 2026-08-20)**：前期单 host + Mac 内网直接可达 → **Caddy 反代+mTLS（无隧道）**，非 frp/chisel（前期 ruled out；跨网/多 host 留 additive 叠加 chisel overlay 或 P12b central backend）；Caddy `tls{client_auth{mode require_and_verify;trusted_ca_cert_file}}` TLS 握手层验客户端证书→转发 webserver loopback、**不**转发 cert PEM（gate 身份独立于 transport）；**mTLS=transport-only**（org/device 客户端证书、单一内部 CA、短命 7d+rotation=吊销靠过期、非 per-user cert——那属 P12b）+ **信任边界单一在 P9 gate**（per-user authn+scope authz+identity 标记）；**工具门禁 defense-in-depth**（P9 gate 标 `identity.kind`→P7 preset 按身份套受限 allowlist：business-user 禁 bash/code-runtime-shell/file-write/str-replace-editor/AskUserQuestion 委派；gate `tools/pre-execute` hook 兜底——resolve briefing deferred disallowedTools 为 identity-scoped）；PAT 服务端 resolve 不经 transport（G3）；token 轮换/吊销=P9 AccessLink(rotatedFrom/TTL/fail-closed)+mTLS cert 短命+rotation+PAT 重粘。prototype `prototypes/p10-intranet-tunneling/` 26/26 全绿（trust-boundary+工具门禁决策流 8 scenario）。**解锁 P12b**（拓扑=前期 single-host、多 host=P12b 生产 scope，依赖解除）；毕业雾「内网穿透技术选型」→新雾「跨网/多 host 部署形态」。〔tickets/phase-2/P10-intranet-tunneling.md〕

- **credentials keychain 生产硬化 (P12b prototype, resolved 2026-08-20)**：6 决策（grilling 推荐→确认）——runtime-exfil ACL **DEFER 到 P12c**（`research/p12b-keychain-acl-feasibility.md` cited TN3137/TN3133/kSecClassGenericPassword/SecAccessControlCreateFlags/forum 691188 + 实测双向确认 security-CLI-only 不可达：accessor=`/usr/bin/security` Apple 签名、harness 与 bash 经同 CLI 子进程读不可区分、`security` CLI 无 per-item Touch-ID/`SecAccessControl`、identity-ACL 需 Developer-ID 签名 harness binary，当前 harness 是 tsx 脚本无 binary 可签）；**LAND** 真实 `packages/credentials/credentials-keychain/`（per-user CRUD `service=ref,account=userId` + G3 staged fallback 注入 + locked-keychain/auto-lock/teardown-lock + injectable `SecurityRunner`/`KeychainFallback` + `securityCli` v8-ignored + production 错误区分 miss vs fault + live macOS e2e 21/21 + per-file 100% 覆盖；`invariant.ts` no-op companion 镜像 credentials-local）+ **branding in seam**（`packages/credentials/credentials/src/brand.ts`：`UserId`/`ScopeId` `Branded<>` + lowercase factory `userId()`/`scopeId()` 镜像 seam 自身 `credentialRef`，`types.ts` `CredentialAddress` branded 兑现 P12 延后 + AGENTS「cross-boundary id branded」，additive seam-内无 tsconfig.base 改）；locked-keychain=at-rest+when-locked 收窄 runtime-exfil 到解锁窗口（未消除，诚实文档；`unlockPassword`=新 secret-to-protect）；**DEFER** 多 host central backend（P10 single-host，YAGNI）+ **SPLIT** cert-CRL（transport 层正交，入 Not-yet-specified fog）。解锁 G3 stable per-user PAT 必填阶段。〔tickets/phase-2/P12b-credentials-keychain-hardening.md, packages/credentials/credentials-keychain/, research/p12b-keychain-acl-feasibility.md〕

- **intranet 重 embedder 部署 (T2 task, resolved 2026-08-20)**：AGA-embeddings NO——live-probe 6 探针实证（预发 AGA 网关，key 经 credentials seam 文件 `~/.dsh/.credentials.yaml` 不入库/不进 env/不打印）：`GET /api/v1/models` 200（10 chat 模型、无 `text-embedding-*`）+ chat pong 200（native generation 控制证 key/AGA 可达、非鉴权）+ 4 端点变体全 404（native `/api/v1/services/embeddings/text-embedding/text-embedding` v3/v4 + OpenAI 兼容 `/v1/embeddings` + compat-mode `/compatible-mode/v1/embeddings`；体空+无 request_id+~50-61ms=网关级路由未找到，非 DashScope 服务级错误→AGA 选择性 relay chat generation、不含 embeddings）；**佐证 P13 Q2**「AGA 不提供向量模型已确认」（推断→live 实证）。→ intranet 重 embedder = **独立推理 sidecar 非 AGA**，落 P5 `ctx.embedder` 外置档 `InfinityEmbedder`（OpenAI 兼容 `POST /v1/embeddings {model,input}`→`data[].{embedding,index}`，跑 bge-m3/Qwen3-Embedding）；serving 框架/模型（Infinity/TEI/Ollama + bge-m3/Qwen3）= 用户部署 ops、map 不裁定；`InferenceError`→BM25-only 降级（rbi/P5 既定梯）；seam 契约不变、P13 引擎逻辑（首期 BM25-only）不变。省 sidecar 指望落空（P5 fog yes 分支证伪）。〔research/t2-aga-embeddings-probe.md, tickets/phase-2/T2-aga-embeddings-live-probe.md, research/p2-dashscope-wire.md〕

- **eval 迁 TS (G2 grilling, resolved 2026-08-20)**：TS `packages/eval/` 重实现 rbi-eval 编排设计（非代码），包 TS SDK `DeepSeekHarness.run()`（`dsh-sdk-client`，"owns the client contract"）作 `AgentResponder`；判分 (ii) DELIVERY+EXECUTION（答案比对+取数结果集比对经 ODPS）不用 sqlglot/SQL-form（数据等价优先）——**更正 R3**：R3 偏 Python 基于不完整 TS 画像，`packages/sdk/client` 是规范客户端、`run()`→`RunResult{finalResponse,events}` events 带 tool/call+tool/result 全轨迹 → agentic+DELIVERY 判分 TS 完全可行；python/ 包不修订 Q10（= 可选客户端孪生非 pandas 驱动；pandas/numpy/ML 永久性属 Python 语言 runtime=Q9 域，code-runtime 当前 TS-only、Python backend 未发布=Q9 gap）；P11 用 da-fresh EvalCase（非 P6 zod-mirror，BI 专属）；无 mid-turn cancel→Promise.race 超时；丢 SQL 卫生断言=已知 trade-off。对抗审查 9 claim 无一驳至迫使改动（A/B/G VERIFIED、C/D/E/F/H PARTIAL、I REFUTED 反向强化）。解锁 P11。〔research/g2-eval-ts-review.md, r3-multiturn-eval-hook.md, tickets/phase-4/G2-eval-ts-vs-python.md〕

- **Pipeline vs goal/todo/plan 实验 (G1 grilling, resolved 2026-08-20)**：2×2 变体(Gating×Planning: A=gate开plan关 / B=关开 / C=开开 / D=关关) × 2 模型配置(思考轴+能力轴) staged 实验；主指标=端到端答案正确率(execution-match strict、correct/declined/wrong 三分、declined≠wrong)；决策规则 ship 最高 / 实际等效≤3pp→更简 / per-model 分歧>5pp 触发路由；Planning 因子={goal,todo}(plan-mode 作 B-内部 Level-2)；eval case=RBI 161 单轮分层；设计(不跑,跑须 P11+P7b)→毕业 G1b 执行票。〔tickets/phase-misc/G1-pipeline-vs-goal-todo.md〕

- **NL→SQL 引擎 (P13 prototype, resolved 2026-08-20)**：极简 (B) 路径（完整 (C) 已判单期不可行）on P6 substrate——BM25 schema-linking 经 P5 `ctx.retrieval` seam（首期纯 BM25-only·不阻塞 T2，**AGA 不提供向量模型已确认**，向量侧升级=用户自部署经 P5 外置 embedder）+ 移植 RBI `v2-baseline.md` §3 staged SOP+§6+§5+工具目录+MAX_SQL_PER_TURN+P7 四阶段适配（方言 grounding 从 query conventions seam）+ 复用 P4 `ctx.query.execute`（3-state）+ execution-feedback 自纠错（`QueryOutcome.failed`→LLM 读 error 重写→近重复门防重发，对齐 BIRD-FIXER/Genie Inspect）+ 薄 regex 守卫替 sqlglot（方案 1 regex+方案 4 轻量 JSON path 解析合体，挂 `agent/turn-stopping` 填 P7 `sql_syntax_gate` 槽返 `GateResult`，判罚 error/warning/fail-open，F2 同源，守卫数据从 P6 substrate+检索拿）+ eval gate（自带最小版对齐 P11/G2：da-fresh EvalCase+EXECUTION 判分+dsh-llm-replay+轻量 runner 不经真 harness session，诚实门值<RBI 73.8%）；**drop** plan_query/sqlglot AST critic/answer-RAG/cross-encoder/sqlglot_dialect。prototype `prototypes/p13-nl2sql-engine/` 9 scenarios 全绿；conventions 归 query 包（P4 seam）。〔research/p13-sql-critic-alternatives.md, p6-nl2sql-feasibility.md, tickets/phase-3/P13-nl2sql-engine.md〕 prototypes/p13-nl2sql-engine/
- **NL→SQL 引擎生产硬化 (P13b prototype, resolved 2026-08-20)**：P13 throwaway → 生产 `packages/data/nl2sql-engine/`（TS Cordis Service + ctx.query seam + 本地 RetrievalLinker/CriticGuardData 接口+薄默认 [Q1: P5/P6 生产未 ship，毕业 P5b/P6b additive swap]）+ conventions `packages/query/query-maxcompute/{conventions.yaml, src/conventions.ts}` loader（F1 js-yaml RBI conventions.py:32）+ bundle row + critic gate-only（Q2 critic 逻辑+critiqueSql+sqlSyntaxGate+GateResult 全在 nl2sql-engine，P7b sql_syntax_gate slot delegate 单向无环；Q4 仅 sql_syntax_gate，search_data_sources 唯一 model-facing tool）+ eval-gate-minimal（Q3 ship now，generate() eval-only，agent-loop-driven P7，F6 真 runner→P11）+ code-review-low 7 fix。Deferred: F3/F4/F5(保留 regex 不换 sqlglot)/F6；search_data_sources ctx.tools 注册=deferred sub-item（dsh-tools API grounding；preset row 暂 commented forward-compatible）。9/9 spec + tsc clean + cordis-config 124 pass。解锁 P7b（critic API+GateResult+CriticCtx 契约 ship）。〔packages/data/nl2sql-engine/, tickets/phase-3/P13b-nl2sql-engine-prod-hardening.md〕

- **search_data_sources model-facing tool 注册 (P13b deferred sub-item, resolved 2026-08-20, commit 0e1a0fdf25)**：dsh-tools `defineTool` + `ctx.tools.register` API grounded（首个 model-facing tool，解锁未来 load_*/query_data/critique_sql/evaluate_sql_quality/present_*）；新包 `packages/data/tool-search-data-sources/`，UNDERSTANDING tool 直调 nl2sql-engine `Bm25Linker`（Q1 薄默认；ctx.nl2sql 仅 getConventions 无 retrieval 方法）；corpus 空薄默认至 P6b ctx.schema（callable-but-unwired 非 broken mount）；additive swap P5b ctx.retrieval / P6b ctx.schema 契约不变；preset `tool-search-data-sources` 行解注释。〔packages/data/tool-search-data-sources/, tickets/phase-3/P13b-nl2sql-engine-prod-hardening.md〕

- **acp/ 删除 fallout (R5 research, resolved 2026-08-20)**：删 packages/acp/ 非单包编辑——级联 acp-demo（examples/acp-demo/src/index.ts:17 全仓唯一裸 import @deepseek-ai/dsh-acp + ctx.plugin 组合、3 测试）+ examples/acp-agent/（7 .ts 测试含 ~70 snapshot 场景 + 57 cordis 配置，经 acp-demo 传递 break）；acp-snapshot 零依赖 acp/（用 @agentclientprotocol/sdk + 假 agent fixture）存活、3 spec 仍绿；headless/jsonrpc/web（7 文件只用 normalizer）+ packages/bundle/data-agent/（0 acp 引用）不受影响。(c) 纯产品仓库删 acp/ 可行且与 da 干净分离，但需同删 acp-demo + acp-agent + 5 manifest/文档行 + lock 重生成；Q2 disable-only 零成本优先。〔research/r5-acp-fallout.md, tickets/phase-misc/R5-acp-fallout.md〕

- **llm-dashscope 4xx 错误体 mis-parse fix (P2b task, resolved 2026-08-20)**：200+error-body 假设证伪（live-probe 4 错误 case 全 4xx、无一 200+err-body）；但同源 !response.ok 路径捞出真 bug——AGA 4xx 错误体是 SSE 框架却标 application/json（content-type 不可靠判别器），adapter response.json() 在 SSE 框架抛、catch 吞、code/message/request_id 全丢（分类仍对按 status）。fix：adapter.ts 加 parseErrorBody（先 JSON.parse 再 parseSse drain、content-type-agnostic，恢复 code/message/request_id）+ bodyAsStream helper + 7 测试（adapter.spec 28→35、包 72 全绿、tsc+oxlint 净）；2xx/translate.ts 不动（200+err-body 未实证、不跟 rbi 防御 speculative 形状，若将来 live 实证再单开）。〔research/p2b-dashscope-200-error-body.md, tickets/phase-misc/P2b-dashscope-200-error-body.md〕

- **dashscope queue keep-alive (P2c task, resolved 2026-08-20)**：300s streamIdleTimeoutMs 默认经 live-probe + idleWatchdog 语义实证安全——4 并发流式 hold 368-498ms（s-rt 133-262ms、无 429/无排队、远<300s 约 600×），keep-alive :HTTP_STATUS/200 comment 首字节→onComment→watchdog.pulse 立即 rearm 300s、流式期间每 event pulse（max gap 45ms 约 6600× 余量）；唯一无 keep-alive 风险窗=fetch pending hold>300s，实测<0.5s、文档「数十秒」<100s 皆远<300s → 风险不兑现，无 fix。〔research/p2c-dashscope-queue-keepalive.md, tickets/phase-misc/P2c-dashscope-queue-keepalive.md〕

- **phase-gate 生产硬化 (P7b prototype, resolved 2026-08-20)**：真 `packages/data/phase-gate/`（TS、Schemastery、真 Cordis `ctx.on` 7 hooks：`ctx.tools.guard` 硬白名单 D5 / `agent/turn-stopping` serial / `tools/post-execute` / `agent/request` per-phase reasoning / `system-prompt/assemble` persona option C / `llm/stream` F5 计 / `agent/pre-step` F6 计 + `agent/status` F4；per-agent 状态 keyed per session；rbi budgets 默认）+ 真 `apps/cli/config/agent-presets/data-agent/agent.cordis.yml`（`isolate:phaseGate` realm 行 + 注释 da 工具行 TODO）+ P13 critic fold（`src/critic.ts` regex+JSON path 替 sqlglot stub，`sql_syntax_gate` 槽，数据从 post-execute-captured state 自含无 P6 依赖）。**控制流精炼**：`agent/turn-stopping` serial void（return 被 agent-loop 丢弃）→ 副作用控制（state mutation + `agent.inject` 续航/重试 + `honest_decline`，**非** P7 stub 的 return-based）。F1 `ctx.tools.execute` 程序化经 guard（验）接 UNDERSTANDING forced_load；F2 `extractSqlCandidate` 单源 post-execute block；F3 独立 stall timer + `agent.cancel`；F4 `agent/status` idle→running 重置（非 turn/start，inbox/inserted 无 target 改 fallback）；F5 `llm/stream` 计；F6 `pre-step` 计 + turn-stopping decline。**bundle re-interpret**：phase-gate 挂 preset（agent 平面）非 bundle（bundle 无该行）；P7b 顺带修 P2 遗留（web-app+data-agent bundle package.json 缺 `dsh-llm-dashscope` dep→解锁 verify-cordis-config，additive）。typecheck-clean + 16/16 scenario（port P7 8 + critic）+ verify-cordis-config 124 files + commit-time oxlint 0 error（full type-aware 7 非阻塞 defer）。〔packages/data/phase-gate/, apps/cli/config/agent-presets/data-agent/, tickets/phase-3/P7b-phase-gate-hardening.md〕
- **G3 stable per-user PAT provisioning 接线 (G3b prototype, resolved 2026-08-20)**：META=A——落 Stratum A（P9-独立 scaffolding）+ 开 P9b 延后 Stratum B（P9-依赖真实 per-user provisioning）。7 决策：①1a mount 形态=(a) 新薄 host/composite package（static Config 接 yml 标量 + apply 程序式构造 plain file-shim fallback 复用 credentials-local `parseCredentialsDocument`+`launchEnvironmentOf`、非 Service 避 double-register + `ctx.plugin(KeychainCredentialProvider,{runner:securityCli,fallback:shim,...})`；bundle `- id: credentials disabled:true` 关 base local + mount host；Cordis 核实 `vendor/cordis/src/service.ts`+`registry.ts`：Service 构造 auto-register `ctx.reflect.provide`→fallback 须 plain shim、`ctx.plugin` 无 static Config 透传 injectable config）+ 1b autoLock:300+`unlockPasswordSource: interactive`(默认,安全)|env(无值守 fallback,弱化锁)|none——**SPEC 到 G3c**（⚠️ **global-writes gap**：P12b `KeychainFallback` read-only `{resolve,describe}` ⊥ 全局凭证写需求（Models 页写 DEEPSEEK_API_KEY、更新 T1 全局 PAT）；keychain `set(no userId)` throw 不 delegate；挂成 ctx.credentials 暴露此 gap，G3c 须解 writable-fallback / set-delegation / double-provide-verify + interactive prompt tty/非 tty）；②P3 `resolve(ref,{userId: ctx.identity.current()?.userId})` + identity 硬 inject（Cordis 无 soft optional，访问未 inject 抛 "cannot get property"）——**LAND** 27/27 green（今日 no-op→global stub，P9b 填→per-user）；④`perUserFallbackRefs?:Set<CredentialRef>` 门控（per-user miss ref∈set→fallback/∉→undefined stable 必填；无 userId 总走 fallback 保全局凭证；default undefined=all=early 后兼容 P12b）——**LAND** 28 green（dormant till G3c mount）；⑥P8b `resolveIdentity` 读 `ctx.identity.current()` + audit `static inject=['identity']`——**LAND** 13/13 green；③⑤ per-user 自助 set + 必填 UX——**DEFER P9b**（P9 生产包未落）；⑦scope 正交 **VERIFIED**（`query-maxcompute/src/index.ts:266` `resolve(ref)` 无 scopeId address；per-scope 在 sidecar set_credentials+OdpsConfig region 非 ctx.credentials；无 {scopeId} caller→keychain userId-only 够，不扩 YAGNI）。新包 `packages/identity/identity/`（`ctx.identity` seam stub，`current()` 返 undefined，P9b 填真值）。〔tickets/phase-1/G3b-per-user-pat-stable-wiring.md, packages/{identity/identity,credentials/credentials-keychain,subagent/subagent-qoder,data/audit}/, tickets/phase-2/G3c-credentials-keychain-host-mount.md + P9b-admin-access-isolation-hardening.md〕
- **credentials-keychain-host mount (G3c prototype, resolved 2026-08-20)**：global-writes gap 解法=(A) writable fallback shim（(C) Cordis 证伪：`vendor/cordis/src/reflect.ts` `provide` 同名同 scope 双 provide throw → 须 disable base credentials + 单 keychain provider）。`KeychainFallback` += 可选 `set?/unset?`（additive）；keychain `set/unset(no userId)` delegate fallback。credentials-local additive export `renderDocument`（comment-preserving）。新包 `packages/credentials/credentials-keychain-host/`（function plugin 无 static Config 让 injectable runner 到 apply + `await ctx.plugin(KeychainCredentialProvider)` 确保 ensureKeychain + writable file-shim fallback 复用 parseCredentialsDocument/renderDocument/writeFileAtomic/withFileLock 非 Service 避 double-register + unlockPasswordSource interactive/env/none + perUserFallbackRefs string[]→Set）；typecheck-clean + 6/6 tests green（ctx.credentials=keychain + per-user 隔离 + 全局写经 shim comment-preserving + unset + fallback-off stable + env-shadow 拒）。bundle 接线 **opt-in（文档化非 active）**——active 行使 data-agent boot 强依赖 macOS+keychain，非 mac CI/dev 崩；同 P12b 先例不在 bundle 挂 active，host README 文档化 opt-in。毕业雾「global-writes gap」。〔tickets/phase-2/G3c-credentials-keychain-host-mount.md, packages/credentials/credentials-keychain-host/, packages/credentials/credentials-local/src/index.ts(+export renderDocument), packages/credentials/credentials-keychain/src/index.ts(KeychainFallback+set/unst delegate)〕
- **eval harness 生产硬化 (P11b prototype, resolved 2026-08-20)**：P11 proto → 生产 `packages/eval/eval/`（`@deepseek-ai/dsh-eval`，TS 纯库 zero-seam-dep，35 文件 ~3300 行；不注册 ctx.eval，注入 Responder/CaseSqlExecutor/JudgeProvider + 结构 view RunResultView/QueryOutcomeView；deps=zod+js-yaml）。7 grilled 决策：(1) judge 接受 variance（temp0+JUDGE_MAX_RETRIES=2+1→2→4s+threshold 0.6 rbi-faithful，judge-replay DEFER——judge 是 eval 侧另一路 LLM 不经 spawned runtime）；(2) trigram fuzzy 分离——derailment rbi turnMatchesExpectation n=3 ≥0.35，DELIVERY 短 expected token-containment（gameX≠gameA）；(3) classifyExecutionFailure（rbi l1.py mirror）+mapQueryOutcome（pending→patience refuse）；(4) zero-seam-dep 纯库；(5) eval 不 spawn（宿主拥有）+三层测试（src stub 100%/scenarios.spec 折入烟雾/live e2e 延后）；(6) zod da-fresh EvalCase+YAML/JSON loader；(7) 纯库核心 IN-scope，CLI/persist/pass_at_k DEFER P11c。rbi-faithful 逐条 VERIFIED（classifyError AUTH 逐词+RETRYABLE 补 5 短语、passKVerdict 首非 pass、_char_ngrams n=3、5 match_mode 1:1、submit_turn execution_error 不推进、derailment 映射）；修 proto bug（judge exhaustion 'exhausted'、for(;;) 消死代码）。typecheck-clean+201 tests+coverage per-file 100%（诚实无 trick）+code-review 无 critical/major（2 minor fix：RETRYABLE 5 短语+submitTurn 防御 failureClass??classifyExecutionFailure）。commit 2890812409。毕业雾「D2 (c) keep/regress」→D2c；CLI/persist/pass_at_k→P11c；解锁 G1b（eval 库消费者）。先验 host typecheck gap（NOT P11b）：nl2sql-engine（P13b tsconfig.host ref 缺）+phase-gate（P7b PromptAssembly import）→P13b/P7b follow-up。〔tickets/phase-4/P11b-eval-harness-hardening.md, packages/eval/eval/〕

## Not yet specified

<!-- 雾：in-scope 但还太糊无法 ticket；随 frontier 推进毕业 -->

- 跨网/多 host 部署形态（chisel overlay 到 edge / central backend KMS-Vault）：前期 Mac 内网直接可达→无隧道（P10 resolved，Caddy 反代+mTLS）+ 前期 single-host→macOS Keychain 够（P12b resolved，central backend DEFER 到跨网/多 host 真需求，YAGNI；central backend 自身凭证链同构 secret-to-protect 递归，见 research/p12b-keychain-acl-feasibility.md §7.3）；跨网/多 host 真需求时再 ticket。〔P10 resolved 2026-08-20；P12b resolved 2026-08-20〕
- cert-revocation / transport-secret lifecycle（mTLS client-cert 立即吊销：Caddy native client_auth 不查 CRL/OCSP，P10 code-review B1 surfaced；短命+rotation 前期够，立即吊销需 CRL/custom verifier）：cert 吊销属 transport 层、与 keychain PAT 正交；P12b SPLIT 出（守 keychain 焦点）；部署期 ops 或独立小 grilling 票定 supply-chain/cert 吊销政策。〔P12b resolved 2026-08-20〕
- native keychain binding + harness code-signing（runtime-exfil ACL，P12c ticket）：P12b DEFER 的 per-item Touch-ID ACL 需 native SecItem+`kSecUseDataProtectionKeychain` + Developer-ID 签名 harness binary；**blocked by harness 分发流程建立**（Apple Developer Program、notarization、打包 .app/.exec），非本 map 可独裁。〔P12b resolved 2026-08-20〕
- 数据连接器扩展（mysql/hologres/未来后端）接入时机与形态（`QueryEngine` seam 已留，时机未定）。
- 报告/Excel 二进制产物交付机制（code-runtime pandas 生成 + `ctx.attachments` 复用 vs 专用 deliverable/export 工具；文本报告走 tool-fs write，二进制不走 str-replace-editor/tool-fs——两者仅 UTF-8）。
- per-user 登录硬化（token 轮转/expiry/CSRF/限流/审计；net-new auth 子系统，类 P12b；待 P9 真建 + P10 隧道）。
- query-trio 剩余生产：tool-query Consumer（model-facing + 会话门 G1/G5 + 3-execute，镜像 rbi `execution.py`；C1 吃 SQL，NL→SQL 归 P13）+ engine-wrapper guard chain（CostGuard `estimate_cost`/TimeoutGuard `signal`/RetryGuard/OrphanReaper，镜像 rbi `pipeline.py:run_query_async`+`core/guards/*`，A1-split `ctx.query.execute` 门）——P4 设计、P4b 的 Provider + 极简 `query` Service Definition 已就位作基础；session gates 与 guard chain 落 `query` 的 concrete `execute` 还是独立 guard 插件（`tools/pre-execute`/`tools/execute` around-dispatch）待 grilling 钉。
- intranet 重 embedder 部署形态：chat 走 AGA、**AGA 不提供向量模型已确认**（P13 Q2）→ 向量侧升级=**用户自部署**向量模型经 P5 外置 OpenAI 兼容 embedder 插件（`InfinityEmbedder`），非 AGA relay；T2 live-probe 已佐证 AGA-embeddings NO（2026-08-20，4 端点变体全 404 + chat 200 控制证非鉴权；网关级路由未找到）→ 决策落定见 Decisions so far（独立 sidecar 非 AGA）；残余 serving 框架/模型选型（Infinity/TEI/Ollama + bge-m3/Qwen3-Embedding）= 用户部署 ops，非 map 架构决策。〔P5 resolved 2026-08-20，P13 resolved 2026-08-20〕
- ~~D2 (c) keep/regress 可逆~~ — graduated to [D2c grilling](tickets/phase-misc/D2c-retrieve-tool-keep-regress.md)（P11b eval harness 就绪 2026-08-20 → 可跑召回/歧义数据驱动 keep (b) retrieve-tool escape-hatch 还是 regress (a) pipeline-only）。〔P5 resolved 2026-08-20；P11b resolved 2026-08-20〕
- 是否需审 Qoder internal tool/reasoning stream（非仅终态 call outcome）——P8 当前只审 `tools/post-execute` 终态 outcome+Credits；若 forensic 合规需全 stream 则开 P3 标的 core-seam 变更票（外部 one-shot not trace-enumerable，见 P3 resolution；P8b resolved 2026-08-20——审 call outcome（终态+Credits）非 stream，stream 仅当 forensic 需才另开 core-seam 票）。
- harness seam 缺口（P7 surfaced，→P7b 解）：(a) 无 question-start seam——`turn/start` 重置会破 per-kick 预算（rbi per_turn=per 用户问题=per kick 多 turn），须检测首 turn / `current_phase`→UNDERSTANDING 重置 question-scoped 计数器；(b) 无原生 stall/no-events watchdog（rbi `_watch_for_stall` 300s 须插件独立 timer，排除 `ctx.awaiting_input`）；(c) 插件程序化调工具路径（forced_load 须 `ctx.tools.execute` 经 guard——存在性/路由待验）。〔P7b〕

- ship-default-orchestration 决策（da 出厂默认 A/B/C/D 哪个）——fed by G1b 实验报告（ship 推荐）；G1b 跑完报告后才 specifiable→毕业。〔G1 resolved 2026-08-20〕
- per-model 路由决策（是否按模型分编排）——fed by G1b per-model 分歧信号（分歧>5pp 才触发毕业路由票，Q2 secondary）；条件毕业。〔G1 resolved 2026-08-20〕


- ~~critic dedup（P13b/P7b 并发产物）~~ — graduated to [host-typecheck-wiring](tickets/phase-misc/host-typecheck-wiring.md)（P11b resolved 2026-08-20 surfaced host-typecheck 先验 gap + 未提交 critic-dedup WIP；新 task 票收尾：commit/revert WIP + nl2sql-engine tsconfig.host ref + phase-gate PromptAssembly）。

## Out of scope

<!-- 超出 destination；closed，不毕业 -->

- reverse-bi 两个 evolution flywheel（Prompt Evolution + Golden-Case Corpus Evolution）、query-acceleration、9 前端页面、prompts/format-templates/flows/context 版本化超结构——成熟期/UX，可后期回挂，当前不迁（Q3 裁剪）。
- rbi-agent core/ 的 ReAct loop/session/MCP-client/LLM——退役用 harness 等价物，不重新实现。
- 物理删除 code-agent 包——Q2 修正为 disable-only 不删（保升级路径）；实际"删除"留作 (c) 纯产品仓库重构时。
- "用 Qoder 内置模型当主 LLM"——无干净路径（Qoder SDK 无模型 API），不做。
- rbi-web（FastAPI+React 9 页）——不迁，harness apps + 新插件替代。
