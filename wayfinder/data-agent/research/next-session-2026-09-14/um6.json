{
  "currentExemptGroups": [
    "acp",
    "boot",
    "bundle",
    "hooks",
    "sdk",
    "util"
  ],
  "groupsMissingReadme": [
    "embedder",
    "query",
    "retrieval"
  ],
  "recommend": {
    "chosen": "add-group-READMEs",
    "rationale": "VERIFIED: verify-subsystem-pages.ts emits exactly 5 violations (data + eval: README exists but no docs/subsystems link; embedder + query + retrieval: NO group README at all). The three missing-README groups are NOT candidates for exemption because a real, current subsystems page already owns their contracts: docs/subsystems/data-agent.md documents ctx.embedder (anchor #ctxembedder--embedderservice-abstract-seam, line 92, Source: packages/embedder/embedder/src/index.ts) and ctx.query (anchor #ctxquery--queryengine-abstract-seam, line 378, Source: packages/query/query/src/index.ts). Retrieval is part of the same data-agent overlay pipeline (its provider retrieval-inproc consumes ctx.embedder and feeds the nl2sql schema-linking documented in data-agent.md). Exemption is reserved (per the file's own doc-comment) for groups that genuinely own no standalone subsystem reference (acp=protocol transport, boot=bin lib, bundle=composition carriers, hooks=protocol bridges, sdk=out-of-process clients, util=low-level primitives). embedder/query/retrieval have a first-class owning subsystem page, so linking it (add-group-READMEs) is the correct/honest fix; exempting them would falsely assert 'no owning page exists.' The group READMEs also add reader-navigable structure (child package tables) that the exemption would not. Cost is bounded and matches the ticket estimate: 9 new files (README.md + README.zh.md + README.i18n.yaml x3), 1 session."
  },
  "regenTouchesDataAgentMd": {
    "touches": true,
    "evidence": "gen-cordis-catalog.ts computeOutputs() (lines ~1085-1113) iterates every SERVICE_PAGE/EVENT_SCOPE_PAGE target and pushes both `${SUBSYSTEMS_DIR}/${page}` and its `.zh.md` side to outputs, splicing the generated cordis-surface region between the '<!-- BEGIN GENERATED cordis-surface -->' markers. SERVICE_PAGE (lines 125-137) maps audit, embedder, identity, nl2sql, schema, scopes, evidenceQuery, criticCtx, query, managementSession, patrol, resultCache, resultGateway ALL to 'data-agent.md'; EVENT_SCOPE_PAGE (lines 233-237) maps scopes/patrol/management-session/evidence/admin to 'data-agent.md'. Therefore gen-cordis-catalog OWNS and rewrites docs/subsystems/data-agent.{md,zh.md} on regen. VERIFIED current state: `gen-cordis-catalog.ts --check` = '99 generated file(s)/region(s) are up to date' (data-agent.md currently FRESH, so a regen today is a no-op). The Risk-3 / UM4 coupling is forward: if UM4 Scope 3 changes the src that projects onto these services (e.g. apiproxy removal / results-RPC re-home / preset/agent-presets index changes), the next gen-cordis-catalog run WILL rewrite data-agent.{md,zh.md}'s generated region + re-record data-agent.i18n.yaml. gen-doc-graphs.ts does NOT target data-agent.md by path (it emits GENERATED graph regions into other doc pages; grep for 'data-agent'/'subsystems' shows only a generic subsystems/core.md reference at line 1617, no data-agent.md emission)."
  },
  "regenCommands": [
    "pnpm run verify-subsystem-pages   # gate under test; currently exits 1 with 5 violations",
    "pnpm run verify-cordis-catalog     # = tsx scripts/gen-cordis-catalog.ts --check; VERIFIED '99 up to date' (data-agent.md fresh now)",
    "pnpm run gen-cordis-catalog        # regen data-agent.{md,zh.md} region — ONLY needed if UM4 src lands (Risk 3); no-op today",
    "pnpm run verify-doc-graphs         # = gen-doc-graphs.ts --check",
    "pnpm run verify-translation-pairing --write packages/embedder/README.md   # re-record i18n.yaml for each new pair (embedder/query/retrieval)",
    "pnpm run doc-sync                  # full gate aggregate — final green check for the 5 leaves"
  ],
  "linkEditLocations": [
    {
      "file": "packages/embedder/README.md",
      "edit": "CREATE new group README (does not exist). Include a 'Related documentation' entry linking ../../docs/subsystems/data-agent.md#ctxembedder--embedderservice-abstract-seam — this is the reader-visible direct docs/subsystems/*.md link the audit requires (regex ^\\.\\./\\.\\./docs/subsystems/[^/#?]+\\.md(#...)?$)."
    },
    {
      "file": "packages/embedder/README.zh.md",
      "edit": "CREATE zh pair side; link ../../docs/subsystems/data-agent.zh.md (locale-matched, avoids wrong-locale-link translation debt)."
    },
    {
      "file": "packages/embedder/README.i18n.yaml",
      "edit": "CREATE pairing record via `pnpm run verify-translation-pairing --write packages/embedder/README.md`."
    },
    {
      "file": "packages/query/README.md",
      "edit": "CREATE new group README; link ../../docs/subsystems/data-agent.md#ctxquery--queryengine-abstract-seam."
    },
    {
      "file": "packages/query/README.zh.md",
      "edit": "CREATE zh pair side; link ../../docs/subsystems/data-agent.zh.md."
    },
    {
      "file": "packages/query/README.i18n.yaml",
      "edit": "CREATE pairing record via verify-translation-pairing --write."
    },
    {
      "file": "packages/retrieval/README.md",
      "edit": "CREATE new group README; link ../../docs/subsystems/data-agent.md (no dedicated ctx.retrieval anchor exists in data-agent.md — only embedder/query/evidenceQuery anchors present; link the page root or the #ctxembedder anchor since retrieval-inproc consumes ctx.embedder)."
    },
    {
      "file": "packages/retrieval/README.zh.md",
      "edit": "CREATE zh pair side; link ../../docs/subsystems/data-agent.zh.md."
    },
    {
      "file": "packages/retrieval/README.i18n.yaml",
      "edit": "CREATE pairing record via verify-translation-pairing --write."
    },
    {
      "file": "packages/data/README.md",
      "edit": "OUT-OF-SCOPE for the 3 triplets but part of the same gate: add a docs/subsystems/data-agent.md link (add a 'Related documentation' line — README exists at 26 lines, currently 0 subsystems links). Sibling zh + i18n re-record."
    },
    {
      "file": "packages/eval/README.md",
      "edit": "OUT-OF-SCOPE for the 3 triplets but part of the same gate: add a docs/subsystems/*.md link. NOTE eval is a pure library (no ctx-key, no data-agent.md service mapping) — this group is the ONE genuine exemption candidate among the 5; if no suitable subsystems page fits, add a GROUPS_WITHOUT_SUBSYSTEM_PAGE entry for eval instead of a forced link."
    }
  ],
  "tripletDrafts": [
    {
      "group": "embedder",
      "md": "---\ndescription: \"The embedder capability group: the abstract ctx.embedder / Reranker seam plus the default FakeHash stub and the external OpenAI-compatible HTTP provider for the data agent's retrieval/vectorization pipeline.\"\nkind: \"package-group\"\n---\n\n# embedder/ — embedding & rerank seam family\n\nEnglish | [中文](README.zh.md)\n\n## Summary\n\nThe `embedder/` group owns the data agent's embedding and rerank capability. The core `embedder` package defines the abstract `EmbedderService` (`ctx.embedder`) contract — `dim`, `modelId`, async `embed(texts) → float[][]` — plus the Reranker peer protocol (injected post-RRF) and the `InferenceError` taxonomy (unavailable / timeout / not_ready / dim_mismatch) that triggers BM25-only degradation in retrieval providers. Two providers ship: `embedder-fakehash` is the zero-dependency default (deterministic sha256 vectors, retrieval works out of the box), and `embedder-http` calls an external OpenAI-compatible endpoint (InfinityEmbedder, user-self-deployed heavy tier). All are **product** packages built in P5b; each README owns its per-package contract. Activation of the heavy tier is gated on D2c keep/regress evaluation.\n\n## Table of Contents\n\n- [Packages](#packages)\n- [Related documentation](#related-documentation)\n- [Dev Note](#dev-note)\n\n-----\n\n<a id=\"packages\"></a>\n## Packages\n\n| Package | Role | ctx key |\n|---|---|---|\n| [`embedder/`](embedder/README.md) | Abstract `EmbedderService` seam + Reranker peer protocol + `InferenceError` vocabulary | `ctx.embedder` |\n| [`embedder-fakehash/`](embedder-fakehash/README.md) | Zero-dependency deterministic sha256 stub provider (default tier) + FakeReranker peer | registers on `ctx.embedder` |\n| [`embedder-http/`](embedder-http/README.md) | External OpenAI-compatible HTTP provider (InfinityEmbedder) + InfinityReranker (heavy tier) | registers on `ctx.embedder` |\n\n-----\n\n<a id=\"related-documentation\"></a>\n## Related documentation\n\n- [Data Agent subsystem — `ctx.embedder`](../../docs/subsystems/data-agent.md#ctxembedder--embedderservice-abstract-seam) — the generated Cordis-surface contract for the embedder seam and its place in the data-agent overlay.\n\n<a id=\"dev-note\"></a>\n## Dev Note\n\nNone.\n",
      "zhMd": "---\ndescription: \"embedder 能力组：抽象 ctx.embedder / Reranker 接缝，加上默认的 FakeHash stub 和外部 OpenAI 兼容 HTTP 提供方，用于数据代理的检索/向量化流水线。\"\nkind: \"package-group\"\n---\n\n# embedder/ — 嵌入与重排接缝族\n\n[English](README.md) | 中文\n\n## 概要\n\n`embedder/` 组拥有数据代理的嵌入与重排能力。核心 `embedder` 包定义抽象 `EmbedderService`（`ctx.embedder`）契约——`dim`、`modelId`、异步 `embed(texts) → float[][]`——加上 Reranker 对等协议（RRF 后注入）和 `InferenceError` 分类（unavailable / timeout / not_ready / dim_mismatch），后者在检索提供方中触发 BM25-only 降级。两个提供方交付：`embedder-fakehash` 为零依赖默认（确定性 sha256 向量，检索开箱即用），`embedder-http` 调用外部 OpenAI 兼容端点（InfinityEmbedder，用户自部署重档）。均为 **product** 包，于 P5b 建设；各 README 拥有其单包契约。重档激活取决于 D2c keep/regress 评估。\n\n## 目录\n\n- [Packages](#packages)\n- [Related documentation](#related-documentation)\n- [Dev Note](#dev-note)\n\n-----\n\n<a id=\"packages\"></a>\n## Packages\n\n| 包 | 职责 | ctx key |\n|---|---|---|\n| [`embedder/`](embedder/README.md) | 抽象 `EmbedderService` 接缝 + Reranker 对等协议 + `InferenceError` 词汇 | `ctx.embedder` |\n| [`embedder-fakehash/`](embedder-fakehash/README.md) | 零依赖确定性 sha256 stub 提供方（默认档）+ FakeReranker 对等 | 注册于 `ctx.embedder` |\n| [`embedder-http/`](embedder-http/README.md) | 外部 OpenAI 兼容 HTTP 提供方（InfinityEmbedder）+ InfinityReranker（重档） | 注册于 `ctx.embedder` |\n\n-----\n\n<a id=\"related-documentation\"></a>\n## Related documentation\n\n- [Data Agent 子系统 — `ctx.embedder`](../../docs/subsystems/data-agent.zh.md#ctxembedder--embedderservice-abstract-seam) — embedder 接缝的生成 Cordis-surface 契约及其在 data-agent overlay 中的位置。\n\n<a id=\"dev-note\"></a>\n## Dev Note\n\n无。\n",
      "i18nYaml": "# Bilingual-pair consistency record (docs/i18n/README.md): the git blob hash of each\n# side as of the last confirmed-consistent state. Both languages carry equal authority;\n# after editing either side, bring the other along and re-record with:\n#   pnpm run verify-translation-pairing --write packages/embedder/README.md\n# (Generate the two hashes with verify-translation-pairing --write; do NOT hand-author them.)\nREADME.md: <run verify-translation-pairing --write to fill>\nREADME.zh.md: <run verify-translation-pairing --write to fill>\n"
    },
    {
      "group": "query",
      "md": "---\ndescription: \"The query-engine capability group: the abstract ctx.query seam for NL-to-SQL execution, the MaxCompute provider, and the per-engine convention/tool packages for the data agent.\"\nkind: \"package-group\"\n---\n\n# query/ — query-engine seam family\n\nEnglish | [中文](README.zh.md)\n\n## Summary\n\nThe `query/` group owns the data agent's SQL-execution capability. The core `query` package defines the abstract `QueryEngine` (`ctx.query`) contract — the four seam operations `execute` / `attach` / `cancel` / `getProgress`, the `getConventions()` dialect-grounding seam, and the 3-state `QueryOutcome` vocabulary (Completed / Pending / Failed) — and is the Def half of the query-trio. `query-maxcompute` is the first Provider (raw MCP SDK client over a stdio sidecar), `query-postgres` a second engine, and `query-tool` the model-facing consumer surface. NL-to-SQL translation is deliberately out of scope (C1: the seam accepts strict SQL; NL→SQL lives in the semantic layer). All are **product** packages built across P4; each README owns its per-package contract.\n\n## Table of Contents\n\n- [Packages](#packages)\n- [Related documentation](#related-documentation)\n- [Dev Note](#dev-note)\n\n-----\n\n<a id=\"packages\"></a>\n## Packages\n\n| Package | Role | ctx key |\n|---|---|---|\n| [`query/`](query/README.md) | Abstract `QueryEngine` seam (Def): 4 seam ops + `getConventions()` + `QueryOutcome` | `ctx.query` |\n| [`query-maxcompute/`](query-maxcompute/README.md) | MaxCompute Provider over a stdio MCP sidecar with per-call credential push | registers on `ctx.query` |\n| [`query-postgres/`](query-postgres/README.md) | Postgres query-engine Provider | registers on `ctx.query` |\n| [`query-tool/`](query-tool/README.md) | Model-facing query tool consumer surface over `ctx.query.execute` | consumes `ctx.query` |\n\n-----\n\n<a id=\"related-documentation\"></a>\n## Related documentation\n\n- [Data Agent subsystem — `ctx.query`](../../docs/subsystems/data-agent.md#ctxquery--queryengine-abstract-seam) — the generated Cordis-surface contract for the query-engine seam and its place in the data-agent overlay.\n\n<a id=\"dev-note\"></a>\n## Dev Note\n\nNone.\n",
      "zhMd": "---\ndescription: \"query-engine 能力组：用于 NL-to-SQL 执行的抽象 ctx.query 接缝、MaxCompute 提供方，以及数据代理的每引擎约定/工具包。\"\nkind: \"package-group\"\n---\n\n# query/ — 查询引擎接缝族\n\n[English](README.md) | 中文\n\n## 概要\n\n`query/` 组拥有数据代理的 SQL 执行能力。核心 `query` 包定义抽象 `QueryEngine`（`ctx.query`）契约——四个接缝操作 `execute` / `attach` / `cancel` / `getProgress`、`getConventions()` 方言接地接缝，以及三态 `QueryOutcome` 词汇（Completed / Pending / Failed）——是 query-trio 的 Def 半部分。`query-maxcompute` 为首个 Provider（经 stdio sidecar 的原始 MCP SDK client），`query-postgres` 为第二引擎，`query-tool` 为面向模型的消费方面。NL-to-SQL 翻译刻意不在范围内（C1：接缝接受严格 SQL；NL→SQL 属语义层）。均为 **product** 包，跨 P4 建设；各 README 拥有其单包契约。\n\n## 目录\n\n- [Packages](#packages)\n- [Related documentation](#related-documentation)\n- [Dev Note](#dev-note)\n\n-----\n\n<a id=\"packages\"></a>\n## Packages\n\n| 包 | 职责 | ctx key |\n|---|---|---|\n| [`query/`](query/README.md) | 抽象 `QueryEngine` 接缝（Def）：4 个接缝操作 + `getConventions()` + `QueryOutcome` | `ctx.query` |\n| [`query-maxcompute/`](query-maxcompute/README.md) | 经 stdio MCP sidecar 的 MaxCompute Provider，含每次调用凭证推送 | 注册于 `ctx.query` |\n| [`query-postgres/`](query-postgres/README.md) | Postgres 查询引擎 Provider | 注册于 `ctx.query` |\n| [`query-tool/`](query-tool/README.md) | 基于 `ctx.query.execute` 的面向模型查询工具消费方面 | 消费 `ctx.query` |\n\n-----\n\n<a id=\"related-documentation\"></a>\n## Related documentation\n\n- [Data Agent 子系统 — `ctx.query`](../../docs/subsystems/data-agent.zh.md#ctxquery--queryengine-abstract-seam) — 查询引擎接缝的生成 Cordis-surface 契约及其在 data-agent overlay 中的位置。\n\n<a id=\"dev-note\"></a>\n## Dev Note\n\n无。\n",
      "i18nYaml": "# Bilingual-pair consistency record (docs/i18n/README.md): the git blob hash of each\n# side as of the last confirmed-consistent state. Both languages carry equal authority;\n# after editing either side, bring the other along and re-record with:\n#   pnpm run verify-translation-pairing --write packages/query/README.md\n# (Generate the two hashes with verify-translation-pairing --write; do NOT hand-author them.)\nREADME.md: <run verify-translation-pairing --write to fill>\nREADME.zh.md: <run verify-translation-pairing --write to fill>\n"
    },
    {
      "group": "retrieval",
      "md": "---\ndescription: \"The retrieval capability group: the abstract ctx.retrieval hybrid-search seam and the in-process BM25 + vector + RRF provider for the data agent's schema-linking pipeline.\"\nkind: \"package-group\"\n---\n\n# retrieval/ — hybrid-retrieval seam family\n\nEnglish | [中文](README.zh.md)\n\n## Summary\n\nThe `retrieval/` group owns the data agent's schema-linking and context-fetch retrieval capability. The core `retrieval` package defines the abstract `ctx.retrieval` contract — `retrieve(query, {topK, mode}) → readonly RetrievalHit[]` — the seam half that providers implement, consumed with a soft fallback by `search_data_sources` (probes `ctx.get('retrieval')`; degrades to the synchronous `Bm25Linker` when absent). `retrieval-inproc` is the default in-process provider: BM25 + in-memory vector cosine over `ctx.embedder` + RRF (k=60), degrading to BM25-only on `InferenceError`. All are **product** packages built in P5b; each README owns its per-package contract. The hybrid provider is opt-in, gated on D2c keep/regress evaluation. This group depends on the `embedder/` seam (`ctx.embedder`).\n\n## Table of Contents\n\n- [Packages](#packages)\n- [Related documentation](#related-documentation)\n- [Dev Note](#dev-note)\n\n-----\n\n<a id=\"packages\"></a>\n## Packages\n\n| Package | Role | ctx key |\n|---|---|---|\n| [`retrieval/`](retrieval/README.md) | Abstract retrieval seam (Def): `retrieve` contract + `RetrievalHit` vocabulary | `ctx.retrieval` |\n| [`retrieval-inproc/`](retrieval-inproc/README.md) | In-process hybrid Provider: BM25 + vector cosine + RRF k=60, `ctx.embedder`-dependent, BM25-only degradation | registers on `ctx.retrieval` |\n\n-----\n\n<a id=\"related-documentation\"></a>\n## Related documentation\n\n- [Data Agent subsystem](../../docs/subsystems/data-agent.md#ctxembedder--embedderservice-abstract-seam) — the data-agent overlay this retrieval pipeline serves; the retrieval seam consumes the embedder contract documented there and feeds the nl2sql schema-linking / `search_data_sources` surface. (No dedicated `ctx.retrieval` Cordis-surface section exists yet — the seam is pipeline-internal; the embedder anchor is the nearest owning contract.)\n\n<a id=\"dev-note\"></a>\n## Dev Note\n\nNone.\n",
      "zhMd": "---\ndescription: \"retrieval 能力组：抽象 ctx.retrieval 混合检索接缝，以及用于数据代理 schema-linking 流水线的进程内 BM25 + 向量 + RRF 提供方。\"\nkind: \"package-group\"\n---\n\n# retrieval/ — 混合检索接缝族\n\n[English](README.md) | 中文\n\n## 概要\n\n`retrieval/` 组拥有数据代理的 schema-linking 与上下文获取检索能力。核心 `retrieval` 包定义抽象 `ctx.retrieval` 契约——`retrieve(query, {topK, mode}) → readonly RetrievalHit[]`——即提供方实现的接缝半部分，由 `search_data_sources` 以软回退方式消费（探测 `ctx.get('retrieval')`；缺失时降级到同步 `Bm25Linker`）。`retrieval-inproc` 为默认进程内提供方：BM25 + 基于 `ctx.embedder` 的内存向量余弦 + RRF（k=60），在 `InferenceError` 时降级为 BM25-only。均为 **product** 包，于 P5b 建设；各 README 拥有其单包契约。混合提供方为 opt-in，取决于 D2c keep/regress 评估。本组依赖 `embedder/` 接缝（`ctx.embedder`）。\n\n## 目录\n\n- [Packages](#packages)\n- [Related documentation](#related-documentation)\n- [Dev Note](#dev-note)\n\n-----\n\n<a id=\"packages\"></a>\n## Packages\n\n| 包 | 职责 | ctx key |\n|---|---|---|\n| [`retrieval/`](retrieval/README.md) | 抽象检索接缝（Def）：`retrieve` 契约 + `RetrievalHit` 词汇 | `ctx.retrieval` |\n| [`retrieval-inproc/`](retrieval-inproc/README.md) | 进程内混合 Provider：BM25 + 向量余弦 + RRF k=60，依赖 `ctx.embedder`，BM25-only 降级 | 注册于 `ctx.retrieval` |\n\n-----\n\n<a id=\"related-documentation\"></a>\n## Related documentation\n\n- [Data Agent 子系统](../../docs/subsystems/data-agent.zh.md#ctxembedder--embedderservice-abstract-seam) — 本检索流水线服务的 data-agent overlay；检索接缝消费那里记录的 embedder 契约，并馈入 nl2sql schema-linking / `search_data_sources` 面。（暂无专用 `ctx.retrieval` Cordis-surface 章节——该接缝为管线内部；embedder 锚点为最近的拥有契约。）\n\n<a id=\"dev-note\"></a>\n## Dev Note\n\n无。\n",
      "i18nYaml": "# Bilingual-pair consistency record (docs/i18n/README.md): the git blob hash of each\n# side as of the last confirmed-consistent state. Both languages carry equal authority;\n# after editing either side, bring the other along and re-record with:\n#   pnpm run verify-translation-pairing --write packages/retrieval/README.md\n# (Generate the two hashes with verify-translation-pairing --write; do NOT hand-author them.)\nREADME.md: <run verify-translation-pairing --write to fill>\nREADME.zh.md: <run verify-translation-pairing --write to fill>\n"
    }
  ],
  "recommendation": "Choose add-group-READMEs (not exemption) for embedder/query/retrieval. VERIFIED FACTS: (1) scripts/verify-subsystem-pages.ts GROUPS_WITHOUT_SUBSYSTEM_PAGE at lines 17-24 = exactly {acp, boot, bundle, hooks, sdk, util} — confirmed; all six have group READMEs and zero subsystems links (consistent with exemption). (2) Actual `verify-subsystem-pages` run exits 1 with EXACTLY 5 violations, matching the ticket: packages/data/README.md + packages/eval/README.md exist but carry no docs/subsystems link; packages/embedder, packages/query, packages/retrieval have NO group README.md at all (only child package dirs). (3) The 3 missing-README groups have a genuine owning subsystem page — docs/subsystems/data-agent.md documents ctx.embedder (anchor #ctxembedder--embedderservice-abstract-seam, Source: packages/embedder/embedder/src/index.ts) and ctx.query (anchor #ctxquery--queryengine-abstract-seam, Source: packages/query/query/src/index.ts); retrieval is part of the same overlay pipeline (retrieval-inproc consumes ctx.embedder). Exemption is only honest when NO owning page exists (see the file's doc-comment), so linking the existing page is correct. NOTE: there is NO dedicated ctx.retrieval Cordis-surface section in data-agent.md (grep found none) — the retrieval group README links the page (embedder anchor) rather than a retrieval anchor. RISK 3 (UM4 coupling) CONFIRMED: gen-cordis-catalog.ts SERVICE_PAGE (lines 125-137) maps embedder/query/audit/nl2sql/schema/scopes/evidenceQuery/etc. -> 'data-agent.md', and computeOutputs() writes both data-agent.md and data-agent.zh.md between the generated cordis-surface markers. So the UM6 regen (gen-cordis-catalog) OWNS/TOUCHES docs/subsystems/data-agent.md. Current state is FRESH ('verify-cordis-catalog' = '99 up to date'), so a regen today is a no-op; the coupling is forward — if UM4 Scope 3 lands src changes, gen-cordis-catalog will rewrite data-agent.{md,zh.md} + re-record data-agent.i18n.yaml. gen-doc-graphs does NOT emit data-agent.md by path. SCOPE SPLIT: the 3 triplet drafts (embedder/query/retrieval = 9 new files) are the core action; data/eval each need one link line added (data -> data-agent.md; eval is a pure library with no service mapping and is the one legitimate exemption candidate if no page fits). Link form the audit requires: ../../docs/subsystems/<page>.md[#anchor] from packages/<group>/README.md. Estimate matches ticket: 1 session. KEY FILES: /Users/mckenzie/workspace/deepseek-harness-da/scripts/verify-subsystem-pages.ts (lines 17-24 exemption list, line 57 link regex), /Users/mckenzie/workspace/deepseek-harness-da/scripts/gen-cordis-catalog.ts (lines 125-137 SERVICE_PAGE, ~1085-1113 computeOutputs write loop), /Users/mckenzie/workspace/deepseek-harness-da/docs/subsystems/data-agent.md (anchors line 92 embedder, line 378 query), exemplar triplets /Users/mckenzie/workspace/deepseek-harness-da/packages/{data,eval}/README.{md,zh.md,i18n.yaml}, positive link exemplars /Users/mckenzie/workspace/deepseek-harness-da/packages/{guard,llm}/README.md.",
  "confidence": "high"
}