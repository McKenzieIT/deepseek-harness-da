{
  "winner": "F1",
  "rationale": "F1 wins on all five criteria, verified against the actual repo. CORRECTNESS: F1 is the only option that gets all 65 depth-4 pairs green for pairing AND reaches 11/12 (12/12 with a trivial honestly-disclosed 2-file follow-up) while preserving the Overview as a distinct TOC entry. Ground-truth: the tocPairs filter (gen-package-readme-skeleton.ts L407: `if (heading === words.summary || heading === words.toc || heading === words.devNote) continue`) excludes only words.summary(概述)/toc/devNote. After F1 renames ZH 概述→Overview, the heading is no longer words.summary → it IS included in tocPairs → Overview stays a TOC entry. F2 renames EN Overview→Summary → the heading IS words.summary → EXCLUDED → Overview content dropped from TOC on all 16 (verified content-rich: audit's 'additive Cordis Service that observes tools/post-execute...' contract spec becomes TOC-unreachable = genuine nav regression). F2's runPlan claims 12/12 but this is INCORRECT — verified doc-standard.spec.ts 'maps package README kinds' iterates packageReadmes() (ALL depths, no filter) and readFrontmatter ASSERTS `---` present (not null); the 2 depth-3 group READMEs (packages/data/README.md, packages/eval/README.md — confirmed no frontmatter, start with `# data/`/`# eval`) fail regardless of F1/F2/F3 since all three only retrofit depth-4. F1 surfaces this residual honestly (11/12→12/12 with 2-file frontmatter edit); F2 does NOT surface it (overstated 12/12). F3 yields 10/12 after gen-only (16 collision still lack headings + frontmatter), 12/12 only after a separate ~1.5-session manual 16-pass PLUS the same 2-file depth-3 edit. SIGNATURE-SAFETY: all three preserve idempotency (planRetrofits hasFrontmatter skip at L542) + --check gate; verified signature pushes node.depth only (translation-pairing.ts:377) so both text renames are signature-invisible. F1's edge-case analysis (frontmatter stripped + re-run → collision set empty since Summary 概述 maps to EN Summary not Overview) is the most thorough. F3 has a documented --check blind spot (16 skipped from drift). MINIMAL DISRUPTION: F1 renames ZH 概述→Overview on 16, but verified those 16 already render `## Model Experience` + `## Known Limitations and Deferred Work` in English (audit ZH L26/L34) — English structural headings are the established convention, and 264 conformant packages establish the Summary+概述 pattern. F2 renames EN authored text AND loses the Overview nav entry + creates semantic mismatch (contract spec under 'Summary'). F3 touches no generator-authored text but defers the identical F1-style rename to manual. RECURRENCE: F1's overviewCollisionIndices detector auto-fires on future collision packages → auto-fixes; F2's helper auto-fires on future EN Overview → auto-renames (with nav loss); F3 EXPLICITLY does NOT prevent recurrence (self-admitted 'does NOT prevent recurrence on new packages'). COST: F1 = 5 diffs (3 logic + 2 pure-JSDoc) + ~1 session + trivial 2-file follow-up = best value; F2 = 2 diffs + hidden nav-regression cost + overstated-12/12 rework; F3 = 3 diffs + ~1.5-2 sessions (manual 16-pass cost driver, no idempotent re-run protection). The prior recon recommendation of F1 is VERIFIED, not overturned.",
  "applyRisk": "PRIMARY: F1 generator-diff-only yields 11/12, NOT 12/12 — the main session MUST also add frontmatter to the 2 depth-3 GROUP READMEs (packages/data/README.md, packages/eval/README.md) with `kind: package-group` + a one-line description to close the 'maps package README kinds to their documentation standards' test (verified: that test scans ALL depths via packageReadmes(), and readFrontmatter ASSERTS `---` present). This is a trivial 2-file ~6-line manual edit but if skipped the session reports 11/12 and the fix looks incomplete. F1's runPlan documents this as Step 5 RESIDUAL. SECONDARY: 65 README.i18n.yaml sidecar re-records (Step 4 `verify-translation-pairing --write` per pair) are mechanical but if skipped, corpus verify-translation-pairing reports the 65 as 'out of sync' (content changed since last blob-hash record). TERTIARY: the authored ZH text change 概述→Overview on 16 packages is Chinese-reader-facing — verified mitigated (those 16 already use English for Model Experience + Known Limitations headings; 264 conformant packages establish the Summary+概述 pattern) but a reviewer call is still warranted per the decision-doc. QUATERNARY: wiring the generator (package.json + run-gates.ts + gate-coverage.manifest.json) is a 4-file clone of the gen-tsconfig-paths precedent — straightforward but must complete before the 130-file run. The CORE F1 pairing fix (概述/Overview rename + re-derive) is independent of all residuals and gets all 65 depth-4 pairs green regardless.",
  "confidence": "high",
  "runnerUps": [
    "F2",
    "F3"
  ],
  "graftIdeas": "From F3: graft the per-run console.warn candidate-list idea onto F1 — when overviewCollisionIndices is non-empty, emit a non-blocking info log naming the collision packages so the operator has visibility into which files were auto-retrofitted (F1 currently does this silently). F3's hasGaiSuOverviewCollision predicate is clean but F1's overviewCollisionIndices (returning indices) is strictly more informative and already supersedes it — do not downgrade. From F2: the smaller helper footprint (one function vs F1's renameOverviewCollisions + const + PairPlan field) is appealing, but F1's actual LOGIC change is only ~3 functions (the other 2 diffs are pure-JSDoc invariant comments with no code change), so the size gap is cosmetic and not worth grafting F2's EN-side approach which carries the confirmed nav-loss regression. Update the file-header KNOWN-BUG note (currently 'blocks wiring') to reflect that F1 FIXES the bug — F3's honest 'does NOT prevent recurrence' documentation practice is worth mirroring: F1 should state in that note that the fix now prevents recurrence."
}
