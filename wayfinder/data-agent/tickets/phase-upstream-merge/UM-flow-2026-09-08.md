# UM flow 2026-09-08 — upstream-sync + data-agent 改造 + durable 方法 全流程

> 承接 UM13 research（re-sync 可行性 + 449 impact 核验完成）。本 doc 规划整个 flow + 票 + blocking + 工程机制。

## Destination（项目层，来自 `map.md`）

把 `deepseek-harness-da` fork 改造成 `deepseek-harness-data-agent`（data agent）。本 flow 是其中的 **upstream-sync 维度**：同步最新 + 全解冲突 + data-agent 按最新逻辑改造 + durable 方法。**不是**整个 data-agent transformation（多月 additive 数据能力叠加是 map 里别的工作）。

## 核心 3 需求

1. 同步 upstream dsh 最新代码。
2. 全解冲突 + data-agent 按 dsh 最新逻辑做**适配性改造**（不是小修补——见 adaptive 守门 UM-ADAPT）。
3. 一套工程方法：后续 upstream 每次更新快速定位"哪里要变"+ 生成新票。

## Flow（phases + tickets + blocking）

```mermaid
flowchart TB
    P0["Phase 0 DONE<br/>d347e703 merge 6b7610d45a + tsc fix c28b928fa9"]
    UM14["UM14 task multi-session<br/>re-sync c389f96bf3 + 50 conflict + seam3/4 break + workspace-files"]
    UM16["UM16 task<br/>synced base fix 根 entry → build green"]
    UMARCH["UM-ARCH task<br/>架构图+依赖图+gen/verify/update"]
    UMADAPT["UM-ADAPT research<br/>逐移位 adaptive-vs-surface"]
    RDA1P1["R-DA-CLIENT-RUNTIME Phase-1<br/>AFK-safe: 3 trivial + zombie 冗余清理<br/>(7 包/29 文件, 不是 45)"]
    RDA1P2["R-DA-CLIENT-RUNTIME Phase-2<br/>adaptive 4 presenter 迁移+包删除"]
    RDA2["R-DA-UI-PRESENTER-COMPOSITION grill<br/>4 presenter 注册模式设计决策"]
    CORDIS["UM-CORDIS-REGEN<br/>result-cache typert + regen catalogs"]
    UM10["UM10 mod typecheck/lint/CI 验证"]
    UM12["UM12 GA-FORK-CI re-sweep"]
    UM11["UM11 mod PR synced+改造+green"]
    UM15["UM15 durable sync 方法"]
    P0 --> UM14
    UM14 --> UM16
    UM14 --> UMARCH
    UMARCH --> UMADAPT
    UM14 --> UMADAPT
    UM14 --> RDA1P1
    UM14 --> CORDIS
    RDA1P1 --> CORDIS
    UMARCH --> RDA2
    UMADAPT --> RDA2
    RDA2 --> RDA1P2
    RDA1P1 --> RDA1P2
    UM16 & RDA1P2 & CORDIS --> UM10
    UM10 --> UM12
    UM12 --> UM11
    UMARCH --> UM15
    UMADAPT --> UM15
```

## Phase 叙述

- **Phase A（re-sync）** — UM14：re-merge `c389f96bf3` + 50 conflict + seam 3/4 break 迁移 + workspace-files 采纳。交付 synced 分支（build 仍破，根 entry upstream-shared）。
- **Phase B（data-agent 改造 + build 绿）** — 分**清障线**（Phase-B-clear）和**适配性改造主线**（Phase-B-adapt），并行推进：
  - **Phase-B-clear**（AFK-safe，可并行）：UM-ARCH regen-from-synced（synced base 8112743d69 上重生成权威 dsh 图）+ **R-DA-CLIENT-RUNTIME-DECOMMISSION Phase-1**（3 trivial 包迁移 + 删 zombie 冗余 root 声明 + 删死 apiproxy ref；7 包/29 文件不是 45——见票 2026-09-09 update）+ UM-CORDIS-REGEN（result-cache typert + regen catalogs）+ UM16（synced base 修 build:official 根 entry）。**这些是清障，不是适配性改造**——为主线扫清起点。
  - **Phase-B-adapt**（适配性改造主线，序列化）：UM-ADAPT（逐移位 adaptive 判定，UM-ARCH+UM14 后跑；每移位识别 why → 判架构冲突 → 判据 5 条）→ **R-DA-UI-PRESENTER-COMPOSITION grill**（view-registry seam 上的设计决策：A 走 `ConversationViewRegistry` vs B keep-standalone；这是 4 presenter 包适配性改造的**设计闸门**）→ **R-DA-CLIENT-RUNTIME-DECOMMISSION Phase-2**（4 presenter 按选定模式真迁移 + boot wiring 重分布 + zombie 包本体删除）+ cordis regen 第二轮（如需）。**这条线才是 UM-flow 核心需求②的实体**——防糊弄、真适配。
  交付 synced+改造+build 绿+data-agent 对齐 latest 逻辑。
- **Phase C（验证+PR+durable 方法）** — UM10（验证）→ UM12（GA-FORK-CI）→ UM11（PR）。UM15（durable 方法）并行，UM-ARCH+UM-ADAPT 后。

## 工程机制（demand ③，跨 UM-ARCH + UM15 + UM-ADAPT）

- **架构图 + 依赖图**（UM-ARCH）：dsh + data-agent Mermaid + data-agent→upstream 依赖（含 violation）+ gen/verify/update-on-change。= impact analyzer 的输入模型 + adaptive 守门的对齐参考。
- **impact analyzer**（UM15）：upstream 更新 → diff 旧 dsh 图 vs 新 → 架构移位 → 交叉依赖图 → data-agent 哪些受影响 → adaptive vs surface（自动 UM-ADAPT）→ ticket 候选。
- **adaptive 守门**（UM-ADAPT）：每移位 ①why ②data-agent 当前 ③冲突 ④adaptive 改造。防"小修补糊弄"。
- **staleness + cadence + merge 纪律**（UM15）：N-behind 告警 + sync 频率 + 94-conflict-resolution 形式化。

## 原 UM 修改/退休

| 票 | 处置 |
|---|---|
| UM1-9 | DONE（d347e703 merge 94 冲突解决，closed）。不退休——是历史 + UM14 re-sync 的 base；UM4 @Remote re-home、UM7 zombie 保留在 UM14 时 re-validate。 |
| UM10 | **修改**——补 tsc 已修（`c28b928fa9`）+ tsdown 根 entry upstream-shared + re-sync 修不了；验证 pivot 到 post-re-sync（Phase C）。 |
| UM11 | **修改/重构**——从"PR d347e703 merge"改为"PR synced+改造+build-green 分支"。 |
| UM12 | **保留**——defer 到 post-re-sync。 |
| UM13 | **DONE**——research 完成，UM14/UM15/UM-ARCH/UM-ADAPT 的 basis。 |

## v1 草图（手画，UM-ARCH gen 脚本生成后替换）

**图 1 — dsh（upstream）架构**（v1，wiring 需 gen 脚本核实）：
```mermaid
flowchart TB
    subgraph Host["dsh — Host face (Node/server)"]
        Boot["boot: app-boot + cmdline"]
        Bundle["① bundle composition: base·headless·sdk-app·sdk-minimal·web-app·acp-app"]
        Api["② @Remote+api-remotes: gateway·remotes·session/settings/workspace-controller"]
        Core["core: agent·agent-loop·tools·scope·system-prompt"]
        Session["session: persistence·format·projection·query"]
        Llm["llm: llm·deepseek·dashscope·pi-ai·retry"]
        Shell["shell·terminal·subprocess·sandbox"]
        Fs["fs·spill·compaction"]
        Web["web·web-search·web-fetch"]
        HostPkg["host: webserver·directory-picker·frontend-static"]
        Skill["skill·subagent·workflow·jobs·mcp·plan·todo·goal"]
        Cred["credentials·identity·settings·authorization"]
    end
    subgraph Client["dsh — Client face (browser)"]
        Conn["③ ./client public exports (client/connection RPC)"]
        Mod["④ client-modules loader"]
        Ui["client/ui-* · store · resources"]
    end
    Boot --> Bundle --> Api
    Core --> Session --> Llm
    Core --> Shell --> Fs
    Core --> Web
    HostPkg --> Api
    Core --> Skill
    Core --> Cred
    Mod --> Ui
    Conn --> Ui
    Api -. "@Remote RPC" .-> Conn
    Api ==>|"⑤ api-remotes client assembly"| Conn
```

**图 2 — dsh-data-agent 架构**（v1）：
```mermaid
flowchart TB
    subgraph DA["dsh-data-agent — additive layer"]
        DABundle["packages/bundle/data-agent (composed profile/bundle)"]
        Pipeline["4-phase data pipeline"]
        Retrieval["① retrieval: retrieval-inproc·embedder"]
        Query["② query: query·query-tool·maxcompute·postgres"]
        Guard["③ guard: phase-gate·patrol-mode"]
        Eval["④ eval: eval·eval-runner·eval-cli"]
        Semantic["semantic-layer (语义层+知识图谱)·schema-gateway·scope-registry"]
        DataTools["data tools: discover-relations·compute·retrieve·critique-sql·evaluate-sql·load-{table,event}-definition·search-{schema,data-sources}·present-{table,clarification}·update-table-config·get-{coverage,definition}·list-domains·reachability-delta·revert-edit·trigger-eval·resolve-term·discover-alt-labels·suggest-followups"]
        ResultCache["result-cache·result-cache-memory"]
        Audit["audit·evidence-query"]
        LlmAdapters["LLM adapters: llm-dashscope·Qoder(credentials PAT)"]
        Intranet["intranet/security: RBI gate·per-game isolation·tool 门禁"]
        DABundle --> Pipeline
        Pipeline --> Retrieval & Query & Guard & Eval
        Retrieval --> Semantic
        Query --> Semantic
        Guard --> DataTools
        Eval --> DataTools
        DataTools --> ResultCache
        Semantic --> Audit
        LlmAdapters --> Pipeline
        Intranet -. gates .-> Pipeline
    end
```

**依赖图（data-agent → upstream，对齐参考）**：

| data-agent 组件 | upstream seam/包 | 契约 | 对齐状态 |
|---|---|---|---|
| bundle/data-agent | ① bundle composition | public (cordis.patch.yml) | 对齐 |
| bundle/data-agent | ② @Remote/api-remotes | public (UM4 re-home) | 对齐（re-sync re-validate：32↑） |
| data tools/pipeline | ② @Remote results-RPC | public (result-cache/remote.ts) | 对齐（UM4-B） |
| LLM 适配 | llm seam (deepseek/dashscope/pi-ai) | public | 对齐 |
| 内网安全 | host webserver + credentials | public | 对齐 |
| client-modules 用法 | ④ client-modules loader | public | 对齐（re-validate：DshClientManifest rename） |
| client/connection 用法 | ③ client/connection | public | 对齐（re-validate：ConnectionRecoveryConfig rename） |
| **client/runtime（zombie）** | **③ ./client public exports** | **⚠ VIOLATION（zombie 内部）** | **R-DA-CLIENT-RUNTIME-DECOMMISSION**（**7 包/29 文件**，非 45——2026-09-09 subagent survey 修正）；Phase-1 AFK-safe 迁 3 trivial + 清 zombie 冗余；Phase-2 blocked by R-DA-UI-PRESENTER-COMPOSITION 设计闸门，做 4 presenter 真迁移 + 包删除 |
| workspace-files（NEW） | ② api/workspace-files @Remote | 新 seam | **待采纳（UM14）** |

> v1 草图的诚实边界：包名准确（来自 tsconfig.host.json refs），wiring 是推断；UM-ARCH 的 gen 脚本从跨包 import 生成权威图后替换本节。

---

## 2026-09-09 session update（Phase-B-clear done + grill Plan B + Q3 + 3 后续票）

**Phase-B-clear 全 done + 主 session 铁律重验**：
- **UM-ARCH regen-from-synced** ✓（commit `038d8b51ce`，`verify-architecture-graph` green，workspace-files authoritative；worktree `dsh-arch-regen`；PR deferred Phase-C）。
- **UM-CORDIS-REGEN subtasks 1+2** ✓（commits `cc6d9e714e`+`193b018827`，typert surface fix + gen-cordis-api regen；worktree `dsh-cordis`；subtask 3 deferred——blocked by RootOwnerProps homing）。
- **R-DA-CLIENT-RUNTIME-DECOMMISSION Phase-1** ✓（5 `[R-DA-P1]` commits，3 trivial 包迁出 zombie + 删 root-slot/apiproxy ref；worktree `dsh-rda-p1`）。
- **UM16 root-entry** ✓（3 `[UM16]` commits，`build:lib:host` green on node 24；worktree `dsh-um16`；`build:official` 未绿——325 apiproxy-removal errors 独立 workstream）。

**Phase-B-adapt grill done → Plan B**：R-DA-UI-PRESENTER-COMPOSITION = **Plan B**（keep `tool.call.toolview` + repoint imports + re-home helpers + isLatestTurn gap；非 Plan A——architecturally misplaced）。**ADR-0002** 双语记录翻转。Phase-2（4 presenter 迁移）+ Round 2（isLatestTurn gap）deferred 后续 session。

**Q3 apiproxy-fallout research → mixed verdict**：UM-APIPROXY-REMOVAL-CLIENT-TYPE-ANALYSIS resolved（~68 surface + ~122 adaptive；types REPLACED not moved）。Spawn 3 后续票：`UM-CLIENT-CONFIG-CLEANUP`（surface D+E）、`UM-CONNECTION-FIXTURE-DEAD-APICLIENT`（adaptive A）、`R-DA-TYPERT-REMOTE-REGISTRATION`（adaptive B+C+G sprint，18 包）。

**UM1-9 triage（2026-09-09）**：UM1/UM3/UM5/UM7/UM8/UM9 archived（done/resolved-by-upstream via re-sync `8112743d69`/superseded）；UM2 folded→UM12；UM4+UM6 leave-open（R-DA/Phase-2 + blocked-on-R-DA）。详 `.tmp/next-5-triage.md`。

**UM-ADAPT per-shift**：seam-4（diverged→Plan B）、workspace-files（not-applicable）、invariant-cleanup（needs-adaptive-but-mechanical→UM16）。Session A seam-3/4 admin slice landed（commit `9ba8638eac`，R-DA-admin，push deferred Phase-C）。

**Deferred（非本 session）**：Phase-C pushes（UM-ARCH PR + R-DA-admin，node 24，分支集成后）、Phase-2 grill Round 2（isLatestTurn gap）、3 后续票执行、Typert Remote sprint（多 session）。


---

## 2026-09-10 session update（Phase C 线 A：UM10 + UM16 resolved；UM12 成 frontier）

**线 A 实跑结论**（票据详情在 [UM10](UM10-verify-typecheck-lint-ci-gates.md) Resolution，此处只索引）：

| gate | 结果 |
|---|---|
| `tsc -b tsconfig.client.json --force` | **1 → 0**。Phase-2 记的「144→0」少算一个，`git blame` 证该错出自 `eb9e4cf05c` 自身 |
| `pnpm run build:official` | **GREEN** → **UM16 resolved**，原记 325 apiproxy-removal errors 已全清 |
| full `lint`（typeAware） | **93 errors**（判 pre-existing：oxlint 解不出 Cordis service handle，tsc = 0 反证）→ 毕业 2 票 |
| `check:ci:static` | **19/26 → 23 passed/22 failed**，无新增失败 |

**Phase 状态更新**：

- **Phase A / B 全部 resolved**：UM13 ✓、UM14 ✓、UM-ARCH ✓、UM-CORDIS-REGEN ✓(partial，子任务 3 deferred)、UM16 ✓、R-DA Phase-1+2 ✓、R-DA-UI-PRESENTER-COMPOSITION ✓。**UM-ADAPT 仍开**（per-shift 收尾）。
- **Phase C 进度**：UM10 ✓ → **UM12 = 当前 frontier**（unblocked）→ UM11 仍 blocked。UM15 并行可做。
- **flow 图的 blocking 需订正**：票据 header 里 UM12 写 `Blocked by: UM11`，与 UM11 的 `Blocked by: UM10 + UM12` **互锁成环**。以本 doc mermaid 的 `UM10 → UM12 → UM11` 为准，两票 header 已订正。

**新毕业 2 票**（均 unblocked，可并行 AFK）：
- [UM-DATA-SRC-DTS-POLLUTION](UM-DATA-SRC-DTS-POLLUTION.md) — 84 个生成物写进 `packages/data/*/src/`，把 lint 从 93 抬到 1980；需定位产出者而非只加 gitignore。**blocks 下一票**。
- [UM-LINT-TYPEAWARE-CORDIS](UM-LINT-TYPEAWARE-CORDIS-false-positives.md) — 93 条 `error`-typed 假阳性怎么算过 gate（A 修解析 / B 作用域 disable / C 关规则或记 known-red）。

**重评解除的 blocking**（2026-09-10）：
- **UM4 unblocked**（UM1/UM3 archived + R-DA 完成）。且 UM10 发现 UM4 的 results-RPC re-home 留尾：bundle 缺 `@deepseek-ai/dsh-result-cache` 依赖声明 + `tsconfig.base.json` 缺 `/src/*` 映射 → `verify-cordis-config` 红。
- **UM6** 仅剩 UM4 一个前置。
- **A23（worktree/branch 收尾）unblocked**——原阻塞的 9 个并行 session worktree 实测现存 **0**。清理清单已落 [UM11](UM11-pr-merge-post-cleanup.md)。

**喂给 UM15 的方法论教训**：`verify-architecture-graph` **不在** `check:ci:static` 组内，所以 static sweep 抓不到架构图 stale——而删包必然让它 stale。UM15 的 durable 方法须把「包增删 → 触发 regen 清单」形式化，且把组外 gate 纳入 sweep。

---

## 2026-09-14 session update（Phase C 并行：4 subagent + 主 session 落地；27/18；3 票收口 + 新票 UM-MERGE-INTEGRITY）

**实跑结论**（详情在各票 Resolution）：

| 维度 | 结果 |
|---|---|
| `check:ci:static` | **23/22 → 27 passed/18 failed**，`comm` 比对零新增失败（两轮独立复核） |
| 翻绿门 | `Cordis config`、`cordis catalog`、`doc budgets`、`documentation site checks`（4 门） |
| full lint | **93 errors + 1 warning**（稳定、无产物污染基线） |
| resync 工作树 | **首次 `git status` 完全干净（0 行）** —— 此前多 session 带 84 untracked |
| `pnpm install --frozen-lockfile` | **PASS**（此前 CI install 本来就挂，Phase-2 删 client/runtime 未重生成 lockfile） |

**收口 3 票**：
- [UM4](UM4-apiproxy-rehome-results-rpc-remote.md) **Scope2 收口**（commit `025db697ab`，`verify-cordis-config` GREEN）。Scope3（A6 race）仍 open。
- [UM-DATA-SRC-DTS-POLLUTION](UM-DATA-SRC-DTS-POLLUTION.md) **resolved**（commit `6514ade8fc`）。票据首要嫌疑**被证伪**：37 个 tsconfig 全同构，无配置可修，产出者=单次手发裸 tsc（取证：`src/` 与 `lib/types/` 副本逐字节相同，唯一差别在 `.d.ts.map` sources 字段）。用户选全面清理：删 84+identity 4+scope-registry 4 已提交产物 + 移除 `.gitignore:52-60` + `.oxlintrc.json` 加护栏。lint 1980→93，护栏后仍 93（不吞真信号）。
- [UM12](UM12-post-merge-ga-fork-ci-resweep.md) **未 resolved 但大幅前进**：13 门「待逐门归因」**全部归因**，三个主根因（RC-Z 僵尸复活 / RC-P2 Phase-2 残留 / RC-M merge 期错位）解释 9 门。订正本票两处自记错误：`config catalog` 归因串到了 `Cordis inspect catalog`；「`gen-* --check` ⇒ regen 可解」对 4 门为假（预检 throw 在生成之前，且 `cordis catalog` 的 throw 还**掩盖了**真实 staleness）。

**新票 UM-MERGE-INTEGRITY**（[UM-MERGE-INTEGRITY-LOSSY-BOTH-WAYS.md](UM-MERGE-INTEGRITY-LOSSY-BOTH-WAYS.md)）：2026-09-07 merge **双向有损** —— 丢了 upstream 的 `ui-settings-models/src/client/{slot-contract,operations}.ts`（无普通 commit 删过）+ 复活了 upstream 已删的 `packages/examples/{jsonrpc-demo,agent-spine-demo}`（三个删除 commit 均为 `d347e70390` 的 ancestor）。**两个方向都逃过 `tsc`**（丢的文件无 fork 侧 importer；多的包能独立编译），所以 `build:official` 绿对 merge 完整性**什么都没证明**。**硬阻塞 UM11**。需枚举完整集合（只读 git，适合 subagent）。

**两个此前无人记录的阻塞**：
1. **CI 的 install 步骤在本分支上原本就失败**（Phase-2 删 client/runtime 未重生成 lockfile）→ CI 里一门 gate 都跑不起来 → UM10/UM12 的所有 gate 数字都是**纯本地测量**。`40449bfa93` 已修。
2. **`~/.gitconfig core.symlinks=false`** 让两树共 16 个 tracked symlink 落成路径文本（确证波及 1 门，非仓库缺陷，修 checkout 不产生 tracked 改动）。每次新 checkout 必重修。

**UM15**：S4 subagent 交了覆盖 6 块的完整设计草案，落盘 [`research/um15-durable-sync-design-2026-09-10.md`](../../research/um15-durable-sync-design-2026-09-10.md)（69KB）。用户已定 **Decision 7 = (b)**：首片做 §1 staleness + §6 regen 清单 + meta-gate。主 session 补了 §2.4.bis「merge 完整性门」（来自 UM-MERGE-INTEGRITY 教训）。仍需 grilling 7 项。

**Phase 状态**：
- Phase C：UM10 ✓ → UM12 大幅前进（27/18，仍 open）→ UM11 仍 blocked（**新增硬阻塞 UM-MERGE-INTEGRITY**）→ UM15 首片待实现 + 7 项 grilling。
- **新毕业票**：无（UM-MERGE-INTEGRITY 是 Phase C 自身发现，非毕业）。
- **下 session 主轴**：见 [`prompts/next-session-2026-09-14-phase-c-merge-integrity-5gates-um15.md`](../prompts/next-session-2026-09-14-phase-c-merge-integrity-5gates-um15.md) —— 3 条并行线（UM-MERGE-INTEGRITY 枚举 / 5 门红 / UM15 首片）。

**残留 18 门红**（5 门有精确 patch 待落，见 UM12 待办表）：`runtime closure`、`constraints`、`export jsdoc`(3)、`translation pairing`、`client UI i18n`、`package dependencies`(74)、`package invariants`、`config catalog`、`tsconfig paths`、`application entrypoints`、`subsystem pages`、`package README model experience`、`agent note format`、`markdown links`(剩 1)、`doc graphs`、`documentation standard tests`、`type equivalence`、`Cordis inspect catalog`。

---

## 2026-09-14 session update（第二轮 Phase C：3 subagent 并行 + 主 session 串行落地；31/14）

**实跑结论**（详情各在其票 Resolution，此处只索引）：

| 维度 | 结果 |
|---|---|
| `check:ci:static` | **27/18 → 31 passed/14 failed**，`comm` 比对**零新增失败** |
| 翻绿门 | `doc graphs`、`Cordis inspect catalog`、`config catalog`、`package README model experience`（4 门） |
| 减少门 | `application entrypoints` 13→10、`subsystem pages` 6→5 |
| commits（resync） | `4b7e15e920`（L8）、`4d6bb8be8b`（L4）、`bcf4776f1d`（僵尸删除 + RC-P2 allowlist） |

### ⚠ 基点勘误（影响此前所有 pre-existing 判定）

`558e6f4f66` **不是 fork parent**，它是 merge#1 `6b7610d45a` 的**后代**（`merge-base --is-ancestor` = YES）。真 fork parent 是 **`65bf3cddc9`**。且**有两次 upstream merge**（M1=`6b7610d45a` 吸收 `d347e70390`；M2=`8112743d69` 吸收 `c389f96bf3`），两窗口首尾相接无重叠。此前在 `558e6f4f66` 上做的"正面确证"只能排除 M2，**不能排除 M1** —— 而 UM-MERGE-INTEGRITY 归咎的正是 M1。

### 三票进展

- **[UM-MERGE-INTEGRITY](UM-MERGE-INTEGRITY-LOSSY-BOTH-WAYS.md)**：两方向均穷举。方向 B = M1 复活 100 条 / M2 为 0，活 25 条 5 组（票原记 2 组），2 组已删、2 组零门收益有意 defer、1 组与包 re-port 耦合。方向 A 文件级恰好 2 条全归 M1，判误丢（~95%）但**修文档不恢复文件**（20/27 个回退文件已同时偏离 base 与 upstream，恢复 = 真特性 merge）。**最大发现超出原框：M1 把整个 `ui-settings-models` 包回退到 merge-base（≈30 文件），`git diff-tree --cc` 结构上看不见，`tsc` 全绿。**
- **[UM12](UM12-post-merge-ga-fork-ci-resweep.md)**：pre-merge 基线 **28/9** 建立 → 18 门红分成 **A6/B5/C7**。推翻本票三条自记结论（详见票）。另发现 **pre-merge 的 `--frozen-lockfile` 是干净的** → 「CI install 挂」的窗口**起于 merge**，不在之前；且 `cordis catalog` + `module graph` 两门 pre-merge 红、现已绿（merge 期工作修好了两门 pre-existing gate，此前无人记录）。
- **[UM15](UM15-durable-upstream-sync-method.md)**：新增两条 durable-method 输入 —— ① **完整性门要三道不是两道**（删除未应用 / 新增被丢弃 / **修改被回退**，第三道抓的正是整包回退）② **同仓生成器对翻译义务不一致**（`gen-module-graph` 写三件，`gen-doc-graphs` 只写英文 → 任何 regen 必破 `translation pairing`）。

### 需要决策（非机械，已攒给人）

1. **`tsconfig paths`**：门推荐的 `gen-tsconfig-paths` 实跑产出**无效 JSON**（fork 的 `"@deepseek-ai/dsh-*"` 通配块在生成区之后，生成器不写尾逗号）。「fork 保留通配 vs 采纳 upstream ~120 条显式 alias」是设计取舍。
2. **生成文档的翻译义务**：`gen-doc-graphs` 的 6 个英文产物各有人工评审 `.zh.md`。是补译 / 让生成器带上 zh / 还是把生成文档从配对哈希里豁免？
3. **`ui-settings-models` 整包 re-port**：需新票，且 UM11 的 PR 描述必须写明这条，否则仍是在有损 merge 上声称非回归。

### Phase 状态

Phase C：UM10 ✓ → UM12 大幅前进（31/14，仍 open）→ UM11 仍 blocked（UM-MERGE-INTEGRITY 已从"未穷举"降级为"已量化"，但整包回退需入 PR 描述）→ UM15 首片实现仍待落地 + 7 项 grilling 未过。
