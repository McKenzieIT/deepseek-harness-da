# G2 — eval 迁 TS vs 保留 Python

**Type**: grilling
**Phase**: 4
**Status**: Unblocked

**Question**: `packages/eval/`（TS）vs 保留 Python rbi-eval？影响 python/ 去留。

**Note**: R3 偏 Python（JSON-RPC SDK `Session.run()`）→ 此 grilling 有 R3 的 research 支撑。
