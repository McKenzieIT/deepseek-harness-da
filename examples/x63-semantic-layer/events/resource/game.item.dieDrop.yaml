name: game.item.dieDrop
event_filter: event = 'game.item.dieDrop'
description: '角色死亡掉落：产生物品掉落时记录（特指角色身上掉落到地面的情况）

  参数：掉落id、掉落列表'
domains:
- 装备道具
params_fields:
  dropId:
    type: string
    description: '产生掉落时生成，格式如：

      ''"drop_" + droper_eid + "_" +  timestamp'
  dropList:
    type: array
    description: '格式如：

      [

      {

      itemId：

      itemDur：

      belongTo：

      },

      ……

      ]'
daily_count: 4.0
total_count: 16
active_days: 4
tech_domain: role_private
confirmation:
  status: draft
  confirmed_by: onboarding-10000334-event-bootstrap
  confirmed_at: '2026-07-27'
coverage:
  scenarios:
  - 角色死亡掉落 事件次数与独立角色数趋势（按 ds/hh）
  limitations:
  - 事件定义来自 10000334司测埋点.xlsx（参数枚举标注『待补充』的以实际日志为准）；daily_count/total_count 来自 MaxCompute ds=20260628..20260726 司测前联调日志（约 6
    个活跃日），量级不代表司测/上线后水平；正式司测窗口为 20260727-20260728，司测结束后应重跑分布刷新量级
  - 主体粒度默认按 role_id 统计 UV，未逐事件 maxc 实测复核
  coverage_status: draft
