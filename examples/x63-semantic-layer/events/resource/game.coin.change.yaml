name: game.coin.change
event_filter: event = 'game.coin.change'
description: '货币变更：货币发生变更时记录（含局内、局外）

  参数：货币id、变更行为、变更数量、变更前数量、变更后数量、变更原因1、变更原因2、变更原因3'
domains:
- 资源产销
params_fields:
  coinId:
    type: int
    description: 对应Virtual配置表中的id
  action:
    type: int
    description: 1-获得；2-消耗
  amount:
    type: int
    description: 变更数量
  beforeAmount:
    type: int
    description: 变更前数量
  afterAmount:
    type: int
    description: 变更后数量
  reason1:
    type: string
    description: 详见下面的原因枚举（制作时持续补充）
  reason2:
    type: string
    description: 变更原因2
  reason3:
    type: string
    description: 变更原因3
daily_count: 40.3
total_count: 242
active_days: 6
tech_domain: pay
confirmation:
  status: draft
  confirmed_by: onboarding-10000334-event-bootstrap
  confirmed_at: '2026-07-27'
coverage:
  scenarios:
  - 货币变更 事件次数与独立角色数趋势（按 ds/hh）
  limitations:
  - 事件定义来自 10000334司测埋点.xlsx（参数枚举标注『待补充』的以实际日志为准）；daily_count/total_count 来自 MaxCompute ds=20260628..20260726 司测前联调日志（约 6
    个活跃日），量级不代表司测/上线后水平；正式司测窗口为 20260727-20260728，司测结束后应重跑分布刷新量级
  - 主体粒度默认按 role_id 统计 UV，未逐事件 maxc 实测复核
  coverage_status: draft
