name: game.battle.pve.start
event_filter: event = 'game.battle.pve.start'
description: '进入PVE战斗：角色触发怪物的主动仇恨时记录

  说明：需要维护一个白名单列表，只针对列表中的怪物进行打点

  参数：怪物id、战斗id、进战时的hp、使用的员工id、携带的物品id

  （含腰包和扩展背包中的物品）'
domains:
- 战斗关卡
params_fields:
  npcId:
    type: int
    description: 对应NPC配置表中的id
  reatedBattleId:
    type: string
    description: 唯一标识，标识同一场战斗
  hp:
    type: int
    description: 进入战斗时的角色hp
  equipedParagonId:
    type: int
    description: 使用的员工id
  equipItemList:
    type: array
    description: '格式如：

      [

      {

      equipItemSlot：装备槽位id（如果放腰包中，则是物品Uid）

      equipItemSubSlot：子背包槽位id，

      equipItemId：装备物品id，

      equipItemUid：装备物品唯一id，

      equipItemDur：装备物品耐久，

      },

      ……

      ]'
daily_count: 190.5
total_count: 762
active_days: 4
tech_domain: role_private
confirmation:
  status: draft
  confirmed_by: onboarding-10000334-event-bootstrap
  confirmed_at: '2026-07-27'
coverage:
  scenarios:
  - 进入PVE战斗 事件次数与独立角色数趋势（按 ds/hh）
  limitations:
  - 事件定义来自 10000334司测埋点.xlsx（参数枚举标注『待补充』的以实际日志为准）；daily_count/total_count 来自 MaxCompute ds=20260628..20260726 司测前联调日志（约 6
    个活跃日），量级不代表司测/上线后水平；正式司测窗口为 20260727-20260728，司测结束后应重跑分布刷新量级
  - 主体粒度默认按 role_id 统计 UV，未逐事件 maxc 实测复核
  coverage_status: draft
