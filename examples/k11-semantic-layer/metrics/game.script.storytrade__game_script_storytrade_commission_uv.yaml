kind: metric
name: game.script.storytrade__game_script_storytrade_commission_uv
description: 触发的 distinct 委托id数（去重 GET_JSON_OBJECT(params,'$.id')）
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.id'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.id')
    source: game.script.storytrade
    time_grain: ""
relations:
  - type: derived_from
    target: game.script.storytrade
    description: 机械提取自 game.script.storytrade 的 metrics 块（key=game_script_storytrade_commission_uv）
