kind: metric
name: game.champion.sendFlyTxt__champion_sendfly_txt_role_uv
description: 发送弹幕的角色去重数（分析主体粒度 role_id）
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.champion.sendFlyTxt
    time_grain: ""
relations:
  - type: derived_from
    target: game.champion.sendFlyTxt
    description: 机械提取自 game.champion.sendFlyTxt 的 metrics 块（key=champion_sendfly_txt_role_uv）
