kind: metric
name: dws_10000251_role_summary_base_di__pay_role_uv
description: 累计付费角色数（当日增量角色中累计付费金额 > 0 的角色数）
domains:
  - 付费经济
  - 用户生命周期
computation:
  sql: SUM(CASE WHEN pay_amt_std > 0 THEN 1 ELSE 0 END)
  metadata:
    aggregation: sum
    field: CASE WHEN pay_amt_std > 0 THEN 1 ELSE 0 END
    source: dws_10000251_role_summary_base_di
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_role_summary_base_di
    description: 机械提取自 dws_10000251_role_summary_base_di 的 metrics 块（key=pay_role_uv）
