kind: metric
name: game.charmStone.setLock__game_charmStone_setLock_unlock_cnt
description: 灵石被解锁(isLock=false)的次数
domains:
  - 探索收集
computation:
  sql: SUM(CASE WHEN GET_JSON_OBJECT(params,'$.charmStone.isLock')='false' THEN 1 ELSE 0 END)
  metadata:
    aggregation: sum
    field: CASE WHEN GET_JSON_OBJECT(params,'$.charmStone.isLock')='false' THEN 1 ELSE 0 END
    source: game.charmStone.setLock
    time_grain: ""
relations:
  - type: derived_from
    target: game.charmStone.setLock
    description: 机械提取自 game.charmStone.setLock 的 metrics 块（key=game_charmStone_setLock_unlock_cnt）
