kind: metric
name: game.team.setTeamName__game_team_setTeamName_account_uv
description: 触发命名操作的去重账号数
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT account_id)
  metadata:
    aggregation: count_distinct
    field: account_id
    source: game.team.setTeamName
    time_grain: ""
relations:
  - type: derived_from
    target: game.team.setTeamName
    description: 机械提取自 game.team.setTeamName 的 metrics 块（key=game_team_setTeamName_account_uv）
