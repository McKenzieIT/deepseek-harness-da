kind: metric
name: game.battle.quit__game_battle_quit_dungeon_uv
description: 被放弃的副本种类数
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.dungeonId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.dungeonId')
    source: game.battle.quit
    time_grain: ""
relations:
  - type: derived_from
    target: game.battle.quit
    description: 机械提取自 game.battle.quit 的 metrics 块（key=game_battle_quit_dungeon_uv）
