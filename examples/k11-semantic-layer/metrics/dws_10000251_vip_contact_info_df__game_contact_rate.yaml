kind: metric
name: dws_10000251_vip_contact_info_df__game_contact_rate
description: 游戏内建联率（已建联账号数/总账号数）
domains:
  - 用户生命周期
computation:
  sql: SUM(CASE WHEN is_game_contact=1 THEN 1 ELSE 0 END) / NULLIF(COUNT(*),0)
  metadata:
    aggregation: sum
    field: CASE WHEN is_game_contact=1 THEN 1 ELSE 0 END) / NULLIF(COUNT(*),0
    source: dws_10000251_vip_contact_info_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_vip_contact_info_df
    description: 机械提取自 dws_10000251_vip_contact_info_df 的 metrics 块（key=game_contact_rate）
