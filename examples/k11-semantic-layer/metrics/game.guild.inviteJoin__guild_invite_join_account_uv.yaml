kind: metric
name: game.guild.inviteJoin__guild_invite_join_account_uv
description: 发起邀请的去重账号数
domains:
  - 社交公会
computation:
  sql: COUNT(DISTINCT account_id)
  metadata:
    aggregation: count_distinct
    field: account_id
    source: game.guild.inviteJoin
    time_grain: ""
relations:
  - type: derived_from
    target: game.guild.inviteJoin
    description: 机械提取自 game.guild.inviteJoin 的 metrics 块（key=guild_invite_join_account_uv）
