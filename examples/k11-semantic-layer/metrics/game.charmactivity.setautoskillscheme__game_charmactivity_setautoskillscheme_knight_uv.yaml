kind: metric
name: game.charmactivity.setautoskillscheme__game_charmactivity_setautoskillscheme_knight_uv
description: 涉及的骑士去重数（knightId）
domains:
  - 探索收集
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params,'$.knightId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params,'$.knightId')
    source: game.charmactivity.setautoskillscheme
    time_grain: ""
relations:
  - type: derived_from
    target: game.charmactivity.setautoskillscheme
    description: 机械提取自 game.charmactivity.setautoskillscheme 的 metrics 块（key=game_charmactivity_setautoskillscheme_knight_uv）
