kind: metric
name: game.rogue.elementSelectRefresh__game_rogue_elementSelectRefresh_avg_per_role
description: 平均每角色刷新元素选择的次数(人均刷新频率)
domains:
  - 战斗关卡
computation:
  sql: COUNT(*) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: count
    field: "*) / NULLIF(COUNT(DISTINCT role_id), 0"
    source: game.rogue.elementSelectRefresh
    time_grain: ""
relations:
  - type: derived_from
    target: game.rogue.elementSelectRefresh
    description: 机械提取自 game.rogue.elementSelectRefresh 的 metrics 块（key=game_rogue_elementSelectRefresh_avg_per_role）
