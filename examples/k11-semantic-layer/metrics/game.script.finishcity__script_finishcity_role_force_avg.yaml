kind: metric
name: game.script.finishcity__script_finishcity_role_force_avg
description: 完成方角色的平均战力
domains:
  - 自定义
computation:
  sql: AVG(CAST(GET_JSON_OBJECT(params, '$.roleForce') AS BIGINT))
  metadata:
    aggregation: avg
    field: CAST(GET_JSON_OBJECT(params, '$.roleForce') AS BIGINT)
    source: game.script.finishcity
    time_grain: ""
relations:
  - type: derived_from
    target: game.script.finishcity
    description: 机械提取自 game.script.finishcity 的 metrics 块（key=script_finishcity_role_force_avg）
