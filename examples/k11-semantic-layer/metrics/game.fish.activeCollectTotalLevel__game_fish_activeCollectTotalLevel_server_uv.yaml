kind: metric
name: game.fish.activeCollectTotalLevel__game_fish_activeCollectTotalLevel_server_uv
description: 触发一键领取图鉴总等级奖励的去重游戏服数
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: game.fish.activeCollectTotalLevel
    time_grain: ""
relations:
  - type: derived_from
    target: game.fish.activeCollectTotalLevel
    description: 机械提取自 game.fish.activeCollectTotalLevel 的 metrics 块（key=game_fish_activeCollectTotalLevel_server_uv）
