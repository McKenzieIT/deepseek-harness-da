kind: metric
name: game.rogue.choosesoul__game_rogue_choosesoul_role_uv
description: 触发将魂选择的角色数（去重）
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.rogue.choosesoul
    time_grain: ""
relations:
  - type: derived_from
    target: game.rogue.choosesoul
    description: 机械提取自 game.rogue.choosesoul 的 metrics 块（key=game_rogue_choosesoul_role_uv）
