kind: metric
name: game.yanwu.match__yanwu_match_avg_duration
description: 演武对战平均耗时(秒)
domains:
  - 战斗关卡
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params,'$.duration') AS BIGINT)) / NULLIF(COUNT(*), 0)
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params,'$.duration') AS BIGINT)) / NULLIF(COUNT(*), 0
    source: game.yanwu.match
    time_grain: ""
relations:
  - type: derived_from
    target: game.yanwu.match
    description: 机械提取自 game.yanwu.match 的 metrics 块（key=yanwu_match_avg_duration）
