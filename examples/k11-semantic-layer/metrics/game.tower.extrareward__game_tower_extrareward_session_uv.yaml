kind: metric
name: game.tower.extrareward__game_tower_extrareward_session_uv
description: 去重奖励会话 sessionId 数（粗略反映独立奖励批次）
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params,'$.sessionId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params,'$.sessionId')
    source: game.tower.extrareward
    time_grain: ""
relations:
  - type: derived_from
    target: game.tower.extrareward
    description: 机械提取自 game.tower.extrareward 的 metrics 块（key=game_tower_extrareward_session_uv）
