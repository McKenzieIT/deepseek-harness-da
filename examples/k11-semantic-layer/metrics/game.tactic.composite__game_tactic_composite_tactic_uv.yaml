kind: metric
name: game.tactic.composite__game_tactic_composite_tactic_uv
description: 被合成的去重战法种类数（tacticId）
domains:
  - 角色成长
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params,'$.tacticId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params,'$.tacticId')
    source: game.tactic.composite
    time_grain: ""
relations:
  - type: derived_from
    target: game.tactic.composite
    description: 机械提取自 game.tactic.composite 的 metrics 块（key=game_tactic_composite_tactic_uv）
