kind: metric
name: dws_10000251_univ_acc_server_tag_df__pay_rate
description: 付费转化率(付费账号数/活跃账号数),按行级 AVG 近似;建议配合 pay_acc_cnt/active_acc_cnt 复核;pay_amt_std 重名列须先消歧
domains:
  - 用户生命周期
  - 付费经济
  - 系统监控
computation:
  sql: AVG(CASE WHEN pay_amt_std > 0 THEN 1 ELSE 0 END)
  metadata:
    aggregation: avg
    field: CASE WHEN pay_amt_std > 0 THEN 1 ELSE 0 END
    source: dws_10000251_univ_acc_server_tag_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_univ_acc_server_tag_df
    description: 机械提取自 dws_10000251_univ_acc_server_tag_df 的 metrics 块（key=pay_rate）
