kind: metric
name: dws_10000251_univ_dev_tag_df__device_cnt
description: 设备数(本表固定 user_type=1,直接 COUNT DISTINCT user_id 即为设备数,跨分区须锁定单一 ds)
domains:
  - 用户生命周期
  - 付费经济
  - 角色成长
computation:
  sql: COUNT(DISTINCT user_id)
  metadata:
    aggregation: count_distinct
    field: user_id
    source: dws_10000251_univ_dev_tag_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_univ_dev_tag_df
    description: 机械提取自 dws_10000251_univ_dev_tag_df 的 metrics 块（key=device_cnt）
