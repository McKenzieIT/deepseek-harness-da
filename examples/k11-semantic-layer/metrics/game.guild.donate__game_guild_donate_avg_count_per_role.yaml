kind: metric
name: game.guild.donate__game_guild_donate_avg_count_per_role
description: 人均捐献次数（count_sum / role_uv，NULLIF 防零除）
domains:
  - 社交公会
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.count') AS BIGINT)) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.count') AS BIGINT)) / NULLIF(COUNT(DISTINCT role_id), 0
    source: game.guild.donate
    time_grain: ""
relations:
  - type: derived_from
    target: game.guild.donate
    description: 机械提取自 game.guild.donate 的 metrics 块（key=game_guild_donate_avg_count_per_role）
