kind: metric
name: dws_10000251_vip_acc_tag_df__pay_arpu_l30d
description: 最近30天付费账号ARPU（总金额/付费账号数，NULLIF防除零）
domains:
  - 付费经济
  - 用户生命周期
computation:
  sql: SUM(pay_amt_l30d) / NULLIF(COUNT(DISTINCT CASE WHEN pay_amt_l30d > 0 THEN user_id END), 0)
  metadata:
    aggregation: sum
    field: pay_amt_l30d) / NULLIF(COUNT(DISTINCT CASE WHEN pay_amt_l30d > 0 THEN user_id END), 0
    source: dws_10000251_vip_acc_tag_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_vip_acc_tag_df
    description: 机械提取自 dws_10000251_vip_acc_tag_df 的 metrics 块（key=pay_arpu_l30d）
