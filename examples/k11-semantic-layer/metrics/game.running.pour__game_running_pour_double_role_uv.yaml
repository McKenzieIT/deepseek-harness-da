kind: metric
name: game.running.pour__game_running_pour_double_role_uv
description: 使用倍投(useDouble=true)的去重角色数
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT CASE WHEN GET_JSON_OBJECT(params, '$.useDouble') = 'true' THEN role_id END)
  metadata:
    aggregation: count_distinct
    field: CASE WHEN GET_JSON_OBJECT(params, '$.useDouble') = 'true' THEN role_id END
    source: game.running.pour
    time_grain: ""
relations:
  - type: derived_from
    target: game.running.pour
    description: 机械提取自 game.running.pour 的 metrics 块（key=game_running_pour_double_role_uv）
