kind: metric
name: dws_10000251_acc_summary_df__arppu_std
description: 累计付费账号ARPU（元/付费账号）：累计付费总金额 / 累计付费账号数
domains:
  - 用户生命周期
  - 付费经济
computation:
  sql: SUM(pay_amt_std) / NULLIF(COUNT(DISTINCT CASE WHEN pay_amt_std > 0 THEN account_id END), 0)
  metadata:
    aggregation: sum
    field: pay_amt_std) / NULLIF(COUNT(DISTINCT CASE WHEN pay_amt_std > 0 THEN account_id END), 0
    source: dws_10000251_acc_summary_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_acc_summary_df
    description: 机械提取自 dws_10000251_acc_summary_df 的 metrics 块（key=arppu_std）
