kind: metric
name: game.waractivity.burning__burning_avg_candle_per_role
description: 人均点亮蜡烛数（蜡烛总数/参与角色数）
domains:
  - 自定义
computation:
  sql: CAST(SUM(CAST(GET_JSON_OBJECT(params, '$.caddleNum') AS BIGINT)) AS DOUBLE) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: ""
    field: ""
    source: game.waractivity.burning
    time_grain: ""
relations:
  - type: derived_from
    target: game.waractivity.burning
    description: 机械提取自 game.waractivity.burning 的 metrics 块（key=burning_avg_candle_per_role）
