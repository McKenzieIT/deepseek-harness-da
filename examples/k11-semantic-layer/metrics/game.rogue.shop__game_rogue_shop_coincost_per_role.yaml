kind: metric
name: game.rogue.shop__game_rogue_shop_coincost_per_role
description: 人均战场币消费（消费总量/去重角色数）
domains:
  - 战斗关卡
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params,'$.coinCost') AS BIGINT)) / NULLIF(COUNT(DISTINCT role_id),0)
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params,'$.coinCost') AS BIGINT)) / NULLIF(COUNT(DISTINCT role_id),0
    source: game.rogue.shop
    time_grain: ""
relations:
  - type: derived_from
    target: game.rogue.shop
    description: 机械提取自 game.rogue.shop 的 metrics 块（key=game_rogue_shop_coincost_per_role）
