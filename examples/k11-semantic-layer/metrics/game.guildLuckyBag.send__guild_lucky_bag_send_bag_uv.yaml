kind: metric
name: game.guildLuckyBag.send__guild_lucky_bag_send_bag_uv
description: 发送的去重福袋uid数
domains:
  - 社交公会
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.bagUid'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.bagUid')
    source: game.guildLuckyBag.send
    time_grain: ""
relations:
  - type: derived_from
    target: game.guildLuckyBag.send
    description: 机械提取自 game.guildLuckyBag.send 的 metrics 块（key=guild_lucky_bag_send_bag_uv）
