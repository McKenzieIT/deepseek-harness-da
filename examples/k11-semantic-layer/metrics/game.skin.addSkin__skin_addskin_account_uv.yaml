kind: metric
name: game.skin.addSkin__skin_addskin_account_uv
description: 添加皮肤的活跃账号数（去重 account_id，账号维度补充）
domains:
  - 装备道具
computation:
  sql: COUNT(DISTINCT account_id)
  metadata:
    aggregation: count_distinct
    field: account_id
    source: game.skin.addSkin
    time_grain: ""
relations:
  - type: derived_from
    target: game.skin.addSkin
    description: 机械提取自 game.skin.addSkin 的 metrics 块（key=skin_addskin_account_uv）
