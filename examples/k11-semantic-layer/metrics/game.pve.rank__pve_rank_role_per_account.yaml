kind: metric
name: game.pve.rank__pve_rank_role_per_account
description: 平均每账号参与角色数(独立角色/独立账号；衡量多角色账号占比)
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT role_id) / NULLIF(COUNT(DISTINCT account_id), 0)
  metadata:
    aggregation: count_distinct
    field: role_id) / NULLIF(COUNT(DISTINCT account_id), 0
    source: game.pve.rank
    time_grain: ""
relations:
  - type: derived_from
    target: game.pve.rank
    description: 机械提取自 game.pve.rank 的 metrics 块（key=pve_rank_role_per_account）
