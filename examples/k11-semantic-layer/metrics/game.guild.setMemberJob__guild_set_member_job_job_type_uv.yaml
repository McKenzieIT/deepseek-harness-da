kind: metric
name: game.guild.setMemberJob__guild_set_member_job_job_type_uv
description: 涉及去重职位类型数
domains:
  - 社交公会
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.job'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.job')
    source: game.guild.setMemberJob
    time_grain: ""
relations:
  - type: derived_from
    target: game.guild.setMemberJob
    description: 机械提取自 game.guild.setMemberJob 的 metrics 块（key=guild_set_member_job_job_type_uv）
