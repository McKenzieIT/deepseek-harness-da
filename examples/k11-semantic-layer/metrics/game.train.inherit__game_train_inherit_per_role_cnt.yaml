kind: metric
name: game.train.inherit__game_train_inherit_per_role_cnt
description: 人均武将继承次数（操作次数 / 去重角色数）
domains:
  - 角色成长
computation:
  sql: COUNT(*) * 1.0 / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: count
    field: "*) * 1.0 / NULLIF(COUNT(DISTINCT role_id), 0"
    source: game.train.inherit
    time_grain: ""
relations:
  - type: derived_from
    target: game.train.inherit
    description: 机械提取自 game.train.inherit 的 metrics 块（key=game_train_inherit_per_role_cnt）
