kind: metric
name: game.script.finishtalk__game_script_finishtalk_cnt
description: 剧本对话完成事件总次数（每次完成一段对话计一次）
domains:
  - 自定义
computation:
  sql: COUNT(*)
  metadata:
    aggregation: count
    field: "*"
    source: game.script.finishtalk
    time_grain: ""
relations:
  - type: derived_from
    target: game.script.finishtalk
    description: 机械提取自 game.script.finishtalk 的 metrics 块（key=game_script_finishtalk_cnt）
