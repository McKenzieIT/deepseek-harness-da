kind: metric
name: game.rogue.pointLvlup__game_rogue_pointLvlup_new_level_max
description: 周期内角色达到的最高军功新等级
domains:
  - 战斗关卡
computation:
  sql: MAX(CAST(GET_JSON_OBJECT(params, '$.newLevel') AS BIGINT))
  metadata:
    aggregation: max
    field: CAST(GET_JSON_OBJECT(params, '$.newLevel') AS BIGINT)
    source: game.rogue.pointLvlup
    time_grain: ""
relations:
  - type: derived_from
    target: game.rogue.pointLvlup
    description: 机械提取自 game.rogue.pointLvlup 的 metrics 块（key=game_rogue_pointLvlup_new_level_max）
