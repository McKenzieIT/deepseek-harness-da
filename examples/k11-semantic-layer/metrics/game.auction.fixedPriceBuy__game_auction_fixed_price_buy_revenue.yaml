kind: metric
name: game.auction.fixedPriceBuy__game_auction_fixed_price_buy_revenue
description: 一口价购买总金额(price 累加)
domains:
  - 付费经济
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params,'$.price') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params,'$.price') AS BIGINT)
    source: game.auction.fixedPriceBuy
    time_grain: ""
relations:
  - type: derived_from
    target: game.auction.fixedPriceBuy
    description: 机械提取自 game.auction.fixedPriceBuy 的 metrics 块（key=game_auction_fixed_price_buy_revenue）
