kind: metric
name: game.social.guild.addpoints__guild_addpoints_cnt
description: 公会活跃值增加事件触发总次数
domains:
  - 社交公会
computation:
  sql: COUNT(*)
  metadata:
    aggregation: count
    field: "*"
    source: game.social.guild.addpoints
    time_grain: ""
relations:
  - type: derived_from
    target: game.social.guild.addpoints
    description: 机械提取自 game.social.guild.addpoints 的 metrics 块（key=guild_addpoints_cnt）
