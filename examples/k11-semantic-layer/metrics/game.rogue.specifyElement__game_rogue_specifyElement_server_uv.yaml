kind: metric
name: game.rogue.specifyElement__game_rogue_specifyElement_server_uv
description: 触发指定元素的服务器去重数
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: game.rogue.specifyElement
    time_grain: ""
relations:
  - type: derived_from
    target: game.rogue.specifyElement
    description: 机械提取自 game.rogue.specifyElement 的 metrics 块（key=game_rogue_specifyElement_server_uv）
