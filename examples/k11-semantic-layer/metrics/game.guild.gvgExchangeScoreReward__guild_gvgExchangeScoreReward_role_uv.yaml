kind: metric
name: game.guild.gvgExchangeScoreReward__guild_gvgExchangeScoreReward_role_uv
description: 触发兑换积分奖励的去重角色数（分析主体粒度=role_id）
domains:
  - 社交公会
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.guild.gvgExchangeScoreReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.guild.gvgExchangeScoreReward
    description: 机械提取自 game.guild.gvgExchangeScoreReward 的 metrics 块（key=guild_gvgExchangeScoreReward_role_uv）
