kind: metric
name: game.guild.modifyInfo__game_guild_modifyInfo_per_role_modify_cnt
description: 人均公会信息修改次数（按角色去重），用于识别高频修改者
domains:
  - 社交公会
computation:
  sql: COUNT(*) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: count
    field: "*) / NULLIF(COUNT(DISTINCT role_id), 0"
    source: game.guild.modifyInfo
    time_grain: ""
relations:
  - type: derived_from
    target: game.guild.modifyInfo
    description: 机械提取自 game.guild.modifyInfo 的 metrics 块（key=game_guild_modifyInfo_per_role_modify_cnt）
