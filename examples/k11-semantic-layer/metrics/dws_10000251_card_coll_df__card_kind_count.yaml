kind: metric
name: dws_10000251_card_coll_df__card_kind_count
description: 在架卡牌种类数（去重card_id）
domains:
  - 探索收集
  - 角色成长
computation:
  sql: COUNT(DISTINCT card_id)
  metadata:
    aggregation: count_distinct
    field: card_id
    source: dws_10000251_card_coll_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_card_coll_df
    description: 机械提取自 dws_10000251_card_coll_df 的 metrics 块（key=card_kind_count）
