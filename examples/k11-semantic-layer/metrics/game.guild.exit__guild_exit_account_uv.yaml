kind: metric
name: game.guild.exit__guild_exit_account_uv
description: 退出公会的去重账号数（账号维度归因）
domains:
  - 社交公会
computation:
  sql: COUNT(DISTINCT account_id)
  metadata:
    aggregation: count_distinct
    field: account_id
    source: game.guild.exit
    time_grain: ""
relations:
  - type: derived_from
    target: game.guild.exit
    description: 机械提取自 game.guild.exit 的 metrics 块（key=guild_exit_account_uv）
