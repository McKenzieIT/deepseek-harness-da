kind: metric
name: game.springfestival.preheattrade__springfestival_preheattrade_trade_per_role
description: 人均购买商品次数（事件总行数 / 去重角色数）
domains:
  - 自定义
computation:
  sql: COUNT(*) * 1.0 / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: count
    field: "*) * 1.0 / NULLIF(COUNT(DISTINCT role_id), 0"
    source: game.springfestival.preheattrade
    time_grain: ""
relations:
  - type: derived_from
    target: game.springfestival.preheattrade
    description: 机械提取自 game.springfestival.preheattrade 的 metrics 块（key=springfestival_preheattrade_trade_per_role）
