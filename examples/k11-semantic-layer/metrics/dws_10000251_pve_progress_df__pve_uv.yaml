kind: metric
name: dws_10000251_pve_progress_df__pve_uv
description: 当日参与 PVE 的角色数（去重 UV）
domains:
  - 战斗关卡
  - 角色成长
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: dws_10000251_pve_progress_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_pve_progress_df
    description: 机械提取自 dws_10000251_pve_progress_df 的 metrics 块（key=pve_uv）
