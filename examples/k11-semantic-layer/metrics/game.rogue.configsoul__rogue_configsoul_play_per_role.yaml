kind: metric
name: game.rogue.configsoul__rogue_configsoul_play_per_role
description: 平均每个角色发起的古战场副本挑战次数(playId去重/角色数)
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.playId')) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.playId')) / NULLIF(COUNT(DISTINCT role_id), 0
    source: game.rogue.configsoul
    time_grain: ""
relations:
  - type: derived_from
    target: game.rogue.configsoul
    description: 机械提取自 game.rogue.configsoul 的 metrics 块（key=rogue_configsoul_play_per_role）
