kind: metric
name: game.shop.buy__shop_buy_goods_uv
description: 被购买的货物去重数
domains:
  - 付费经济
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params,'$.goodsId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params,'$.goodsId')
    source: game.shop.buy
    time_grain: ""
relations:
  - type: derived_from
    target: game.shop.buy
    description: 机械提取自 game.shop.buy 的 metrics 块（key=shop_buy_goods_uv）
