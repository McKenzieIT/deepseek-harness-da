kind: metric
name: game.scriptActivity.taskBattlepassMissionOnekeyReward__bp_onekey_reward_theme_uv
description: 涉及的 distinct 战令活动ID数（按 themeId 去重）
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.themeId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.themeId')
    source: game.scriptActivity.taskBattlepassMissionOnekeyReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.scriptActivity.taskBattlepassMissionOnekeyReward
    description: 机械提取自 game.scriptActivity.taskBattlepassMissionOnekeyReward 的 metrics 块（key=bp_onekey_reward_theme_uv）
