kind: metric
name: game.fish.applyAllow__game_fish_applyAllow_server_uv
description: 涉及同意申请操作的去重游戏服数
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: game.fish.applyAllow
    time_grain: ""
relations:
  - type: derived_from
    target: game.fish.applyAllow
    description: 机械提取自 game.fish.applyAllow 的 metrics 块（key=game_fish_applyAllow_server_uv）
