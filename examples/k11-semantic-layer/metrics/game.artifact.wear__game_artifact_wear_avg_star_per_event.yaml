kind: metric
name: game.artifact.wear__game_artifact_wear_avg_star_per_event
description: 单次穿戴平均兵符星级
domains:
  - 角色成长
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.artifact.star') AS BIGINT)) / NULLIF(COUNT(*), 0)
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.artifact.star') AS BIGINT)) / NULLIF(COUNT(*), 0
    source: game.artifact.wear
    time_grain: ""
relations:
  - type: derived_from
    target: game.artifact.wear
    description: 机械提取自 game.artifact.wear 的 metrics 块（key=game_artifact_wear_avg_star_per_event）
