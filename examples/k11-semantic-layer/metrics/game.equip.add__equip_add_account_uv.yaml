kind: metric
name: game.equip.add__equip_add_account_uv
description: 获取装备的去重账号数
domains:
  - 装备道具
computation:
  sql: COUNT(DISTINCT account_id)
  metadata:
    aggregation: count_distinct
    field: account_id
    source: game.equip.add
    time_grain: ""
relations:
  - type: derived_from
    target: game.equip.add
    description: 机械提取自 game.equip.add 的 metrics 块（key=equip_add_account_uv）
