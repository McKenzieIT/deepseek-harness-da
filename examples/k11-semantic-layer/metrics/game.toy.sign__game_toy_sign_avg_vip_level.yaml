kind: metric
name: game.toy.sign__game_toy_sign_avg_vip_level
description: 签到角色平均 vip 等级（vipLevel 均值，付费层级画像）
domains:
  - 探索收集
computation:
  sql: AVG(CAST(GET_JSON_OBJECT(params, '$.vipLevel') AS BIGINT))
  metadata:
    aggregation: avg
    field: CAST(GET_JSON_OBJECT(params, '$.vipLevel') AS BIGINT)
    source: game.toy.sign
    time_grain: ""
relations:
  - type: derived_from
    target: game.toy.sign
    description: 机械提取自 game.toy.sign 的 metrics 块（key=game_toy_sign_avg_vip_level）
