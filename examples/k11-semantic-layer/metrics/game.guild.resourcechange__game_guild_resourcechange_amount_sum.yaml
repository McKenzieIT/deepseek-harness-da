kind: metric
name: game.guild.resourcechange__game_guild_resourcechange_amount_sum
description: 公会资源变更量绝对值合计(amount 求和)
domains:
  - 社交公会
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params,'$.amount') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params,'$.amount') AS BIGINT)
    source: game.guild.resourcechange
    time_grain: ""
relations:
  - type: derived_from
    target: game.guild.resourcechange
    description: 机械提取自 game.guild.resourcechange 的 metrics 块（key=game_guild_resourcechange_amount_sum）
