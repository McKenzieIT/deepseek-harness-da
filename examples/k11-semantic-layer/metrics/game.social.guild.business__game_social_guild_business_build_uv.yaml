kind: metric
name: game.social.guild.business__game_social_guild_business_build_uv
description: 涉及的经营建筑物去重数（去重 buildId）
domains:
  - 社交公会
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params,'$.buildId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params,'$.buildId')
    source: game.social.guild.business
    time_grain: ""
relations:
  - type: derived_from
    target: game.social.guild.business
    description: 机械提取自 game.social.guild.business 的 metrics 块（key=game_social_guild_business_build_uv）
