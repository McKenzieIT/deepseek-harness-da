kind: metric
name: dws_10000251_univ_acc_tag_df__d30_retention_rate
description: 近30日连续活跃率(最近活跃间隔<=30天的账号占比,基于快照口径近似)
domains:
  - 用户生命周期
  - 付费经济
computation:
  sql: AVG(CASE WHEN act_ymd_diff<=30 THEN 1 ELSE 0 END)
  metadata:
    aggregation: avg
    field: CASE WHEN act_ymd_diff<=30 THEN 1 ELSE 0 END
    source: dws_10000251_univ_acc_tag_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_univ_acc_tag_df
    description: 机械提取自 dws_10000251_univ_acc_tag_df 的 metrics 块（key=d30_retention_rate）
