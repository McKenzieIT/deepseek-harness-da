kind: metric
name: dws_10000251_selfhelp_tag_df__account_uv
description: 当日账号 UV(user_type=2)，账号主体去重用户数
domains:
  - 用户生命周期
  - 系统监控
computation:
  sql: COUNT(DISTINCT CASE WHEN user_type=2 THEN user_id END)
  metadata:
    aggregation: count_distinct
    field: CASE WHEN user_type=2 THEN user_id END
    source: dws_10000251_selfhelp_tag_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_selfhelp_tag_df
    description: 机械提取自 dws_10000251_selfhelp_tag_df 的 metrics 块（key=account_uv）
