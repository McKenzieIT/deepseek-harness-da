kind: metric
name: game.artifact.wear__game_artifact_wear_role_uv
description: 穿戴兵符的去重角色数（分析主体）
domains:
  - 角色成长
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.artifact.wear
    time_grain: ""
relations:
  - type: derived_from
    target: game.artifact.wear
    description: 机械提取自 game.artifact.wear 的 metrics 块（key=game_artifact_wear_role_uv）
