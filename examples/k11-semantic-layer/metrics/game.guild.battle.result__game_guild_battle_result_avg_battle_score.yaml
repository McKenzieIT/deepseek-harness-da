kind: metric
name: game.guild.battle.result__game_guild_battle_result_avg_battle_score
description: 人均本方公会战积分（积分合计/去重角色数，分母用 NULLIF 防除零）
domains:
  - 社交公会
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params,'$.battleScore') AS BIGINT)) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params,'$.battleScore') AS BIGINT)) / NULLIF(COUNT(DISTINCT role_id), 0
    source: game.guild.battle.result
    time_grain: ""
relations:
  - type: derived_from
    target: game.guild.battle.result
    description: 机械提取自 game.guild.battle.result 的 metrics 块（key=game_guild_battle_result_avg_battle_score）
