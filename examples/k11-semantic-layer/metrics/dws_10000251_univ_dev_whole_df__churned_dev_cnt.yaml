kind: metric
name: dws_10000251_univ_dev_whole_df__churned_dev_cnt
description: 沉默付费设备数(曾付费但近 30 天未活跃)；用于沉默付费/流失召回识别，时间窗口可按业务调整
domains:
  - 用户生命周期
  - 付费经济
computation:
  sql: COUNT(DISTINCT CASE WHEN pay_amt_std > 0 AND act_tm_lst < DATE_SUB(CURRENT_DATE(), 30) THEN user_id ELSE NULL END)
  metadata:
    aggregation: count_distinct
    field: CASE WHEN pay_amt_std > 0 AND act_tm_lst < DATE_SUB(CURRENT_DATE(), 30) THEN user_id ELSE NULL END
    source: dws_10000251_univ_dev_whole_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_univ_dev_whole_df
    description: 机械提取自 dws_10000251_univ_dev_whole_df 的 metrics 块（key=churned_dev_cnt）
