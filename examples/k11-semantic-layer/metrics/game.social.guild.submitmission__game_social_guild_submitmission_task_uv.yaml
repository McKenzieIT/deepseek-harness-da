kind: metric
name: game.social.guild.submitmission__game_social_guild_submitmission_task_uv
description: 去重公会任务id数（取自 params.taskId）。
domains:
  - 社交公会
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.taskId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.taskId')
    source: game.social.guild.submitmission
    time_grain: ""
relations:
  - type: derived_from
    target: game.social.guild.submitmission
    description: 机械提取自 game.social.guild.submitmission 的 metrics 块（key=game_social_guild_submitmission_task_uv）
