kind: metric
name: dws_10000251_univ_dev_server_tag_df__arpu
description: ARPU 平均每活跃设备付费金额,累计付费总额除以活跃设备数,用 NULLIF 防除零;单位:元
domains:
  - 用户生命周期
  - 付费经济
  - 角色成长
  - 系统监控
computation:
  sql: SUM(pay_amt_std) * 1.0 / NULLIF(COUNT(DISTINCT user_id), 0)
  metadata:
    aggregation: sum
    field: pay_amt_std) * 1.0 / NULLIF(COUNT(DISTINCT user_id), 0
    source: dws_10000251_univ_dev_server_tag_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_univ_dev_server_tag_df
    description: 机械提取自 dws_10000251_univ_dev_server_tag_df 的 metrics 块（key=arpu）
