kind: metric
name: game.toy.resonanceLevelup__game_toy_resonance_levelup_cell_uv
description: 升级涉及的去重共鸣格子数（cellId）
domains:
  - 探索收集
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.cellId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.cellId')
    source: game.toy.resonanceLevelup
    time_grain: ""
relations:
  - type: derived_from
    target: game.toy.resonanceLevelup
    description: 机械提取自 game.toy.resonanceLevelup 的 metrics 块（key=game_toy_resonance_levelup_cell_uv）
