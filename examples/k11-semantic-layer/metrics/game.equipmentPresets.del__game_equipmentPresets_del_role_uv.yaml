kind: metric
name: game.equipmentPresets.del__game_equipmentPresets_del_role_uv
description: 删除装备预设的去重角色数（主体粒度）
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.equipmentPresets.del
    time_grain: ""
relations:
  - type: derived_from
    target: game.equipmentPresets.del
    description: 机械提取自 game.equipmentPresets.del 的 metrics 块（key=game_equipmentPresets_del_role_uv）
