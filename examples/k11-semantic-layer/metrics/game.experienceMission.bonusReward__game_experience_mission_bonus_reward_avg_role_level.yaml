kind: metric
name: game.experienceMission.bonusReward__game_experience_mission_bonus_reward_avg_role_level
description: 领奖角色平均等级（params.roleLevel 均值，反映该终大奖的目标段位）
domains:
  - 角色成长
computation:
  sql: AVG(CAST(GET_JSON_OBJECT(params, '$.roleLevel') AS BIGINT))
  metadata:
    aggregation: avg
    field: CAST(GET_JSON_OBJECT(params, '$.roleLevel') AS BIGINT)
    source: game.experienceMission.bonusReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.experienceMission.bonusReward
    description: 机械提取自 game.experienceMission.bonusReward 的 metrics 块（key=game_experience_mission_bonus_reward_avg_role_level）
