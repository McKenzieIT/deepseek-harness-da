kind: metric
name: game.tactic.train__tactic_train_fragment_sum
description: 战法特训产出的碎片总数（count 字段累加）
domains:
  - 角色成长
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.count') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.count') AS BIGINT)
    source: game.tactic.train
    time_grain: ""
relations:
  - type: derived_from
    target: game.tactic.train
    description: 机械提取自 game.tactic.train 的 metrics 块（key=tactic_train_fragment_sum）
