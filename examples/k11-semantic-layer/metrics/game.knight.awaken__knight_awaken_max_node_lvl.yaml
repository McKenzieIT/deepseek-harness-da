kind: metric
name: game.knight.awaken__knight_awaken_max_node_lvl
description: 觉醒达到的最大节点等级
domains:
  - 角色成长
computation:
  sql: MAX(CAST(GET_JSON_OBJECT(params, '$.nodeLvl') AS BIGINT))
  metadata:
    aggregation: max
    field: CAST(GET_JSON_OBJECT(params, '$.nodeLvl') AS BIGINT)
    source: game.knight.awaken
    time_grain: ""
relations:
  - type: derived_from
    target: game.knight.awaken
    description: 机械提取自 game.knight.awaken 的 metrics 块（key=knight_awaken_max_node_lvl）
