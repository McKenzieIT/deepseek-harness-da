kind: metric
name: game.champion.resetGuess__champion_reset_guess_server_uv
description: 触发取消竞猜的去重游戏服数
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: game.champion.resetGuess
    time_grain: ""
relations:
  - type: derived_from
    target: game.champion.resetGuess
    description: 机械提取自 game.champion.resetGuess 的 metrics 块（key=champion_reset_guess_server_uv）
