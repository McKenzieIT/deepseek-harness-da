kind: metric
name: game.script.hangupaccelerate__script_hangupaccelerate_addexp_sum
description: 加速获得的经验总和
domains:
  - 自定义
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.addExp') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.addExp') AS BIGINT)
    source: game.script.hangupaccelerate
    time_grain: ""
relations:
  - type: derived_from
    target: game.script.hangupaccelerate
    description: 机械提取自 game.script.hangupaccelerate 的 metrics 块（key=script_hangupaccelerate_addexp_sum）
