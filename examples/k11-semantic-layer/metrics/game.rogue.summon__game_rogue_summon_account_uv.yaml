kind: metric
name: game.rogue.summon__game_rogue_summon_account_uv
description: 发起招募的去重账号数
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT account_id)
  metadata:
    aggregation: count_distinct
    field: account_id
    source: game.rogue.summon
    time_grain: ""
relations:
  - type: derived_from
    target: game.rogue.summon
    description: 机械提取自 game.rogue.summon 的 metrics 块（key=game_rogue_summon_account_uv）
