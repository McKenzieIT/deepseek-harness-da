kind: metric
name: game.role.changeCard__changeCard_role_uv
description: 修改名帖的去重角色数
domains:
  - 用户生命周期
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.role.changeCard
    time_grain: ""
relations:
  - type: derived_from
    target: game.role.changeCard
    description: 机械提取自 game.role.changeCard 的 metrics 块（key=changeCard_role_uv）
