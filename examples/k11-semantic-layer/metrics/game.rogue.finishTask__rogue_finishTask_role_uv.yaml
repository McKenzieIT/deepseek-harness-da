kind: metric
name: game.rogue.finishTask__rogue_finishTask_role_uv
description: 完成图鉴任务的去重角色数（分析主体粒度=role_id）
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.rogue.finishTask
    time_grain: ""
relations:
  - type: derived_from
    target: game.rogue.finishTask
    description: 机械提取自 game.rogue.finishTask 的 metrics 块（key=rogue_finishTask_role_uv）
