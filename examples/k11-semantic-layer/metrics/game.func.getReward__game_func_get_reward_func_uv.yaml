kind: metric
name: game.func.getReward__game_func_get_reward_func_uv
description: 被领取奖励的去重功能id数量（funcId 去重）
domains:
  - 系统监控
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.funcId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.funcId')
    source: game.func.getReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.func.getReward
    description: 机械提取自 game.func.getReward 的 metrics 块（key=game_func_get_reward_func_uv）
