kind: metric
name: dws_10000251_univ_acc_server_tag_df__active_acc_cnt
description: 活跃账号数(指定 ds 分区下的去重账号数;本表固定 user_type=2 账号口径,user_id 即账号ID,无需按 user_type 过滤);衡量账号规模
domains:
  - 用户生命周期
  - 付费经济
  - 系统监控
computation:
  sql: COUNT(DISTINCT user_id)
  metadata:
    aggregation: count_distinct
    field: user_id
    source: dws_10000251_univ_acc_server_tag_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_univ_acc_server_tag_df
    description: 机械提取自 dws_10000251_univ_acc_server_tag_df 的 metrics 块（key=active_acc_cnt）
