kind: metric
name: dws_10000251_role_tag_pbt2_df__active_role_uv_1w
description: 近7日活跃角色UV
domains:
  - 角色成长
  - 付费经济
computation:
  sql: COUNT(DISTINCT CASE WHEN act_days_1w > 0 THEN role_id END)
  metadata:
    aggregation: count_distinct
    field: CASE WHEN act_days_1w > 0 THEN role_id END
    source: dws_10000251_role_tag_pbt2_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_role_tag_pbt2_df
    description: 机械提取自 dws_10000251_role_tag_pbt2_df 的 metrics 块（key=active_role_uv_1w）
