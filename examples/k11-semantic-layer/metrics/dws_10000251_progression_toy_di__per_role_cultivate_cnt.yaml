kind: metric
name: dws_10000251_progression_toy_di__per_role_cultivate_cnt
description: 人均当日培养次数（总次数/培养角色UV）。
domains:
  - 角色成长
computation:
  sql: COUNT(*) * 1.0 / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: count
    field: "*) * 1.0 / NULLIF(COUNT(DISTINCT role_id), 0"
    source: dws_10000251_progression_toy_di
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_progression_toy_di
    description: 机械提取自 dws_10000251_progression_toy_di 的 metrics 块（key=per_role_cultivate_cnt）
