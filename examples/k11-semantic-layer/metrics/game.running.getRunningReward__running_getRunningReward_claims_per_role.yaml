kind: metric
name: game.running.getRunningReward__running_getRunningReward_claims_per_role
description: 每个领取角色的平均领取次数（>1 表示同一角色当日多次领取）
domains:
  - 自定义
computation:
  sql: CAST(COUNT(*) AS DOUBLE) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: ""
    field: ""
    source: game.running.getRunningReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.running.getRunningReward
    description: 机械提取自 game.running.getRunningReward 的 metrics 块（key=running_getRunningReward_claims_per_role）
