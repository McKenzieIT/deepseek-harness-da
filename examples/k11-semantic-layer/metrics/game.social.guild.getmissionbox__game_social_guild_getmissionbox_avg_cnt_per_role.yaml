kind: metric
name: game.social.guild.getmissionbox__game_social_guild_getmissionbox_avg_cnt_per_role
description: 人均领取次数（cnt / role_uv，NULLIF 防零除）
domains:
  - 社交公会
computation:
  sql: COUNT(*) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: count
    field: "*) / NULLIF(COUNT(DISTINCT role_id), 0"
    source: game.social.guild.getmissionbox
    time_grain: ""
relations:
  - type: derived_from
    target: game.social.guild.getmissionbox
    description: 机械提取自 game.social.guild.getmissionbox 的 metrics 块（key=game_social_guild_getmissionbox_avg_cnt_per_role）
