kind: metric
name: game.social.guild.addpoints__guild_addpoints_avg_add_points_per_role
description: 人均(角色)新增活跃点数(新增点数总和/去重角色数)
domains:
  - 社交公会
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params,'$.guildActiveAddPoints') AS BIGINT)) / NULLIF(COUNT(DISTINCT role_id),0)
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params,'$.guildActiveAddPoints') AS BIGINT)) / NULLIF(COUNT(DISTINCT role_id),0
    source: game.social.guild.addpoints
    time_grain: ""
relations:
  - type: derived_from
    target: game.social.guild.addpoints
    description: 机械提取自 game.social.guild.addpoints 的 metrics 块（key=guild_addpoints_avg_add_points_per_role）
