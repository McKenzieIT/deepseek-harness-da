kind: metric
name: game.running.getRunningReward__running_getRunningReward_vip_avg
description: 领取角色的平均 VIP 等级
domains:
  - 自定义
computation:
  sql: AVG(CAST(GET_JSON_OBJECT(params, '$.vipLevel') AS BIGINT))
  metadata:
    aggregation: avg
    field: CAST(GET_JSON_OBJECT(params, '$.vipLevel') AS BIGINT)
    source: game.running.getRunningReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.running.getRunningReward
    description: 机械提取自 game.running.getRunningReward 的 metrics 块（key=running_getRunningReward_vip_avg）
