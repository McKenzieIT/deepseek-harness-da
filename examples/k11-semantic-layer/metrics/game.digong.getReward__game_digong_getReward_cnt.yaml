kind: metric
name: game.digong.getReward__game_digong_getReward_cnt
description: 地宫领奖事件总次数（COUNT(*)）
domains:
  - 自定义
computation:
  sql: COUNT(*)
  metadata:
    aggregation: count
    field: "*"
    source: game.digong.getReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.digong.getReward
    description: 机械提取自 game.digong.getReward 的 metrics 块（key=game_digong_getReward_cnt）
