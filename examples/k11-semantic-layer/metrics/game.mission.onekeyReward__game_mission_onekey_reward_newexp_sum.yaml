kind: metric
name: game.mission.onekeyReward__game_mission_onekey_reward_newexp_sum
description: 一键领奖获得积分(newExp)总和
domains:
  - 角色成长
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.newExp') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.newExp') AS BIGINT)
    source: game.mission.onekeyReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.mission.onekeyReward
    description: 机械提取自 game.mission.onekeyReward 的 metrics 块（key=game_mission_onekey_reward_newexp_sum）
