kind: metric
name: game.rogue.end__game_rogue_end_play_uv
description: 古战场副本挑战次数（playId 去重，每次挑战唯一标识）
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.playId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.playId')
    source: game.rogue.end
    time_grain: ""
relations:
  - type: derived_from
    target: game.rogue.end
    description: 机械提取自 game.rogue.end 的 metrics 块（key=game_rogue_end_play_uv）
