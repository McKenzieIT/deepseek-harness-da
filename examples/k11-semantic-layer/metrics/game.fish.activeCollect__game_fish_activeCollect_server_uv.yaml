kind: metric
name: game.fish.activeCollect__game_fish_activeCollect_server_uv
description: 触发钓鱼图鉴激活的去重服务器数
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: game.fish.activeCollect
    time_grain: ""
relations:
  - type: derived_from
    target: game.fish.activeCollect
    description: 机械提取自 game.fish.activeCollect 的 metrics 块（key=game_fish_activeCollect_server_uv）
