kind: metric
name: game.equipmentPresets.del__game_equipmentPresets_del_preset_uv
description: 被删除的去重装备预设id数
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params,'$.id'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params,'$.id')
    source: game.equipmentPresets.del
    time_grain: ""
relations:
  - type: derived_from
    target: game.equipmentPresets.del
    description: 机械提取自 game.equipmentPresets.del 的 metrics 块（key=game_equipmentPresets_del_preset_uv）
