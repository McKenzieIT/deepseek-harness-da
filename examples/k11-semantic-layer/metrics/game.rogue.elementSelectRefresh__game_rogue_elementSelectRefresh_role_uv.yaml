kind: metric
name: game.rogue.elementSelectRefresh__game_rogue_elementSelectRefresh_role_uv
description: 触发元素选择刷新的去重角色数(分析主体)
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.rogue.elementSelectRefresh
    time_grain: ""
relations:
  - type: derived_from
    target: game.rogue.elementSelectRefresh
    description: 机械提取自 game.rogue.elementSelectRefresh 的 metrics 块（key=game_rogue_elementSelectRefresh_role_uv）
