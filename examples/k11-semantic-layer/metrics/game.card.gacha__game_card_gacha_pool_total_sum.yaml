kind: metric
name: game.card.gacha__game_card_gacha_pool_total_sum
description: 当期累计卡池抽卡总次数（totalCnt 求和，状态快照累加）
domains:
  - 探索收集
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params,'$.totalCnt') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params,'$.totalCnt') AS BIGINT)
    source: game.card.gacha
    time_grain: ""
relations:
  - type: derived_from
    target: game.card.gacha
    description: 机械提取自 game.card.gacha 的 metrics 块（key=game_card_gacha_pool_total_sum）
