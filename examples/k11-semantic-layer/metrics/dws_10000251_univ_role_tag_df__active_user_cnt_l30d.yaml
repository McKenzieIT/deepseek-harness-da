kind: metric
name: dws_10000251_univ_role_tag_df__active_user_cnt_l30d
description: 近30日活跃角色数(最近两次活跃间隔<30 天的去重角色数)
domains:
  - 用户生命周期
  - 付费经济
  - 角色成长
computation:
  sql: COUNT(DISTINCT CASE WHEN act_ymd_diff < 30 THEN user_id END)
  metadata:
    aggregation: count_distinct
    field: CASE WHEN act_ymd_diff < 30 THEN user_id END
    source: dws_10000251_univ_role_tag_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_univ_role_tag_df
    description: 机械提取自 dws_10000251_univ_role_tag_df 的 metrics 块（key=active_user_cnt_l30d）
