kind: metric
name: game.toy.wear__toy_wear_resonance_role_uv
description: 触发共振（resonance.group 非空）的去重角色数
domains:
  - 探索收集
computation:
  sql: COUNT(DISTINCT CASE WHEN GET_JSON_OBJECT(params, '$.resonance.group') IS NOT NULL THEN role_id END)
  metadata:
    aggregation: count_distinct
    field: CASE WHEN GET_JSON_OBJECT(params, '$.resonance.group') IS NOT NULL THEN role_id END
    source: game.toy.wear
    time_grain: ""
relations:
  - type: derived_from
    target: game.toy.wear
    description: 机械提取自 game.toy.wear 的 metrics 块（key=toy_wear_resonance_role_uv）
