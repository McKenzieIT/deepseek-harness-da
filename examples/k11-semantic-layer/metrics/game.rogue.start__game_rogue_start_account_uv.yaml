kind: metric
name: game.rogue.start__game_rogue_start_account_uv
description: 发起古战场挑战的账号去重数
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT account_id)
  metadata:
    aggregation: count_distinct
    field: account_id
    source: game.rogue.start
    time_grain: ""
relations:
  - type: derived_from
    target: game.rogue.start
    description: 机械提取自 game.rogue.start 的 metrics 块（key=game_rogue_start_account_uv）
