kind: metric
name: game.intervalShop.getFreeReward__game_intervalShop_getFreeReward_role_uv
description: 领取免费奖励的去重角色数
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.intervalShop.getFreeReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.intervalShop.getFreeReward
    description: 机械提取自 game.intervalShop.getFreeReward 的 metrics 块（key=game_intervalShop_getFreeReward_role_uv）
