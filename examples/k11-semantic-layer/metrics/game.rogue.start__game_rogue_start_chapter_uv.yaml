kind: metric
name: game.rogue.start__game_rogue_start_chapter_uv
description: 涉及章节去重数
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params,'$.chapter'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params,'$.chapter')
    source: game.rogue.start
    time_grain: ""
relations:
  - type: derived_from
    target: game.rogue.start
    description: 机械提取自 game.rogue.start 的 metrics 块（key=game_rogue_start_chapter_uv）
