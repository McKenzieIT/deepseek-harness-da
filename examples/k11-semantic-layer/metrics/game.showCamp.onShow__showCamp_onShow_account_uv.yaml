kind: metric
name: game.showCamp.onShow__showCamp_onShow_account_uv
description: 营地展示触达账号数（去重）
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT account_id)
  metadata:
    aggregation: count_distinct
    field: account_id
    source: game.showCamp.onShow
    time_grain: ""
relations:
  - type: derived_from
    target: game.showCamp.onShow
    description: 机械提取自 game.showCamp.onShow 的 metrics 块（key=showCamp_onShow_account_uv）
