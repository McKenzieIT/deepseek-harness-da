kind: metric
name: game.guild.getMissionBox__guild_get_mission_box_gold_sum
description: 领取宝箱产出的充值钻石总量
domains:
  - 社交公会
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.coinList.gold') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.coinList.gold') AS BIGINT)
    source: game.guild.getMissionBox
    time_grain: ""
relations:
  - type: derived_from
    target: game.guild.getMissionBox
    description: 机械提取自 game.guild.getMissionBox 的 metrics 块（key=guild_get_mission_box_gold_sum）
