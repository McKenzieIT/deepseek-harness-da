kind: metric
name: game.rogue.chooseevent__game_rogue_chooseevent_account_uv
description: 触发事件选择的账号去重数
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT account_id)
  metadata:
    aggregation: count_distinct
    field: account_id
    source: game.rogue.chooseevent
    time_grain: ""
relations:
  - type: derived_from
    target: game.rogue.chooseevent
    description: 机械提取自 game.rogue.chooseevent 的 metrics 块（key=game_rogue_chooseevent_account_uv）
