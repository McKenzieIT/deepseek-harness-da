kind: metric
name: game.guild.submitMission__game_guild_submitMission_server_uv
description: 提交公会任务的去重游戏服数
domains:
  - 社交公会
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: game.guild.submitMission
    time_grain: ""
relations:
  - type: derived_from
    target: game.guild.submitMission
    description: 机械提取自 game.guild.submitMission 的 metrics 块（key=game_guild_submitMission_server_uv）
