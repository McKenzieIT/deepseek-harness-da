kind: metric
name: game.toy.gacha__game_toy_gacha_pulls_sum
description: 当期累计抽卡次数（count 求和）
domains:
  - 探索收集
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params,'$.count') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params,'$.count') AS BIGINT)
    source: game.toy.gacha
    time_grain: ""
relations:
  - type: derived_from
    target: game.toy.gacha
    description: 机械提取自 game.toy.gacha 的 metrics 块（key=game_toy_gacha_pulls_sum）
