kind: metric
name: game.scriptActivity.getPreheatSignReward__getpreheatsignreward_role_per_server
description: 平均每服领取角色数
domains:
  - 自定义
computation:
  sql: ROUND(COUNT(DISTINCT role_id) * 1.0 / NULLIF(COUNT(DISTINCT server_id), 0), 2)
  metadata:
    aggregation: ""
    field: ""
    source: game.scriptActivity.getPreheatSignReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.scriptActivity.getPreheatSignReward
    description: 机械提取自 game.scriptActivity.getPreheatSignReward 的 metrics 块（key=getpreheatsignreward_role_per_server）
