kind: metric
name: dws_10000251_algo_role_act_pay_df__acc_uv
description: 当日活跃账号数（DISTINCT acc_id）
domains:
  - 付费经济
  - 用户生命周期
computation:
  sql: COUNT(DISTINCT acc_id)
  metadata:
    aggregation: count_distinct
    field: acc_id
    source: dws_10000251_algo_role_act_pay_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_algo_role_act_pay_df
    description: 机械提取自 dws_10000251_algo_role_act_pay_df 的 metrics 块（key=acc_uv）
