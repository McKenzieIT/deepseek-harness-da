kind: metric
name: game.role.snapshot__game_role_snapshot_force_sum
description: 在线角色战力总和（全服在线战力快照）
domains:
  - 用户生命周期
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params,'$.roleForce') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params,'$.roleForce') AS BIGINT)
    source: game.role.snapshot
    time_grain: ""
relations:
  - type: derived_from
    target: game.role.snapshot
    description: 机械提取自 game.role.snapshot 的 metrics 块（key=game_role_snapshot_force_sum）
