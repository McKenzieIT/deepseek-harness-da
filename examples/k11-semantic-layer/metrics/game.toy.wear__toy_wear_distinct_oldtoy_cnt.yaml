kind: metric
name: game.toy.wear__toy_wear_distinct_oldtoy_cnt
description: 当日被替换下的去重旧玩具 id 数（params.oldToy.id）
domains:
  - 探索收集
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.oldToy.id'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.oldToy.id')
    source: game.toy.wear
    time_grain: ""
relations:
  - type: derived_from
    target: game.toy.wear
    description: 机械提取自 game.toy.wear 的 metrics 块（key=toy_wear_distinct_oldtoy_cnt）
