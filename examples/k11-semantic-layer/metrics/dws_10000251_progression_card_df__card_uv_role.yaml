kind: metric
name: dws_10000251_progression_card_df__card_uv_role
description: 持有卡牌的角色UV(日全量快照，按ds切片)
domains:
  - 角色成长
  - 装备道具
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: dws_10000251_progression_card_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_progression_card_df
    description: 机械提取自 dws_10000251_progression_card_df 的 metrics 块（key=card_uv_role）
