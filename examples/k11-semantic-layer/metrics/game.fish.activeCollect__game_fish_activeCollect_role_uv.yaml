kind: metric
name: game.fish.activeCollect__game_fish_activeCollect_role_uv
description: 触发钓鱼图鉴激活的去重角色数
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.fish.activeCollect
    time_grain: ""
relations:
  - type: derived_from
    target: game.fish.activeCollect
    description: 机械提取自 game.fish.activeCollect 的 metrics 块（key=game_fish_activeCollect_role_uv）
