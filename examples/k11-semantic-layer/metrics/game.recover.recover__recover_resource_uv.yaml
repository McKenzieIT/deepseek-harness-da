kind: metric
name: game.recover.recover__recover_resource_uv
description: distinct 恢复性资源类型数（按 recoverId 去重）
domains:
  - 资源产销
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.recoverId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.recoverId')
    source: game.recover.recover
    time_grain: ""
relations:
  - type: derived_from
    target: game.recover.recover
    description: 机械提取自 game.recover.recover 的 metrics 块（key=recover_resource_uv）
