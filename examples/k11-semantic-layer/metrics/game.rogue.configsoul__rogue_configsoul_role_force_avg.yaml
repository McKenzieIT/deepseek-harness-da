kind: metric
name: game.rogue.configsoul__rogue_configsoul_role_force_avg
description: 配置将魂时的角色战力(roleForce)均值
domains:
  - 战斗关卡
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.roleForce') AS BIGINT)) / NULLIF(COUNT(*), 0)
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.roleForce') AS BIGINT)) / NULLIF(COUNT(*), 0
    source: game.rogue.configsoul
    time_grain: ""
relations:
  - type: derived_from
    target: game.rogue.configsoul
    description: 机械提取自 game.rogue.configsoul 的 metrics 块（key=rogue_configsoul_role_force_avg）
