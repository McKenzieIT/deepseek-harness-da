kind: metric
name: game.script.finishtalk__game_script_finishtalk_role_uv
description: 完成剧本对话的去重角色数（分析主体粒度 role_id）
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.script.finishtalk
    time_grain: ""
relations:
  - type: derived_from
    target: game.script.finishtalk
    description: 机械提取自 game.script.finishtalk 的 metrics 块（key=game_script_finishtalk_role_uv）
