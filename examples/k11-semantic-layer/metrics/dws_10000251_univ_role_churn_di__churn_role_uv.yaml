kind: metric
name: dws_10000251_univ_role_churn_di__churn_role_uv
description: 当日新增流失角色数（_di日增量，UV）。实测 ds=20260730 得 4114。
domains:
  - 用户生命周期
  - 付费经济
computation:
  sql: COUNT(DISTINCT user_id)
  metadata:
    aggregation: count_distinct
    field: user_id
    source: dws_10000251_univ_role_churn_di
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_univ_role_churn_di
    description: 机械提取自 dws_10000251_univ_role_churn_di 的 metrics 块（key=churn_role_uv）
