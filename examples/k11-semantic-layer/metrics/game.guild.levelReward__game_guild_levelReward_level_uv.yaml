kind: metric
name: game.guild.levelReward__game_guild_levelReward_level_uv
description: 涉及的公会等级档位数（去重 level；100%非空实测，ds=20260729 实测27档）
domains:
  - 社交公会
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.level'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.level')
    source: game.guild.levelReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.guild.levelReward
    description: 机械提取自 game.guild.levelReward 的 metrics 块（key=game_guild_levelReward_level_uv）
