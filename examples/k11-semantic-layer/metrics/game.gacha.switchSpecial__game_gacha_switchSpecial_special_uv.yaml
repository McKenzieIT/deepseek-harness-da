kind: metric
name: game.gacha.switchSpecial__game_gacha_switchSpecial_special_uv
description: 涉及的新手集市 id（specialId）去重数
domains:
  - 探索收集
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params,'$.specialId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params,'$.specialId')
    source: game.gacha.switchSpecial
    time_grain: ""
relations:
  - type: derived_from
    target: game.gacha.switchSpecial
    description: 机械提取自 game.gacha.switchSpecial 的 metrics 块（key=game_gacha_switchSpecial_special_uv）
