kind: metric
name: dws_10000251_role_play_active_di__step_reached_uv
description: 当日达成上手最终阶段的角色数（去重），衡量上手转化规模
domains:
  - 战斗关卡
  - 角色成长
computation:
  sql: COUNT(DISTINCT CASE WHEN step > 0 THEN role_id END)
  metadata:
    aggregation: count_distinct
    field: CASE WHEN step > 0 THEN role_id END
    source: dws_10000251_role_play_active_di
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_role_play_active_di
    description: 机械提取自 dws_10000251_role_play_active_di 的 metrics 块（key=step_reached_uv）
