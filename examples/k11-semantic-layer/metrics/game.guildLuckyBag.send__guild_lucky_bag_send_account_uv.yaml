kind: metric
name: game.guildLuckyBag.send__guild_lucky_bag_send_account_uv
description: 发送福袋的去重账号数
domains:
  - 社交公会
computation:
  sql: COUNT(DISTINCT account_id)
  metadata:
    aggregation: count_distinct
    field: account_id
    source: game.guildLuckyBag.send
    time_grain: ""
relations:
  - type: derived_from
    target: game.guildLuckyBag.send
    description: 机械提取自 game.guildLuckyBag.send 的 metrics 块（key=guild_lucky_bag_send_account_uv）
