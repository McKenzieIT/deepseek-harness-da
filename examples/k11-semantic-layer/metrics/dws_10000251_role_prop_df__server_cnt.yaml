kind: metric
name: dws_10000251_role_prop_df__server_cnt
description: 当日有角色资产记录的活跃区服数。
domains:
  - 角色成长
  - 装备道具
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: dws_10000251_role_prop_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_role_prop_df
    description: 机械提取自 dws_10000251_role_prop_df 的 metrics 块（key=server_cnt）
