kind: metric
name: game.task.act__game_task_act_task_uv
description: 涉及的任务id数(去重 taskId；100%非空实测)
domains:
  - 角色成长
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.taskId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.taskId')
    source: game.task.act
    time_grain: ""
relations:
  - type: derived_from
    target: game.task.act
    description: 机械提取自 game.task.act 的 metrics 块（key=game_task_act_task_uv）
