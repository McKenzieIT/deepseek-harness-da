kind: metric
name: game.scriptActivity.getRunningSignReward__scriptActivity_getRunningSignReward_avg_sign_max_day
description: 平均签到领取位置（事件级加权平均）
domains:
  - 自定义
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params,'$.signRewardMaxDay') AS BIGINT)) / NULLIF(COUNT(*),0)
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params,'$.signRewardMaxDay') AS BIGINT)) / NULLIF(COUNT(*),0
    source: game.scriptActivity.getRunningSignReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.scriptActivity.getRunningSignReward
    description: 机械提取自 game.scriptActivity.getRunningSignReward 的 metrics 块（key=scriptActivity_getRunningSignReward_avg_sign_max_day）
