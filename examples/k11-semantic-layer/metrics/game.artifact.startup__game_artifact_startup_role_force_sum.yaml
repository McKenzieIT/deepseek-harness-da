kind: metric
name: game.artifact.startup__game_artifact_startup_role_force_sum
description: 触发升星角色的战力总和（角色战力快照累加）
domains:
  - 角色成长
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.roleForce') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.roleForce') AS BIGINT)
    source: game.artifact.startup
    time_grain: ""
relations:
  - type: derived_from
    target: game.artifact.startup
    description: 机械提取自 game.artifact.startup 的 metrics 块（key=game_artifact_startup_role_force_sum）
