kind: metric
name: dws_10000251_univ_acc_server_tag_df__brand_acc_cnt
description: 有设备品牌记录的去重账号数,用于设备分布拆分
domains:
  - 用户生命周期
  - 付费经济
  - 系统监控
computation:
  sql: COUNT(DISTINCT CASE WHEN dev_brand_lst IS NOT NULL AND dev_brand_lst <> '' THEN user_id END)
  metadata:
    aggregation: count_distinct
    field: CASE WHEN dev_brand_lst IS NOT NULL AND dev_brand_lst <> '' THEN user_id END
    source: dws_10000251_univ_acc_server_tag_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_univ_acc_server_tag_df
    description: 机械提取自 dws_10000251_univ_acc_server_tag_df 的 metrics 块（key=brand_acc_cnt）
