kind: metric
name: game.recover.recover__recover_role_uv
description: 触发恢复的 distinct 角色数（分析主体为 role_id）
domains:
  - 资源产销
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.recover.recover
    time_grain: ""
relations:
  - type: derived_from
    target: game.recover.recover
    description: 机械提取自 game.recover.recover 的 metrics 块（key=recover_role_uv）
