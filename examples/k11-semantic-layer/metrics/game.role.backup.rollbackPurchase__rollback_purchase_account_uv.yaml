kind: metric
name: game.role.backup.rollbackPurchase__rollback_purchase_account_uv
description: 触发回滚补发的去重账号数
domains:
  - 用户生命周期
computation:
  sql: COUNT(DISTINCT account_id)
  metadata:
    aggregation: count_distinct
    field: account_id
    source: game.role.backup.rollbackPurchase
    time_grain: ""
relations:
  - type: derived_from
    target: game.role.backup.rollbackPurchase
    description: 机械提取自 game.role.backup.rollbackPurchase 的 metrics 块（key=rollback_purchase_account_uv）
