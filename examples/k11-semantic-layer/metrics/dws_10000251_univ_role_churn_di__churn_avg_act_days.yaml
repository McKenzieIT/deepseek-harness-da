kind: metric
name: dws_10000251_univ_role_churn_di__churn_avg_act_days
description: 流失角色平均累计活跃天数=累计活跃天数和/流失角色数（NULLIF防除零）。
domains:
  - 用户生命周期
  - 付费经济
computation:
  sql: SUM(act_days_std) / NULLIF(COUNT(DISTINCT user_id), 0)
  metadata:
    aggregation: sum
    field: act_days_std) / NULLIF(COUNT(DISTINCT user_id), 0
    source: dws_10000251_univ_role_churn_di
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_univ_role_churn_di
    description: 机械提取自 dws_10000251_univ_role_churn_di 的 metrics 块（key=churn_avg_act_days）
