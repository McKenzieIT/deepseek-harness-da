kind: metric
name: game.script.fetchhangup__script_fetchhangup_server_uv
description: 领取挂机奖励的去重游戏服数
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: game.script.fetchhangup
    time_grain: ""
relations:
  - type: derived_from
    target: game.script.fetchhangup
    description: 机械提取自 game.script.fetchhangup 的 metrics 块（key=script_fetchhangup_server_uv）
