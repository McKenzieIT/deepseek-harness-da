kind: metric
name: game.springfestival.preheattrade__springfestival_preheattrade_trade_uv
description: 被购买的预热商品去重数量（按 tradeId 商品id 去重）
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.tradeId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.tradeId')
    source: game.springfestival.preheattrade
    time_grain: ""
relations:
  - type: derived_from
    target: game.springfestival.preheattrade
    description: 机械提取自 game.springfestival.preheattrade 的 metrics 块（key=springfestival_preheattrade_trade_uv）
