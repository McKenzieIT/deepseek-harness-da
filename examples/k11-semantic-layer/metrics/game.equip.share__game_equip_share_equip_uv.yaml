kind: metric
name: game.equip.share__game_equip_share_equip_uv
description: 分享出去重装备唯一id数
domains:
  - 装备道具
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.equipUid'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.equipUid')
    source: game.equip.share
    time_grain: ""
relations:
  - type: derived_from
    target: game.equip.share
    description: 机械提取自 game.equip.share 的 metrics 块（key=game_equip_share_equip_uv）
