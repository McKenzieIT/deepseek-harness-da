kind: metric
name: dws_10000251_univ_dev_whole_df__arpu
description: 累计 ARPU(全体设备人均累计付费)；累计付费总额 / 全体设备数，用 NULLIF 防除零；分母为全体设备(含免费)，与 ARPPU 区分
domains:
  - 用户生命周期
  - 付费经济
computation:
  sql: SUM(pay_amt_std) * 1.0 / NULLIF(COUNT(DISTINCT user_id), 0)
  metadata:
    aggregation: sum
    field: pay_amt_std) * 1.0 / NULLIF(COUNT(DISTINCT user_id), 0
    source: dws_10000251_univ_dev_whole_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_univ_dev_whole_df
    description: 机械提取自 dws_10000251_univ_dev_whole_df 的 metrics 块（key=arpu）
