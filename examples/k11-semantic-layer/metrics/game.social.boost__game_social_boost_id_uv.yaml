kind: metric
name: game.social.boost__game_social_boost_id_uv
description: 去重悬赏单数（唯一 boostId）
domains:
  - 社交公会
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.boostId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.boostId')
    source: game.social.boost
    time_grain: ""
relations:
  - type: derived_from
    target: game.social.boost
    description: 机械提取自 game.social.boost 的 metrics 块（key=game_social_boost_id_uv）
