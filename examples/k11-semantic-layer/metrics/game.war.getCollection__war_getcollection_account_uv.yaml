kind: metric
name: game.war.getCollection__war_getcollection_account_uv
description: 参与收集的账号数（去重）
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT account_id)
  metadata:
    aggregation: count_distinct
    field: account_id
    source: game.war.getCollection
    time_grain: ""
relations:
  - type: derived_from
    target: game.war.getCollection
    description: 机械提取自 game.war.getCollection 的 metrics 块（key=war_getcollection_account_uv）
