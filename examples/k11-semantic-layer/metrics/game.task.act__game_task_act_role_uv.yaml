kind: metric
name: game.task.act__game_task_act_role_uv
description: 触发任务行为的独立角色数(去重 role_id；主体粒度为角色级)
domains:
  - 角色成长
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.task.act
    time_grain: ""
relations:
  - type: derived_from
    target: game.task.act
    description: 机械提取自 game.task.act 的 metrics 块（key=game_task_act_role_uv）
