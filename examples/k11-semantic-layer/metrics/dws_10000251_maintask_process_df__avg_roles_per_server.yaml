kind: metric
name: dws_10000251_maintask_process_df__avg_roles_per_server
description: 区服平均角色数：角色UV / 区服数
domains:
  - 战斗关卡
  - 角色成长
computation:
  sql: CAST(COUNT(DISTINCT role_id) AS DOUBLE) / NULLIF(COUNT(DISTINCT server_id), 0)
  metadata:
    aggregation: ""
    field: ""
    source: dws_10000251_maintask_process_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_maintask_process_df
    description: 机械提取自 dws_10000251_maintask_process_df 的 metrics 块（key=avg_roles_per_server）
