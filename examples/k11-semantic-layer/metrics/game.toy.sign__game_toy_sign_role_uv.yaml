kind: metric
name: game.toy.sign__game_toy_sign_role_uv
description: 签到角色去重数（分析主体粒度 role_id）
domains:
  - 探索收集
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.toy.sign
    time_grain: ""
relations:
  - type: derived_from
    target: game.toy.sign
    description: 机械提取自 game.toy.sign 的 metrics 块（key=game_toy_sign_role_uv）
