kind: metric
name: game.role.changeBadges__change_badges_account_uv
description: 触发徽章变更的去重账号数
domains:
  - 用户生命周期
computation:
  sql: COUNT(DISTINCT account_id)
  metadata:
    aggregation: count_distinct
    field: account_id
    source: game.role.changeBadges
    time_grain: ""
relations:
  - type: derived_from
    target: game.role.changeBadges
    description: 机械提取自 game.role.changeBadges 的 metrics 块（key=change_badges_account_uv）
