kind: metric
name: game.equip.onekeyWear__equip_onekeyWear_knight_uv
description: 被穿戴装备涉及的独立武将数(去重 knightId；事件特有维度，scout 实测 60 个)
domains:
  - 装备道具
computation:
  sql: COUNT(DISTINCT CAST(GET_JSON_OBJECT(params, '$.knightId') AS BIGINT))
  metadata:
    aggregation: count_distinct
    field: CAST(GET_JSON_OBJECT(params, '$.knightId') AS BIGINT)
    source: game.equip.onekeyWear
    time_grain: ""
relations:
  - type: derived_from
    target: game.equip.onekeyWear
    description: 机械提取自 game.equip.onekeyWear 的 metrics 块（key=equip_onekeyWear_knight_uv）
