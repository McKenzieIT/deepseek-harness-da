kind: metric
name: game.guild.kickout__guild_kickout_target_uv
description: 被踢出的独立目标数(去重 targetUid；与 role_uv 互补衡量受影响面)
domains:
  - 社交公会
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.targetUid'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.targetUid')
    source: game.guild.kickout
    time_grain: ""
relations:
  - type: derived_from
    target: game.guild.kickout
    description: 机械提取自 game.guild.kickout 的 metrics 块（key=guild_kickout_target_uv）
