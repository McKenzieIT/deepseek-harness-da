kind: metric
name: game.knight.onekeyawaken__game_knight_onekeyawaken_with_knight_rate
description: 携带 knightId 的操作占比(数据完整性指标)
domains:
  - 角色成长
computation:
  sql: CAST(SUM(CASE WHEN GET_JSON_OBJECT(params, '$.knightId') IS NOT NULL THEN 1 ELSE 0 END) AS DOUBLE) / NULLIF(COUNT(*), 0)
  metadata:
    aggregation: ""
    field: ""
    source: game.knight.onekeyawaken
    time_grain: ""
relations:
  - type: derived_from
    target: game.knight.onekeyawaken
    description: 机械提取自 game.knight.onekeyawaken 的 metrics 块（key=game_knight_onekeyawaken_with_knight_rate）
