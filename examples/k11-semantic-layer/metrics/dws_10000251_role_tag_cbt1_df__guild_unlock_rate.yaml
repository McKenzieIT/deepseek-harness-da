kind: metric
name: dws_10000251_role_tag_cbt1_df__guild_unlock_rate
description: 公会解锁率（解锁角色数/全量角色数）
domains:
  - 用户生命周期
  - 角色成长
computation:
  sql: SUM(CASE WHEN guild_unlock_status = 1 THEN 1 ELSE 0 END) / NULLIF(COUNT(*), 0)
  metadata:
    aggregation: sum
    field: CASE WHEN guild_unlock_status = 1 THEN 1 ELSE 0 END) / NULLIF(COUNT(*), 0
    source: dws_10000251_role_tag_cbt1_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_role_tag_cbt1_df
    description: 机械提取自 dws_10000251_role_tag_cbt1_df 的 metrics 块（key=guild_unlock_rate）
