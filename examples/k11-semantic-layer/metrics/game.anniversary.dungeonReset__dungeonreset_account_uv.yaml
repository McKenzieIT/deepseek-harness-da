kind: metric
name: game.anniversary.dungeonReset__dungeonreset_account_uv
description: 触发周年庆关卡重置的去重账号数(去重基础视图 account_id,即 params.accountId)
domains:
  - 付费经济
computation:
  sql: COUNT(DISTINCT account_id)
  metadata:
    aggregation: count_distinct
    field: account_id
    source: game.anniversary.dungeonReset
    time_grain: ""
relations:
  - type: derived_from
    target: game.anniversary.dungeonReset
    description: 机械提取自 game.anniversary.dungeonReset 的 metrics 块（key=dungeonreset_account_uv）
