kind: metric
name: game.team.share__team_share_role_uv
description: 分享阵容的去重角色数（分析主体 role_id）
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.team.share
    time_grain: ""
relations:
  - type: derived_from
    target: game.team.share
    description: 机械提取自 game.team.share 的 metrics 块（key=team_share_role_uv）
