kind: metric
name: game.mission.onekeyReward__game_mission_onekey_reward_per_role_cnt
description: 人均领取次数（事件次数 / 去重角色数）
domains:
  - 角色成长
computation:
  sql: COUNT(*) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: count
    field: "*) / NULLIF(COUNT(DISTINCT role_id), 0"
    source: game.mission.onekeyReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.mission.onekeyReward
    description: 机械提取自 game.mission.onekeyReward 的 metrics 块（key=game_mission_onekey_reward_per_role_cnt）
