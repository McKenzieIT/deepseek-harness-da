kind: metric
name: game.maze.move__maze_move_avg_per_role
description: 人均迷宫移动次数（总次数/去重角色数）
domains:
  - 战斗关卡
computation:
  sql: ROUND(COUNT(*) * 1.0 / NULLIF(COUNT(DISTINCT role_id), 0), 2)
  metadata:
    aggregation: ""
    field: ""
    source: game.maze.move
    time_grain: ""
relations:
  - type: derived_from
    target: game.maze.move
    description: 机械提取自 game.maze.move 的 metrics 块（key=maze_move_avg_per_role）
