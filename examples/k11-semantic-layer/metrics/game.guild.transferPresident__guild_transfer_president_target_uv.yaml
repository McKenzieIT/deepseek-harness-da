kind: metric
name: game.guild.transferPresident__guild_transfer_president_target_uv
description: 接收会长职位的受让方角色数（targetUid 去重数，新会长数）
domains:
  - 社交公会
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params,'$.targetUid'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params,'$.targetUid')
    source: game.guild.transferPresident
    time_grain: ""
relations:
  - type: derived_from
    target: game.guild.transferPresident
    description: 机械提取自 game.guild.transferPresident 的 metrics 块（key=guild_transfer_president_target_uv）
