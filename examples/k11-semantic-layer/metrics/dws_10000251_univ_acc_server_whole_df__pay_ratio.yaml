kind: metric
name: dws_10000251_univ_acc_server_whole_df__pay_ratio
description: 累计付费率(付费账号数/快照内账号总数)；用 AVG(CASE WHEN pay_amt_std>0 THEN 1.0 ELSE 0 END) 避免除零，区间[0,1]
domains:
  - 用户生命周期
  - 付费经济
computation:
  sql: AVG(CASE WHEN pay_amt_std > 0 THEN 1.0 ELSE 0 END)
  metadata:
    aggregation: avg
    field: CASE WHEN pay_amt_std > 0 THEN 1.0 ELSE 0 END
    source: dws_10000251_univ_acc_server_whole_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_univ_acc_server_whole_df
    description: 机械提取自 dws_10000251_univ_acc_server_whole_df 的 metrics 块（key=pay_ratio）
