kind: metric
name: game.commonflag.add__game_commonflag_add_role_per_server
description: 单服平均标记写入角色数(NULLIF 防 0 除)
domains:
  - 系统监控
computation:
  sql: ROUND(CAST(COUNT(DISTINCT role_id) AS DOUBLE) / NULLIF(COUNT(DISTINCT server_id), 0), 2)
  metadata:
    aggregation: ""
    field: ""
    source: game.commonflag.add
    time_grain: ""
relations:
  - type: derived_from
    target: game.commonflag.add
    description: 机械提取自 game.commonflag.add 的 metrics 块（key=game_commonflag_add_role_per_server）
