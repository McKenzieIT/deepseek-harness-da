kind: metric
name: game.equip.starup__equip_starup_per_role
description: 人均升星次数（总次数/参与角色数）
domains:
  - 装备道具
computation:
  sql: ROUND(CAST(COUNT(*) AS DOUBLE) / NULLIF(COUNT(DISTINCT role_id), 0), 2)
  metadata:
    aggregation: ""
    field: ""
    source: game.equip.starup
    time_grain: ""
relations:
  - type: derived_from
    target: game.equip.starup
    description: 机械提取自 game.equip.starup 的 metrics 块（key=equip_starup_per_role）
