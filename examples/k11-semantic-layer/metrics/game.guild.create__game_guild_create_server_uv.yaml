kind: metric
name: game.guild.create__game_guild_create_server_uv
description: 发生公会创建的去重游戏服数
domains:
  - 社交公会
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: game.guild.create
    time_grain: ""
relations:
  - type: derived_from
    target: game.guild.create
    description: 机械提取自 game.guild.create 的 metrics 块（key=game_guild_create_server_uv）
