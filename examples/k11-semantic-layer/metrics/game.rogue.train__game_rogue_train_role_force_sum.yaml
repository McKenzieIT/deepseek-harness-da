kind: metric
name: game.rogue.train__game_rogue_train_role_force_sum
description: 触发激活天赋角色的战力(roleForce)累计值
domains:
  - 战斗关卡
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.roleForce') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.roleForce') AS BIGINT)
    source: game.rogue.train
    time_grain: ""
relations:
  - type: derived_from
    target: game.rogue.train
    description: 机械提取自 game.rogue.train 的 metrics 块（key=game_rogue_train_role_force_sum）
