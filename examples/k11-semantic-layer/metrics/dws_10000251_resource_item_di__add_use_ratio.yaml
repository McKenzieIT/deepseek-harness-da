kind: metric
name: dws_10000251_resource_item_di__add_use_ratio
description: 消耗/增加比（当日消耗总量占增加总量比例）
domains:
  - 资源产销
  - 装备道具
computation:
  sql: SUM(use_amount) / NULLIF(SUM(add_amount), 0)
  metadata:
    aggregation: sum
    field: use_amount) / NULLIF(SUM(add_amount), 0
    source: dws_10000251_resource_item_di
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_resource_item_di
    description: 机械提取自 dws_10000251_resource_item_di 的 metrics 块（key=add_use_ratio）
