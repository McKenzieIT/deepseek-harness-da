kind: metric
name: game.guild.battle.result__game_guild_battle_result_cnt
description: 公会战结算事件总记录数（按行计数，单日维度）
domains:
  - 社交公会
computation:
  sql: COUNT(*)
  metadata:
    aggregation: count
    field: "*"
    source: game.guild.battle.result
    time_grain: ""
relations:
  - type: derived_from
    target: game.guild.battle.result
    description: 机械提取自 game.guild.battle.result 的 metrics 块（key=game_guild_battle_result_cnt）
