kind: metric
name: game.team.setTeamDetail__team_setteamdetail_avg_actions_per_role
description: 人均编队设置动作次数（总次数 / 去重角色数）
domains:
  - 自定义
computation:
  sql: COUNT(*) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: count
    field: "*) / NULLIF(COUNT(DISTINCT role_id), 0"
    source: game.team.setTeamDetail
    time_grain: ""
relations:
  - type: derived_from
    target: game.team.setTeamDetail
    description: 机械提取自 game.team.setTeamDetail 的 metrics 块（key=team_setteamdetail_avg_actions_per_role）
