kind: metric
name: game.monthlyCard.reissueReward__game_monthlyCard_reissueReward_role_uv
description: 月卡补发奖励覆盖的去重角色数
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.monthlyCard.reissueReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.monthlyCard.reissueReward
    description: 机械提取自 game.monthlyCard.reissueReward 的 metrics 块（key=game_monthlyCard_reissueReward_role_uv）
