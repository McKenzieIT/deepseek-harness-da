kind: metric
name: game.fantasyrecharge.finalreward__fantasyrecharge_finalreward_sendgold_sum
description: 领取最终奖励涉及充值赠送钻石(sendgold)总量（实测 ds=20260729 为 0）
domains:
  - 付费经济
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.coinList.sendgold') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.coinList.sendgold') AS BIGINT)
    source: game.fantasyrecharge.finalreward
    time_grain: ""
relations:
  - type: derived_from
    target: game.fantasyrecharge.finalreward
    description: 机械提取自 game.fantasyrecharge.finalreward 的 metrics 块（key=fantasyrecharge_finalreward_sendgold_sum）
