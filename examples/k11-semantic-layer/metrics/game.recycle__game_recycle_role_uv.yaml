kind: metric
name: game.recycle__game_recycle_role_uv
description: 触发资源回收的去重角色数（分析主体粒度 role_id）
domains:
  - 资源产销
  - 装备道具
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.recycle
    time_grain: ""
relations:
  - type: derived_from
    target: game.recycle
    description: 机械提取自 game.recycle 的 metrics 块（key=game_recycle_role_uv）
