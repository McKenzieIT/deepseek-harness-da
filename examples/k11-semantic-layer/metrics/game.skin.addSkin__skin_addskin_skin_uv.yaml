kind: metric
name: game.skin.addSkin__skin_addskin_skin_uv
description: 被添加的去重皮肤种类数（取自 params.skinId，衡量皮肤发放广度）
domains:
  - 装备道具
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.skinId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.skinId')
    source: game.skin.addSkin
    time_grain: ""
relations:
  - type: derived_from
    target: game.skin.addSkin
    description: 机械提取自 game.skin.addSkin 的 metrics 块（key=skin_addskin_skin_uv）
