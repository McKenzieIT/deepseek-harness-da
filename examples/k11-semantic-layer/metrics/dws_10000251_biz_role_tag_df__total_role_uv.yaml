kind: metric
name: dws_10000251_biz_role_tag_df__total_role_uv
description: 当日快照角色总数(去重 user_id)，主体=角色(user_type=3)
domains:
  - 角色成长
  - 系统监控
computation:
  sql: COUNT(DISTINCT user_id)
  metadata:
    aggregation: count_distinct
    field: user_id
    source: dws_10000251_biz_role_tag_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_biz_role_tag_df
    description: 机械提取自 dws_10000251_biz_role_tag_df 的 metrics 块（key=total_role_uv）
