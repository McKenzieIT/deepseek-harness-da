kind: metric
name: dws_10000251_univ_acc_whole_df__active_age_days
description: 平均活跃时长(天)=最近活跃-首次活跃(act_tm_lst - act_tm_fst)
domains:
  - 用户生命周期
  - 付费经济
computation:
  sql: AVG(CASE WHEN act_tm_lst IS NOT NULL AND act_tm_fst IS NOT NULL THEN DATEDIFF(act_tm_lst, act_tm_fst, 'dd') END)
  metadata:
    aggregation: avg
    field: CASE WHEN act_tm_lst IS NOT NULL AND act_tm_fst IS NOT NULL THEN DATEDIFF(act_tm_lst, act_tm_fst, 'dd') END
    source: dws_10000251_univ_acc_whole_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_univ_acc_whole_df
    description: 机械提取自 dws_10000251_univ_acc_whole_df 的 metrics 块（key=active_age_days）
