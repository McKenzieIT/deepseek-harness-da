kind: metric
name: game.rogue.end__game_rogue_end_talent_points_sum
description: 古战场副本获得天赋点数总量（talentPoints 求和）
domains:
  - 战斗关卡
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.talentPoints') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.talentPoints') AS BIGINT)
    source: game.rogue.end
    time_grain: ""
relations:
  - type: derived_from
    target: game.rogue.end
    description: 机械提取自 game.rogue.end 的 metrics 块（key=game_rogue_end_talent_points_sum）
