kind: metric
name: game.showCamp.unlock__showCamp_unlock_server_uv
description: 触发营地解锁的去重服务器数
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: game.showCamp.unlock
    time_grain: ""
relations:
  - type: derived_from
    target: game.showCamp.unlock
    description: 机械提取自 game.showCamp.unlock 的 metrics 块（key=showCamp_unlock_server_uv）
