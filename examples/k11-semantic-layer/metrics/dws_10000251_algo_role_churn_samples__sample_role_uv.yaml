kind: metric
name: dws_10000251_algo_role_churn_samples__sample_role_uv
description: 去重角色样本UV（同角色在不同预测窗口/版本下多次出现）
domains:
  - 用户生命周期
  - 系统监控
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: dws_10000251_algo_role_churn_samples
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_algo_role_churn_samples
    description: 机械提取自 dws_10000251_algo_role_churn_samples 的 metrics 块（key=sample_role_uv）
