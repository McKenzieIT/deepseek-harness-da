kind: metric
name: dws_10000251_univ_dev_tag_df__first_day_pay_rate
description: 新增首日付费率(付费设备数占比,0-1 比率,应在新增设备集合上计算)
domains:
  - 用户生命周期
  - 付费经济
  - 角色成长
computation:
  sql: AVG(CASE WHEN pay_amt_f1d > 0 THEN 1 ELSE 0 END)
  metadata:
    aggregation: avg
    field: CASE WHEN pay_amt_f1d > 0 THEN 1 ELSE 0 END
    source: dws_10000251_univ_dev_tag_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_univ_dev_tag_df
    description: 机械提取自 dws_10000251_univ_dev_tag_df 的 metrics 块（key=first_day_pay_rate）
