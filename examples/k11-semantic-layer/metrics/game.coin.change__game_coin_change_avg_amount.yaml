kind: metric
name: game.coin.change__game_coin_change_avg_amount
description: 单次货币变化平均变化量
domains:
  - 付费经济
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params,'$.amount') AS BIGINT)) / NULLIF(COUNT(*), 0)
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params,'$.amount') AS BIGINT)) / NULLIF(COUNT(*), 0
    source: game.coin.change
    time_grain: ""
relations:
  - type: derived_from
    target: game.coin.change
    description: 机械提取自 game.coin.change 的 metrics 块（key=game_coin_change_avg_amount）
