kind: metric
name: game.welfare.retroactiveMonthlySign__retroactive_monthly_sign_role_uv
description: 补签去重角色数（分析主体 role_id 粒度）
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.welfare.retroactiveMonthlySign
    time_grain: ""
relations:
  - type: derived_from
    target: game.welfare.retroactiveMonthlySign
    description: 机械提取自 game.welfare.retroactiveMonthlySign 的 metrics 块（key=retroactive_monthly_sign_role_uv）
