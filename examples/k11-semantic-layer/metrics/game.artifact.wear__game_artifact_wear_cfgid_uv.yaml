kind: metric
name: game.artifact.wear__game_artifact_wear_cfgid_uv
description: 被穿戴兵符的去重配置id数（兵符种类）
domains:
  - 角色成长
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.artifact.cfgId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.artifact.cfgId')
    source: game.artifact.wear
    time_grain: ""
relations:
  - type: derived_from
    target: game.artifact.wear
    description: 机械提取自 game.artifact.wear 的 metrics 块（key=game_artifact_wear_cfgid_uv）
