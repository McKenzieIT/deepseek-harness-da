kind: metric
name: game.resort.rankReward__resort_rank_reward_role_uv
description: 触发头衔奖励的去重角色数
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.resort.rankReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.resort.rankReward
    description: 机械提取自 game.resort.rankReward 的 metrics 块（key=resort_rank_reward_role_uv）
