kind: metric
name: game.war.getMissionReward__game_war_get_mission_reward_server_uv
description: 领取奖励覆盖的去重游戏服数
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: game.war.getMissionReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.war.getMissionReward
    description: 机械提取自 game.war.getMissionReward 的 metrics 块（key=game_war_get_mission_reward_server_uv）
