kind: metric
name: dws_10000251_card_pve_act_di__used_card_cnt
description: 当日被使用的卡牌种类数（去重card_id）
domains:
  - 战斗关卡
  - 装备道具
computation:
  sql: COUNT(DISTINCT card_id)
  metadata:
    aggregation: count_distinct
    field: card_id
    source: dws_10000251_card_pve_act_di
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_card_pve_act_di
    description: 机械提取自 dws_10000251_card_pve_act_di 的 metrics 块（key=used_card_cnt）
