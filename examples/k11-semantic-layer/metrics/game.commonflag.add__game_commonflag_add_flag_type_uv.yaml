kind: metric
name: game.commonflag.add__game_commonflag_add_flag_type_uv
description: 被设置的去重标记类型数(type；scout 实测当日仅 type=1)
domains:
  - 系统监控
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.type'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.type')
    source: game.commonflag.add
    time_grain: ""
relations:
  - type: derived_from
    target: game.commonflag.add
    description: 机械提取自 game.commonflag.add 的 metrics 块（key=game_commonflag_add_flag_type_uv）
