kind: metric
name: game.charmStone.refinement__charm_stone_refinement_charmstone_uv
description: 被洗练的去重灵石数（按 charmStone.uid）
domains:
  - 探索收集
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.charmStone.uid'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.charmStone.uid')
    source: game.charmStone.refinement
    time_grain: ""
relations:
  - type: derived_from
    target: game.charmStone.refinement
    description: 机械提取自 game.charmStone.refinement 的 metrics 块（key=charm_stone_refinement_charmstone_uv）
