kind: metric
name: game.guild.battle.result__game_guild_battle_result_guild_uv
description: 公会战结算涉及的去重本方公会数（单日）
domains:
  - 社交公会
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params,'$.guildId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params,'$.guildId')
    source: game.guild.battle.result
    time_grain: ""
relations:
  - type: derived_from
    target: game.guild.battle.result
    description: 机械提取自 game.guild.battle.result 的 metrics 块（key=game_guild_battle_result_guild_uv）
