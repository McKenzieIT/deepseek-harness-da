kind: metric
name: game.pve.daily__game_pve_daily_stage_uv
description: 命中关卡去重数
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params,'$.stageId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params,'$.stageId')
    source: game.pve.daily
    time_grain: ""
relations:
  - type: derived_from
    target: game.pve.daily
    description: 机械提取自 game.pve.daily 的 metrics 块（key=game_pve_daily_stage_uv）
