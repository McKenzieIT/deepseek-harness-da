kind: metric
name: dws_10000251_acc_summary_di__daily_pay_arpu
description: 当日付费账号 ARPU（元/付费账号）
domains:
  - 用户生命周期
  - 付费经济
computation:
  sql: SUM(pay_amt_std) / NULLIF(COUNT(DISTINCT CASE WHEN pay_amt_std > 0 THEN account_id END), 0)
  metadata:
    aggregation: sum
    field: pay_amt_std) / NULLIF(COUNT(DISTINCT CASE WHEN pay_amt_std > 0 THEN account_id END), 0
    source: dws_10000251_acc_summary_di
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_acc_summary_di
    description: 机械提取自 dws_10000251_acc_summary_di 的 metrics 块（key=daily_pay_arpu）
