kind: metric
name: dws_10000251_card_pve_di__card_pve_win_rate
description: 卡牌胜率（胜利次数/参与次数）
domains:
  - 战斗关卡
  - 装备道具
computation:
  sql: SUM(win_pv) / NULLIF(SUM(part_pv), 0)
  metadata:
    aggregation: sum
    field: win_pv) / NULLIF(SUM(part_pv), 0
    source: dws_10000251_card_pve_di
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_card_pve_di
    description: 机械提取自 dws_10000251_card_pve_di 的 metrics 块（key=card_pve_win_rate）
