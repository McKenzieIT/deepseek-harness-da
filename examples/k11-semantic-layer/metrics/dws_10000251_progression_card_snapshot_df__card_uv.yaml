kind: metric
name: dws_10000251_progression_card_snapshot_df__card_uv
description: 卡牌快照中不同卡牌类型数(snapshot维度的卡牌池宽度)
domains:
  - 角色成长
  - 探索收集
computation:
  sql: COUNT(DISTINCT card_id)
  metadata:
    aggregation: count_distinct
    field: card_id
    source: dws_10000251_progression_card_snapshot_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_progression_card_snapshot_df
    description: 机械提取自 dws_10000251_progression_card_snapshot_df 的 metrics 块（key=card_uv）
