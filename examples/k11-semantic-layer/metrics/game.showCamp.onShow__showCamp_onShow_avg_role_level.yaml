kind: metric
name: game.showCamp.onShow__showCamp_onShow_avg_role_level
description: 触发营地展示角色的平均等级
domains:
  - 自定义
computation:
  sql: AVG(CAST(GET_JSON_OBJECT(params, '$.roleLevel') AS BIGINT))
  metadata:
    aggregation: avg
    field: CAST(GET_JSON_OBJECT(params, '$.roleLevel') AS BIGINT)
    source: game.showCamp.onShow
    time_grain: ""
relations:
  - type: derived_from
    target: game.showCamp.onShow
    description: 机械提取自 game.showCamp.onShow 的 metrics 块（key=showCamp_onShow_avg_role_level）
