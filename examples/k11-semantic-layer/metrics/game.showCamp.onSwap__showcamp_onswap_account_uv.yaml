kind: metric
name: game.showCamp.onSwap__showcamp_onswap_account_uv
description: 触发营地交换的去重账号数
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT account_id)
  metadata:
    aggregation: count_distinct
    field: account_id
    source: game.showCamp.onSwap
    time_grain: ""
relations:
  - type: derived_from
    target: game.showCamp.onSwap
    description: 机械提取自 game.showCamp.onSwap 的 metrics 块（key=showcamp_onswap_account_uv）
