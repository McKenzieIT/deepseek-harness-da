kind: metric
name: game.guildActivity.bonus__guild_activity_bonus_cnt
description: 公会活跃分红领取事件触发次数(每次分红领取记1条)
domains:
  - 社交公会
computation:
  sql: COUNT(*)
  metadata:
    aggregation: count
    field: "*"
    source: game.guildActivity.bonus
    time_grain: ""
relations:
  - type: derived_from
    target: game.guildActivity.bonus
    description: 机械提取自 game.guildActivity.bonus 的 metrics 块（key=guild_activity_bonus_cnt）
