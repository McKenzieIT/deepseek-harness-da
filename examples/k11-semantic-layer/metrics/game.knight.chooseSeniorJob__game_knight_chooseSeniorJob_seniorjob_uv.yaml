kind: metric
name: game.knight.chooseSeniorJob__game_knight_chooseSeniorJob_seniorjob_uv
description: 被选择进阶职业的去重职业类型数（取自 params.seniorJob）
domains:
  - 角色成长
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.seniorJob'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.seniorJob')
    source: game.knight.chooseSeniorJob
    time_grain: ""
relations:
  - type: derived_from
    target: game.knight.chooseSeniorJob
    description: 机械提取自 game.knight.chooseSeniorJob 的 metrics 块（key=game_knight_chooseSeniorJob_seniorjob_uv）
