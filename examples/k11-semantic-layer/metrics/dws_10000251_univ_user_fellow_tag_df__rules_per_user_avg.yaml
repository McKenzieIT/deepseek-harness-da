kind: metric
name: dws_10000251_univ_user_fellow_tag_df__rules_per_user_avg
description: 当日命中用户的人均命中规则数(逗号串近似口径);分子为去重规则数,分母为去重用户数,除零用 NULLIF 保护
domains:
  - 社交公会
computation:
  sql: COUNT(DISTINCT rule_id)*1.0/NULLIF(COUNT(DISTINCT user_id),0)
  metadata:
    aggregation: count_distinct
    field: rule_id)*1.0/NULLIF(COUNT(DISTINCT user_id),0
    source: dws_10000251_univ_user_fellow_tag_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_univ_user_fellow_tag_df
    description: 机械提取自 dws_10000251_univ_user_fellow_tag_df 的 metrics 块（key=rules_per_user_avg）
