kind: metric
name: game.resort.accept__resort_accept_cnt
description: 代打接受事件总次数（每次接受代打请求计一次）
domains:
  - 自定义
computation:
  sql: COUNT(*)
  metadata:
    aggregation: count
    field: "*"
    source: game.resort.accept
    time_grain: ""
relations:
  - type: derived_from
    target: game.resort.accept
    description: 机械提取自 game.resort.accept 的 metrics 块（key=resort_accept_cnt）
