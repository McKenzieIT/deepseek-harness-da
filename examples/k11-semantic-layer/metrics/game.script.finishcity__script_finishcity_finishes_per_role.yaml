kind: metric
name: game.script.finishcity__script_finishcity_finishes_per_role
description: 每个角色的平均完成城池次数（>1 表示同角色当日完成多座城池/多剧本）
domains:
  - 自定义
computation:
  sql: CAST(COUNT(*) AS DOUBLE) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: ""
    field: ""
    source: game.script.finishcity
    time_grain: ""
relations:
  - type: derived_from
    target: game.script.finishcity
    description: 机械提取自 game.script.finishcity 的 metrics 块（key=script_finishcity_finishes_per_role）
