kind: metric
name: game.war.setQishi__war_setQishi_qishi_id_uv
description: 配置不同气势 id 的去重角色数（探查气势配置多样性）
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT CASE WHEN GET_JSON_OBJECT(params,'$.id') IS NOT NULL THEN role_id END)
  metadata:
    aggregation: count_distinct
    field: CASE WHEN GET_JSON_OBJECT(params,'$.id') IS NOT NULL THEN role_id END
    source: game.war.setQishi
    time_grain: ""
relations:
  - type: derived_from
    target: game.war.setQishi
    description: 机械提取自 game.war.setQishi 的 metrics 块（key=war_setQishi_qishi_id_uv）
