kind: metric
name: game.role.create__game_role_create_server_uv
description: 发生创角的游戏服去重数
domains:
  - 用户生命周期
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: game.role.create
    time_grain: ""
relations:
  - type: derived_from
    target: game.role.create
    description: 机械提取自 game.role.create 的 metrics 块（key=game_role_create_server_uv）
