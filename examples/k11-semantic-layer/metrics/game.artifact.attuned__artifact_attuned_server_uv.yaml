kind: metric
name: game.artifact.attuned__artifact_attuned_server_uv
description: 发生兵符鉴定的去重游戏服数
domains:
  - 角色成长
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: game.artifact.attuned
    time_grain: ""
relations:
  - type: derived_from
    target: game.artifact.attuned
    description: 机械提取自 game.artifact.attuned 的 metrics 块（key=artifact_attuned_server_uv）
