kind: metric
name: game.war.activityEnterPerformDungeon__game_war_activity_enter_perform_dungeon_dungeon_uv
description: 进入命中的不同剧情关卡id数（dungeonId 去重）
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.dungeonId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.dungeonId')
    source: game.war.activityEnterPerformDungeon
    time_grain: ""
relations:
  - type: derived_from
    target: game.war.activityEnterPerformDungeon
    description: 机械提取自 game.war.activityEnterPerformDungeon 的 metrics 块（key=game_war_activity_enter_perform_dungeon_dungeon_uv）
