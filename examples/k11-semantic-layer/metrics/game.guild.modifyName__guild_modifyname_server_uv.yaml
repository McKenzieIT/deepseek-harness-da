kind: metric
name: game.guild.modifyName__guild_modifyname_server_uv
description: 涉及改名的游戏服去重数
domains:
  - 社交公会
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: game.guild.modifyName
    time_grain: ""
relations:
  - type: derived_from
    target: game.guild.modifyName
    description: 机械提取自 game.guild.modifyName 的 metrics 块（key=guild_modifyname_server_uv）
