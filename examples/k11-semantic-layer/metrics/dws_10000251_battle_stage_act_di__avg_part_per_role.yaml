kind: metric
name: dws_10000251_battle_stage_act_di__avg_part_per_role
description: 当日人均玩法参与次数（当日总参与次数/活跃角色数）
domains:
  - 战斗关卡
  - 用户生命周期
computation:
  sql: SUM(part_pv_di) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: sum
    field: part_pv_di) / NULLIF(COUNT(DISTINCT role_id), 0
    source: dws_10000251_battle_stage_act_di
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_battle_stage_act_di
    description: 机械提取自 dws_10000251_battle_stage_act_di 的 metrics 块（key=avg_part_per_role）
