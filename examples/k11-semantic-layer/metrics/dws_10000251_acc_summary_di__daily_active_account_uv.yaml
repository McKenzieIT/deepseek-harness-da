kind: metric
name: dws_10000251_acc_summary_di__daily_active_account_uv
description: 当日活跃账号数（DAU），account_id 去重
domains:
  - 用户生命周期
  - 付费经济
computation:
  sql: COUNT(DISTINCT account_id)
  metadata:
    aggregation: count_distinct
    field: account_id
    source: dws_10000251_acc_summary_di
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_acc_summary_di
    description: 机械提取自 dws_10000251_acc_summary_di 的 metrics 块（key=daily_active_account_uv）
