kind: metric
name: game.qice.missionReward__qice_missionReward_role_level_avg
description: 领取角色的平均角色等级
domains:
  - 自定义
computation:
  sql: AVG(CAST(GET_JSON_OBJECT(params, '$.roleLevel') AS BIGINT))
  metadata:
    aggregation: avg
    field: CAST(GET_JSON_OBJECT(params, '$.roleLevel') AS BIGINT)
    source: game.qice.missionReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.qice.missionReward
    description: 机械提取自 game.qice.missionReward 的 metrics 块（key=qice_missionReward_role_level_avg）
