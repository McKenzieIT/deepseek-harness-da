kind: metric
name: game.role.create__game_role_create_cnt
description: 创角事件总次数（含同一角色可能的多次上报）
domains:
  - 用户生命周期
computation:
  sql: COUNT(*)
  metadata:
    aggregation: count
    field: "*"
    source: game.role.create
    time_grain: ""
relations:
  - type: derived_from
    target: game.role.create
    description: 机械提取自 game.role.create 的 metrics 块（key=game_role_create_cnt）
