kind: metric
name: game.welfare.monthlySignDayReward__game_welfare_monthly_sign_day_reward_diamond_sum
description: 领奖时记录的紫金余额合计（快照余额，非当日产出）
domains:
  - 自定义
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.coinList.diamond') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.coinList.diamond') AS BIGINT)
    source: game.welfare.monthlySignDayReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.welfare.monthlySignDayReward
    description: 机械提取自 game.welfare.monthlySignDayReward 的 metrics 块（key=game_welfare_monthly_sign_day_reward_diamond_sum）
