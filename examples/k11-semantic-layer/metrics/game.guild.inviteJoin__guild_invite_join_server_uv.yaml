kind: metric
name: game.guild.inviteJoin__guild_invite_join_server_uv
description: 产生邀请行为的去重游戏服数
domains:
  - 社交公会
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: game.guild.inviteJoin
    time_grain: ""
relations:
  - type: derived_from
    target: game.guild.inviteJoin
    description: 机械提取自 game.guild.inviteJoin 的 metrics 块（key=guild_invite_join_server_uv）
