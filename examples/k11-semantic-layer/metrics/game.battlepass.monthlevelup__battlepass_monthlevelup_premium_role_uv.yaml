kind: metric
name: game.battlepass.monthlevelup__battlepass_monthlevelup_premium_role_uv
description: 高级/珍藏版战令（付费版）等级提升的去重角色数
domains:
  - 角色成长
computation:
  sql: COUNT(DISTINCT CASE WHEN CAST(GET_JSON_OBJECT(params,'$.enabledStatus') AS INT) IN (1,2) THEN role_id END)
  metadata:
    aggregation: count_distinct
    field: CASE WHEN CAST(GET_JSON_OBJECT(params,'$.enabledStatus') AS INT) IN (1,2) THEN role_id END
    source: game.battlepass.monthlevelup
    time_grain: ""
relations:
  - type: derived_from
    target: game.battlepass.monthlevelup
    description: 机械提取自 game.battlepass.monthlevelup 的 metrics 块（key=battlepass_monthlevelup_premium_role_uv）
