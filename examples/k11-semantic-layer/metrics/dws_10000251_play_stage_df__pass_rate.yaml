kind: metric
name: dws_10000251_play_stage_df__pass_rate
description: 通关率(通关记录数/总记录数，行级)
domains:
  - 战斗关卡
  - 角色成长
computation:
  sql: SUM(CASE WHEN pass_status = 1 THEN 1 ELSE 0 END) / NULLIF(COUNT(*), 0)
  metadata:
    aggregation: sum
    field: CASE WHEN pass_status = 1 THEN 1 ELSE 0 END) / NULLIF(COUNT(*), 0
    source: dws_10000251_play_stage_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_play_stage_df
    description: 机械提取自 dws_10000251_play_stage_df 的 metrics 块（key=pass_rate）
