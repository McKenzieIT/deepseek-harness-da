kind: metric
name: game.coin.change__game_coin_change_amount_sum
description: 货币变化总量(amount 累加，获得+消耗合计绝对流量)
domains:
  - 付费经济
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params,'$.amount') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params,'$.amount') AS BIGINT)
    source: game.coin.change
    time_grain: ""
relations:
  - type: derived_from
    target: game.coin.change
    description: 机械提取自 game.coin.change 的 metrics 块（key=game_coin_change_amount_sum）
