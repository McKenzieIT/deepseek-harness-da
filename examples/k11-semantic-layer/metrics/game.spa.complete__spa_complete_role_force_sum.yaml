kind: metric
name: game.spa.complete__spa_complete_role_force_sum
description: 触发事件角色的战力累计（GET_JSON_OBJECT 取 params.roleForce 求和）
domains:
  - 自定义
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.roleForce') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.roleForce') AS BIGINT)
    source: game.spa.complete
    time_grain: ""
relations:
  - type: derived_from
    target: game.spa.complete
    description: 机械提取自 game.spa.complete 的 metrics 块（key=spa_complete_role_force_sum）
