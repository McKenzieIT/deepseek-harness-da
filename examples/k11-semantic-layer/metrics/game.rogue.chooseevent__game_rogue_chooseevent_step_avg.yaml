kind: metric
name: game.rogue.chooseevent__game_rogue_chooseevent_step_avg
description: 事件选择发生时的平均步数（step）
domains:
  - 战斗关卡
computation:
  sql: AVG(CAST(GET_JSON_OBJECT(params,'$.step') AS BIGINT))
  metadata:
    aggregation: avg
    field: CAST(GET_JSON_OBJECT(params,'$.step') AS BIGINT)
    source: game.rogue.chooseevent
    time_grain: ""
relations:
  - type: derived_from
    target: game.rogue.chooseevent
    description: 机械提取自 game.rogue.chooseevent 的 metrics 块（key=game_rogue_chooseevent_step_avg）
