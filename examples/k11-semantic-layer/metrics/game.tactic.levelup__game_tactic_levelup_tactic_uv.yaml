kind: metric
name: game.tactic.levelup__game_tactic_levelup_tactic_uv
description: 被升级的去重战法id数
domains:
  - 角色成长
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.tacticId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.tacticId')
    source: game.tactic.levelup
    time_grain: ""
relations:
  - type: derived_from
    target: game.tactic.levelup
    description: 机械提取自 game.tactic.levelup 的 metrics 块（key=game_tactic_levelup_tactic_uv）
