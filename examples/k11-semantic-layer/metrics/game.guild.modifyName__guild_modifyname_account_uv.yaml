kind: metric
name: game.guild.modifyName__guild_modifyname_account_uv
description: 改名的去重账号数
domains:
  - 社交公会
computation:
  sql: COUNT(DISTINCT account_id)
  metadata:
    aggregation: count_distinct
    field: account_id
    source: game.guild.modifyName
    time_grain: ""
relations:
  - type: derived_from
    target: game.guild.modifyName
    description: 机械提取自 game.guild.modifyName 的 metrics 块（key=guild_modifyname_account_uv）
