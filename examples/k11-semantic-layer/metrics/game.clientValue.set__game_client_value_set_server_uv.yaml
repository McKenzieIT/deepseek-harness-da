kind: metric
name: game.clientValue.set__game_client_value_set_server_uv
description: 涉及的游戏服数量（去重）
domains:
  - 系统监控
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: game.clientValue.set
    time_grain: ""
relations:
  - type: derived_from
    target: game.clientValue.set
    description: 机械提取自 game.clientValue.set 的 metrics 块（key=game_client_value_set_server_uv）
