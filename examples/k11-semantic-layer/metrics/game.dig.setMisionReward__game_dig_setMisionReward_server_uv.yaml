kind: metric
name: game.dig.setMisionReward__game_dig_setMisionReward_server_uv
description: 触发挖宝设置任务奖励的去重游戏服数
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: game.dig.setMisionReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.dig.setMisionReward
    description: 机械提取自 game.dig.setMisionReward 的 metrics 块（key=game_dig_setMisionReward_server_uv）
