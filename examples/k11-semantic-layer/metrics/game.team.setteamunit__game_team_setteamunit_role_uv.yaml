kind: metric
name: game.team.setteamunit__game_team_setteamunit_role_uv
description: 设置阵容的去重角色数（主体粒度）
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.team.setteamunit
    time_grain: ""
relations:
  - type: derived_from
    target: game.team.setteamunit
    description: 机械提取自 game.team.setteamunit 的 metrics 块（key=game_team_setteamunit_role_uv）
