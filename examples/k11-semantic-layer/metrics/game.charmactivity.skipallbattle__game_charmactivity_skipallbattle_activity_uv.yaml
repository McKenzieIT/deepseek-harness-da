kind: metric
name: game.charmactivity.skipallbattle__game_charmactivity_skipallbattle_activity_uv
description: 涉及的灵器活动去重数（activityId）
domains:
  - 探索收集
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params,'$.activityId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params,'$.activityId')
    source: game.charmactivity.skipallbattle
    time_grain: ""
relations:
  - type: derived_from
    target: game.charmactivity.skipallbattle
    description: 机械提取自 game.charmactivity.skipallbattle 的 metrics 块（key=game_charmactivity_skipallbattle_activity_uv）
