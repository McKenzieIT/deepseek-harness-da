kind: metric
name: game.champion.sendFlyTxt__champion_sendfly_txt_fail_cnt
description: 弹幕发送失败次数（sendResult=0）
domains:
  - 战斗关卡
computation:
  sql: SUM(CASE WHEN GET_JSON_OBJECT(params,'$.sendResult') = '0' THEN 1 ELSE 0 END)
  metadata:
    aggregation: sum
    field: CASE WHEN GET_JSON_OBJECT(params,'$.sendResult') = '0' THEN 1 ELSE 0 END
    source: game.champion.sendFlyTxt
    time_grain: ""
relations:
  - type: derived_from
    target: game.champion.sendFlyTxt
    description: 机械提取自 game.champion.sendFlyTxt 的 metrics 块（key=champion_sendfly_txt_fail_cnt）
