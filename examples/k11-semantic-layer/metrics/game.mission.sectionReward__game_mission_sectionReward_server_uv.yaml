kind: metric
name: game.mission.sectionReward__game_mission_sectionReward_server_uv
description: 领奖覆盖游戏服去重数
domains:
  - 角色成长
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: game.mission.sectionReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.mission.sectionReward
    description: 机械提取自 game.mission.sectionReward 的 metrics 块（key=game_mission_sectionReward_server_uv）
