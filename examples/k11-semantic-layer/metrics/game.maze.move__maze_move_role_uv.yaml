kind: metric
name: game.maze.move__maze_move_role_uv
description: 触发迷宫移动的去重角色数
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.maze.move
    time_grain: ""
relations:
  - type: derived_from
    target: game.maze.move
    description: 机械提取自 game.maze.move 的 metrics 块（key=maze_move_role_uv）
