kind: metric
name: game.war.setQishi__war_setQishi_vip_level_avg
description: 触发事件角色平均 VIP 等级
domains:
  - 战斗关卡
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params,'$.vipLevel') AS BIGINT)) / NULLIF(COUNT(CAST(GET_JSON_OBJECT(params,'$.vipLevel') AS BIGINT)),0)
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params,'$.vipLevel') AS BIGINT)) / NULLIF(COUNT(CAST(GET_JSON_OBJECT(params,'$.vipLevel') AS BIGINT)),0
    source: game.war.setQishi
    time_grain: ""
relations:
  - type: derived_from
    target: game.war.setQishi
    description: 机械提取自 game.war.setQishi 的 metrics 块（key=war_setQishi_vip_level_avg）
