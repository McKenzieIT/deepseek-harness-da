kind: metric
name: game.train.inherit__game_train_inherit_role_uv
description: 执行武将继承的去重角色数
domains:
  - 角色成长
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.train.inherit
    time_grain: ""
relations:
  - type: derived_from
    target: game.train.inherit
    description: 机械提取自 game.train.inherit 的 metrics 块（key=game_train_inherit_role_uv）
