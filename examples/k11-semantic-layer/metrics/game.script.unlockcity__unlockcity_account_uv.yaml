kind: metric
name: game.script.unlockcity__unlockcity_account_uv
description: 解锁城池的去重账号数
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT account_id)
  metadata:
    aggregation: count_distinct
    field: account_id
    source: game.script.unlockcity
    time_grain: ""
relations:
  - type: derived_from
    target: game.script.unlockcity
    description: 机械提取自 game.script.unlockcity 的 metrics 块（key=unlockcity_account_uv）
