kind: metric
name: game.dig.useDigItem__game_dig_useDigItem_dig_count_sum
description: 挖掘次数累计总和(按 digCount 字段汇总)
domains:
  - 自定义
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.digCount') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.digCount') AS BIGINT)
    source: game.dig.useDigItem
    time_grain: ""
relations:
  - type: derived_from
    target: game.dig.useDigItem
    description: 机械提取自 game.dig.useDigItem 的 metrics 块（key=game_dig_useDigItem_dig_count_sum）
