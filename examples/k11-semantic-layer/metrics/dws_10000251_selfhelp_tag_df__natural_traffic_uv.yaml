kind: metric
name: dws_10000251_selfhelp_tag_df__natural_traffic_uv
description: 当日自然量用户数(attr_media_name=自然量)，未付费媒体自然流量
domains:
  - 用户生命周期
  - 系统监控
computation:
  sql: SUM(CASE WHEN GET_JSON_OBJECT(udd,'$.attr_media_name')='自然量' THEN 1 ELSE 0 END)
  metadata:
    aggregation: sum
    field: CASE WHEN GET_JSON_OBJECT(udd,'$.attr_media_name')='自然量' THEN 1 ELSE 0 END
    source: dws_10000251_selfhelp_tag_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_selfhelp_tag_df
    description: 机械提取自 dws_10000251_selfhelp_tag_df 的 metrics 块（key=natural_traffic_uv）
