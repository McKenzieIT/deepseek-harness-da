kind: metric
name: game.equipmentPresets.set__game_equipmentPresets_set_preset_uv
description: 配置的去重装备预设id数（按预设id去重）
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.id'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.id')
    source: game.equipmentPresets.set
    time_grain: ""
relations:
  - type: derived_from
    target: game.equipmentPresets.set
    description: 机械提取自 game.equipmentPresets.set 的 metrics 块（key=game_equipmentPresets_set_preset_uv）
