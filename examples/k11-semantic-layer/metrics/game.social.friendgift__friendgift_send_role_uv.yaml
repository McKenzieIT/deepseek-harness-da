kind: metric
name: game.social.friendgift__friendgift_send_role_uv
description: 单次赠礼参与角色数（去重）
domains:
  - 社交公会
computation:
  sql: COUNT(DISTINCT CASE WHEN GET_JSON_OBJECT(params,'$.action')='1' THEN role_id END)
  metadata:
    aggregation: count_distinct
    field: CASE WHEN GET_JSON_OBJECT(params,'$.action')='1' THEN role_id END
    source: game.social.friendgift
    time_grain: ""
relations:
  - type: derived_from
    target: game.social.friendgift
    description: 机械提取自 game.social.friendgift 的 metrics 块（key=friendgift_send_role_uv）
