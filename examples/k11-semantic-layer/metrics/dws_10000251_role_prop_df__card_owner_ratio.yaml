kind: metric
name: dws_10000251_role_prop_df__card_owner_ratio
description: 持有武将角色占当日角色UV的比例。
domains:
  - 角色成长
  - 装备道具
computation:
  sql: COUNT(IF(own_card_list IS NOT NULL AND own_card_list != '[]', 1, NULL)) * 1.0 / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: count
    field: IF(own_card_list IS NOT NULL AND own_card_list != '[]', 1, NULL)) * 1.0 / NULLIF(COUNT(DISTINCT role_id), 0
    source: dws_10000251_role_prop_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_role_prop_df
    description: 机械提取自 dws_10000251_role_prop_df 的 metrics 块（key=card_owner_ratio）
