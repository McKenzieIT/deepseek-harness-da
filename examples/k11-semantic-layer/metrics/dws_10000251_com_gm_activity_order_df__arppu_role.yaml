kind: metric
name: dws_10000251_com_gm_activity_order_df__arppu_role
description: 角色人均付费金额（分，NULLIF防分母为0）
domains:
  - 付费经济
  - 系统监控
computation:
  sql: SUM(pay_amt) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: sum
    field: pay_amt) / NULLIF(COUNT(DISTINCT role_id), 0
    source: dws_10000251_com_gm_activity_order_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_com_gm_activity_order_df
    description: 机械提取自 dws_10000251_com_gm_activity_order_df 的 metrics 块（key=arppu_role）
