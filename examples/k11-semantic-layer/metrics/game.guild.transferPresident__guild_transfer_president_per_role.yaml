kind: metric
name: game.guild.transferPresident__guild_transfer_president_per_role
description: 单个转让方平均发起的会长转让次数（总次数/转让方角色数，使用 NULLIF 防止除零）
domains:
  - 社交公会
computation:
  sql: COUNT(*) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: count
    field: "*) / NULLIF(COUNT(DISTINCT role_id), 0"
    source: game.guild.transferPresident
    time_grain: ""
relations:
  - type: derived_from
    target: game.guild.transferPresident
    description: 机械提取自 game.guild.transferPresident 的 metrics 块（key=guild_transfer_president_per_role）
