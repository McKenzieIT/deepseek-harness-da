kind: metric
name: game.commonflag.add__game_commonflag_add_role_uv
description: 触发标记写入的去重角色数(主体粒度 role_id)
domains:
  - 系统监控
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.commonflag.add
    time_grain: ""
relations:
  - type: derived_from
    target: game.commonflag.add
    description: 机械提取自 game.commonflag.add 的 metrics 块（key=game_commonflag_add_role_uv）
