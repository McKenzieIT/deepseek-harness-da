kind: metric
name: game.toy.gachaMissionOnekey__gacha_mission_onekey_avg_per_role
description: 平均每个角色触发抽卡任务一键领取的次数（人均领取频次）
domains:
  - 探索收集
computation:
  sql: ROUND(COUNT(*) * 1.0 / NULLIF(COUNT(DISTINCT role_id), 0), 2)
  metadata:
    aggregation: ""
    field: ""
    source: game.toy.gachaMissionOnekey
    time_grain: ""
relations:
  - type: derived_from
    target: game.toy.gachaMissionOnekey
    description: 机械提取自 game.toy.gachaMissionOnekey 的 metrics 块（key=gacha_mission_onekey_avg_per_role）
