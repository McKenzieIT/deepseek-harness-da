kind: metric
name: game.battlepass.reward__game_battlepass_reward_oneclick_uv
description: 一键领取(rewaredLevel=0)的去重角色数
domains:
  - 角色成长
computation:
  sql: COUNT(DISTINCT CASE WHEN GET_JSON_OBJECT(params, '$.rewaredLevel') = '0' THEN role_id END)
  metadata:
    aggregation: count_distinct
    field: CASE WHEN GET_JSON_OBJECT(params, '$.rewaredLevel') = '0' THEN role_id END
    source: game.battlepass.reward
    time_grain: ""
relations:
  - type: derived_from
    target: game.battlepass.reward
    description: 机械提取自 game.battlepass.reward 的 metrics 块（key=game_battlepass_reward_oneclick_uv）
