kind: metric
name: game.guild.share__game_guild_share_world_cnt
description: 分享至世界频道（params.shareWorld=true）的次数
domains:
  - 社交公会
computation:
  sql: SUM(CASE WHEN GET_JSON_OBJECT(params, '$.shareWorld') = 'true' THEN 1 ELSE 0 END)
  metadata:
    aggregation: sum
    field: CASE WHEN GET_JSON_OBJECT(params, '$.shareWorld') = 'true' THEN 1 ELSE 0 END
    source: game.guild.share
    time_grain: ""
relations:
  - type: derived_from
    target: game.guild.share
    description: 机械提取自 game.guild.share 的 metrics 块（key=game_guild_share_world_cnt）
