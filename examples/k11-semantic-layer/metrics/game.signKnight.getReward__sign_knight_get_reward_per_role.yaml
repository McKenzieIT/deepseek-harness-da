kind: metric
name: game.signKnight.getReward__sign_knight_get_reward_per_role
description: 领取奖励的人均领取次数（次数/去重角色数）
domains:
  - 自定义
computation:
  sql: COUNT(*) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: count
    field: "*) / NULLIF(COUNT(DISTINCT role_id), 0"
    source: game.signKnight.getReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.signKnight.getReward
    description: 机械提取自 game.signKnight.getReward 的 metrics 块（key=sign_knight_get_reward_per_role）
