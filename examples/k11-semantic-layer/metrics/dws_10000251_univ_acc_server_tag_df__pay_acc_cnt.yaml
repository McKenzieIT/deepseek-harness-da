kind: metric
name: dws_10000251_univ_acc_server_tag_df__pay_acc_cnt
description: 累计付费账号数(截至 ds 累计付费金额>0 的去重账号数);pay_amt_std 存在 bigint/double 重名列,生产 SQL 须先按数仓确认口径并对列消歧后再引用
domains:
  - 用户生命周期
  - 付费经济
  - 系统监控
computation:
  sql: COUNT(DISTINCT CASE WHEN pay_amt_std > 0 THEN user_id END)
  metadata:
    aggregation: count_distinct
    field: CASE WHEN pay_amt_std > 0 THEN user_id END
    source: dws_10000251_univ_acc_server_tag_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_univ_acc_server_tag_df
    description: 机械提取自 dws_10000251_univ_acc_server_tag_df 的 metrics 块（key=pay_acc_cnt）
