kind: metric
name: game.charge.refund__game_charge_refund_amt_per_role
description: 平均每个退款角色的退款数量
domains:
  - 付费经济
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params,'$.amount') AS BIGINT)) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params,'$.amount') AS BIGINT)) / NULLIF(COUNT(DISTINCT role_id), 0
    source: game.charge.refund
    time_grain: ""
relations:
  - type: derived_from
    target: game.charge.refund
    description: 机械提取自 game.charge.refund 的 metrics 块（key=game_charge_refund_amt_per_role）
