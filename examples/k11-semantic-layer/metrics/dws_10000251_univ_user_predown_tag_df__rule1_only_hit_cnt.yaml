kind: metric
name: dws_10000251_univ_user_predown_tag_df__rule1_only_hit_cnt
description: 仅命中原生规则(rule1)而未命中扩展规则的行数（行级计数，非去重用户数）
domains:
  - 用户生命周期
  - 资源产销
computation:
  sql: SUM(CASE WHEN rule1 = 1 AND rule2 = 0 THEN 1 ELSE 0 END)
  metadata:
    aggregation: sum
    field: CASE WHEN rule1 = 1 AND rule2 = 0 THEN 1 ELSE 0 END
    source: dws_10000251_univ_user_predown_tag_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_univ_user_predown_tag_df
    description: 机械提取自 dws_10000251_univ_user_predown_tag_df 的 metrics 块（key=rule1_only_hit_cnt）
