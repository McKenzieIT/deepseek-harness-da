kind: metric
name: game.war.getMissionReward__game_war_get_mission_reward_role_uv
description: 领取奖励的去重角色数
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.war.getMissionReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.war.getMissionReward
    description: 机械提取自 game.war.getMissionReward 的 metrics 块（key=game_war_get_mission_reward_role_uv）
