kind: metric
name: game.home.practice__game_home_practice_cnt
description: 武将特训触发总次数（每次特训计一次）
domains:
  - 自定义
computation:
  sql: COUNT(*)
  metadata:
    aggregation: count
    field: "*"
    source: game.home.practice
    time_grain: ""
relations:
  - type: derived_from
    target: game.home.practice
    description: 机械提取自 game.home.practice 的 metrics 块（key=game_home_practice_cnt）
