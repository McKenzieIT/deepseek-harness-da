kind: metric
name: game.springfestival.preheattrade__springfestival_preheattrade_activity_uv
description: 关联的预热活动去重数量（按 activityId 活动id 去重）
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.activityId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.activityId')
    source: game.springfestival.preheattrade
    time_grain: ""
relations:
  - type: derived_from
    target: game.springfestival.preheattrade
    description: 机械提取自 game.springfestival.preheattrade 的 metrics 块（key=springfestival_preheattrade_activity_uv）
