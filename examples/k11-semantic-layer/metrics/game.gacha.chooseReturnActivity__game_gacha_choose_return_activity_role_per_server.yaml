kind: metric
name: game.gacha.chooseReturnActivity__game_gacha_choose_return_activity_role_per_server
description: 平均每游戏服参与返场卡池选择的角色数，反映参与集中度
domains:
  - 探索收集
computation:
  sql: CAST(COUNT(DISTINCT role_id) AS DOUBLE) / NULLIF(COUNT(DISTINCT server_id), 0)
  metadata:
    aggregation: ""
    field: ""
    source: game.gacha.chooseReturnActivity
    time_grain: ""
relations:
  - type: derived_from
    target: game.gacha.chooseReturnActivity
    description: 机械提取自 game.gacha.chooseReturnActivity 的 metrics 块（key=game_gacha_choose_return_activity_role_per_server）
