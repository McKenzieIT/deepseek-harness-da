kind: metric
name: dws_10000251_play_process_df__role_uv
description: 玩法进度角色数（去重role_id），衡量玩法覆盖的角色规模（_df快照口径）
domains:
  - 战斗关卡
  - 角色成长
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: dws_10000251_play_process_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_play_process_df
    description: 机械提取自 dws_10000251_play_process_df 的 metrics 块（key=role_uv）
