kind: metric
name: game.social.guild.member__game_social_guild_member_total_member_records
description: 全部公会快照中成员记录总数（按 members 数组内 uid 键出现次数近似数组长度后求和）
domains:
  - 社交公会
computation:
  sql: SUM(REGEXP_COUNT(params,'"uid":'))
  metadata:
    aggregation: sum
    field: REGEXP_COUNT(params,'"uid":')
    source: game.social.guild.member
    time_grain: ""
relations:
  - type: derived_from
    target: game.social.guild.member
    description: 机械提取自 game.social.guild.member 的 metrics 块（key=game_social_guild_member_total_member_records）
