kind: metric
name: game.welfare.chapterStarReward__chapterStarReward_per_role_cnt
description: 人均领取次数（总次数/去重角色数），NULLIF 防止除零。
domains:
  - 自定义
  - 用户生命周期
  - 资源产销
computation:
  sql: COUNT(*) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: count
    field: "*) / NULLIF(COUNT(DISTINCT role_id), 0"
    source: game.welfare.chapterStarReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.welfare.chapterStarReward
    description: 机械提取自 game.welfare.chapterStarReward 的 metrics 块（key=chapterStarReward_per_role_cnt）
