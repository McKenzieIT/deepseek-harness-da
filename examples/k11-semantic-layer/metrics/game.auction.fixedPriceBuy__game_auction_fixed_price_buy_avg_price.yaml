kind: metric
name: game.auction.fixedPriceBuy__game_auction_fixed_price_buy_avg_price
description: 单次一口价购买平均金额
domains:
  - 付费经济
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params,'$.price') AS BIGINT)) / NULLIF(COUNT(*), 0)
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params,'$.price') AS BIGINT)) / NULLIF(COUNT(*), 0
    source: game.auction.fixedPriceBuy
    time_grain: ""
relations:
  - type: derived_from
    target: game.auction.fixedPriceBuy
    description: 机械提取自 game.auction.fixedPriceBuy 的 metrics 块（key=game_auction_fixed_price_buy_avg_price）
