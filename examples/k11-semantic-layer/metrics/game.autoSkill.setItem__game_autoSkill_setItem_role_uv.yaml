kind: metric
name: game.autoSkill.setItem__game_autoSkill_setItem_role_uv
description: 触发自动战斗方案设置的去重角色数（主体粒度 role_id）
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.autoSkill.setItem
    time_grain: ""
relations:
  - type: derived_from
    target: game.autoSkill.setItem
    description: 机械提取自 game.autoSkill.setItem 的 metrics 块（key=game_autoSkill_setItem_role_uv）
