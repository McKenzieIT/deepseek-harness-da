kind: metric
name: dws_10000251_univ_dev_whole_df__avg_pay_amt_per_payer
description: 付费设备人均累计付费金额(ARPPU，标准化口径)；累计付费总额 / 付费设备数，反映付费深度；与 arpu(全体人均) 区分
domains:
  - 用户生命周期
  - 付费经济
computation:
  sql: SUM(pay_amt_std) * 1.0 / NULLIF(COUNT(DISTINCT CASE WHEN pay_amt_std > 0 THEN user_id ELSE NULL END), 0)
  metadata:
    aggregation: sum
    field: pay_amt_std) * 1.0 / NULLIF(COUNT(DISTINCT CASE WHEN pay_amt_std > 0 THEN user_id ELSE NULL END), 0
    source: dws_10000251_univ_dev_whole_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_univ_dev_whole_df
    description: 机械提取自 dws_10000251_univ_dev_whole_df 的 metrics 块（key=avg_pay_amt_per_payer）
