kind: metric
name: game.springfestival.commonMissionReward__springfestival_common_mission_reward_activity_uv
description: 被领取奖励的去重活动(activityId)数
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.activityId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.activityId')
    source: game.springfestival.commonMissionReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.springfestival.commonMissionReward
    description: 机械提取自 game.springfestival.commonMissionReward 的 metrics 块（key=springfestival_common_mission_reward_activity_uv）
