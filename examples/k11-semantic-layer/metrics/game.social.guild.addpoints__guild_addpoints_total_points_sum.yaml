kind: metric
name: game.social.guild.addpoints__guild_addpoints_total_points_sum
description: 当前公会累计活跃值总和(来自 params.guildActivePoints)
domains:
  - 社交公会
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params,'$.guildActivePoints') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params,'$.guildActivePoints') AS BIGINT)
    source: game.social.guild.addpoints
    time_grain: ""
relations:
  - type: derived_from
    target: game.social.guild.addpoints
    description: 机械提取自 game.social.guild.addpoints 的 metrics 块（key=guild_addpoints_total_points_sum）
