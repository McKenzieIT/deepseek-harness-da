kind: metric
name: game.chargeShop.buy__game_chargeShop_buy_amount_sum
description: 购买货物总数量（amount 字段求和）
domains:
  - 付费经济
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.amount') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.amount') AS BIGINT)
    source: game.chargeShop.buy
    time_grain: ""
relations:
  - type: derived_from
    target: game.chargeShop.buy
    description: 机械提取自 game.chargeShop.buy 的 metrics 块（key=game_chargeShop_buy_amount_sum）
