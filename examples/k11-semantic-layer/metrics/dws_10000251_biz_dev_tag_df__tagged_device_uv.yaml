kind: metric
name: dws_10000251_biz_dev_tag_df__tagged_device_uv
description: 被标记为参与任意大事件(biz_name 非空，如 CBT1/CBT2)的设备数。
domains:
  - 用户生命周期
  - 系统监控
computation:
  sql: COUNT(DISTINCT CASE WHEN biz_name IS NOT NULL THEN user_id END)
  metadata:
    aggregation: count_distinct
    field: CASE WHEN biz_name IS NOT NULL THEN user_id END
    source: dws_10000251_biz_dev_tag_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_biz_dev_tag_df
    description: 机械提取自 dws_10000251_biz_dev_tag_df 的 metrics 块（key=tagged_device_uv）
