kind: metric
name: game.toy.techonekeyactive__techonekeyactive_role_uv
description: 触发一键科技激活的去重角色数（分析主体为 role_id）
domains:
  - 探索收集
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.toy.techonekeyactive
    time_grain: ""
relations:
  - type: derived_from
    target: game.toy.techonekeyactive
    description: 机械提取自 game.toy.techonekeyactive 的 metrics 块（key=techonekeyactive_role_uv）
