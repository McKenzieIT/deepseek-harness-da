kind: metric
name: game.toy.sign__game_toy_sign_signin_per_role
description: 人均签到次数 = 日志数 / 角色去重数（NULLIF 防零除，反映复签率）
domains:
  - 探索收集
computation:
  sql: COUNT(*) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: count
    field: "*) / NULLIF(COUNT(DISTINCT role_id), 0"
    source: game.toy.sign
    time_grain: ""
relations:
  - type: derived_from
    target: game.toy.sign
    description: 机械提取自 game.toy.sign 的 metrics 块（key=game_toy_sign_signin_per_role）
