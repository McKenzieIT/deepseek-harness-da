kind: metric
name: dws_10000251_resource_item_di__net_amount
description: 当日道具净增量（增加-消耗）
domains:
  - 资源产销
  - 装备道具
computation:
  sql: SUM(add_amount) - SUM(use_amount)
  metadata:
    aggregation: sum
    field: add_amount) - SUM(use_amount
    source: dws_10000251_resource_item_di
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_resource_item_di
    description: 机械提取自 dws_10000251_resource_item_di 的 metrics 块（key=net_amount）
