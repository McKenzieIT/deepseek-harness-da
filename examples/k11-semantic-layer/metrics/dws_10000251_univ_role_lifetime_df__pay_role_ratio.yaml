kind: metric
name: dws_10000251_univ_role_lifetime_df__pay_role_ratio
description: 付费角色占比（累计付费金额>0 的角色数 / 总角色数）
domains:
  - 用户生命周期
  - 付费经济
computation:
  sql: SUM(CASE WHEN pay_amt_std > 0 THEN 1 ELSE 0 END) / NULLIF(COUNT(*), 0)
  metadata:
    aggregation: sum
    field: CASE WHEN pay_amt_std > 0 THEN 1 ELSE 0 END) / NULLIF(COUNT(*), 0
    source: dws_10000251_univ_role_lifetime_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_univ_role_lifetime_df
    description: 机械提取自 dws_10000251_univ_role_lifetime_df 的 metrics 块（key=pay_role_ratio）
