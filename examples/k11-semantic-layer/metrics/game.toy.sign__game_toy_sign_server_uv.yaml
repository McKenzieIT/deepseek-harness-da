kind: metric
name: game.toy.sign__game_toy_sign_server_uv
description: 涉及签到的游戏服去重数
domains:
  - 探索收集
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: game.toy.sign
    time_grain: ""
relations:
  - type: derived_from
    target: game.toy.sign
    description: 机械提取自 game.toy.sign 的 metrics 块（key=game_toy_sign_server_uv）
