kind: metric
name: game.charmactivity.gachaswitchactivity__gachaswitchactivity_per_role_switch_cnt
description: 人均切换次数(总切换次数/独立角色数；衡量反复切换强度)
domains:
  - 探索收集
computation:
  sql: COUNT(*) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: count
    field: "*) / NULLIF(COUNT(DISTINCT role_id), 0"
    source: game.charmactivity.gachaswitchactivity
    time_grain: ""
relations:
  - type: derived_from
    target: game.charmactivity.gachaswitchactivity
    description: 机械提取自 game.charmactivity.gachaswitchactivity 的 metrics 块（key=gachaswitchactivity_per_role_switch_cnt）
