kind: metric
name: game.war.getCollection__war_getcollection_item_uv
description: 被收集的收集品种类数（按收集品id去重）
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.id'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.id')
    source: game.war.getCollection
    time_grain: ""
relations:
  - type: derived_from
    target: game.war.getCollection
    description: 机械提取自 game.war.getCollection 的 metrics 块（key=war_getcollection_item_uv）
