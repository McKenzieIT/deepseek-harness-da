kind: metric
name: game.equip.add__equip_add_role_uv
description: 获取装备的去重角色数（分析主体粒度）
domains:
  - 装备道具
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.equip.add
    time_grain: ""
relations:
  - type: derived_from
    target: game.equip.add
    description: 机械提取自 game.equip.add 的 metrics 块（key=equip_add_role_uv）
