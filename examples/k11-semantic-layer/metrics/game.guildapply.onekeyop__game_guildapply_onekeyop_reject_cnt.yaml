kind: metric
name: game.guildapply.onekeyop__game_guildapply_onekeyop_reject_cnt
description: 拒绝加入的审批操作次数（op=0）
domains:
  - 社交公会
computation:
  sql: SUM(CASE WHEN GET_JSON_OBJECT(params, '$.op') = '0' THEN 1 ELSE 0 END)
  metadata:
    aggregation: sum
    field: CASE WHEN GET_JSON_OBJECT(params, '$.op') = '0' THEN 1 ELSE 0 END
    source: game.guildapply.onekeyop
    time_grain: ""
relations:
  - type: derived_from
    target: game.guildapply.onekeyop
    description: 机械提取自 game.guildapply.onekeyop 的 metrics 块（key=game_guildapply_onekeyop_reject_cnt）
