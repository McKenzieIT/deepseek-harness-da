kind: metric
name: game.battlepass.buyExp__game_battlepass_buyexp_role_uv
description: 购买战令经验的去重角色数
domains:
  - 角色成长
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.battlepass.buyExp
    time_grain: ""
relations:
  - type: derived_from
    target: game.battlepass.buyExp
    description: 机械提取自 game.battlepass.buyExp 的 metrics 块（key=game_battlepass_buyexp_role_uv）
