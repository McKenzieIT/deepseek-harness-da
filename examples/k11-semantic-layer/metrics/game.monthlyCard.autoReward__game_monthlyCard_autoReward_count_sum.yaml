kind: metric
name: game.monthlyCard.autoReward__game_monthlyCard_autoReward_count_sum
description: 月卡自动发奖的累计发奖次数(params.count 求和)
domains:
  - 自定义
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params,'$.count') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params,'$.count') AS BIGINT)
    source: game.monthlyCard.autoReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.monthlyCard.autoReward
    description: 机械提取自 game.monthlyCard.autoReward 的 metrics 块（key=game_monthlyCard_autoReward_count_sum）
