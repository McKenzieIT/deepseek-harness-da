kind: metric
name: game.home.catchupgrade__game_home_catchupgrade_cnt
description: 家园捕捞等级突破总次数
domains:
  - 自定义
computation:
  sql: COUNT(*)
  metadata:
    aggregation: count
    field: "*"
    source: game.home.catchupgrade
    time_grain: ""
relations:
  - type: derived_from
    target: game.home.catchupgrade
    description: 机械提取自 game.home.catchupgrade 的 metrics 块（key=game_home_catchupgrade_cnt）
