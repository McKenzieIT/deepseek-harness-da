kind: metric
name: game.resort.share__game_resort_share_account_uv
description: 分享卡片触发的账号去重数
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT account_id)
  metadata:
    aggregation: count_distinct
    field: account_id
    source: game.resort.share
    time_grain: ""
relations:
  - type: derived_from
    target: game.resort.share
    description: 机械提取自 game.resort.share 的 metrics 块（key=game_resort_share_account_uv）
