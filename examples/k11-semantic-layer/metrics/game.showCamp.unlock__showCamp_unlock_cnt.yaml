kind: metric
name: game.showCamp.unlock__showCamp_unlock_cnt
description: 营地解锁事件总次数
domains:
  - 自定义
computation:
  sql: COUNT(*)
  metadata:
    aggregation: count
    field: "*"
    source: game.showCamp.unlock
    time_grain: ""
relations:
  - type: derived_from
    target: game.showCamp.unlock
    description: 机械提取自 game.showCamp.unlock 的 metrics 块（key=showCamp_unlock_cnt）
