kind: metric
name: game.social.guild.getmissionbox__game_social_guild_getmissionbox_energy_sum
description: 体力累计量
domains:
  - 社交公会
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.coinList.energy') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.coinList.energy') AS BIGINT)
    source: game.social.guild.getmissionbox
    time_grain: ""
relations:
  - type: derived_from
    target: game.social.guild.getmissionbox
    description: 机械提取自 game.social.guild.getmissionbox 的 metrics 块（key=game_social_guild_getmissionbox_energy_sum）
