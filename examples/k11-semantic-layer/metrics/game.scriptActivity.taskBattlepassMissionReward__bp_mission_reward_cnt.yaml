kind: metric
name: game.scriptActivity.taskBattlepassMissionReward__bp_mission_reward_cnt
description: 战令任务奖励领取总次数
domains:
  - 自定义
computation:
  sql: COUNT(*)
  metadata:
    aggregation: count
    field: "*"
    source: game.scriptActivity.taskBattlepassMissionReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.scriptActivity.taskBattlepassMissionReward
    description: 机械提取自 game.scriptActivity.taskBattlepassMissionReward 的 metrics 块（key=bp_mission_reward_cnt）
