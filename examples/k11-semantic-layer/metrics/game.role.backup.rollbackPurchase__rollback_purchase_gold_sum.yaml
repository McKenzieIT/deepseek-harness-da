kind: metric
name: game.role.backup.rollbackPurchase__rollback_purchase_gold_sum
description: 回滚补发充值钻石总量
domains:
  - 用户生命周期
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.coinList.gold') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.coinList.gold') AS BIGINT)
    source: game.role.backup.rollbackPurchase
    time_grain: ""
relations:
  - type: derived_from
    target: game.role.backup.rollbackPurchase
    description: 机械提取自 game.role.backup.rollbackPurchase 的 metrics 块（key=rollback_purchase_gold_sum）
