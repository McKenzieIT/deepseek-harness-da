kind: metric
name: dws_10000251_selfhelp_new_pay_df_extend_temp__new_pay_lt1_uv
description: 当日新增且当日付费用户数（lt=1 即 act_ymd=act_ymd_fst 的新增当日付费用户），_df 快照去重。
domains:
  - 付费经济
  - 用户生命周期
computation:
  sql: COUNT(DISTINCT CASE WHEN lt = 1 THEN user_id END)
  metadata:
    aggregation: count_distinct
    field: CASE WHEN lt = 1 THEN user_id END
    source: dws_10000251_selfhelp_new_pay_df_extend_temp
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_selfhelp_new_pay_df_extend_temp
    description: 机械提取自 dws_10000251_selfhelp_new_pay_df_extend_temp 的 metrics 块（key=new_pay_lt1_uv）
