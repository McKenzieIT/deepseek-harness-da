kind: metric
name: game.gmActivity.dayBattlePassGetReward__game_gmActivity_dayBattlePassGetReward_paid_reward_cnt
description: 领取付费轨道(paid=true)奖励的次数
domains:
  - 自定义
computation:
  sql: SUM(CASE WHEN GET_JSON_OBJECT(params,'$.paid') = 'true' THEN 1 ELSE 0 END)
  metadata:
    aggregation: sum
    field: CASE WHEN GET_JSON_OBJECT(params,'$.paid') = 'true' THEN 1 ELSE 0 END
    source: game.gmActivity.dayBattlePassGetReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.gmActivity.dayBattlePassGetReward
    description: 机械提取自 game.gmActivity.dayBattlePassGetReward 的 metrics 块（key=game_gmActivity_dayBattlePassGetReward_paid_reward_cnt）
