kind: metric
name: game.recycle__game_recycle_cnt
description: 资源回收事件总条数（每次回收行为一条流水）
domains:
  - 资源产销
  - 装备道具
computation:
  sql: COUNT(*)
  metadata:
    aggregation: count
    field: "*"
    source: game.recycle
    time_grain: ""
relations:
  - type: derived_from
    target: game.recycle
    description: 机械提取自 game.recycle 的 metrics 块（key=game_recycle_cnt）
