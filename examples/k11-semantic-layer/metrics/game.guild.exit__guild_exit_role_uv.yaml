kind: metric
name: game.guild.exit__guild_exit_role_uv
description: 退出公会的去重角色数（分析主体 role_id）
domains:
  - 社交公会
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.guild.exit
    time_grain: ""
relations:
  - type: derived_from
    target: game.guild.exit
    description: 机械提取自 game.guild.exit 的 metrics 块（key=guild_exit_role_uv）
