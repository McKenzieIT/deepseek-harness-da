kind: metric
name: game.tactic.train__tactic_train_role_level_avg
description: 特训角色的平均角色等级
domains:
  - 角色成长
computation:
  sql: AVG(CAST(GET_JSON_OBJECT(params, '$.roleLevel') AS BIGINT))
  metadata:
    aggregation: avg
    field: CAST(GET_JSON_OBJECT(params, '$.roleLevel') AS BIGINT)
    source: game.tactic.train
    time_grain: ""
relations:
  - type: derived_from
    target: game.tactic.train
    description: 机械提取自 game.tactic.train 的 metrics 块（key=tactic_train_role_level_avg）
