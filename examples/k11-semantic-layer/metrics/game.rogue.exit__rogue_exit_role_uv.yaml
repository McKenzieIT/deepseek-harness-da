kind: metric
name: game.rogue.exit__rogue_exit_role_uv
description: 古战场退出的去重角色数（分析主体 role_id）
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.rogue.exit
    time_grain: ""
relations:
  - type: derived_from
    target: game.rogue.exit
    description: 机械提取自 game.rogue.exit 的 metrics 块（key=rogue_exit_role_uv）
