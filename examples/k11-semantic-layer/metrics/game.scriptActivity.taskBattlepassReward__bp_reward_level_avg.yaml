kind: metric
name: game.scriptActivity.taskBattlepassReward__bp_reward_level_avg
description: 领取时战令等级(level)的平均值
domains:
  - 自定义
computation:
  sql: AVG(CAST(GET_JSON_OBJECT(params,'$.level') AS BIGINT))
  metadata:
    aggregation: avg
    field: CAST(GET_JSON_OBJECT(params,'$.level') AS BIGINT)
    source: game.scriptActivity.taskBattlepassReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.scriptActivity.taskBattlepassReward
    description: 机械提取自 game.scriptActivity.taskBattlepassReward 的 metrics 块（key=bp_reward_level_avg）
