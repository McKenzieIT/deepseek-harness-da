kind: metric
name: game.guild.inviteJoin__guild_invite_join_cnt
description: 公会邀请加入事件总次数（按行计）
domains:
  - 社交公会
computation:
  sql: COUNT(*)
  metadata:
    aggregation: count
    field: "*"
    source: game.guild.inviteJoin
    time_grain: ""
relations:
  - type: derived_from
    target: game.guild.inviteJoin
    description: 机械提取自 game.guild.inviteJoin 的 metrics 块（key=guild_invite_join_cnt）
