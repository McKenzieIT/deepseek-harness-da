kind: metric
name: game.realm.levelUp__game_realm_levelUp_cost_exp_sum
description: 境界升级消耗经验总量（costExp 求和）
domains:
  - 战斗关卡
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.costExp') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.costExp') AS BIGINT)
    source: game.realm.levelUp
    time_grain: ""
relations:
  - type: derived_from
    target: game.realm.levelUp
    description: 机械提取自 game.realm.levelUp 的 metrics 块（key=game_realm_levelUp_cost_exp_sum）
