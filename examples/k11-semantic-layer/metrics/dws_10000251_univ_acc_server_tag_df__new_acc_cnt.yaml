kind: metric
name: dws_10000251_univ_acc_server_tag_df__new_acc_cnt
description: 当日新增账号数(首次活跃日等于最近活跃日,即 ds 当日首次活跃的账号);依赖 ds 即最近活跃日的前提
domains:
  - 用户生命周期
  - 付费经济
  - 系统监控
computation:
  sql: COUNT(DISTINCT CASE WHEN act_tm_fst = act_tm_lst THEN user_id END)
  metadata:
    aggregation: count_distinct
    field: CASE WHEN act_tm_fst = act_tm_lst THEN user_id END
    source: dws_10000251_univ_acc_server_tag_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_univ_acc_server_tag_df
    description: 机械提取自 dws_10000251_univ_acc_server_tag_df 的 metrics 块（key=new_acc_cnt）
