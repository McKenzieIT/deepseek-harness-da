kind: metric
name: dws_10000251_item_circle_di__net_amt_per_role
description: 人均净库存剩余数量（剩余总量/参与角色数）
domains:
  - 资源产销
  - 装备道具
computation:
  sql: SUM(net_amt) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: sum
    field: net_amt) / NULLIF(COUNT(DISTINCT role_id), 0
    source: dws_10000251_item_circle_di
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_item_circle_di
    description: 机械提取自 dws_10000251_item_circle_di 的 metrics 块（key=net_amt_per_role）
