kind: metric
name: game.toy.gachaMissionOnekey__gacha_mission_onekey_account_uv
description: 触发抽卡任务一键领取的去重账号数
domains:
  - 探索收集
computation:
  sql: COUNT(DISTINCT account_id)
  metadata:
    aggregation: count_distinct
    field: account_id
    source: game.toy.gachaMissionOnekey
    time_grain: ""
relations:
  - type: derived_from
    target: game.toy.gachaMissionOnekey
    description: 机械提取自 game.toy.gachaMissionOnekey 的 metrics 块（key=gacha_mission_onekey_account_uv）
