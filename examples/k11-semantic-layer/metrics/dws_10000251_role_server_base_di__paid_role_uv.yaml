kind: metric
name: dws_10000251_role_server_base_di__paid_role_uv
description: 当日累计付费角色数（pay_amt_std>0）
domains:
  - 用户生命周期
  - 付费经济
computation:
  sql: COUNT(DISTINCT CASE WHEN pay_amt_std > 0 THEN role_id END)
  metadata:
    aggregation: count_distinct
    field: CASE WHEN pay_amt_std > 0 THEN role_id END
    source: dws_10000251_role_server_base_di
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_role_server_base_di
    description: 机械提取自 dws_10000251_role_server_base_di 的 metrics 块（key=paid_role_uv）
