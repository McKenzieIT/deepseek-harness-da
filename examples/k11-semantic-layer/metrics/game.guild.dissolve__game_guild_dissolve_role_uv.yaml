kind: metric
name: game.guild.dissolve__game_guild_dissolve_role_uv
description: 触发/涉及解散的角色去重数（分析主体）
domains:
  - 社交公会
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.guild.dissolve
    time_grain: ""
relations:
  - type: derived_from
    target: game.guild.dissolve
    description: 机械提取自 game.guild.dissolve 的 metrics 块（key=game_guild_dissolve_role_uv）
