kind: metric
name: dws_10000251_pve_progress_di__active_role_uv
description: 当日参与PVE挑战的去重角色数（role_id 去重，角色粒度UV）
domains:
  - 战斗关卡
  - 角色成长
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: dws_10000251_pve_progress_di
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_pve_progress_di
    description: 机械提取自 dws_10000251_pve_progress_di 的 metrics 块（key=active_role_uv）
