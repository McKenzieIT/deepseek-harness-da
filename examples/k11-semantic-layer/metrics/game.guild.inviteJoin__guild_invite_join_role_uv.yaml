kind: metric
name: game.guild.inviteJoin__guild_invite_join_role_uv
description: 发起邀请的去重角色数（邀请方UV）
domains:
  - 社交公会
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.guild.inviteJoin
    time_grain: ""
relations:
  - type: derived_from
    target: game.guild.inviteJoin
    description: 机械提取自 game.guild.inviteJoin 的 metrics 块（key=guild_invite_join_role_uv）
