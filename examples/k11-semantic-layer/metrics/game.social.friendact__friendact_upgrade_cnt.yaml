kind: metric
name: game.social.friendact__friendact_upgrade_cnt
description: 好友关系升级（action=2）动作次数
domains:
  - 社交公会
computation:
  sql: SUM(CASE WHEN GET_JSON_OBJECT(params,'$.action')='2' THEN 1 ELSE 0 END)
  metadata:
    aggregation: sum
    field: CASE WHEN GET_JSON_OBJECT(params,'$.action')='2' THEN 1 ELSE 0 END
    source: game.social.friendact
    time_grain: ""
relations:
  - type: derived_from
    target: game.social.friendact
    description: 机械提取自 game.social.friendact 的 metrics 块（key=friendact_upgrade_cnt）
