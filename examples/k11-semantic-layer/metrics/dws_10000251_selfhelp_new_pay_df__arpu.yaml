kind: metric
name: dws_10000251_selfhelp_new_pay_df__arpu
description: 人均付费金额(元)=付费总金额/新付费用户数
domains:
  - 付费经济
  - 用户生命周期
computation:
  sql: SUM(pay_amt) / NULLIF(COUNT(DISTINCT user_id), 0)
  metadata:
    aggregation: sum
    field: pay_amt) / NULLIF(COUNT(DISTINCT user_id), 0
    source: dws_10000251_selfhelp_new_pay_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_selfhelp_new_pay_df
    description: 机械提取自 dws_10000251_selfhelp_new_pay_df 的 metrics 块（key=arpu）
