kind: metric
name: dws_10000251_univ_role_server_whole_df__lost_role_cnt_30d
description: 流失倾向角色数(最近活跃距快照日 ds 超过 30 天的角色去重数;act_tm_lst 为空则不计入)。
domains:
  - 用户生命周期
  - 付费经济
  - 角色成长
computation:
  sql: COUNT(DISTINCT CASE WHEN DATEDIFF(TO_DATE(ds,'yyyymmdd'), act_tm_lst, 'dd')>30 THEN user_id END)
  metadata:
    aggregation: count_distinct
    field: CASE WHEN DATEDIFF(TO_DATE(ds,'yyyymmdd'), act_tm_lst, 'dd')>30 THEN user_id END
    source: dws_10000251_univ_role_server_whole_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_univ_role_server_whole_df
    description: 机械提取自 dws_10000251_univ_role_server_whole_df 的 metrics 块（key=lost_role_cnt_30d）
