kind: metric
name: dws_10000251_univ_role_server_whole_df__pay_rate
description: 累计付费率=累计付费角色数/累计活跃角色数(除零保护;分母为累计口径,非当日活跃率)。
domains:
  - 用户生命周期
  - 付费经济
  - 角色成长
computation:
  sql: COUNT(DISTINCT CASE WHEN pay_amt_std>0 THEN user_id END)*1.0/NULLIF(COUNT(DISTINCT user_id),0)
  metadata:
    aggregation: count_distinct
    field: CASE WHEN pay_amt_std>0 THEN user_id END)*1.0/NULLIF(COUNT(DISTINCT user_id),0
    source: dws_10000251_univ_role_server_whole_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_univ_role_server_whole_df
    description: 机械提取自 dws_10000251_univ_role_server_whole_df 的 metrics 块（key=pay_rate）
