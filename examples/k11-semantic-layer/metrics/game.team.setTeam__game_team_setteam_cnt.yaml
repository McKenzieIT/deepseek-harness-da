kind: metric
name: game.team.setTeam__game_team_setteam_cnt
description: 设置阵容事件总次数（含同一角色多次切换）
domains:
  - 自定义
computation:
  sql: COUNT(*)
  metadata:
    aggregation: count
    field: "*"
    source: game.team.setTeam
    time_grain: ""
relations:
  - type: derived_from
    target: game.team.setTeam
    description: 机械提取自 game.team.setTeam 的 metrics 块（key=game_team_setteam_cnt）
