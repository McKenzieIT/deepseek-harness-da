kind: metric
name: game.mission.sectionReward__game_mission_sectionReward_type_uv
description: 领奖涉及的奖励类型(type)去重数
domains:
  - 角色成长
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.type'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.type')
    source: game.mission.sectionReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.mission.sectionReward
    description: 机械提取自 game.mission.sectionReward 的 metrics 块（key=game_mission_sectionReward_type_uv）
