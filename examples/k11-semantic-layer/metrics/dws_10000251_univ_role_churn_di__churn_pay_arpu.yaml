kind: metric
name: dws_10000251_univ_role_churn_di__churn_pay_arpu
description: 流失付费角色ARPU=流失付费总额/流失付费角色数（元/人，NULLIF防除零）。
domains:
  - 用户生命周期
  - 付费经济
computation:
  sql: SUM(pay_amt_std) / NULLIF(COUNT(DISTINCT CASE WHEN pay_amt_std > 0 THEN user_id END), 0)
  metadata:
    aggregation: sum
    field: pay_amt_std) / NULLIF(COUNT(DISTINCT CASE WHEN pay_amt_std > 0 THEN user_id END), 0
    source: dws_10000251_univ_role_churn_di
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_univ_role_churn_di
    description: 机械提取自 dws_10000251_univ_role_churn_di 的 metrics 块（key=churn_pay_arpu）
