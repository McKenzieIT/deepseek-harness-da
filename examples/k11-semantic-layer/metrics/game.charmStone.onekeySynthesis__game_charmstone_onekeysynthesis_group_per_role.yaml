kind: metric
name: game.charmStone.onekeySynthesis__game_charmstone_onekeysynthesis_group_per_role
description: 人均合成组数 = 累计组数 / 角色去重数（NULLIF 防零除）
domains:
  - 探索收集
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params,'$.num') AS BIGINT)) / NULLIF(COUNT(DISTINCT role_id),0)
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params,'$.num') AS BIGINT)) / NULLIF(COUNT(DISTINCT role_id),0
    source: game.charmStone.onekeySynthesis
    time_grain: ""
relations:
  - type: derived_from
    target: game.charmStone.onekeySynthesis
    description: 机械提取自 game.charmStone.onekeySynthesis 的 metrics 块（key=game_charmstone_onekeysynthesis_group_per_role）
