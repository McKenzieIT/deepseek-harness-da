kind: metric
name: game.scriptActivity.scriptMissionReward__script_mission_reward_addscore_per_role
description: 领取奖励的人均活动积分增量
domains:
  - 自定义
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params,'$.addScore') AS BIGINT)) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params,'$.addScore') AS BIGINT)) / NULLIF(COUNT(DISTINCT role_id), 0
    source: game.scriptActivity.scriptMissionReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.scriptActivity.scriptMissionReward
    description: 机械提取自 game.scriptActivity.scriptMissionReward 的 metrics 块（key=script_mission_reward_addscore_per_role）
