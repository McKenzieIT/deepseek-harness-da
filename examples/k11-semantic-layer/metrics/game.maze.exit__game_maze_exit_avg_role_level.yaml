kind: metric
name: game.maze.exit__game_maze_exit_avg_role_level
description: 退出迷宫时的角色平均等级（来自 params.roleLevel）
domains:
  - 战斗关卡
computation:
  sql: AVG(CAST(GET_JSON_OBJECT(params, '$.roleLevel') AS BIGINT))
  metadata:
    aggregation: avg
    field: CAST(GET_JSON_OBJECT(params, '$.roleLevel') AS BIGINT)
    source: game.maze.exit
    time_grain: ""
relations:
  - type: derived_from
    target: game.maze.exit
    description: 机械提取自 game.maze.exit 的 metrics 块（key=game_maze_exit_avg_role_level）
