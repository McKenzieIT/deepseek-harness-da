kind: metric
name: game.task.act__game_task_act_task_type_uv
description: 涉及的任务类型数(去重 taskType；100%非空实测，4种)
domains:
  - 角色成长
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.taskType'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.taskType')
    source: game.task.act
    time_grain: ""
relations:
  - type: derived_from
    target: game.task.act
    description: 机械提取自 game.task.act 的 metrics 块（key=game_task_act_task_type_uv）
