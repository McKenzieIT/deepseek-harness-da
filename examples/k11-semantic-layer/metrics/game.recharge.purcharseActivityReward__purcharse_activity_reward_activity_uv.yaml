kind: metric
name: game.recharge.purcharseActivityReward__purcharse_activity_reward_activity_uv
description: 涉及的去重活动数(基于 activityId)
domains:
  - 付费经济
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.activityId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.activityId')
    source: game.recharge.purcharseActivityReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.recharge.purcharseActivityReward
    description: 机械提取自 game.recharge.purcharseActivityReward 的 metrics 块（key=purcharse_activity_reward_activity_uv）
