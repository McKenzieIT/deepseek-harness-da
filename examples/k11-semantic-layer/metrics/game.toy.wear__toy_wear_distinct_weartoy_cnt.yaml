kind: metric
name: game.toy.wear__toy_wear_distinct_weartoy_cnt
description: 当日被穿戴的去重玩具 id 数（params.wearToy.id，反映玩具流转活跃度）
domains:
  - 探索收集
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.wearToy.id'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.wearToy.id')
    source: game.toy.wear
    time_grain: ""
relations:
  - type: derived_from
    target: game.toy.wear
    description: 机械提取自 game.toy.wear 的 metrics 块（key=toy_wear_distinct_weartoy_cnt）
