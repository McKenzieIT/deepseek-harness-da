kind: metric
name: game.knightnatalchart.unlockscheme__game_knightnatalchart_unlockscheme_knight_uv
description: 被解锁方案的去重武将数（取自 params.knightId）
domains:
  - 角色成长
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.knightId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.knightId')
    source: game.knightnatalchart.unlockscheme
    time_grain: ""
relations:
  - type: derived_from
    target: game.knightnatalchart.unlockscheme
    description: 机械提取自 game.knightnatalchart.unlockscheme 的 metrics 块（key=game_knightnatalchart_unlockscheme_knight_uv）
