kind: metric
name: game.customLevel.cancelMatch__game_customLevel_cancelMatch_account_uv
description: 演武场取消匹配的去重账号数，跨角色归并
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT account_id)
  metadata:
    aggregation: count_distinct
    field: account_id
    source: game.customLevel.cancelMatch
    time_grain: ""
relations:
  - type: derived_from
    target: game.customLevel.cancelMatch
    description: 机械提取自 game.customLevel.cancelMatch 的 metrics 块（key=game_customLevel_cancelMatch_account_uv）
