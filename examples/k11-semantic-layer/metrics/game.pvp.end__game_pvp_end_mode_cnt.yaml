kind: metric
name: game.pvp.end__game_pvp_end_mode_cnt
description: PVP模式种类数(1竞技场无bp/2竞技场有bp/3休闲/4演武场)
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT CAST(GET_JSON_OBJECT(params, '$.mode') AS BIGINT))
  metadata:
    aggregation: count_distinct
    field: CAST(GET_JSON_OBJECT(params, '$.mode') AS BIGINT)
    source: game.pvp.end
    time_grain: ""
relations:
  - type: derived_from
    target: game.pvp.end
    description: 机械提取自 game.pvp.end 的 metrics 块（key=game_pvp_end_mode_cnt）
