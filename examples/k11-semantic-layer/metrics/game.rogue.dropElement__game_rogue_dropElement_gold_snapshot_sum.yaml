kind: metric
name: game.rogue.dropElement__game_rogue_dropElement_gold_snapshot_sum
description: 丢弃时点充值钻石快照总量
domains:
  - 战斗关卡
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.coinList.gold') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.coinList.gold') AS BIGINT)
    source: game.rogue.dropElement
    time_grain: ""
relations:
  - type: derived_from
    target: game.rogue.dropElement
    description: 机械提取自 game.rogue.dropElement 的 metrics 块（key=game_rogue_dropElement_gold_snapshot_sum）
