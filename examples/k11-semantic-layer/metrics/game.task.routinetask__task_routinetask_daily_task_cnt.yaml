kind: metric
name: game.task.routinetask__task_routinetask_daily_task_cnt
description: 每日任务(taskType=1)日志行数
domains:
  - 角色成长
computation:
  sql: SUM(CASE WHEN GET_JSON_OBJECT(params,'$.taskType')='1' THEN 1 ELSE 0 END)
  metadata:
    aggregation: sum
    field: CASE WHEN GET_JSON_OBJECT(params,'$.taskType')='1' THEN 1 ELSE 0 END
    source: game.task.routinetask
    time_grain: ""
relations:
  - type: derived_from
    target: game.task.routinetask
    description: 机械提取自 game.task.routinetask 的 metrics 块（key=task_routinetask_daily_task_cnt）
