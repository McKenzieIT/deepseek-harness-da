kind: metric
name: game.guild.resourcechange__game_guild_resourcechange_server_uv
description: 涉及公会资源变更的去重游戏服数
domains:
  - 社交公会
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: game.guild.resourcechange
    time_grain: ""
relations:
  - type: derived_from
    target: game.guild.resourcechange
    description: 机械提取自 game.guild.resourcechange 的 metrics 块（key=game_guild_resourcechange_server_uv）
