kind: metric
name: game.guild.resourcechange__game_guild_resourcechange_role_uv
description: 触发公会资源变更的去重角色数(分析主体=role_id)
domains:
  - 社交公会
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.guild.resourcechange
    time_grain: ""
relations:
  - type: derived_from
    target: game.guild.resourcechange
    description: 机械提取自 game.guild.resourcechange 的 metrics 块（key=game_guild_resourcechange_role_uv）
