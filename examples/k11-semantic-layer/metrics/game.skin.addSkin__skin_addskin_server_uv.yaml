kind: metric
name: game.skin.addSkin__skin_addskin_server_uv
description: 涉及的游戏服数量（去重 server_id，渠道/服务器分布）
domains:
  - 装备道具
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: game.skin.addSkin
    time_grain: ""
relations:
  - type: derived_from
    target: game.skin.addSkin
    description: 机械提取自 game.skin.addSkin 的 metrics 块（key=skin_addskin_server_uv）
