kind: metric
name: game.guild.create__game_guild_create_account_uv
description: 创建公会的去重账号数
domains:
  - 社交公会
computation:
  sql: COUNT(DISTINCT account_id)
  metadata:
    aggregation: count_distinct
    field: account_id
    source: game.guild.create
    time_grain: ""
relations:
  - type: derived_from
    target: game.guild.create
    description: 机械提取自 game.guild.create 的 metrics 块（key=game_guild_create_account_uv）
