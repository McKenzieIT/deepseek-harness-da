kind: metric
name: game.train.card__train_card_card_uv
description: 被养成的武将种类数（去重 params.id）
domains:
  - 角色成长
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.id'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.id')
    source: game.train.card
    time_grain: ""
relations:
  - type: derived_from
    target: game.train.card
    description: 机械提取自 game.train.card 的 metrics 块（key=train_card_card_uv）
