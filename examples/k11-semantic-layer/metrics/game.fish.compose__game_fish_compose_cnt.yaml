kind: metric
name: game.fish.compose__game_fish_compose_cnt
description: 钓鱼合成事件触发总次数
domains:
  - 自定义
computation:
  sql: COUNT(*)
  metadata:
    aggregation: count
    field: "*"
    source: game.fish.compose
    time_grain: ""
relations:
  - type: derived_from
    target: game.fish.compose
    description: 机械提取自 game.fish.compose 的 metrics 块（key=game_fish_compose_cnt）
