kind: metric
name: game.autoSkill.setItem__game_autoSkill_setItem_scheme_uv
description: 被设置的去重方案id数（方案id 即 params.id）
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params,'$.id'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params,'$.id')
    source: game.autoSkill.setItem
    time_grain: ""
relations:
  - type: derived_from
    target: game.autoSkill.setItem
    description: 机械提取自 game.autoSkill.setItem 的 metrics 块（key=game_autoSkill_setItem_scheme_uv）
