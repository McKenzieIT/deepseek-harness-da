kind: metric
name: game.anniversary.fetchBlankFloorReward__fetchBlankFloorReward_per_role_cnt
description: 人均领取次数（总次数 / 去重角色数）
domains:
  - 付费经济
computation:
  sql: COUNT(*) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: count
    field: "*) / NULLIF(COUNT(DISTINCT role_id), 0"
    source: game.anniversary.fetchBlankFloorReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.anniversary.fetchBlankFloorReward
    description: 机械提取自 game.anniversary.fetchBlankFloorReward 的 metrics 块（key=fetchBlankFloorReward_per_role_cnt）
