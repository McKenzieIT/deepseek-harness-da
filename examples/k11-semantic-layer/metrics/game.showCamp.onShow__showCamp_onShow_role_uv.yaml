kind: metric
name: game.showCamp.onShow__showCamp_onShow_role_uv
description: 营地展示触达角色数（去重，角色为分析主体）
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.showCamp.onShow
    time_grain: ""
relations:
  - type: derived_from
    target: game.showCamp.onShow
    description: 机械提取自 game.showCamp.onShow 的 metrics 块（key=showCamp_onShow_role_uv）
