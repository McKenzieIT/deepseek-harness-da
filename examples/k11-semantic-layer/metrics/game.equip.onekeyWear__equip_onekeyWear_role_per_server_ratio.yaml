kind: metric
name: game.equip.onekeyWear__equip_onekeyWear_role_per_server_ratio
description: 平均每区服一键穿戴角色数(角色UV/区服UV；衡量区服集中度)
domains:
  - 装备道具
computation:
  sql: ROUND(CAST(COUNT(DISTINCT role_id) AS DOUBLE) / NULLIF(COUNT(DISTINCT server_id), 0), 2)
  metadata:
    aggregation: ""
    field: ""
    source: game.equip.onekeyWear
    time_grain: ""
relations:
  - type: derived_from
    target: game.equip.onekeyWear
    description: 机械提取自 game.equip.onekeyWear 的 metrics 块（key=equip_onekeyWear_role_per_server_ratio）
