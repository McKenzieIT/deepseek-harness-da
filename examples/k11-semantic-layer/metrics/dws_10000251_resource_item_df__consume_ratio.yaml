kind: metric
name: dws_10000251_resource_item_df__consume_ratio
description: 产出消耗比=累计消耗/累计产出，衡量资源流转效率与囤积度
domains:
  - 资源产销
  - 角色成长
computation:
  sql: SUM(use_amount) / NULLIF(SUM(add_amount), 0)
  metadata:
    aggregation: sum
    field: use_amount) / NULLIF(SUM(add_amount), 0
    source: dws_10000251_resource_item_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_resource_item_df
    description: 机械提取自 dws_10000251_resource_item_df 的 metrics 块（key=consume_ratio）
