kind: metric
name: dws_10000251_univ_acc_act_di__new_pay_acc
description: 当日新增且付费账号数,新增账号中当日即付费的账号数,用于评估新增变现转化
domains:
  - 用户生命周期
  - 付费经济
computation:
  sql: SUM(CASE WHEN act_fst=1 AND pay=1 THEN 1 ELSE 0 END)
  metadata:
    aggregation: sum
    field: CASE WHEN act_fst=1 AND pay=1 THEN 1 ELSE 0 END
    source: dws_10000251_univ_acc_act_di
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_univ_acc_act_di
    description: 机械提取自 dws_10000251_univ_acc_act_di 的 metrics 块（key=new_pay_acc）
