kind: metric
name: dws_10000251_item_circle_di__role_uv
description: 当日发生物品流水的角色去重数(UV)
domains:
  - 资源产销
  - 装备道具
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: dws_10000251_item_circle_di
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_item_circle_di
    description: 机械提取自 dws_10000251_item_circle_di 的 metrics 块（key=role_uv）
