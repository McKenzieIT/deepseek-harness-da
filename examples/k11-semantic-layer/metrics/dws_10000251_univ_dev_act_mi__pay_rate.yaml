kind: metric
name: dws_10000251_univ_dev_act_mi__pay_rate
description: 月付费率（付费设备数/活跃设备数，NULLIF 防除零）
domains:
  - 用户生命周期
  - 付费经济
computation:
  sql: SUM(CASE WHEN pay=1 THEN 1 ELSE 0 END) * 1.0 / NULLIF(COUNT(DISTINCT user_id), 0)
  metadata:
    aggregation: sum
    field: CASE WHEN pay=1 THEN 1 ELSE 0 END) * 1.0 / NULLIF(COUNT(DISTINCT user_id), 0
    source: dws_10000251_univ_dev_act_mi
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_univ_dev_act_mi
    description: 机械提取自 dws_10000251_univ_dev_act_mi 的 metrics 块（key=pay_rate）
