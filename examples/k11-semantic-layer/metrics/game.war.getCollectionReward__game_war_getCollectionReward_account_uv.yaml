kind: metric
name: game.war.getCollectionReward__game_war_getCollectionReward_account_uv
description: 领取收集品奖励的去重账号数
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT account_id)
  metadata:
    aggregation: count_distinct
    field: account_id
    source: game.war.getCollectionReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.war.getCollectionReward
    description: 机械提取自 game.war.getCollectionReward 的 metrics 块（key=game_war_getCollectionReward_account_uv）
