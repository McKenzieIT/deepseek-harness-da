kind: metric
name: game.recover.recover__recover_server_uv
description: 涉及的 distinct 游戏服数
domains:
  - 资源产销
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: game.recover.recover
    time_grain: ""
relations:
  - type: derived_from
    target: game.recover.recover
    description: 机械提取自 game.recover.recover 的 metrics 块（key=recover_server_uv）
