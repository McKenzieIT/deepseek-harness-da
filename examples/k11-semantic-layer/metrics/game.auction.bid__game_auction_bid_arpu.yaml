kind: metric
name: game.auction.bid__game_auction_bid_arpu
description: 人均竞价金额（price_sum / role_uv）
domains:
  - 付费经济
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params,'$.price') AS BIGINT)) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params,'$.price') AS BIGINT)) / NULLIF(COUNT(DISTINCT role_id), 0
    source: game.auction.bid
    time_grain: ""
relations:
  - type: derived_from
    target: game.auction.bid
    description: 机械提取自 game.auction.bid 的 metrics 块（key=game_auction_bid_arpu）
