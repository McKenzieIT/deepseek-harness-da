kind: metric
name: game.resort.accept__resort_accept_accepts_per_role
description: 每个接受方角色的平均接受次数（>1 表示同一代打方当日多次接受代打）
domains:
  - 自定义
computation:
  sql: CAST(COUNT(*) AS DOUBLE) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: ""
    field: ""
    source: game.resort.accept
    time_grain: ""
relations:
  - type: derived_from
    target: game.resort.accept
    description: 机械提取自 game.resort.accept 的 metrics 块（key=resort_accept_accepts_per_role）
