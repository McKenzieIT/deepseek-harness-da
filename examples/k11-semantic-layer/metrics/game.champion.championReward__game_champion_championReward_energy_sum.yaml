kind: metric
name: game.champion.championReward__game_champion_championReward_energy_sum
description: 奖励发放体力总量
domains:
  - 战斗关卡
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.coinList.energy') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.coinList.energy') AS BIGINT)
    source: game.champion.championReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.champion.championReward
    description: 机械提取自 game.champion.championReward 的 metrics 块（key=game_champion_championReward_energy_sum）
