kind: metric
name: game.guild.inviteJoin__guild_invite_join_target_uv
description: 被邀请的去重目标玩家数（targetUid）
domains:
  - 社交公会
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params,'$.targetUid'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params,'$.targetUid')
    source: game.guild.inviteJoin
    time_grain: ""
relations:
  - type: derived_from
    target: game.guild.inviteJoin
    description: 机械提取自 game.guild.inviteJoin 的 metrics 块（key=guild_invite_join_target_uv）
