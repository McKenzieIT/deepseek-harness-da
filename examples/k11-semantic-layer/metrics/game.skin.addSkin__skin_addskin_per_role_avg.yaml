kind: metric
name: game.skin.addSkin__skin_addskin_per_role_avg
description: 人均添加皮肤次数（总次数 / 去重角色数），NULLIF 防止零角色时除零
domains:
  - 装备道具
computation:
  sql: CAST(COUNT(*) AS DOUBLE) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: ""
    field: ""
    source: game.skin.addSkin
    time_grain: ""
relations:
  - type: derived_from
    target: game.skin.addSkin
    description: 机械提取自 game.skin.addSkin 的 metrics 块（key=skin_addskin_per_role_avg）
