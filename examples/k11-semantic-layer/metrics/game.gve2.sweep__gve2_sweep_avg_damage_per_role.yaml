kind: metric
name: game.gve2.sweep__gve2_sweep_avg_damage_per_role
description: 参与扫荡角色的平均伤害（累计伤害/去重角色数，NULLIF防0除）
domains:
  - 战斗关卡
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.damage') AS BIGINT)) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.damage') AS BIGINT)) / NULLIF(COUNT(DISTINCT role_id), 0
    source: game.gve2.sweep
    time_grain: ""
relations:
  - type: derived_from
    target: game.gve2.sweep
    description: 机械提取自 game.gve2.sweep 的 metrics 块（key=gve2_sweep_avg_damage_per_role）
