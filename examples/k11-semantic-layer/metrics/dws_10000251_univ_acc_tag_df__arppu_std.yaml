kind: metric
name: dws_10000251_univ_acc_tag_df__arppu_std
description: 累计 ARPPU(付费用户人均付费金额,元)
domains:
  - 用户生命周期
  - 付费经济
computation:
  sql: SUM(CAST(pay_amt_std AS double))*1.0/NULLIF(COUNT(DISTINCT CASE WHEN CAST(pay_amt_std AS double)>0 THEN user_id END),0)
  metadata:
    aggregation: sum
    field: CAST(pay_amt_std AS double))*1.0/NULLIF(COUNT(DISTINCT CASE WHEN CAST(pay_amt_std AS double)>0 THEN user_id END),0
    source: dws_10000251_univ_acc_tag_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_univ_acc_tag_df
    description: 机械提取自 dws_10000251_univ_acc_tag_df 的 metrics 块（key=arppu_std）
