kind: metric
name: dws_10000251_role_common_feature_di__vip_role_count
description: VIP 角色数,VIP 等级>0 的角色数量,衡量付费特权用户规模
domains:
  - 角色成长
  - 社交公会
  - 付费经济
computation:
  sql: COUNT(DISTINCT CASE WHEN CAST(viplevel AS DOUBLE) > 0 THEN role_id END)
  metadata:
    aggregation: count_distinct
    field: CASE WHEN CAST(viplevel AS DOUBLE) > 0 THEN role_id END
    source: dws_10000251_role_common_feature_di
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_role_common_feature_di
    description: 机械提取自 dws_10000251_role_common_feature_di 的 metrics 块（key=vip_role_count）
