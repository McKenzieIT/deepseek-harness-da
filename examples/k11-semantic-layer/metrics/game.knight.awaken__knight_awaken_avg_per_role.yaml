kind: metric
name: game.knight.awaken__knight_awaken_avg_per_role
description: 单角色平均觉醒次数
domains:
  - 角色成长
computation:
  sql: COUNT(*) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: count
    field: "*) / NULLIF(COUNT(DISTINCT role_id), 0"
    source: game.knight.awaken
    time_grain: ""
relations:
  - type: derived_from
    target: game.knight.awaken
    description: 机械提取自 game.knight.awaken 的 metrics 块（key=knight_awaken_avg_per_role）
