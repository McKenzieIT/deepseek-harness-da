kind: metric
name: game.rogue.choosebuff__game_rogue_choosebuff_per_role_avg
description: 人均BUFF选择次数
domains:
  - 战斗关卡
computation:
  sql: COUNT(*) * 1.0 / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: count
    field: "*) * 1.0 / NULLIF(COUNT(DISTINCT role_id), 0"
    source: game.rogue.choosebuff
    time_grain: ""
relations:
  - type: derived_from
    target: game.rogue.choosebuff
    description: 机械提取自 game.rogue.choosebuff 的 metrics 块（key=game_rogue_choosebuff_per_role_avg）
