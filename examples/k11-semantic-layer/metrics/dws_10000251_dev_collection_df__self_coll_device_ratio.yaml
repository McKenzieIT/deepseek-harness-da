kind: metric
name: dws_10000251_dev_collection_df__self_coll_device_ratio
description: 自归集设备占比（utdid 即为归集主设备的行数占比，反映归集收敛度）
domains:
  - 系统监控
computation:
  sql: SUM(CASE WHEN utdid = coll_utdid THEN 1 ELSE 0 END) / NULLIF(COUNT(*), 0)
  metadata:
    aggregation: sum
    field: CASE WHEN utdid = coll_utdid THEN 1 ELSE 0 END) / NULLIF(COUNT(*), 0
    source: dws_10000251_dev_collection_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_dev_collection_df
    description: 机械提取自 dws_10000251_dev_collection_df 的 metrics 块（key=self_coll_device_ratio）
