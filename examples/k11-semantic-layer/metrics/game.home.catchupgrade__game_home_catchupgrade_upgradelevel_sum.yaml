kind: metric
name: game.home.catchupgrade__game_home_catchupgrade_upgradelevel_sum
description: 突破新等级累计值
domains:
  - 自定义
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params,'$.upgradeLevel') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params,'$.upgradeLevel') AS BIGINT)
    source: game.home.catchupgrade
    time_grain: ""
relations:
  - type: derived_from
    target: game.home.catchupgrade
    description: 机械提取自 game.home.catchupgrade 的 metrics 块（key=game_home_catchupgrade_upgradelevel_sum）
