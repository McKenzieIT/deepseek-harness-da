kind: metric
name: game.pve.end__game_pve_end_round_sum
description: 战斗回合数总和
domains:
  - 战斗关卡
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params,'$.round') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params,'$.round') AS BIGINT)
    source: game.pve.end
    time_grain: ""
relations:
  - type: derived_from
    target: game.pve.end
    description: 机械提取自 game.pve.end 的 metrics 块（key=game_pve_end_round_sum）
