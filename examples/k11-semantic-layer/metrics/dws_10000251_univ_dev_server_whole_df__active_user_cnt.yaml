kind: metric
name: dws_10000251_univ_dev_server_whole_df__active_user_cnt
description: 去重活跃设备数（act_tm_fst 非空即曾活跃的设备）。基于累计全量快照，统计该快照中 act_tm_fst 非空的设备，非当日活跃口径。
domains:
  - 付费经济
  - 用户生命周期
computation:
  sql: COUNT(DISTINCT CASE WHEN act_tm_fst IS NOT NULL THEN user_id END)
  metadata:
    aggregation: count_distinct
    field: CASE WHEN act_tm_fst IS NOT NULL THEN user_id END
    source: dws_10000251_univ_dev_server_whole_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_univ_dev_server_whole_df
    description: 机械提取自 dws_10000251_univ_dev_server_whole_df 的 metrics 块（key=active_user_cnt）
