kind: metric
name: game.tactic.train__tactic_train_fragment_per_role_avg
description: 每个特训角色的平均碎片产出量
domains:
  - 角色成长
computation:
  sql: CAST(SUM(CAST(GET_JSON_OBJECT(params, '$.count') AS BIGINT)) AS DOUBLE) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: ""
    field: ""
    source: game.tactic.train
    time_grain: ""
relations:
  - type: derived_from
    target: game.tactic.train
    description: 机械提取自 game.tactic.train 的 metrics 块（key=tactic_train_fragment_per_role_avg）
