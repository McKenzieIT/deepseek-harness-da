kind: metric
name: game.teamConquest.quit__team_conquest_quit_role_uv
description: 退出组队讨伐的去重角色数（分析主体默认 role_id）
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.teamConquest.quit
    time_grain: ""
relations:
  - type: derived_from
    target: game.teamConquest.quit
    description: 机械提取自 game.teamConquest.quit 的 metrics 块（key=team_conquest_quit_role_uv）
