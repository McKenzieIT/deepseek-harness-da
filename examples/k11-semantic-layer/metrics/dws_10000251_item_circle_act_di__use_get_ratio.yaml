kind: metric
name: dws_10000251_item_circle_act_di__use_get_ratio
description: 使用/获得数量比，衡量物品产出消耗结构
domains:
  - 资源产销
  - 装备道具
computation:
  sql: SUM(use_amt) / NULLIF(SUM(get_amt), 0)
  metadata:
    aggregation: sum
    field: use_amt) / NULLIF(SUM(get_amt), 0
    source: dws_10000251_item_circle_act_di
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_item_circle_act_di
    description: 机械提取自 dws_10000251_item_circle_act_di 的 metrics 块（key=use_get_ratio）
