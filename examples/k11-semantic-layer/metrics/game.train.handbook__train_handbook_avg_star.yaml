kind: metric
name: game.train.handbook__train_handbook_avg_star
description: 触发时角色当前总星数平均值
domains:
  - 角色成长
computation:
  sql: AVG(CAST(GET_JSON_OBJECT(params, '$.star') AS BIGINT))
  metadata:
    aggregation: avg
    field: CAST(GET_JSON_OBJECT(params, '$.star') AS BIGINT)
    source: game.train.handbook
    time_grain: ""
relations:
  - type: derived_from
    target: game.train.handbook
    description: 机械提取自 game.train.handbook 的 metrics 块（key=train_handbook_avg_star）
