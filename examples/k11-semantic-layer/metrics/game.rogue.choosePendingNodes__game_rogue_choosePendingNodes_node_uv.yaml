kind: metric
name: game.rogue.choosePendingNodes__game_rogue_choosePendingNodes_node_uv
description: 被替换的去重节点id数
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.nodeId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.nodeId')
    source: game.rogue.choosePendingNodes
    time_grain: ""
relations:
  - type: derived_from
    target: game.rogue.choosePendingNodes
    description: 机械提取自 game.rogue.choosePendingNodes 的 metrics 块（key=game_rogue_choosePendingNodes_node_uv）
