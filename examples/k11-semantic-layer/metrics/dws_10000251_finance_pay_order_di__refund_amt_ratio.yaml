kind: metric
name: dws_10000251_finance_pay_order_di__refund_amt_ratio
description: 退款金额占比=退款金额/成功金额,用 NULLIF 防除零
domains:
  - 付费经济
  - 用户生命周期
computation:
  sql: SUM(CASE WHEN status=3 THEN pay_amt ELSE 0 END)*1.0/NULLIF(SUM(CASE WHEN status=1 THEN pay_amt ELSE 0 END),0)
  metadata:
    aggregation: sum
    field: CASE WHEN status=3 THEN pay_amt ELSE 0 END)*1.0/NULLIF(SUM(CASE WHEN status=1 THEN pay_amt ELSE 0 END),0
    source: dws_10000251_finance_pay_order_di
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_finance_pay_order_di
    description: 机械提取自 dws_10000251_finance_pay_order_di 的 metrics 块（key=refund_amt_ratio）
