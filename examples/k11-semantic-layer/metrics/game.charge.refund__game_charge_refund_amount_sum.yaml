kind: metric
name: game.charge.refund__game_charge_refund_amount_sum
description: 退款数量(amount 字段，参数描述为"数量")总和
domains:
  - 付费经济
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params,'$.amount') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params,'$.amount') AS BIGINT)
    source: game.charge.refund
    time_grain: ""
relations:
  - type: derived_from
    target: game.charge.refund
    description: 机械提取自 game.charge.refund 的 metrics 块（key=game_charge_refund_amount_sum）
