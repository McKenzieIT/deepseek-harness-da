kind: metric
name: game.guild.dissolve__game_guild_dissolve_guild_uv
description: 被解散的公会去重数（按 oldGuildId）
domains:
  - 社交公会
computation:
  sql: COUNT(DISTINCT CAST(GET_JSON_OBJECT(params,'$.oldGuildId') AS BIGINT))
  metadata:
    aggregation: count_distinct
    field: CAST(GET_JSON_OBJECT(params,'$.oldGuildId') AS BIGINT)
    source: game.guild.dissolve
    time_grain: ""
relations:
  - type: derived_from
    target: game.guild.dissolve
    description: 机械提取自 game.guild.dissolve 的 metrics 块（key=game_guild_dissolve_guild_uv）
