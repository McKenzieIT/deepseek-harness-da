kind: metric
name: game.resort.new__resort_new_role_uv
description: 发起代打请求的去重角色数（默认分析主体 role_id）
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.resort.new
    time_grain: ""
relations:
  - type: derived_from
    target: game.resort.new
    description: 机械提取自 game.resort.new 的 metrics 块（key=resort_new_role_uv）
