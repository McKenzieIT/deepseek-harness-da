kind: metric
name: game.running.getCoupon__running_getCoupon_claims_per_role
description: 每个领取角色的平均领取次数（>1 表示同一角色当日多次领取）
domains:
  - 自定义
computation:
  sql: CAST(COUNT(*) AS DOUBLE) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: ""
    field: ""
    source: game.running.getCoupon
    time_grain: ""
relations:
  - type: derived_from
    target: game.running.getCoupon
    description: 机械提取自 game.running.getCoupon 的 metrics 块（key=running_getCoupon_claims_per_role）
