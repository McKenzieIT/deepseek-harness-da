kind: metric
name: game.equip.add__equip_add_euid_uv
description: 获取装备实例的去重装备uid数（按 euid）
domains:
  - 装备道具
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.euid'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.euid')
    source: game.equip.add
    time_grain: ""
relations:
  - type: derived_from
    target: game.equip.add
    description: 机械提取自 game.equip.add 的 metrics 块（key=equip_add_euid_uv）
