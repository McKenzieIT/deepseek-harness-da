kind: metric
name: dws_10000251_bi_role_new_analysis_di__f7_retention_rate
description: 新增首7日角色留存率
domains:
  - 用户生命周期
  - 付费经济
computation:
  sql: SUM(CASE WHEN is_act_f7d = 1 THEN 1 ELSE 0 END) / NULLIF(COUNT(DISTINCT user_id), 0)
  metadata:
    aggregation: sum
    field: CASE WHEN is_act_f7d = 1 THEN 1 ELSE 0 END) / NULLIF(COUNT(DISTINCT user_id), 0
    source: dws_10000251_bi_role_new_analysis_di
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_bi_role_new_analysis_di
    description: 机械提取自 dws_10000251_bi_role_new_analysis_di 的 metrics 块（key=f7_retention_rate）
