kind: metric
name: game.pitchpot__pitchpot_total_count_sum
description: 投壶累计总次数之和（按 totalCount，反映投壶产出总量）
domains:
  - 探索收集
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.totalCount') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.totalCount') AS BIGINT)
    source: game.pitchpot
    time_grain: ""
relations:
  - type: derived_from
    target: game.pitchpot
    description: 机械提取自 game.pitchpot 的 metrics 块（key=pitchpot_total_count_sum）
