kind: metric
name: game.rogue.choosesoul__game_rogue_choosesoul_chapter_uv
description: 覆盖的章节去重数（GET_JSON_OBJECT 取 chapter）
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.chapter'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.chapter')
    source: game.rogue.choosesoul
    time_grain: ""
relations:
  - type: derived_from
    target: game.rogue.choosesoul
    description: 机械提取自 game.rogue.choosesoul 的 metrics 块（key=game_rogue_choosesoul_chapter_uv）
