kind: metric
name: game.guild.applyJoin__game_guild_apply_join_apply_per_role_rate
description: 平均每个角色发起的申请次数
domains:
  - 社交公会
computation:
  sql: COUNT(*) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: count
    field: "*) / NULLIF(COUNT(DISTINCT role_id), 0"
    source: game.guild.applyJoin
    time_grain: ""
relations:
  - type: derived_from
    target: game.guild.applyJoin
    description: 机械提取自 game.guild.applyJoin 的 metrics 块（key=game_guild_apply_join_apply_per_role_rate）
