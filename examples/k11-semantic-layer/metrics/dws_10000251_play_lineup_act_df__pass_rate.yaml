kind: metric
name: dws_10000251_play_lineup_act_df__pass_rate
description: 首通率，首通参与次数/参与次数，分母为0时返回NULL
domains:
  - 战斗关卡
  - 角色成长
computation:
  sql: SUM(pass_pv) / NULLIF(SUM(part_pv), 0)
  metadata:
    aggregation: sum
    field: pass_pv) / NULLIF(SUM(part_pv), 0
    source: dws_10000251_play_lineup_act_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_play_lineup_act_df
    description: 机械提取自 dws_10000251_play_lineup_act_df 的 metrics 块（key=pass_rate）
