kind: metric
name: game.rogue.summon__game_rogue_summon_per_role_avg
description: 人均招募次数(总次数/去重角色数)
domains:
  - 战斗关卡
computation:
  sql: COUNT(*) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: count
    field: "*) / NULLIF(COUNT(DISTINCT role_id), 0"
    source: game.rogue.summon
    time_grain: ""
relations:
  - type: derived_from
    target: game.rogue.summon
    description: 机械提取自 game.rogue.summon 的 metrics 块（key=game_rogue_summon_per_role_avg）
