kind: metric
name: game.fantasyknightselect.select__game_fantasyknightselect_select_role_uv
description: 触发武将自选选将的去重角色数
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.fantasyknightselect.select
    time_grain: ""
relations:
  - type: derived_from
    target: game.fantasyknightselect.select
    description: 机械提取自 game.fantasyknightselect.select 的 metrics 块（key=game_fantasyknightselect_select_role_uv）
