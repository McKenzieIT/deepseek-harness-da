kind: metric
name: game.equip.preciseFlush__game_equip_preciseFlush_part_uv
description: 涉及的装备部位(partId)去重数
domains:
  - 装备道具
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.partId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.partId')
    source: game.equip.preciseFlush
    time_grain: ""
relations:
  - type: derived_from
    target: game.equip.preciseFlush
    description: 机械提取自 game.equip.preciseFlush 的 metrics 块（key=game_equip_preciseFlush_part_uv）
