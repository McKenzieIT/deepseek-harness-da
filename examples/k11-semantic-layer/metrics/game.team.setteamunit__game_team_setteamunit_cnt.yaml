kind: metric
name: game.team.setteamunit__game_team_setteamunit_cnt
description: 阵容设置事件总次数（每次保存/调整阵容计一次）
domains:
  - 自定义
computation:
  sql: COUNT(*)
  metadata:
    aggregation: count
    field: "*"
    source: game.team.setteamunit
    time_grain: ""
relations:
  - type: derived_from
    target: game.team.setteamunit
    description: 机械提取自 game.team.setteamunit 的 metrics 块（key=game_team_setteamunit_cnt）
