kind: metric
name: game.social.guild.battle__game_social_guild_battle_role_uv
description: 参与公会战挑战的去重角色数（分析主体粒度 role_id）
domains:
  - 社交公会
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.social.guild.battle
    time_grain: ""
relations:
  - type: derived_from
    target: game.social.guild.battle
    description: 机械提取自 game.social.guild.battle 的 metrics 块（key=game_social_guild_battle_role_uv）
