kind: metric
name: game.champion.championReward__game_champion_championReward_gold_sum
description: 奖励发放充值钻石总量
domains:
  - 战斗关卡
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.coinList.gold') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.coinList.gold') AS BIGINT)
    source: game.champion.championReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.champion.championReward
    description: 机械提取自 game.champion.championReward 的 metrics 块（key=game_champion_championReward_gold_sum）
