kind: metric
name: game.scriptActivity.levelBattlepassOnekeyReward__level_battlepass_onekey_reward_theme_uv
description: 触发一键领取的活动主题（themeId）去重数
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.themeId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.themeId')
    source: game.scriptActivity.levelBattlepassOnekeyReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.scriptActivity.levelBattlepassOnekeyReward
    description: 机械提取自 game.scriptActivity.levelBattlepassOnekeyReward 的 metrics 块（key=level_battlepass_onekey_reward_theme_uv）
