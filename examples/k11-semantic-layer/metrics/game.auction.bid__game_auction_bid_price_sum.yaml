kind: metric
name: game.auction.bid__game_auction_bid_price_sum
description: 竞价出价金额总和（price 字段累加）
domains:
  - 付费经济
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params,'$.price') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params,'$.price') AS BIGINT)
    source: game.auction.bid
    time_grain: ""
relations:
  - type: derived_from
    target: game.auction.bid
    description: 机械提取自 game.auction.bid 的 metrics 块（key=game_auction_bid_price_sum）
