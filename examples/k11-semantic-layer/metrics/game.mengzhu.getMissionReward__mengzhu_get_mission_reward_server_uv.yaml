kind: metric
name: game.mengzhu.getMissionReward__mengzhu_get_mission_reward_server_uv
description: 覆盖的游戏服数
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: game.mengzhu.getMissionReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.mengzhu.getMissionReward
    description: 机械提取自 game.mengzhu.getMissionReward 的 metrics 块（key=mengzhu_get_mission_reward_server_uv）
