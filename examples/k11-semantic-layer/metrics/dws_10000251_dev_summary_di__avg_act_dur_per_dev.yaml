kind: metric
name: dws_10000251_dev_summary_di__avg_act_dur_per_dev
description: 平均每设备累计活跃时长，单位：秒/设备。
domains:
  - 用户生命周期
  - 付费经济
computation:
  sql: SUM(act_dur_std) / NULLIF(COUNT(DISTINCT utdid), 0)
  metadata:
    aggregation: sum
    field: act_dur_std) / NULLIF(COUNT(DISTINCT utdid), 0
    source: dws_10000251_dev_summary_di
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_dev_summary_di
    description: 机械提取自 dws_10000251_dev_summary_di 的 metrics 块（key=avg_act_dur_per_dev）
