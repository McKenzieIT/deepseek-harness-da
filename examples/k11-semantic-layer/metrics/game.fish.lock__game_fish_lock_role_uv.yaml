kind: metric
name: game.fish.lock__game_fish_lock_role_uv
description: 触发钓鱼锁定的去重角色数
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.fish.lock
    time_grain: ""
relations:
  - type: derived_from
    target: game.fish.lock
    description: 机械提取自 game.fish.lock 的 metrics 块（key=game_fish_lock_role_uv）
