kind: metric
name: game.title.change__game_title_change_account_uv
description: 更换称号的去重账号数
domains:
  - 角色成长
computation:
  sql: COUNT(DISTINCT account_id)
  metadata:
    aggregation: count_distinct
    field: account_id
    source: game.title.change
    time_grain: ""
relations:
  - type: derived_from
    target: game.title.change
    description: 机械提取自 game.title.change 的 metrics 块（key=game_title_change_account_uv）
