kind: metric
name: dws_10000251_progression_card_snapshot_df__total_card_force
description: 全部卡牌战力总和(角色卡牌战力池)
domains:
  - 角色成长
  - 探索收集
computation:
  sql: SUM(CAST(card_force AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(card_force AS BIGINT)
    source: dws_10000251_progression_card_snapshot_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_progression_card_snapshot_df
    description: 机械提取自 dws_10000251_progression_card_snapshot_df 的 metrics 块（key=total_card_force）
