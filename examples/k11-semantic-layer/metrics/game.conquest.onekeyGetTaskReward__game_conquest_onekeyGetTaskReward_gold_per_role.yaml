kind: metric
name: game.conquest.onekeyGetTaskReward__game_conquest_onekeyGetTaskReward_gold_per_role
description: 人均发放充值钻石量
domains:
  - 战斗关卡
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.coinList.gold') AS BIGINT)) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.coinList.gold') AS BIGINT)) / NULLIF(COUNT(DISTINCT role_id), 0
    source: game.conquest.onekeyGetTaskReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.conquest.onekeyGetTaskReward
    description: 机械提取自 game.conquest.onekeyGetTaskReward 的 metrics 块（key=game_conquest_onekeyGetTaskReward_gold_per_role）
