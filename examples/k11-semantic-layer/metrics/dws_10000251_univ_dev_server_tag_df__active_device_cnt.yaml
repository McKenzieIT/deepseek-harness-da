kind: metric
name: dws_10000251_univ_dev_server_tag_df__active_device_cnt
description: 活跃设备数,截至当日累计在该区服有过活跃的设备去重计数;本表固定 user_type=1,user_id 即设备ID,无需按 user_type 分组
domains:
  - 用户生命周期
  - 付费经济
  - 角色成长
  - 系统监控
computation:
  sql: COUNT(DISTINCT user_id)
  metadata:
    aggregation: count_distinct
    field: user_id
    source: dws_10000251_univ_dev_server_tag_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_univ_dev_server_tag_df
    description: 机械提取自 dws_10000251_univ_dev_server_tag_df 的 metrics 块（key=active_device_cnt）
