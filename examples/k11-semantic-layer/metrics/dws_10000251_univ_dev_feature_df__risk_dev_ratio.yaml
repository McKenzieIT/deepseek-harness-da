kind: metric
name: dws_10000251_univ_dev_feature_df__risk_dev_ratio
description: 风险设备占比（risk_level>0 设备 / 全量设备）
domains:
  - 用户生命周期
  - 系统监控
computation:
  sql: SUM(CASE WHEN risk_level>0 THEN 1 ELSE 0 END) / NULLIF(COUNT(DISTINCT user_id),0)
  metadata:
    aggregation: sum
    field: CASE WHEN risk_level>0 THEN 1 ELSE 0 END) / NULLIF(COUNT(DISTINCT user_id),0
    source: dws_10000251_univ_dev_feature_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_univ_dev_feature_df
    description: 机械提取自 dws_10000251_univ_dev_feature_df 的 metrics 块（key=risk_dev_ratio）
