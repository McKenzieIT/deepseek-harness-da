kind: metric
name: game.team.setteamunit__game_team_setteamunit_per_role
description: 单角色平均阵容设置次数（>1 提示同角色多次调整阵容，NULLIF 防 0 除）
domains:
  - 自定义
computation:
  sql: ROUND(CAST(COUNT(*) AS DOUBLE) / NULLIF(COUNT(DISTINCT role_id), 0), 2)
  metadata:
    aggregation: ""
    field: ""
    source: game.team.setteamunit
    time_grain: ""
relations:
  - type: derived_from
    target: game.team.setteamunit
    description: 机械提取自 game.team.setteamunit 的 metrics 块（key=game_team_setteamunit_per_role）
