kind: metric
name: game.train.card__train_card_level_sum
description: 被养成武将等级（level）累加
domains:
  - 角色成长
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.level') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.level') AS BIGINT)
    source: game.train.card
    time_grain: ""
relations:
  - type: derived_from
    target: game.train.card
    description: 机械提取自 game.train.card 的 metrics 块（key=train_card_level_sum）
