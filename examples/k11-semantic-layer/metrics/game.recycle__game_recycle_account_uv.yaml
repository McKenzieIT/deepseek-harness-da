kind: metric
name: game.recycle__game_recycle_account_uv
description: 触发资源回收的去重账号数
domains:
  - 资源产销
  - 装备道具
computation:
  sql: COUNT(DISTINCT account_id)
  metadata:
    aggregation: count_distinct
    field: account_id
    source: game.recycle
    time_grain: ""
relations:
  - type: derived_from
    target: game.recycle
    description: 机械提取自 game.recycle 的 metrics 块（key=game_recycle_account_uv）
