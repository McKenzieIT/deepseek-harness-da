kind: metric
name: dws_10000251_vip_acc_tag_df__vip_account_uv
description: 账号级VIP快照UV（每日全量账号数，user_type=2主体）
domains:
  - 付费经济
  - 用户生命周期
computation:
  sql: COUNT(DISTINCT user_id)
  metadata:
    aggregation: count_distinct
    field: user_id
    source: dws_10000251_vip_acc_tag_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_vip_acc_tag_df
    description: 机械提取自 dws_10000251_vip_acc_tag_df 的 metrics 块（key=vip_account_uv）
