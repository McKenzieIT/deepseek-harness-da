kind: metric
name: game.item.use__game_item_use_account_uv
description: 触发道具使用的去重账号数
domains:
  - 装备道具
computation:
  sql: COUNT(DISTINCT account_id)
  metadata:
    aggregation: count_distinct
    field: account_id
    source: game.item.use
    time_grain: ""
relations:
  - type: derived_from
    target: game.item.use
    description: 机械提取自 game.item.use 的 metrics 块（key=game_item_use_account_uv）
