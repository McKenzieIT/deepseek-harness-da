kind: metric
name: game.scriptActivity.scriptMissionScoreReward__script_mission_score_reward_server_uv
description: 涉及的 distinct 游戏服数
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: game.scriptActivity.scriptMissionScoreReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.scriptActivity.scriptMissionScoreReward
    description: 机械提取自 game.scriptActivity.scriptMissionScoreReward 的 metrics 块（key=script_mission_score_reward_server_uv）
