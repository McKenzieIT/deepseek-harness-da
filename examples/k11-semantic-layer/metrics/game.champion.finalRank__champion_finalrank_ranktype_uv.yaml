kind: metric
name: game.champion.finalRank__champion_finalrank_ranktype_uv
description: 涉及的去重排名类型数
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params,'$.rankType'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params,'$.rankType')
    source: game.champion.finalRank
    time_grain: ""
relations:
  - type: derived_from
    target: game.champion.finalRank
    description: 机械提取自 game.champion.finalRank 的 metrics 块（key=champion_finalrank_ranktype_uv）
