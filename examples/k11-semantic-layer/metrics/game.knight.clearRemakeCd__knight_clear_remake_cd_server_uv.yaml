kind: metric
name: game.knight.clearRemakeCd__knight_clear_remake_cd_server_uv
description: 触发清除CD的去重游戏服数
domains:
  - 角色成长
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: game.knight.clearRemakeCd
    time_grain: ""
relations:
  - type: derived_from
    target: game.knight.clearRemakeCd
    description: 机械提取自 game.knight.clearRemakeCd 的 metrics 块（key=knight_clear_remake_cd_server_uv）
