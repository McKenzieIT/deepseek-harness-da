kind: metric
name: game.match3game.complete__game_match3game_complete_account_uv
description: 完成猜字谜关卡的账号数（去重）
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT account_id)
  metadata:
    aggregation: count_distinct
    field: account_id
    source: game.match3game.complete
    time_grain: ""
relations:
  - type: derived_from
    target: game.match3game.complete
    description: 机械提取自 game.match3game.complete 的 metrics 块（key=game_match3game_complete_account_uv）
