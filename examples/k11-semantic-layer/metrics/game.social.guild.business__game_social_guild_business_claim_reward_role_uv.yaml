kind: metric
name: game.social.guild.business__game_social_guild_business_claim_reward_role_uv
description: 领取经营奖励的角色去重数（action=3）
domains:
  - 社交公会
computation:
  sql: COUNT(DISTINCT CASE WHEN GET_JSON_OBJECT(params,'$.action')='3' THEN role_id END)
  metadata:
    aggregation: count_distinct
    field: CASE WHEN GET_JSON_OBJECT(params,'$.action')='3' THEN role_id END
    source: game.social.guild.business
    time_grain: ""
relations:
  - type: derived_from
    target: game.social.guild.business
    description: 机械提取自 game.social.guild.business 的 metrics 块（key=game_social_guild_business_claim_reward_role_uv）
