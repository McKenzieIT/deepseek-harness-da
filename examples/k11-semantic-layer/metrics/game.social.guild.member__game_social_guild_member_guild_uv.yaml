kind: metric
name: game.social.guild.member__game_social_guild_member_guild_uv
description: 去重公会数（事件分析主体；实测每公会每日1条快照，与 cnt 接近相等）
domains:
  - 社交公会
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params,'$.guildId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params,'$.guildId')
    source: game.social.guild.member
    time_grain: ""
relations:
  - type: derived_from
    target: game.social.guild.member
    description: 机械提取自 game.social.guild.member 的 metrics 块（key=game_social_guild_member_guild_uv）
