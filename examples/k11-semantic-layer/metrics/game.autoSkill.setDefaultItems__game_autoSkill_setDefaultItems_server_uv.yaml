kind: metric
name: game.autoSkill.setDefaultItems__game_autoSkill_setDefaultItems_server_uv
description: 触发设置武将默认技能方案的去重服务器数
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: game.autoSkill.setDefaultItems
    time_grain: ""
relations:
  - type: derived_from
    target: game.autoSkill.setDefaultItems
    description: 机械提取自 game.autoSkill.setDefaultItems 的 metrics 块（key=game_autoSkill_setDefaultItems_server_uv）
