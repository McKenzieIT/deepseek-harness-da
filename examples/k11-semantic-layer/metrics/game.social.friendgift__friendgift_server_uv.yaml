kind: metric
name: game.social.friendgift__friendgift_server_uv
description: 覆盖的服务器数（去重）
domains:
  - 社交公会
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: game.social.friendgift
    time_grain: ""
relations:
  - type: derived_from
    target: game.social.friendgift
    description: 机械提取自 game.social.friendgift 的 metrics 块（key=friendgift_server_uv）
