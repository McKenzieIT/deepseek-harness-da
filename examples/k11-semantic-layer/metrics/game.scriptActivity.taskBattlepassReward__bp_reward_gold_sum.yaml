kind: metric
name: game.scriptActivity.taskBattlepassReward__bp_reward_gold_sum
description: 领取奖励的充值钻石(coinList.gold)总变动量
domains:
  - 自定义
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params,'$.coinList.gold') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params,'$.coinList.gold') AS BIGINT)
    source: game.scriptActivity.taskBattlepassReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.scriptActivity.taskBattlepassReward
    description: 机械提取自 game.scriptActivity.taskBattlepassReward 的 metrics 块（key=bp_reward_gold_sum）
