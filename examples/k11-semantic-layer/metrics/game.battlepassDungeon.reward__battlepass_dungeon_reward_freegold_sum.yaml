kind: metric
name: game.battlepassDungeon.reward__battlepass_dungeon_reward_freegold_sum
description: 发放免费钻石总量(coinList.freegold 求和)
domains:
  - 角色成长
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.coinList.freegold') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.coinList.freegold') AS BIGINT)
    source: game.battlepassDungeon.reward
    time_grain: ""
relations:
  - type: derived_from
    target: game.battlepassDungeon.reward
    description: 机械提取自 game.battlepassDungeon.reward 的 metrics 块（key=battlepass_dungeon_reward_freegold_sum）
