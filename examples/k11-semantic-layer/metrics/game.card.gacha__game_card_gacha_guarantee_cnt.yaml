kind: metric
name: game.card.gacha__game_card_gacha_guarantee_cnt
description: 触发大保底的抽卡次数（guarantee=1）
domains:
  - 探索收集
computation:
  sql: SUM(CASE WHEN GET_JSON_OBJECT(params,'$.guarantee')='1' THEN 1 ELSE 0 END)
  metadata:
    aggregation: sum
    field: CASE WHEN GET_JSON_OBJECT(params,'$.guarantee')='1' THEN 1 ELSE 0 END
    source: game.card.gacha
    time_grain: ""
relations:
  - type: derived_from
    target: game.card.gacha
    description: 机械提取自 game.card.gacha 的 metrics 块（key=game_card_gacha_guarantee_cnt）
