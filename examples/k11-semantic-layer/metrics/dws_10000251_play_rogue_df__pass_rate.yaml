kind: metric
name: dws_10000251_play_rogue_df__pass_rate
description: 通关率=总通关次数/总参与次数
domains:
  - 战斗关卡
  - 角色成长
computation:
  sql: SUM(pass_pv) * 1.0 / NULLIF(SUM(part_pv), 0)
  metadata:
    aggregation: sum
    field: pass_pv) * 1.0 / NULLIF(SUM(part_pv), 0
    source: dws_10000251_play_rogue_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_play_rogue_df
    description: 机械提取自 dws_10000251_play_rogue_df 的 metrics 块（key=pass_rate）
