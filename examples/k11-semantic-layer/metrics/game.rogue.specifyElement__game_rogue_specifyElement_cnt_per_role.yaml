kind: metric
name: game.rogue.specifyElement__game_rogue_specifyElement_cnt_per_role
description: 人均指定元素次数（总记录数/角色去重数）
domains:
  - 战斗关卡
computation:
  sql: CAST(COUNT(*) AS DOUBLE) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: ""
    field: ""
    source: game.rogue.specifyElement
    time_grain: ""
relations:
  - type: derived_from
    target: game.rogue.specifyElement
    description: 机械提取自 game.rogue.specifyElement 的 metrics 块（key=game_rogue_specifyElement_cnt_per_role）
