kind: metric
name: game.scriptActivity.scriptMissionScoreReward__script_mission_score_reward_role_uv
description: 领取过奖励的 distinct 角色数（分析主体为 role_id）
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.scriptActivity.scriptMissionScoreReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.scriptActivity.scriptMissionScoreReward
    description: 机械提取自 game.scriptActivity.scriptMissionScoreReward 的 metrics 块（key=script_mission_score_reward_role_uv）
