kind: metric
name: dws_10000251_selfhelp_new_pay_df_extend_temp__avg_pay_amt_per_user
description: 新增付费用户客单价（元/人），总额除以去重用户数，NULLIF 防除零。
domains:
  - 付费经济
  - 用户生命周期
computation:
  sql: SUM(pay_amt) / NULLIF(COUNT(DISTINCT user_id), 0)
  metadata:
    aggregation: sum
    field: pay_amt) / NULLIF(COUNT(DISTINCT user_id), 0
    source: dws_10000251_selfhelp_new_pay_df_extend_temp
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_selfhelp_new_pay_df_extend_temp
    description: 机械提取自 dws_10000251_selfhelp_new_pay_df_extend_temp 的 metrics 块（key=avg_pay_amt_per_user）
