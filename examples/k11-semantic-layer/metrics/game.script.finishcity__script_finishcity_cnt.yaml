kind: metric
name: game.script.finishcity__script_finishcity_cnt
description: 完成城池事件总次数（每完成一座城池计一次）
domains:
  - 自定义
computation:
  sql: COUNT(*)
  metadata:
    aggregation: count
    field: "*"
    source: game.script.finishcity
    time_grain: ""
relations:
  - type: derived_from
    target: game.script.finishcity
    description: 机械提取自 game.script.finishcity 的 metrics 块（key=script_finishcity_cnt）
