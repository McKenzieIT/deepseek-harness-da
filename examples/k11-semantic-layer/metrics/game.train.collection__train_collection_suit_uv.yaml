kind: metric
name: game.train.collection__train_collection_suit_uv
description: 单日触达的不同套装id数量
domains:
  - 角色成长
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params,'$.suitId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params,'$.suitId')
    source: game.train.collection
    time_grain: ""
relations:
  - type: derived_from
    target: game.train.collection
    description: 机械提取自 game.train.collection 的 metrics 块（key=train_collection_suit_uv）
