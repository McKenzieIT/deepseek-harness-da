kind: metric
name: game.fish.activeCollectTotalLevel__game_fish_activeCollectTotalLevel_cnt
description: 钓鱼一键领取图鉴总等级奖励事件触发总次数
domains:
  - 自定义
computation:
  sql: COUNT(*)
  metadata:
    aggregation: count
    field: "*"
    source: game.fish.activeCollectTotalLevel
    time_grain: ""
relations:
  - type: derived_from
    target: game.fish.activeCollectTotalLevel
    description: 机械提取自 game.fish.activeCollectTotalLevel 的 metrics 块（key=game_fish_activeCollectTotalLevel_cnt）
