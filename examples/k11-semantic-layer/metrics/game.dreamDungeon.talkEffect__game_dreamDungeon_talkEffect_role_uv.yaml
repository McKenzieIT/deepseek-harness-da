kind: metric
name: game.dreamDungeon.talkEffect__game_dreamDungeon_talkEffect_role_uv
description: 触发梦境迷宫对话的去重角色数
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.dreamDungeon.talkEffect
    time_grain: ""
relations:
  - type: derived_from
    target: game.dreamDungeon.talkEffect
    description: 机械提取自 game.dreamDungeon.talkEffect 的 metrics 块（key=game_dreamDungeon_talkEffect_role_uv）
