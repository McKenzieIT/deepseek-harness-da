kind: metric
name: game.script.turntable__game_script_turntable_reward_uv
description: 产出的去重奖励(rewardId)种类数
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params,'$.rewardId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params,'$.rewardId')
    source: game.script.turntable
    time_grain: ""
relations:
  - type: derived_from
    target: game.script.turntable
    description: 机械提取自 game.script.turntable 的 metrics 块（key=game_script_turntable_reward_uv）
