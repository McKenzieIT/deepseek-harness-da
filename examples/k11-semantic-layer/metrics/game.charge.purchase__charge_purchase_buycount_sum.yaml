kind: metric
name: game.charge.purchase__charge_purchase_buycount_sum
description: 购买次数合计(累计 buyCount)
domains:
  - 付费经济
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.buyCount') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.buyCount') AS BIGINT)
    source: game.charge.purchase
    time_grain: ""
relations:
  - type: derived_from
    target: game.charge.purchase
    description: 机械提取自 game.charge.purchase 的 metrics 块（key=charge_purchase_buycount_sum）
