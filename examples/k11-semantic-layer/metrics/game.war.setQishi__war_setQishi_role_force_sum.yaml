kind: metric
name: game.war.setQishi__war_setQishi_role_force_sum
description: 触发事件角色战力累计（参考性求和，未去重）
domains:
  - 战斗关卡
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params,'$.roleForce') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params,'$.roleForce') AS BIGINT)
    source: game.war.setQishi
    time_grain: ""
relations:
  - type: derived_from
    target: game.war.setQishi
    description: 机械提取自 game.war.setQishi 的 metrics 块（key=war_setQishi_role_force_sum）
