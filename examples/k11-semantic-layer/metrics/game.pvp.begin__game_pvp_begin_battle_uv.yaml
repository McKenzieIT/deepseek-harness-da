kind: metric
name: game.pvp.begin__game_pvp_begin_battle_uv
description: 去重战斗局数（按 battleId）
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.battleId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.battleId')
    source: game.pvp.begin
    time_grain: ""
relations:
  - type: derived_from
    target: game.pvp.begin
    description: 机械提取自 game.pvp.begin 的 metrics 块（key=game_pvp_begin_battle_uv）
