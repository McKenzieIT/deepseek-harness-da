kind: metric
name: game.battle.quit__game_battle_quit_avg_role_level
description: 放弃战斗角色的平均等级
domains:
  - 战斗关卡
computation:
  sql: AVG(CAST(GET_JSON_OBJECT(params, '$.roleLevel') AS BIGINT))
  metadata:
    aggregation: avg
    field: CAST(GET_JSON_OBJECT(params, '$.roleLevel') AS BIGINT)
    source: game.battle.quit
    time_grain: ""
relations:
  - type: derived_from
    target: game.battle.quit
    description: 机械提取自 game.battle.quit 的 metrics 块（key=game_battle_quit_avg_role_level）
