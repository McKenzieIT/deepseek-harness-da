kind: metric
name: game.happyLottery.gacha__game_happyLottery_gacha_point_sum
description: 刮刮乐抽卡累计增加的积分总和
domains:
  - 自定义
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params,'$.addPoint') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params,'$.addPoint') AS BIGINT)
    source: game.happyLottery.gacha
    time_grain: ""
relations:
  - type: derived_from
    target: game.happyLottery.gacha
    description: 机械提取自 game.happyLottery.gacha 的 metrics 块（key=game_happyLottery_gacha_point_sum）
