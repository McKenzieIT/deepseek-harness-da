kind: metric
name: dws_10000251_role_server_base_df__pay_role_uv
description: 累计付费角色数：累计付费金额>0的去重角色数
domains:
  - 用户生命周期
  - 付费经济
computation:
  sql: COUNT(DISTINCT CASE WHEN pay_amt_std > 0 THEN role_id END)
  metadata:
    aggregation: count_distinct
    field: CASE WHEN pay_amt_std > 0 THEN role_id END
    source: dws_10000251_role_server_base_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_role_server_base_df
    description: 机械提取自 dws_10000251_role_server_base_df 的 metrics 块（key=pay_role_uv）
