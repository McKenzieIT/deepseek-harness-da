kind: metric
name: game.guildActivity.bonus__guild_activity_bonus_level_uv
description: 涉及的分红等级档位数(去重 level；实测覆盖 2/3/4/6 档)
domains:
  - 社交公会
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.level'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.level')
    source: game.guildActivity.bonus
    time_grain: ""
relations:
  - type: derived_from
    target: game.guildActivity.bonus
    description: 机械提取自 game.guildActivity.bonus 的 metrics 块（key=guild_activity_bonus_level_uv）
