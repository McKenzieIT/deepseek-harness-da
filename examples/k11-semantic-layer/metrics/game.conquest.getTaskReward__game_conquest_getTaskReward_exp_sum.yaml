kind: metric
name: game.conquest.getTaskReward__game_conquest_getTaskReward_exp_sum
description: 奖励发放经验总量
domains:
  - 战斗关卡
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.exp') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.exp') AS BIGINT)
    source: game.conquest.getTaskReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.conquest.getTaskReward
    description: 机械提取自 game.conquest.getTaskReward 的 metrics 块（key=game_conquest_getTaskReward_exp_sum）
