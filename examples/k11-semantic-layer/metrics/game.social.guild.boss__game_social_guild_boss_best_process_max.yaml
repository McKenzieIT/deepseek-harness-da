kind: metric
name: game.social.guild.boss__game_social_guild_boss_best_process_max
description: 个人历史最佳百分比最大值(单日全区服最高 bestProcess；衡量顶尖进度)
domains:
  - 社交公会
computation:
  sql: MAX(CAST(GET_JSON_OBJECT(params, '$.bestProcess') AS BIGINT))
  metadata:
    aggregation: max
    field: CAST(GET_JSON_OBJECT(params, '$.bestProcess') AS BIGINT)
    source: game.social.guild.boss
    time_grain: ""
relations:
  - type: derived_from
    target: game.social.guild.boss
    description: 机械提取自 game.social.guild.boss 的 metrics 块（key=game_social_guild_boss_best_process_max）
