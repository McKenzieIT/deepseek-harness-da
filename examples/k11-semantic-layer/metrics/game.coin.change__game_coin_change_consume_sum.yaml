kind: metric
name: game.coin.change__game_coin_change_consume_sum
description: 货币消耗总量(action=2 的 amount 累加)
domains:
  - 付费经济
computation:
  sql: SUM(CASE WHEN GET_JSON_OBJECT(params,'$.action')='2' THEN CAST(GET_JSON_OBJECT(params,'$.amount') AS BIGINT) ELSE 0 END)
  metadata:
    aggregation: sum
    field: CASE WHEN GET_JSON_OBJECT(params,'$.action')='2' THEN CAST(GET_JSON_OBJECT(params,'$.amount') AS BIGINT) ELSE 0 END
    source: game.coin.change
    time_grain: ""
relations:
  - type: derived_from
    target: game.coin.change
    description: 机械提取自 game.coin.change 的 metrics 块（key=game_coin_change_consume_sum）
