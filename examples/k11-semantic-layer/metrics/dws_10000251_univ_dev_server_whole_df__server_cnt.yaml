kind: metric
name: dws_10000251_univ_dev_server_whole_df__server_cnt
description: 有快照记录的去重区服数，用于评估设备覆盖的区服广度。
domains:
  - 付费经济
  - 用户生命周期
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: dws_10000251_univ_dev_server_whole_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_univ_dev_server_whole_df
    description: 机械提取自 dws_10000251_univ_dev_server_whole_df 的 metrics 块（key=server_cnt）
