kind: metric
name: game.dailyGamble__game_dailygamble_reward_size_sum
description: 每日转盘发放的奖励数量合计（rewardSize 累加）
domains:
  - 自定义
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params,'$.rewardSize') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params,'$.rewardSize') AS BIGINT)
    source: game.dailyGamble
    time_grain: ""
relations:
  - type: derived_from
    target: game.dailyGamble
    description: 机械提取自 game.dailyGamble 的 metrics 块（key=game_dailygamble_reward_size_sum）
