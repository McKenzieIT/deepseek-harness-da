kind: metric
name: game.equip.ware__equip_ware_avg_per_role
description: 人均装备穿戴次数（cnt/role_uv，NULLIF 防 0 除）
domains:
  - 装备道具
computation:
  sql: ROUND(COUNT(*)*1.0/NULLIF(COUNT(DISTINCT role_id),0),2)
  metadata:
    aggregation: ""
    field: ""
    source: game.equip.ware
    time_grain: ""
relations:
  - type: derived_from
    target: game.equip.ware
    description: 机械提取自 game.equip.ware 的 metrics 块（key=equip_ware_avg_per_role）
