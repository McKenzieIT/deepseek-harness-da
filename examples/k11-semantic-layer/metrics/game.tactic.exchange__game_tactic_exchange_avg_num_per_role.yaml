kind: metric
name: game.tactic.exchange__game_tactic_exchange_avg_num_per_role
description: 参与兑换角色的平均兑换数量（总量/去重角色数）
domains:
  - 角色成长
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.tacticNum') AS BIGINT)) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.tacticNum') AS BIGINT)) / NULLIF(COUNT(DISTINCT role_id), 0
    source: game.tactic.exchange
    time_grain: ""
relations:
  - type: derived_from
    target: game.tactic.exchange
    description: 机械提取自 game.tactic.exchange 的 metrics 块（key=game_tactic_exchange_avg_num_per_role）
