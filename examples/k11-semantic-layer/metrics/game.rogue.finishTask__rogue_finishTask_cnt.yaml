kind: metric
name: game.rogue.finishTask__rogue_finishTask_cnt
description: 古战场图鉴任务完成总次数（每条事件=完成一次 taskId）
domains:
  - 战斗关卡
computation:
  sql: COUNT(*)
  metadata:
    aggregation: count
    field: "*"
    source: game.rogue.finishTask
    time_grain: ""
relations:
  - type: derived_from
    target: game.rogue.finishTask
    description: 机械提取自 game.rogue.finishTask 的 metrics 块（key=rogue_finishTask_cnt）
