kind: metric
name: game.equip.preciseCancel__game_equip_preciseCancel_per_role
description: 每个触发角色的平均取消次数（事件行数 / 去重角色数）
domains:
  - 装备道具
computation:
  sql: COUNT(*) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: count
    field: "*) / NULLIF(COUNT(DISTINCT role_id), 0"
    source: game.equip.preciseCancel
    time_grain: ""
relations:
  - type: derived_from
    target: game.equip.preciseCancel
    description: 机械提取自 game.equip.preciseCancel 的 metrics 块（key=game_equip_preciseCancel_per_role）
