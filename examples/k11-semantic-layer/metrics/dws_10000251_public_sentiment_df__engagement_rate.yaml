kind: metric
name: dws_10000251_public_sentiment_df__engagement_rate
description: 互动率(点赞+回复+转发+分享)/阅读，NULLIF防除零
domains:
  - 系统监控
  - 社交公会
computation:
  sql: (SUM(up_cnt) + SUM(reply_cnt) + SUM(repro_cnt) + SUM(share_cnt)) / NULLIF(SUM(view_cnt), 0)
  metadata:
    aggregation: ""
    field: ""
    source: dws_10000251_public_sentiment_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_public_sentiment_df
    description: 机械提取自 dws_10000251_public_sentiment_df 的 metrics 块（key=engagement_rate）
