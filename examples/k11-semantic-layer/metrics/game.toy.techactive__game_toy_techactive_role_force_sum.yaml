kind: metric
name: game.toy.techactive__game_toy_techactive_role_force_sum
description: 激活时刻角色战力(roleForce)累计（瞬时快照求和，非增量，重复角色多次激活会重复计入）
domains:
  - 探索收集
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params,'$.roleForce') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params,'$.roleForce') AS BIGINT)
    source: game.toy.techactive
    time_grain: ""
relations:
  - type: derived_from
    target: game.toy.techactive
    description: 机械提取自 game.toy.techactive 的 metrics 块（key=game_toy_techactive_role_force_sum）
