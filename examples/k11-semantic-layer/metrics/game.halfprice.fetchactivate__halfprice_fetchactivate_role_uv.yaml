kind: metric
name: game.halfprice.fetchactivate__halfprice_fetchactivate_role_uv
description: 命中角色数（分析主体，实测 ds=20260729=563，与事件数一致即每角色单次触发为主）
domains:
  - 付费经济
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.halfprice.fetchactivate
    time_grain: ""
relations:
  - type: derived_from
    target: game.halfprice.fetchactivate
    description: 机械提取自 game.halfprice.fetchactivate 的 metrics 块（key=halfprice_fetchactivate_role_uv）
