kind: metric
name: dws_10000251_risk_role_identification_df__risk_role_uv
description: 风险角色数（risk_level>=2 的去重角色数）
domains:
  - 系统监控
computation:
  sql: COUNT(DISTINCT CASE WHEN risk_level >= 2 THEN user_id END)
  metadata:
    aggregation: count_distinct
    field: CASE WHEN risk_level >= 2 THEN user_id END
    source: dws_10000251_risk_role_identification_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_risk_role_identification_df
    description: 机械提取自 dws_10000251_risk_role_identification_df 的 metrics 块（key=risk_role_uv）
