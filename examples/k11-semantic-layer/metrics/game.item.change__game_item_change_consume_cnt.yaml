kind: metric
name: game.item.change__game_item_change_consume_cnt
description: 消耗类道具变化事件次数(action=2)
domains:
  - 装备道具
computation:
  sql: SUM(CASE WHEN CAST(GET_JSON_OBJECT(params, '$.action') AS BIGINT) = 2 THEN 1 ELSE 0 END)
  metadata:
    aggregation: sum
    field: CASE WHEN CAST(GET_JSON_OBJECT(params, '$.action') AS BIGINT) = 2 THEN 1 ELSE 0 END
    source: game.item.change
    time_grain: ""
relations:
  - type: derived_from
    target: game.item.change
    description: 机械提取自 game.item.change 的 metrics 块（key=game_item_change_consume_cnt）
