kind: metric
name: game.dig.pickupDigItem__game_dig_pickupDigItem_account_uv
description: 触发挖宝拾取的去重账号数
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT account_id)
  metadata:
    aggregation: count_distinct
    field: account_id
    source: game.dig.pickupDigItem
    time_grain: ""
relations:
  - type: derived_from
    target: game.dig.pickupDigItem
    description: 机械提取自 game.dig.pickupDigItem 的 metrics 块（key=game_dig_pickupDigItem_account_uv）
