kind: metric
name: dws_10000251_univ_user_test_tag_df__rule2_hit_rate
description: 扩展规则命中率（命中扩展规则主体数 / 活跃主体数，跨全部 user_type）
domains:
  - 用户生命周期
  - 自定义
computation:
  sql: ROUND(SUM(CASE WHEN rule2 = 1 THEN 1 ELSE 0 END) / NULLIF(COUNT(DISTINCT user_id), 0), 4)
  metadata:
    aggregation: ""
    field: ""
    source: dws_10000251_univ_user_test_tag_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_univ_user_test_tag_df
    description: 机械提取自 dws_10000251_univ_user_test_tag_df 的 metrics 块（key=rule2_hit_rate）
