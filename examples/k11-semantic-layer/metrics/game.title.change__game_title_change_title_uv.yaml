kind: metric
name: game.title.change__game_title_change_title_uv
description: 被切换到的去重目标称号id数（覆盖的称号种类）
domains:
  - 角色成长
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.title'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.title')
    source: game.title.change
    time_grain: ""
relations:
  - type: derived_from
    target: game.title.change
    description: 机械提取自 game.title.change 的 metrics 块（key=game_title_change_title_uv）
