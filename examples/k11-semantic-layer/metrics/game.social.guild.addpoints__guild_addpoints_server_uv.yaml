kind: metric
name: game.social.guild.addpoints__guild_addpoints_server_uv
description: 触发活跃值增加的去重游戏服数
domains:
  - 社交公会
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: game.social.guild.addpoints
    time_grain: ""
relations:
  - type: derived_from
    target: game.social.guild.addpoints
    description: 机械提取自 game.social.guild.addpoints 的 metrics 块（key=guild_addpoints_server_uv）
