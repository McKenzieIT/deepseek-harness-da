kind: metric
name: game.equip.add__equip_add_cfg_id_uv
description: 获取装备的去重装备配置id数（按 id）
domains:
  - 装备道具
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.id'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.id')
    source: game.equip.add
    time_grain: ""
relations:
  - type: derived_from
    target: game.equip.add
    description: 机械提取自 game.equip.add 的 metrics 块（key=equip_add_cfg_id_uv）
