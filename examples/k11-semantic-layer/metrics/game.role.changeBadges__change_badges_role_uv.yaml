kind: metric
name: game.role.changeBadges__change_badges_role_uv
description: 触发徽章变更的去重角色数
domains:
  - 用户生命周期
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.role.changeBadges
    time_grain: ""
relations:
  - type: derived_from
    target: game.role.changeBadges
    description: 机械提取自 game.role.changeBadges 的 metrics 块（key=change_badges_role_uv）
