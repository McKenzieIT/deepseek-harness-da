kind: metric
name: game.social.guild.submitmission__game_social_guild_submitmission_active_add_sum
description: 当日通过公会任务提交增加的活跃度总和（取自 params.activeAdd）。注意 scout 日 ds=20260729 该字段实测为 NULL/未填充。
domains:
  - 社交公会
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.activeAdd') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.activeAdd') AS BIGINT)
    source: game.social.guild.submitmission
    time_grain: ""
relations:
  - type: derived_from
    target: game.social.guild.submitmission
    description: 机械提取自 game.social.guild.submitmission 的 metrics 块（key=game_social_guild_submitmission_active_add_sum）
