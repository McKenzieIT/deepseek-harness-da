kind: metric
name: game.role.backup.rollbackPurchase__rollback_purchase_server_uv
description: 涉及回滚补发的去重游戏服数
domains:
  - 用户生命周期
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: game.role.backup.rollbackPurchase
    time_grain: ""
relations:
  - type: derived_from
    target: game.role.backup.rollbackPurchase
    description: 机械提取自 game.role.backup.rollbackPurchase 的 metrics 块（key=rollback_purchase_server_uv）
