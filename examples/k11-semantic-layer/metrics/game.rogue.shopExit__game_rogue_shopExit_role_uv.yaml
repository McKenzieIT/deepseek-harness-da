kind: metric
name: game.rogue.shopExit__game_rogue_shopExit_role_uv
description: 退出商店的角色数（分析主体 role_id）
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.rogue.shopExit
    time_grain: ""
relations:
  - type: derived_from
    target: game.rogue.shopExit
    description: 机械提取自 game.rogue.shopExit 的 metrics 块（key=game_rogue_shopExit_role_uv）
