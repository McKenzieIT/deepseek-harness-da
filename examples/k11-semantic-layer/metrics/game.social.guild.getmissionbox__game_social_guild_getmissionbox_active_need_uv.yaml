kind: metric
name: game.social.guild.getmissionbox__game_social_guild_getmissionbox_active_need_uv
description: 领取所需的活跃度档位数（去重 activeNeed；ds=20260729 实测5档）
domains:
  - 社交公会
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.activeNeed'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.activeNeed')
    source: game.social.guild.getmissionbox
    time_grain: ""
relations:
  - type: derived_from
    target: game.social.guild.getmissionbox
    description: 机械提取自 game.social.guild.getmissionbox 的 metrics 块（key=game_social_guild_getmissionbox_active_need_uv）
