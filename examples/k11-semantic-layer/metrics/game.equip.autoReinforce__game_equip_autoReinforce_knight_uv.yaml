kind: metric
name: game.equip.autoReinforce__game_equip_autoReinforce_knight_uv
description: 被自动强化的武将种类去重数（knightId，GET_JSON_OBJECT 取值）
domains:
  - 装备道具
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.knightId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.knightId')
    source: game.equip.autoReinforce
    time_grain: ""
relations:
  - type: derived_from
    target: game.equip.autoReinforce
    description: 机械提取自 game.equip.autoReinforce 的 metrics 块（key=game_equip_autoReinforce_knight_uv）
