kind: metric
name: game.battlepass.levelup__battlepass_levelup_premium_cnt
description: 高级/珍藏版战令（enabledStatus>=1）升级事件次数
domains:
  - 角色成长
computation:
  sql: SUM(CASE WHEN CAST(GET_JSON_OBJECT(params, '$.enabledStatus') AS BIGINT) >= 1 THEN 1 ELSE 0 END)
  metadata:
    aggregation: sum
    field: CASE WHEN CAST(GET_JSON_OBJECT(params, '$.enabledStatus') AS BIGINT) >= 1 THEN 1 ELSE 0 END
    source: game.battlepass.levelup
    time_grain: ""
relations:
  - type: derived_from
    target: game.battlepass.levelup
    description: 机械提取自 game.battlepass.levelup 的 metrics 块（key=battlepass_levelup_premium_cnt）
