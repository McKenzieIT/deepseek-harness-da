kind: metric
name: game.guild.exit__guild_exit_per_role_avg
description: 单角色平均退出次数（用于识别多次退会的异常角色）
domains:
  - 社交公会
computation:
  sql: CAST(COUNT(*) AS DOUBLE) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: ""
    field: ""
    source: game.guild.exit
    time_grain: ""
relations:
  - type: derived_from
    target: game.guild.exit
    description: 机械提取自 game.guild.exit 的 metrics 块（key=guild_exit_per_role_avg）
