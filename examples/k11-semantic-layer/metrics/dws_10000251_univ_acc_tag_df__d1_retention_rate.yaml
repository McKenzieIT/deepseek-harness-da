kind: metric
name: dws_10000251_univ_acc_tag_df__d1_retention_rate
description: 近1日连续活跃率(最近活跃间隔<=1天的账号占比,基于快照口径近似,非严格同队列次日留存)
domains:
  - 用户生命周期
  - 付费经济
computation:
  sql: AVG(CASE WHEN act_ymd_diff<=1 THEN 1 ELSE 0 END)
  metadata:
    aggregation: avg
    field: CASE WHEN act_ymd_diff<=1 THEN 1 ELSE 0 END
    source: dws_10000251_univ_acc_tag_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_univ_acc_tag_df
    description: 机械提取自 dws_10000251_univ_acc_tag_df 的 metrics 块（key=d1_retention_rate）
