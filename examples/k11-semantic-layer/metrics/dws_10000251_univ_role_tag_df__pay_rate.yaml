kind: metric
name: dws_10000251_univ_role_tag_df__pay_rate
description: 付费率(付费角色数/角色总数)
domains:
  - 用户生命周期
  - 付费经济
  - 角色成长
computation:
  sql: AVG(CASE WHEN pay_amt_std > 0 THEN 1 ELSE 0 END)
  metadata:
    aggregation: avg
    field: CASE WHEN pay_amt_std > 0 THEN 1 ELSE 0 END
    source: dws_10000251_univ_role_tag_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_univ_role_tag_df
    description: 机械提取自 dws_10000251_univ_role_tag_df 的 metrics 块（key=pay_rate）
