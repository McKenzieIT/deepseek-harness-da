kind: metric
name: dws_10000251_role_tag_basic_df__pay_role_cnt
description: 累计付费角色数（付费渗透）
domains:
  - 角色成长
  - 付费经济
  - 社交公会
computation:
  sql: COUNT(DISTINCT CASE WHEN pay_amt_std > 0 THEN role_id END)
  metadata:
    aggregation: count_distinct
    field: CASE WHEN pay_amt_std > 0 THEN role_id END
    source: dws_10000251_role_tag_basic_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_role_tag_basic_df
    description: 机械提取自 dws_10000251_role_tag_basic_df 的 metrics 块（key=pay_role_cnt）
