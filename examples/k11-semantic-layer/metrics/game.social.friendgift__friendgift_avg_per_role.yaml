kind: metric
name: game.social.friendgift__friendgift_avg_per_role
description: 人均赠收礼事件次数
domains:
  - 社交公会
computation:
  sql: COUNT(*) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: count
    field: "*) / NULLIF(COUNT(DISTINCT role_id), 0"
    source: game.social.friendgift
    time_grain: ""
relations:
  - type: derived_from
    target: game.social.friendgift
    description: 机械提取自 game.social.friendgift 的 metrics 块（key=friendgift_avg_per_role）
