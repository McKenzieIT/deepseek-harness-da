kind: metric
name: game.rogue.choosePendingNodes__game_rogue_choosePendingNodes_avg_per_role
description: 人均选择替换节点次数（总次数/去重角色数）
domains:
  - 战斗关卡
computation:
  sql: ROUND(COUNT(*) * 1.0 / NULLIF(COUNT(DISTINCT role_id), 0), 2)
  metadata:
    aggregation: ""
    field: ""
    source: game.rogue.choosePendingNodes
    time_grain: ""
relations:
  - type: derived_from
    target: game.rogue.choosePendingNodes
    description: 机械提取自 game.rogue.choosePendingNodes 的 metrics 块（key=game_rogue_choosePendingNodes_avg_per_role）
