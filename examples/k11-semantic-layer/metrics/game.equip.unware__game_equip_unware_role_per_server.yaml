kind: metric
name: game.equip.unware__game_equip_unware_role_per_server
description: 平均每服脱装角色数
domains:
  - 装备道具
computation:
  sql: COUNT(DISTINCT role_id) / NULLIF(COUNT(DISTINCT server_id), 0)
  metadata:
    aggregation: count_distinct
    field: role_id) / NULLIF(COUNT(DISTINCT server_id), 0
    source: game.equip.unware
    time_grain: ""
relations:
  - type: derived_from
    target: game.equip.unware
    description: 机械提取自 game.equip.unware 的 metrics 块（key=game_equip_unware_role_per_server）
