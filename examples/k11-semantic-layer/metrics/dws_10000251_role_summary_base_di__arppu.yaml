kind: metric
name: dws_10000251_role_summary_base_di__arppu
description: 累计付费 ARPPU（元/付费角色，累计付费总额 / 累计付费角色数）
domains:
  - 付费经济
  - 用户生命周期
computation:
  sql: SUM(pay_amt_std) / NULLIF(SUM(CASE WHEN pay_amt_std > 0 THEN 1 ELSE 0 END), 0)
  metadata:
    aggregation: sum
    field: pay_amt_std) / NULLIF(SUM(CASE WHEN pay_amt_std > 0 THEN 1 ELSE 0 END), 0
    source: dws_10000251_role_summary_base_di
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_role_summary_base_di
    description: 机械提取自 dws_10000251_role_summary_base_di 的 metrics 块（key=arppu）
