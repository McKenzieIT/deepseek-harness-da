kind: metric
name: game.equip.ware__equip_ware_knight_uv
description: 被穿戴装备的去重武将(knightId)数
domains:
  - 装备道具
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params,'$.knightId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params,'$.knightId')
    source: game.equip.ware
    time_grain: ""
relations:
  - type: derived_from
    target: game.equip.ware
    description: 机械提取自 game.equip.ware 的 metrics 块（key=equip_ware_knight_uv）
