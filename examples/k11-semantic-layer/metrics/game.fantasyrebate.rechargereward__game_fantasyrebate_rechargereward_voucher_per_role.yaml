kind: metric
name: game.fantasyrebate.rechargereward__game_fantasyrebate_rechargereward_voucher_per_role
description: 人均领取代金券数量(总量/去重角色数)
domains:
  - 付费经济
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params,'$.voucherNum') AS BIGINT)) / NULLIF(COUNT(DISTINCT role_id),0)
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params,'$.voucherNum') AS BIGINT)) / NULLIF(COUNT(DISTINCT role_id),0
    source: game.fantasyrebate.rechargereward
    time_grain: ""
relations:
  - type: derived_from
    target: game.fantasyrebate.rechargereward
    description: 机械提取自 game.fantasyrebate.rechargereward 的 metrics 块（key=game_fantasyrebate_rechargereward_voucher_per_role）
