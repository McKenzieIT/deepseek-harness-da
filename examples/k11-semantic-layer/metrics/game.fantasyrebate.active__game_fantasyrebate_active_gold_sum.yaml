kind: metric
name: game.fantasyrebate.active__game_fantasyrebate_active_gold_sum
description: 激活时充值钻石总量（JSON路径未实测验证）
domains:
  - 付费经济
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.coinList.gold') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.coinList.gold') AS BIGINT)
    source: game.fantasyrebate.active
    time_grain: ""
relations:
  - type: derived_from
    target: game.fantasyrebate.active
    description: 机械提取自 game.fantasyrebate.active 的 metrics 块（key=game_fantasyrebate_active_gold_sum）
