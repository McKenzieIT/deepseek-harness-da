kind: metric
name: game.base.setImage__game_base_setImage_cnt
description: 看板娘形象设置事件总发生次数（按角色每次设置计一次）
domains:
  - 自定义
computation:
  sql: COUNT(*)
  metadata:
    aggregation: count
    field: "*"
    source: game.base.setImage
    time_grain: ""
relations:
  - type: derived_from
    target: game.base.setImage
    description: 机械提取自 game.base.setImage 的 metrics 块（key=game_base_setImage_cnt）
