kind: metric
name: dws_10000251_univ_role_server_tag_df__first_pay_rate
description: 首付率（首付金额>0 的角色占比，衡量付费转化；本表全为角色行，分母为角色总数）
domains:
  - 用户生命周期
  - 付费经济
  - 角色成长
computation:
  sql: SUM(CASE WHEN pay_amt_p1d>0 THEN 1 ELSE 0 END)*1.0/NULLIF(COUNT(*),0)
  metadata:
    aggregation: sum
    field: CASE WHEN pay_amt_p1d>0 THEN 1 ELSE 0 END)*1.0/NULLIF(COUNT(*),0
    source: dws_10000251_univ_role_server_tag_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_univ_role_server_tag_df
    description: 机械提取自 dws_10000251_univ_role_server_tag_df 的 metrics 块（key=first_pay_rate）
