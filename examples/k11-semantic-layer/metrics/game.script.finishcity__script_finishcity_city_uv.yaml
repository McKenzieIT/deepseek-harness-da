kind: metric
name: game.script.finishcity__script_finishcity_city_uv
description: 去重被完成的城池 id 数（cityId）
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.cityId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.cityId')
    source: game.script.finishcity
    time_grain: ""
relations:
  - type: derived_from
    target: game.script.finishcity
    description: 机械提取自 game.script.finishcity 的 metrics 块（key=script_finishcity_city_uv）
