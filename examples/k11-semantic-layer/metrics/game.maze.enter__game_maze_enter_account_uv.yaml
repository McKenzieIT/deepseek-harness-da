kind: metric
name: game.maze.enter__game_maze_enter_account_uv
description: 进入迷宫的去重账号数
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT account_id)
  metadata:
    aggregation: count_distinct
    field: account_id
    source: game.maze.enter
    time_grain: ""
relations:
  - type: derived_from
    target: game.maze.enter
    description: 机械提取自 game.maze.enter 的 metrics 块（key=game_maze_enter_account_uv）
