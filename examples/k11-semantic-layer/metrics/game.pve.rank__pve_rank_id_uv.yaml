kind: metric
name: game.pve.rank__pve_rank_id_uv
description: 涉及的排行榜ID数(去重 rankId；100%非空实测)
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.rankId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.rankId')
    source: game.pve.rank
    time_grain: ""
relations:
  - type: derived_from
    target: game.pve.rank
    description: 机械提取自 game.pve.rank 的 metrics 块（key=pve_rank_id_uv）
