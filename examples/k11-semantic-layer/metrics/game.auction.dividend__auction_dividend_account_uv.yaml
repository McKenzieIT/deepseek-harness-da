kind: metric
name: game.auction.dividend__auction_dividend_account_uv
description: 触发拍卖行分红的账号去重数
domains:
  - 付费经济
computation:
  sql: COUNT(DISTINCT account_id)
  metadata:
    aggregation: count_distinct
    field: account_id
    source: game.auction.dividend
    time_grain: ""
relations:
  - type: derived_from
    target: game.auction.dividend
    description: 机械提取自 game.auction.dividend 的 metrics 块（key=auction_dividend_account_uv）
