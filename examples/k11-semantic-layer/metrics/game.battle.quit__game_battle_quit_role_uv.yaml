kind: metric
name: game.battle.quit__game_battle_quit_role_uv
description: 放弃战斗的角色数（分析主体 role_id）
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.battle.quit
    time_grain: ""
relations:
  - type: derived_from
    target: game.battle.quit
    description: 机械提取自 game.battle.quit 的 metrics 块（key=game_battle_quit_role_uv）
