kind: metric
name: game.task.routinetask__task_routinetask_server_uv
description: 覆盖的去重游戏服数量
domains:
  - 角色成长
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: game.task.routinetask
    time_grain: ""
relations:
  - type: derived_from
    target: game.task.routinetask
    description: 机械提取自 game.task.routinetask 的 metrics 块（key=task_routinetask_server_uv）
