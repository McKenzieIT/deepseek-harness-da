kind: metric
name: dws_10000251_role_prop_df__roles_with_cards
description: 当日拥有至少一名武将的角色数（own_card_list非空且非空数组）。
domains:
  - 角色成长
  - 装备道具
computation:
  sql: COUNT(IF(own_card_list IS NOT NULL AND own_card_list != '[]', 1, NULL))
  metadata:
    aggregation: count
    field: IF(own_card_list IS NOT NULL AND own_card_list != '[]', 1, NULL)
    source: dws_10000251_role_prop_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_role_prop_df
    description: 机械提取自 dws_10000251_role_prop_df 的 metrics 块（key=roles_with_cards）
