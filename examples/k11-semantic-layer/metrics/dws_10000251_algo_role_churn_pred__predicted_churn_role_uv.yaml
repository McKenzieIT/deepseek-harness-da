kind: metric
name: dws_10000251_algo_role_churn_pred__predicted_churn_role_uv
description: 预测流失角色数（tag=1）
domains:
  - 用户生命周期
  - 系统监控
computation:
  sql: COUNT(DISTINCT CASE WHEN tag=1 THEN role_id END)
  metadata:
    aggregation: count_distinct
    field: CASE WHEN tag=1 THEN role_id END
    source: dws_10000251_algo_role_churn_pred
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_algo_role_churn_pred
    description: 机械提取自 dws_10000251_algo_role_churn_pred 的 metrics 块（key=predicted_churn_role_uv）
