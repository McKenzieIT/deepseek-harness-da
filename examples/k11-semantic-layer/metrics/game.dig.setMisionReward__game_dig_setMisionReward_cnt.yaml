kind: metric
name: game.dig.setMisionReward__game_dig_setMisionReward_cnt
description: 挖宝设置任务奖励事件总条数（每次角色设置/选择任务奖励计一次）
domains:
  - 自定义
computation:
  sql: COUNT(*)
  metadata:
    aggregation: count
    field: "*"
    source: game.dig.setMisionReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.dig.setMisionReward
    description: 机械提取自 game.dig.setMisionReward 的 metrics 块（key=game_dig_setMisionReward_cnt）
