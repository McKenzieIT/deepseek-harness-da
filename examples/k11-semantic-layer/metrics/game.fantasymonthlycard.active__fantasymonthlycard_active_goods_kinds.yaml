kind: metric
name: game.fantasymonthlycard.active__fantasymonthlycard_active_goods_kinds
description: 涉及的去重充值项种类数
domains:
  - 付费经济
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.goodsId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.goodsId')
    source: game.fantasymonthlycard.active
    time_grain: ""
relations:
  - type: derived_from
    target: game.fantasymonthlycard.active
    description: 机械提取自 game.fantasymonthlycard.active 的 metrics 块（key=fantasymonthlycard_active_goods_kinds）
