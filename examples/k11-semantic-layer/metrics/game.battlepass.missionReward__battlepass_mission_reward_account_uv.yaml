kind: metric
name: game.battlepass.missionReward__battlepass_mission_reward_account_uv
description: 领取战令任务奖励的独立账号数(去重 account_id)
domains:
  - 角色成长
computation:
  sql: COUNT(DISTINCT account_id)
  metadata:
    aggregation: count_distinct
    field: account_id
    source: game.battlepass.missionReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.battlepass.missionReward
    description: 机械提取自 game.battlepass.missionReward 的 metrics 块（key=battlepass_mission_reward_account_uv）
