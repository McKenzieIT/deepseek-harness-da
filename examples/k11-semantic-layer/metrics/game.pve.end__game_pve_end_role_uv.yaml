kind: metric
name: game.pve.end__game_pve_end_role_uv
description: 参与副本战斗的去重角色数（分析主体粒度）
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.pve.end
    time_grain: ""
relations:
  - type: derived_from
    target: game.pve.end
    description: 机械提取自 game.pve.end 的 metrics 块（key=game_pve_end_role_uv）
