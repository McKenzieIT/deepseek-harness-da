kind: metric
name: game.home.catchupgrade__game_home_catchupgrade_account_uv
description: 触发捕捞等级突破的独立账号数
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT account_id)
  metadata:
    aggregation: count_distinct
    field: account_id
    source: game.home.catchupgrade
    time_grain: ""
relations:
  - type: derived_from
    target: game.home.catchupgrade
    description: 机械提取自 game.home.catchupgrade 的 metrics 块（key=game_home_catchupgrade_account_uv）
