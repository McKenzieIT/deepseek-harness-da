kind: metric
name: game.monthlyCard.autoReward__game_monthlyCard_autoReward_account_uv
description: 触发月卡自动发奖的去重账号数
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT account_id)
  metadata:
    aggregation: count_distinct
    field: account_id
    source: game.monthlyCard.autoReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.monthlyCard.autoReward
    description: 机械提取自 game.monthlyCard.autoReward 的 metrics 块（key=game_monthlyCard_autoReward_account_uv）
