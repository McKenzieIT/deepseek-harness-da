kind: metric
name: game.task.act__game_task_act_main_task_uv
description: 涉及的主任务id数(去重 mainTaskId；约26%非空，仅属于主任务的子任务记录计入)
domains:
  - 角色成长
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.mainTaskId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.mainTaskId')
    source: game.task.act
    time_grain: ""
relations:
  - type: derived_from
    target: game.task.act
    description: 机械提取自 game.task.act 的 metrics 块（key=game_task_act_main_task_uv）
