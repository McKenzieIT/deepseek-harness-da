kind: metric
name: game.anniversary.dungeonReset__dungeonreset_gold_per_role
description: 触发角色的平均充值钻石余额(总充值钻石余额快照/去重角色数,分母用 NULLIF 防除零)
domains:
  - 付费经济
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.coinList.gold') AS BIGINT)) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.coinList.gold') AS BIGINT)) / NULLIF(COUNT(DISTINCT role_id), 0
    source: game.anniversary.dungeonReset
    time_grain: ""
relations:
  - type: derived_from
    target: game.anniversary.dungeonReset
    description: 机械提取自 game.anniversary.dungeonReset 的 metrics 块（key=dungeonreset_gold_per_role）
