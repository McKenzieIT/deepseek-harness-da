kind: metric
name: game.war.getMissionReward__game_war_get_mission_reward_avg_role_force
description: 领取奖励角色的平均战力（角色战力字段求和后除以事件行数）
domains:
  - 战斗关卡
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.roleForce') AS BIGINT)) / NULLIF(COUNT(*), 0)
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.roleForce') AS BIGINT)) / NULLIF(COUNT(*), 0
    source: game.war.getMissionReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.war.getMissionReward
    description: 机械提取自 game.war.getMissionReward 的 metrics 块（key=game_war_get_mission_reward_avg_role_force）
