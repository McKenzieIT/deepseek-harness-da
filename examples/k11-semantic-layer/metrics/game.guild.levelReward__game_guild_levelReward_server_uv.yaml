kind: metric
name: game.guild.levelReward__game_guild_levelReward_server_uv
description: 有领取行为发生的去重游戏服数
domains:
  - 社交公会
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: game.guild.levelReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.guild.levelReward
    description: 机械提取自 game.guild.levelReward 的 metrics 块（key=game_guild_levelReward_server_uv）
