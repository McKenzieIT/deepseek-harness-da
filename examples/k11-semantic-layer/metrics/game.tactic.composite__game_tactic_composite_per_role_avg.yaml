kind: metric
name: game.tactic.composite__game_tactic_composite_per_role_avg
description: 人均合成次数（总次数/去重角色数）
domains:
  - 角色成长
computation:
  sql: COUNT(*) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: count
    field: "*) / NULLIF(COUNT(DISTINCT role_id), 0"
    source: game.tactic.composite
    time_grain: ""
relations:
  - type: derived_from
    target: game.tactic.composite
    description: 机械提取自 game.tactic.composite 的 metrics 块（key=game_tactic_composite_per_role_avg）
