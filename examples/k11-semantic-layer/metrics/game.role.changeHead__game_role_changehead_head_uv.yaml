kind: metric
name: game.role.changeHead__game_role_changehead_head_uv
description: 命中的去重头像/头像框id数量
domains:
  - 用户生命周期
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params,'$.headId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params,'$.headId')
    source: game.role.changeHead
    time_grain: ""
relations:
  - type: derived_from
    target: game.role.changeHead
    description: 机械提取自 game.role.changeHead 的 metrics 块（key=game_role_changehead_head_uv）
