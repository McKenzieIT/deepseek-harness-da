kind: metric
name: dws_10000251_play_dungeon_df__pass_rate
description: 通关率（通关记录数 / 参与角色去重数）
domains:
  - 战斗关卡
computation:
  sql: SUM(CASE WHEN pass_status = 1 THEN 1 ELSE 0 END) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: sum
    field: CASE WHEN pass_status = 1 THEN 1 ELSE 0 END) / NULLIF(COUNT(DISTINCT role_id), 0
    source: dws_10000251_play_dungeon_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_play_dungeon_df
    description: 机械提取自 dws_10000251_play_dungeon_df 的 metrics 块（key=pass_rate）
