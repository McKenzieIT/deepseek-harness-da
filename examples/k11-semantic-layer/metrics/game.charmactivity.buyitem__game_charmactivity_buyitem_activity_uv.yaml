kind: metric
name: game.charmactivity.buyitem__game_charmactivity_buyitem_activity_uv
description: 触发购买的活动数量（activityId 去重）
domains:
  - 探索收集
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.activityId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.activityId')
    source: game.charmactivity.buyitem
    time_grain: ""
relations:
  - type: derived_from
    target: game.charmactivity.buyitem
    description: 机械提取自 game.charmactivity.buyitem 的 metrics 块（key=game_charmactivity_buyitem_activity_uv）
