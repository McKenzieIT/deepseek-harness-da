kind: metric
name: game.guild.kickout__guild_kickout_account_uv
description: 执行踢出的独立账号数(去重 account_id)
domains:
  - 社交公会
computation:
  sql: COUNT(DISTINCT account_id)
  metadata:
    aggregation: count_distinct
    field: account_id
    source: game.guild.kickout
    time_grain: ""
relations:
  - type: derived_from
    target: game.guild.kickout
    description: 机械提取自 game.guild.kickout 的 metrics 块（key=guild_kickout_account_uv）
