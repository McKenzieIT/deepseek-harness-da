kind: metric
name: dws_10000251_dev_collection_df__os_count
description: 平台数（去重 os_id 数）
domains:
  - 系统监控
computation:
  sql: COUNT(DISTINCT os_id)
  metadata:
    aggregation: count_distinct
    field: os_id
    source: dws_10000251_dev_collection_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_dev_collection_df
    description: 机械提取自 dws_10000251_dev_collection_df 的 metrics 块（key=os_count）
