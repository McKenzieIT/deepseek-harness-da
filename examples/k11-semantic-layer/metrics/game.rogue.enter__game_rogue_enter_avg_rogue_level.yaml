kind: metric
name: game.rogue.enter__game_rogue_enter_avg_rogue_level
description: 进入古战场的平均难度等级
domains:
  - 战斗关卡
computation:
  sql: AVG(CAST(GET_JSON_OBJECT(params, '$.rogueLevel') AS BIGINT))
  metadata:
    aggregation: avg
    field: CAST(GET_JSON_OBJECT(params, '$.rogueLevel') AS BIGINT)
    source: game.rogue.enter
    time_grain: ""
relations:
  - type: derived_from
    target: game.rogue.enter
    description: 机械提取自 game.rogue.enter 的 metrics 块（key=game_rogue_enter_avg_rogue_level）
