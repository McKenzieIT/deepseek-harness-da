kind: metric
name: dws_10000251_univ_dev_tag_df__paying_device_ratio
description: 付费设备占比(0-1 比率,等价于 pay_conversion_rate 的另一种写法,行级口径)
domains:
  - 用户生命周期
  - 付费经济
  - 角色成长
computation:
  sql: SUM(CASE WHEN pay_amt_std > 0 THEN 1 ELSE 0 END) * 1.0 / NULLIF(COUNT(*), 0)
  metadata:
    aggregation: sum
    field: CASE WHEN pay_amt_std > 0 THEN 1 ELSE 0 END) * 1.0 / NULLIF(COUNT(*), 0
    source: dws_10000251_univ_dev_tag_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_univ_dev_tag_df
    description: 机械提取自 dws_10000251_univ_dev_tag_df 的 metrics 块（key=paying_device_ratio）
