kind: metric
name: game.campaign.dailyReward__game_campaign_dailyReward_role_uv
description: 当日领取历战每日奖励的去重角色数（主体粒度 role_id）
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.campaign.dailyReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.campaign.dailyReward
    description: 机械提取自 game.campaign.dailyReward 的 metrics 块（key=game_campaign_dailyReward_role_uv）
