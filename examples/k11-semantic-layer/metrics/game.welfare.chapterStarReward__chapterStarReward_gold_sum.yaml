kind: metric
name: game.welfare.chapterStarReward__chapterStarReward_gold_sum
description: 主线宝箱奖励发放的充值钻石（coinList.gold）总量。
domains:
  - 自定义
  - 用户生命周期
  - 资源产销
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.coinList.gold') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.coinList.gold') AS BIGINT)
    source: game.welfare.chapterStarReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.welfare.chapterStarReward
    description: 机械提取自 game.welfare.chapterStarReward 的 metrics 块（key=chapterStarReward_gold_sum）
