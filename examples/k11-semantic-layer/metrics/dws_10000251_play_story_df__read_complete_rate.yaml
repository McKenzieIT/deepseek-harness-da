kind: metric
name: dws_10000251_play_story_df__read_complete_rate
description: 完整阅读率（完整阅读条目数 / 总记录数）
domains:
  - 探索收集
  - 角色成长
computation:
  sql: SUM(CASE WHEN read_status=1 THEN 1 ELSE 0 END) / NULLIF(COUNT(*), 0)
  metadata:
    aggregation: sum
    field: CASE WHEN read_status=1 THEN 1 ELSE 0 END) / NULLIF(COUNT(*), 0
    source: dws_10000251_play_story_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_play_story_df
    description: 机械提取自 dws_10000251_play_story_df 的 metrics 块（key=read_complete_rate）
