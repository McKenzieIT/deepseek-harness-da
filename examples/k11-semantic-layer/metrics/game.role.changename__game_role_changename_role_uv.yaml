kind: metric
name: game.role.changename__game_role_changename_role_uv
description: 发起改名的去重角色数（主体粒度，默认统计口径）
domains:
  - 用户生命周期
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.role.changename
    time_grain: ""
relations:
  - type: derived_from
    target: game.role.changename
    description: 机械提取自 game.role.changename 的 metrics 块（key=game_role_changename_role_uv）
