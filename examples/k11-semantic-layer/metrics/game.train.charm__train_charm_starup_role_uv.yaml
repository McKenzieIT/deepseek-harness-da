kind: metric
name: game.train.charm__train_charm_starup_role_uv
description: 进行灵器升星的角色数
domains:
  - 角色成长
computation:
  sql: COUNT(DISTINCT CASE WHEN GET_JSON_OBJECT(params,'$.action') = '1' THEN role_id END)
  metadata:
    aggregation: count_distinct
    field: CASE WHEN GET_JSON_OBJECT(params,'$.action') = '1' THEN role_id END
    source: game.train.charm
    time_grain: ""
relations:
  - type: derived_from
    target: game.train.charm
    description: 机械提取自 game.train.charm 的 metrics 块（key=train_charm_starup_role_uv）
