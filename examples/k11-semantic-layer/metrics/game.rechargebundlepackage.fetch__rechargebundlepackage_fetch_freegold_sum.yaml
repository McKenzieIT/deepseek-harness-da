kind: metric
name: game.rechargebundlepackage.fetch__rechargebundlepackage_fetch_freegold_sum
description: 136礼包产出的免费钻石总量(coinList.freegold 求和)
domains:
  - 付费经济
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.coinList.freegold') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.coinList.freegold') AS BIGINT)
    source: game.rechargebundlepackage.fetch
    time_grain: ""
relations:
  - type: derived_from
    target: game.rechargebundlepackage.fetch
    description: 机械提取自 game.rechargebundlepackage.fetch 的 metrics 块（key=rechargebundlepackage_fetch_freegold_sum）
