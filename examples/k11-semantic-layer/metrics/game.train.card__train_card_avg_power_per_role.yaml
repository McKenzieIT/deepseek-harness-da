kind: metric
name: game.train.card__train_card_avg_power_per_role
description: 人均武将战力累加值（power_sum/role_uv，分母 NULLIF 防零）
domains:
  - 角色成长
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.power') AS BIGINT)) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.power') AS BIGINT)) / NULLIF(COUNT(DISTINCT role_id), 0
    source: game.train.card
    time_grain: ""
relations:
  - type: derived_from
    target: game.train.card
    description: 机械提取自 game.train.card 的 metrics 块（key=train_card_avg_power_per_role）
