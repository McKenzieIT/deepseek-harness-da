kind: metric
name: game.knight.setSkin__knight_set_skin_avg_per_role
description: 单角色平均设置皮肤次数
domains:
  - 角色成长
computation:
  sql: COUNT(*) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: count
    field: "*) / NULLIF(COUNT(DISTINCT role_id), 0"
    source: game.knight.setSkin
    time_grain: ""
relations:
  - type: derived_from
    target: game.knight.setSkin
    description: 机械提取自 game.knight.setSkin 的 metrics 块（key=knight_set_skin_avg_per_role）
