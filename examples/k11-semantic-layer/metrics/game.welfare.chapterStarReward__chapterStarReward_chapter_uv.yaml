kind: metric
name: game.welfare.chapterStarReward__chapterStarReward_chapter_uv
description: 被领取宝箱奖励的去重主线章节数（基于 params.chapterId）。
domains:
  - 自定义
  - 用户生命周期
  - 资源产销
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.chapterId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.chapterId')
    source: game.welfare.chapterStarReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.welfare.chapterStarReward
    description: 机械提取自 game.welfare.chapterStarReward 的 metrics 块（key=chapterStarReward_chapter_uv）
