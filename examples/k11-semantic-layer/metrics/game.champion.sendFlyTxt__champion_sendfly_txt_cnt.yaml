kind: metric
name: game.champion.sendFlyTxt__champion_sendfly_txt_cnt
description: 争霸赛弹幕发送总次数（含成功/失败）
domains:
  - 战斗关卡
computation:
  sql: COUNT(*)
  metadata:
    aggregation: count
    field: "*"
    source: game.champion.sendFlyTxt
    time_grain: ""
relations:
  - type: derived_from
    target: game.champion.sendFlyTxt
    description: 机械提取自 game.champion.sendFlyTxt 的 metrics 块（key=champion_sendfly_txt_cnt）
