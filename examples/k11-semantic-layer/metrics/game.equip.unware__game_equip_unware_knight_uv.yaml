kind: metric
name: game.equip.unware__game_equip_unware_knight_uv
description: 脱下装备涉及的去重武将数
domains:
  - 装备道具
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.knightId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.knightId')
    source: game.equip.unware
    time_grain: ""
relations:
  - type: derived_from
    target: game.equip.unware
    description: 机械提取自 game.equip.unware 的 metrics 块（key=game_equip_unware_knight_uv）
