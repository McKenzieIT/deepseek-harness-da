kind: metric
name: game.koiDraw.changeFloor__koi_draw_change_floor_draw_uv
description: 当日涉及切换层的去重锦鲤彩运活动 drawId 数
domains:
  - 探索收集
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.drawId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.drawId')
    source: game.koiDraw.changeFloor
    time_grain: ""
relations:
  - type: derived_from
    target: game.koiDraw.changeFloor
    description: 机械提取自 game.koiDraw.changeFloor 的 metrics 块（key=koi_draw_change_floor_draw_uv）
