kind: metric
name: game.halfprice.fetchactivate__halfprice_fetchactivate_gold_sum
description: 充值钻石快照合计（实测 ds=20260729=0，领取激活奖励不消耗充值钻石）
domains:
  - 付费经济
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.coinList.gold') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.coinList.gold') AS BIGINT)
    source: game.halfprice.fetchactivate
    time_grain: ""
relations:
  - type: derived_from
    target: game.halfprice.fetchactivate
    description: 机械提取自 game.halfprice.fetchactivate 的 metrics 块（key=halfprice_fetchactivate_gold_sum）
