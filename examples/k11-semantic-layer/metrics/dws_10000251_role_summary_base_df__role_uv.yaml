kind: metric
name: dws_10000251_role_summary_base_df__role_uv
description: 角色数(在库角色全量快照口径,含当日未登录的离线角色)
domains:
  - 用户生命周期
  - 付费经济
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: dws_10000251_role_summary_base_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_role_summary_base_df
    description: 机械提取自 dws_10000251_role_summary_base_df 的 metrics 块（key=role_uv）
