kind: metric
name: dws_10000251_algo_role_act_pay_df__server_cnt
description: 当日覆盖服务器数（DISTINCT server_id）
domains:
  - 付费经济
  - 用户生命周期
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: dws_10000251_algo_role_act_pay_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_algo_role_act_pay_df
    description: 机械提取自 dws_10000251_algo_role_act_pay_df 的 metrics 块（key=server_cnt）
