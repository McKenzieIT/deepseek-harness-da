kind: metric
name: game.equip.onekeyWear__equip_onekeyWear_server_uv
description: 发生一键穿戴的区服数(去重 server_id，反映穿戴功能覆盖面)
domains:
  - 装备道具
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: game.equip.onekeyWear
    time_grain: ""
relations:
  - type: derived_from
    target: game.equip.onekeyWear
    description: 机械提取自 game.equip.onekeyWear 的 metrics 块（key=equip_onekeyWear_server_uv）
