kind: metric
name: game.role.changeCard__changeCard_card_uv
description: 被选用的去重名帖id数
domains:
  - 用户生命周期
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params,'$.card'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params,'$.card')
    source: game.role.changeCard
    time_grain: ""
relations:
  - type: derived_from
    target: game.role.changeCard
    description: 机械提取自 game.role.changeCard 的 metrics 块（key=changeCard_card_uv）
