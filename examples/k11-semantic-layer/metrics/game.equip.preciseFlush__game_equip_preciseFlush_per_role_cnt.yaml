kind: metric
name: game.equip.preciseFlush__game_equip_preciseFlush_per_role_cnt
description: 单角色平均精洗操作次数
domains:
  - 装备道具
computation:
  sql: COUNT(*) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: count
    field: "*) / NULLIF(COUNT(DISTINCT role_id), 0"
    source: game.equip.preciseFlush
    time_grain: ""
relations:
  - type: derived_from
    target: game.equip.preciseFlush
    description: 机械提取自 game.equip.preciseFlush 的 metrics 块（key=game_equip_preciseFlush_per_role_cnt）
