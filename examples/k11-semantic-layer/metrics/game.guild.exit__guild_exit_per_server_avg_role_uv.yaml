kind: metric
name: game.guild.exit__guild_exit_per_server_avg_role_uv
description: 单服平均退出公会的角色数
domains:
  - 社交公会
computation:
  sql: CAST(COUNT(DISTINCT role_id) AS DOUBLE) / NULLIF(COUNT(DISTINCT server_id), 0)
  metadata:
    aggregation: ""
    field: ""
    source: game.guild.exit
    time_grain: ""
relations:
  - type: derived_from
    target: game.guild.exit
    description: 机械提取自 game.guild.exit 的 metrics 块（key=guild_exit_per_server_avg_role_uv）
