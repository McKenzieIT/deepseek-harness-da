kind: metric
name: game.function.previewSpecialReward__previewSpecialReward_role_uv
description: 触发特殊奖励预览领取的去重角色数
domains:
  - 系统监控
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.function.previewSpecialReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.function.previewSpecialReward
    description: 机械提取自 game.function.previewSpecialReward 的 metrics 块（key=previewSpecialReward_role_uv）
