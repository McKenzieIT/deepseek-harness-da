kind: metric
name: dws_10000251_risk_dev_identification_df__account_uv
description: 账号主体(user_type=2)风险标识UV
domains:
  - 系统监控
  - 自定义
computation:
  sql: COUNT(DISTINCT CASE WHEN user_type=2 THEN user_id END)
  metadata:
    aggregation: count_distinct
    field: CASE WHEN user_type=2 THEN user_id END
    source: dws_10000251_risk_dev_identification_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_risk_dev_identification_df
    description: 机械提取自 dws_10000251_risk_dev_identification_df 的 metrics 块（key=account_uv）
