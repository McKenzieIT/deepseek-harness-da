kind: metric
name: game.gacha.buyupitem__gacha_buyupitem_count_sum
description: 购买up道具总数量(按params.count求和)
domains:
  - 探索收集
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params,'$.count') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params,'$.count') AS BIGINT)
    source: game.gacha.buyupitem
    time_grain: ""
relations:
  - type: derived_from
    target: game.gacha.buyupitem
    description: 机械提取自 game.gacha.buyupitem 的 metrics 块（key=gacha_buyupitem_count_sum）
