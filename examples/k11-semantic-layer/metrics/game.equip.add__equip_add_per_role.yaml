kind: metric
name: game.equip.add__equip_add_per_role
description: 人均装备获取次数（总次数/去重角色数，分母为0时返回NULL）
domains:
  - 装备道具
computation:
  sql: CAST(COUNT(*) AS DOUBLE) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: ""
    field: ""
    source: game.equip.add
    time_grain: ""
relations:
  - type: derived_from
    target: game.equip.add
    description: 机械提取自 game.equip.add 的 metrics 块（key=equip_add_per_role）
