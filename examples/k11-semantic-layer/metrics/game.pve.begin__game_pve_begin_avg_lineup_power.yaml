kind: metric
name: game.pve.begin__game_pve_begin_avg_lineup_power
description: 进入副本时的平均阵容战力
domains:
  - 战斗关卡
computation:
  sql: AVG(CAST(GET_JSON_OBJECT(params, '$.lineupPower') AS BIGINT))
  metadata:
    aggregation: avg
    field: CAST(GET_JSON_OBJECT(params, '$.lineupPower') AS BIGINT)
    source: game.pve.begin
    time_grain: ""
relations:
  - type: derived_from
    target: game.pve.begin
    description: 机械提取自 game.pve.begin 的 metrics 块（key=game_pve_begin_avg_lineup_power）
