kind: metric
name: game.fish.applyAllow__game_fish_applyAllow_role_uv
description: 发起同意申请操作的去重角色数（主体粒度）
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.fish.applyAllow
    time_grain: ""
relations:
  - type: derived_from
    target: game.fish.applyAllow
    description: 机械提取自 game.fish.applyAllow 的 metrics 块（key=game_fish_applyAllow_role_uv）
