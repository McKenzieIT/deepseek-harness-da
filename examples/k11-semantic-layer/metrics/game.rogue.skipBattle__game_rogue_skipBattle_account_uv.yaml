kind: metric
name: game.rogue.skipBattle__game_rogue_skipBattle_account_uv
description: 跳过战斗的账号数
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT account_id)
  metadata:
    aggregation: count_distinct
    field: account_id
    source: game.rogue.skipBattle
    time_grain: ""
relations:
  - type: derived_from
    target: game.rogue.skipBattle
    description: 机械提取自 game.rogue.skipBattle 的 metrics 块（key=game_rogue_skipBattle_account_uv）
