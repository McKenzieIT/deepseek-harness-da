kind: metric
name: dws_10000251_univ_dev_tag_df__ltv_f1d
description: 新增首日 LTV(单位:元/设备)
domains:
  - 用户生命周期
  - 付费经济
  - 角色成长
computation:
  sql: SUM(pay_amt_f1d) * 1.0 / NULLIF(COUNT(DISTINCT user_id), 0)
  metadata:
    aggregation: sum
    field: pay_amt_f1d) * 1.0 / NULLIF(COUNT(DISTINCT user_id), 0
    source: dws_10000251_univ_dev_tag_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_univ_dev_tag_df
    description: 机械提取自 dws_10000251_univ_dev_tag_df 的 metrics 块（key=ltv_f1d）
