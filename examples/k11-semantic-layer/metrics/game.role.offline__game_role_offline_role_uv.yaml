kind: metric
name: game.role.offline__game_role_offline_role_uv
description: 下线去重角色数
domains:
  - 用户生命周期
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.role.offline
    time_grain: ""
relations:
  - type: derived_from
    target: game.role.offline
    description: 机械提取自 game.role.offline 的 metrics 块（key=game_role_offline_role_uv）
