kind: metric
name: game.champion.guess__game_champion_guess_atk_def_ratio
description: 攻方/防方事件条数比(防方为0时返回NULL)
domains:
  - 战斗关卡
computation:
  sql: SUM(CASE WHEN GET_JSON_OBJECT(params, '$.side') = '0' THEN 1 ELSE 0 END) / NULLIF(SUM(CASE WHEN GET_JSON_OBJECT(params, '$.side') = '1' THEN 1 ELSE 0 END), 0)
  metadata:
    aggregation: sum
    field: CASE WHEN GET_JSON_OBJECT(params, '$.side') = '0' THEN 1 ELSE 0 END) / NULLIF(SUM(CASE WHEN GET_JSON_OBJECT(params, '$.side') = '1' THEN 1 ELSE 0 END), 0
    source: game.champion.guess
    time_grain: ""
relations:
  - type: derived_from
    target: game.champion.guess
    description: 机械提取自 game.champion.guess 的 metrics 块（key=game_champion_guess_atk_def_ratio）
