kind: metric
name: game.scriptActivity.getPreheatGiftFreeReward__preheatgiftfreereward_cnt_per_server
description: 平均每服领奖事件条数
domains:
  - 自定义
computation:
  sql: COUNT(*) * 1.0 / NULLIF(COUNT(DISTINCT server_id), 0)
  metadata:
    aggregation: count
    field: "*) * 1.0 / NULLIF(COUNT(DISTINCT server_id), 0"
    source: game.scriptActivity.getPreheatGiftFreeReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.scriptActivity.getPreheatGiftFreeReward
    description: 机械提取自 game.scriptActivity.getPreheatGiftFreeReward 的 metrics 块（key=preheatgiftfreereward_cnt_per_server）
