kind: metric
name: game.guild.share__game_guild_share_server_uv
description: 触发公会分享的去重游戏服数
domains:
  - 社交公会
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: game.guild.share
    time_grain: ""
relations:
  - type: derived_from
    target: game.guild.share
    description: 机械提取自 game.guild.share 的 metrics 块（key=game_guild_share_server_uv）
