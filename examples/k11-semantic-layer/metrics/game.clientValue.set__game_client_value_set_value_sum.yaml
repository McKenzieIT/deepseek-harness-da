kind: metric
name: game.clientValue.set__game_client_value_set_value_sum
description: 设置的新 value 累计和（key=2 进度计数器场景下逐次递增会重复累计，更适合用 value_max）
domains:
  - 系统监控
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.value') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.value') AS BIGINT)
    source: game.clientValue.set
    time_grain: ""
relations:
  - type: derived_from
    target: game.clientValue.set
    description: 机械提取自 game.clientValue.set 的 metrics 块（key=game_client_value_set_value_sum）
