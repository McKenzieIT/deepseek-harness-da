kind: metric
name: game.battlepass.missionReward__battlepass_mission_reward_mission_uv
description: 当日被领取奖励的战令任务种类数(去重 missionId)
domains:
  - 角色成长
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.missionId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.missionId')
    source: game.battlepass.missionReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.battlepass.missionReward
    description: 机械提取自 game.battlepass.missionReward 的 metrics 块（key=battlepass_mission_reward_mission_uv）
