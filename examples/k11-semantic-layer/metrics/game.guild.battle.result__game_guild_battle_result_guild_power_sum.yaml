kind: metric
name: game.guild.battle.result__game_guild_battle_result_guild_power_sum
description: 本方公会战力合计（单日所有结算场次累加）
domains:
  - 社交公会
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params,'$.guildPower') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params,'$.guildPower') AS BIGINT)
    source: game.guild.battle.result
    time_grain: ""
relations:
  - type: derived_from
    target: game.guild.battle.result
    description: 机械提取自 game.guild.battle.result 的 metrics 块（key=game_guild_battle_result_guild_power_sum）
