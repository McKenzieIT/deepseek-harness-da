kind: metric
name: game.gacha.switchSpecial__game_gacha_switchSpecial_cnt
description: 切换新手集市卡池日志条数（每次切换一条）
domains:
  - 探索收集
computation:
  sql: COUNT(*)
  metadata:
    aggregation: count
    field: "*"
    source: game.gacha.switchSpecial
    time_grain: ""
relations:
  - type: derived_from
    target: game.gacha.switchSpecial
    description: 机械提取自 game.gacha.switchSpecial 的 metrics 块（key=game_gacha_switchSpecial_cnt）
