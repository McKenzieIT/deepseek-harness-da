kind: metric
name: game.rogue.start__game_rogue_start_avg_lineup_level
description: 单次挑战平均阵容等级
domains:
  - 战斗关卡
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params,'$.lineupLevel') AS BIGINT)) / NULLIF(COUNT(*),0)
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params,'$.lineupLevel') AS BIGINT)) / NULLIF(COUNT(*),0
    source: game.rogue.start
    time_grain: ""
relations:
  - type: derived_from
    target: game.rogue.start
    description: 机械提取自 game.rogue.start 的 metrics 块（key=game_rogue_start_avg_lineup_level）
