kind: metric
name: game.rogue.pointLvlup__game_rogue_pointLvlup_server_uv
description: 有军功升级行为发生的去重游戏服数
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: game.rogue.pointLvlup
    time_grain: ""
relations:
  - type: derived_from
    target: game.rogue.pointLvlup
    description: 机械提取自 game.rogue.pointLvlup 的 metrics 块（key=game_rogue_pointLvlup_server_uv）
