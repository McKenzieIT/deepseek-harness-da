kind: metric
name: dws_10000251_pve_progress_df__pass_uv
description: 当日通关至少一次的角色数
domains:
  - 战斗关卡
  - 角色成长
computation:
  sql: COUNT(DISTINCT CASE WHEN win_times > 0 THEN role_id END)
  metadata:
    aggregation: count_distinct
    field: CASE WHEN win_times > 0 THEN role_id END
    source: dws_10000251_pve_progress_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_pve_progress_df
    description: 机械提取自 dws_10000251_pve_progress_df 的 metrics 块（key=pass_uv）
