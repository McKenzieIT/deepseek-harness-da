kind: metric
name: game.script.lighttalent__game_script_lighttalent_server_uv
description: 触发点亮天赋的去重游戏服数
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: game.script.lighttalent
    time_grain: ""
relations:
  - type: derived_from
    target: game.script.lighttalent
    description: 机械提取自 game.script.lighttalent 的 metrics 块（key=game_script_lighttalent_server_uv）
