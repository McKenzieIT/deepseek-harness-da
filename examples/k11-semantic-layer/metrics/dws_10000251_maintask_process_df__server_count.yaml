kind: metric
name: dws_10000251_maintask_process_df__server_count
description: 当日活跃区服数：快照内去重区服数
domains:
  - 战斗关卡
  - 角色成长
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: dws_10000251_maintask_process_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_maintask_process_df
    description: 机械提取自 dws_10000251_maintask_process_df 的 metrics 块（key=server_count）
