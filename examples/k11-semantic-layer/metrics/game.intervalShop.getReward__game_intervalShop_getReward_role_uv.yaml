kind: metric
name: game.intervalShop.getReward__game_intervalShop_getReward_role_uv
description: 领取奖励的去重角色数
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.intervalShop.getReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.intervalShop.getReward
    description: 机械提取自 game.intervalShop.getReward 的 metrics 块（key=game_intervalShop_getReward_role_uv）
