kind: metric
name: dws_10000251_algo_role_churn_pred__predicted_churn_rate
description: 预测流失率=预测流失角色数/样本角色总数
domains:
  - 用户生命周期
  - 系统监控
computation:
  sql: SUM(CAST(tag AS BIGINT)) / NULLIF(COUNT(*), 0)
  metadata:
    aggregation: sum
    field: CAST(tag AS BIGINT)) / NULLIF(COUNT(*), 0
    source: dws_10000251_algo_role_churn_pred
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_algo_role_churn_pred
    description: 机械提取自 dws_10000251_algo_role_churn_pred 的 metrics 块（key=predicted_churn_rate）
