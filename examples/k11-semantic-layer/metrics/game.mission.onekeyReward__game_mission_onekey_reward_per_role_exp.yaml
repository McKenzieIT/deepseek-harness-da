kind: metric
name: game.mission.onekeyReward__game_mission_onekey_reward_per_role_exp
description: 人均获得积分（newExp 总和 / 去重角色数）
domains:
  - 角色成长
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.newExp') AS BIGINT)) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.newExp') AS BIGINT)) / NULLIF(COUNT(DISTINCT role_id), 0
    source: game.mission.onekeyReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.mission.onekeyReward
    description: 机械提取自 game.mission.onekeyReward 的 metrics 块（key=game_mission_onekey_reward_per_role_exp）
