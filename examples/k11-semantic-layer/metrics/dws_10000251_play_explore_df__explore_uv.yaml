kind: metric
name: dws_10000251_play_explore_df__explore_uv
description: 探索参与角色UV（去重 role_id，按城池明细汇总前需先取角色级 DISTINCT）
domains:
  - 探索收集
  - 角色成长
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: dws_10000251_play_explore_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_play_explore_df
    description: 机械提取自 dws_10000251_play_explore_df 的 metrics 块（key=explore_uv）
