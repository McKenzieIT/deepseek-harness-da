kind: metric
name: dws_10000251_finance_pay_order_di__arpu_role
description: 角色级 ARPU=成功付费金额/付费角色数,单位元,用 NULLIF 防除零
domains:
  - 付费经济
  - 用户生命周期
computation:
  sql: SUM(CASE WHEN status=1 THEN pay_amt ELSE 0 END)*1.0/NULLIF(COUNT(DISTINCT CASE WHEN status=1 THEN role_id END),0)
  metadata:
    aggregation: sum
    field: CASE WHEN status=1 THEN pay_amt ELSE 0 END)*1.0/NULLIF(COUNT(DISTINCT CASE WHEN status=1 THEN role_id END),0
    source: dws_10000251_finance_pay_order_di
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_finance_pay_order_di
    description: 机械提取自 dws_10000251_finance_pay_order_di 的 metrics 块（key=arpu_role）
