kind: metric
name: game.fish.lock__game_fish_lock_new_lock_num_sum
description: 钓鱼锁定后数量(newLockNum)累计总和
domains:
  - 自定义
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params,'$.newLockNum') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params,'$.newLockNum') AS BIGINT)
    source: game.fish.lock
    time_grain: ""
relations:
  - type: derived_from
    target: game.fish.lock
    description: 机械提取自 game.fish.lock 的 metrics 块（key=game_fish_lock_new_lock_num_sum）
