kind: metric
name: game.gacha.missionReward__gacha_mission_reward_cnt
description: 抽卡任务奖励事件总触发次数
domains:
  - 探索收集
computation:
  sql: COUNT(*)
  metadata:
    aggregation: count
    field: "*"
    source: game.gacha.missionReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.gacha.missionReward
    description: 机械提取自 game.gacha.missionReward 的 metrics 块（key=gacha_mission_reward_cnt）
