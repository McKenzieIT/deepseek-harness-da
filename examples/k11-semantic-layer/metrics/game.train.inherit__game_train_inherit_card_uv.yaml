kind: metric
name: game.train.inherit__game_train_inherit_card_uv
description: 继承产出的去重新武将数
domains:
  - 角色成长
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.newCardId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.newCardId')
    source: game.train.inherit
    time_grain: ""
relations:
  - type: derived_from
    target: game.train.inherit
    description: 机械提取自 game.train.inherit 的 metrics 块（key=game_train_inherit_card_uv）
