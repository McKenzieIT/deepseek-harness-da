kind: metric
name: dws_10000251_role_account_inner__role_mapping_fill_rate
description: 内部账号已关联角色的占比（实测全表为 0，role_id 列空缺）
domains:
  - 用户生命周期
  - 系统监控
computation:
  sql: COUNT(role_id) / NULLIF(COUNT(DISTINCT account_id), 0)
  metadata:
    aggregation: count
    field: role_id) / NULLIF(COUNT(DISTINCT account_id), 0
    source: dws_10000251_role_account_inner
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_role_account_inner
    description: 机械提取自 dws_10000251_role_account_inner 的 metrics 块（key=role_mapping_fill_rate）
