kind: metric
name: dws_10000251_biz_role_tag_df__untagged_role_uv
description: 当日未归属任何大事件的角色数
domains:
  - 角色成长
  - 系统监控
computation:
  sql: COUNT(DISTINCT CASE WHEN biz_name IS NULL THEN user_id END)
  metadata:
    aggregation: count_distinct
    field: CASE WHEN biz_name IS NULL THEN user_id END
    source: dws_10000251_biz_role_tag_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_biz_role_tag_df
    description: 机械提取自 dws_10000251_biz_role_tag_df 的 metrics 块（key=untagged_role_uv）
