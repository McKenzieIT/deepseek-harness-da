kind: metric
name: game.guild.submitMission__game_guild_submitMission_role_uv
description: 提交公会任务的去重角色数（分析主体 role_id）
domains:
  - 社交公会
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.guild.submitMission
    time_grain: ""
relations:
  - type: derived_from
    target: game.guild.submitMission
    description: 机械提取自 game.guild.submitMission 的 metrics 块（key=game_guild_submitMission_role_uv）
