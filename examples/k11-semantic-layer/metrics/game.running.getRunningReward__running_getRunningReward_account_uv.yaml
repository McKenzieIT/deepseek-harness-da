kind: metric
name: game.running.getRunningReward__running_getRunningReward_account_uv
description: 领取赛马奖励的去重账号数
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT account_id)
  metadata:
    aggregation: count_distinct
    field: account_id
    source: game.running.getRunningReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.running.getRunningReward
    description: 机械提取自 game.running.getRunningReward 的 metrics 块（key=running_getRunningReward_account_uv）
