kind: metric
name: game.wenji.shareReward__game_wenji_shareReward_reward_id_uv
description: 文姬分享奖励领取的不同奖励id去重数
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.id'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.id')
    source: game.wenji.shareReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.wenji.shareReward
    description: 机械提取自 game.wenji.shareReward 的 metrics 块（key=game_wenji_shareReward_reward_id_uv）
