kind: metric
name: game.springfestival.donate__springfestival_donate_count_per_role
description: 每个捐赠角色的平均捐赠数量
domains:
  - 自定义
computation:
  sql: CAST(SUM(CAST(GET_JSON_OBJECT(params, '$.count') AS BIGINT)) AS DOUBLE) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: ""
    field: ""
    source: game.springfestival.donate
    time_grain: ""
relations:
  - type: derived_from
    target: game.springfestival.donate
    description: 机械提取自 game.springfestival.donate 的 metrics 块（key=springfestival_donate_count_per_role）
