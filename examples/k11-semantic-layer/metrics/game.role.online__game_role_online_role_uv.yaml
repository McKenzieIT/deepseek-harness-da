kind: metric
name: game.role.online__game_role_online_role_uv
description: 当日登录游戏服的去重角色数（主体指标，role_id 粒度）
domains:
  - 用户生命周期
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.role.online
    time_grain: ""
relations:
  - type: derived_from
    target: game.role.online
    description: 机械提取自 game.role.online 的 metrics 块（key=game_role_online_role_uv）
