kind: metric
name: game.gmActivity.orientReward__game_gmActivity_orientReward_rewardcnt_sum
description: gm活动持续奖励累计已发放次数
domains:
  - 自定义
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params,'$.rewardCnt') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params,'$.rewardCnt') AS BIGINT)
    source: game.gmActivity.orientReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.gmActivity.orientReward
    description: 机械提取自 game.gmActivity.orientReward 的 metrics 块（key=game_gmActivity_orientReward_rewardcnt_sum）
