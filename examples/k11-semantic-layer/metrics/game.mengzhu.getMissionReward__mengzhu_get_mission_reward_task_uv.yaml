kind: metric
name: game.mengzhu.getMissionReward__mengzhu_get_mission_reward_task_uv
description: 领取的去重任务种类数(任务id来自 params.id)
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.id'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.id')
    source: game.mengzhu.getMissionReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.mengzhu.getMissionReward
    description: 机械提取自 game.mengzhu.getMissionReward 的 metrics 块（key=mengzhu_get_mission_reward_task_uv）
