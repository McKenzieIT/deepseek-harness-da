kind: metric
name: game.rogue.finishTask__rogue_finishTask_theme_uv
description: 被触发的不同 themeId 数量（主题覆盖广度）
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.themeId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.themeId')
    source: game.rogue.finishTask
    time_grain: ""
relations:
  - type: derived_from
    target: game.rogue.finishTask
    description: 机械提取自 game.rogue.finishTask 的 metrics 块（key=rogue_finishTask_theme_uv）
