kind: metric
name: dws_10000251_biz_dev_tag_df__tagged_device_ratio
description: 被标记设备占全量设备的比例(0-1)，衡量大事件召回覆盖率。
domains:
  - 用户生命周期
  - 系统监控
computation:
  sql: COUNT(DISTINCT CASE WHEN biz_name IS NOT NULL THEN user_id END) * 1.0 / NULLIF(COUNT(DISTINCT user_id), 0)
  metadata:
    aggregation: count_distinct
    field: CASE WHEN biz_name IS NOT NULL THEN user_id END) * 1.0 / NULLIF(COUNT(DISTINCT user_id), 0
    source: dws_10000251_biz_dev_tag_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_biz_dev_tag_df
    description: 机械提取自 dws_10000251_biz_dev_tag_df 的 metrics 块（key=tagged_device_ratio）
