kind: metric
name: game.resort.new__resort_new_per_role_req
description: 每个发起角色的平均代打请求次数（>1 表示同角色当日多次发起）
domains:
  - 自定义
computation:
  sql: CAST(COUNT(*) AS DOUBLE) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: ""
    field: ""
    source: game.resort.new
    time_grain: ""
relations:
  - type: derived_from
    target: game.resort.new
    description: 机械提取自 game.resort.new 的 metrics 块（key=resort_new_per_role_req）
