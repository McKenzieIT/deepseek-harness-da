kind: metric
name: game.intervalShop.getFreeReward__game_intervalShop_getFreeReward_cnt
description: 持续回报商店领取免费奖励事件总次数
domains:
  - 自定义
computation:
  sql: COUNT(*)
  metadata:
    aggregation: count
    field: "*"
    source: game.intervalShop.getFreeReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.intervalShop.getFreeReward
    description: 机械提取自 game.intervalShop.getFreeReward 的 metrics 块（key=game_intervalShop_getFreeReward_cnt）
