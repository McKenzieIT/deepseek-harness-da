kind: metric
name: game.gmActivity.shopExchange__game_gmActivity_shopExchange_server_uv
description: 发生GM活动商店兑换的去重游戏服数
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: game.gmActivity.shopExchange
    time_grain: ""
relations:
  - type: derived_from
    target: game.gmActivity.shopExchange
    description: 机械提取自 game.gmActivity.shopExchange 的 metrics 块（key=game_gmActivity_shopExchange_server_uv）
