kind: metric
name: game.rogue.end__game_rogue_end_account_uv
description: 参与古战场副本的账号数（去重）
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT account_id)
  metadata:
    aggregation: count_distinct
    field: account_id
    source: game.rogue.end
    time_grain: ""
relations:
  - type: derived_from
    target: game.rogue.end
    description: 机械提取自 game.rogue.end 的 metrics 块（key=game_rogue_end_account_uv）
