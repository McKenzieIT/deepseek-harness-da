kind: metric
name: dws_10000251_vip_role_tag_df__role_uv
description: 当日快照角色UV，去重统计参与快照的角色数
domains:
  - 付费经济
  - 角色成长
computation:
  sql: COUNT(DISTINCT user_id)
  metadata:
    aggregation: count_distinct
    field: user_id
    source: dws_10000251_vip_role_tag_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_vip_role_tag_df
    description: 机械提取自 dws_10000251_vip_role_tag_df 的 metrics 块（key=role_uv）
