kind: metric
name: dws_10000251_role_prop_df__role_uv
description: 当日持有资产快照的角色UV（按role_id去重）；_df全量快照，role_uv应等于当日行数。
domains:
  - 角色成长
  - 装备道具
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: dws_10000251_role_prop_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_role_prop_df
    description: 机械提取自 dws_10000251_role_prop_df 的 metrics 块（key=role_uv）
