kind: metric
name: dws_10000251_resource_item_df__role_uv
description: 资源产出消耗角色UV（去重角色数），衡量涉及资源流转的角色规模
domains:
  - 资源产销
  - 角色成长
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: dws_10000251_resource_item_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_resource_item_df
    description: 机械提取自 dws_10000251_resource_item_df 的 metrics 块（key=role_uv）
