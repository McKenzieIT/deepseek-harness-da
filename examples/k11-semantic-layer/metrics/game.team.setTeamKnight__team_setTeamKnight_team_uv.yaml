kind: metric
name: game.team.setTeamKnight__team_setTeamKnight_team_uv
description: 被设置的去重阵容id数（阵容配置多样性）
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.teamId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.teamId')
    source: game.team.setTeamKnight
    time_grain: ""
relations:
  - type: derived_from
    target: game.team.setTeamKnight
    description: 机械提取自 game.team.setTeamKnight 的 metrics 块（key=team_setTeamKnight_team_uv）
