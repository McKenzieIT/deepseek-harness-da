kind: metric
name: game.artifact.gacha__game_artifact_gacha_draw_sum
description: 兵符抽卡累计抽卡次数（params.count 求和）
domains:
  - 角色成长
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.count') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.count') AS BIGINT)
    source: game.artifact.gacha
    time_grain: ""
relations:
  - type: derived_from
    target: game.artifact.gacha
    description: 机械提取自 game.artifact.gacha 的 metrics 块（key=game_artifact_gacha_draw_sum）
