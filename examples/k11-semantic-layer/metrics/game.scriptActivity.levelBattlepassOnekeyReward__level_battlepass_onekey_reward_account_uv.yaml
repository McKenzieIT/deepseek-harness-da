kind: metric
name: game.scriptActivity.levelBattlepassOnekeyReward__level_battlepass_onekey_reward_account_uv
description: 触发一键领取的账号去重数
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT account_id)
  metadata:
    aggregation: count_distinct
    field: account_id
    source: game.scriptActivity.levelBattlepassOnekeyReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.scriptActivity.levelBattlepassOnekeyReward
    description: 机械提取自 game.scriptActivity.levelBattlepassOnekeyReward 的 metrics 块（key=level_battlepass_onekey_reward_account_uv）
