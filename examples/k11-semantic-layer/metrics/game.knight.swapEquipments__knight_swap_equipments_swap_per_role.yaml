kind: metric
name: game.knight.swapEquipments__knight_swap_equipments_swap_per_role
description: 人均装备交换次数(事件数/去重角色数，分母用 NULLIF 防除零)
domains:
  - 角色成长
computation:
  sql: COUNT(*) * 1.0 / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: count
    field: "*) * 1.0 / NULLIF(COUNT(DISTINCT role_id), 0"
    source: game.knight.swapEquipments
    time_grain: ""
relations:
  - type: derived_from
    target: game.knight.swapEquipments
    description: 机械提取自 game.knight.swapEquipments 的 metrics 块（key=knight_swap_equipments_swap_per_role）
