kind: metric
name: game.charmStone.synthesis__charm_stone_synthesis_template_uv
description: 使用的合成模板去重数
domains:
  - 探索收集
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.modelId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.modelId')
    source: game.charmStone.synthesis
    time_grain: ""
relations:
  - type: derived_from
    target: game.charmStone.synthesis
    description: 机械提取自 game.charmStone.synthesis 的 metrics 块（key=charm_stone_synthesis_template_uv）
