kind: metric
name: game.tactic.exchange__game_tactic_exchange_tactic_num_sum
description: 战法碎片兑换总数量（按 tacticNum 求和）
domains:
  - 角色成长
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.tacticNum') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.tacticNum') AS BIGINT)
    source: game.tactic.exchange
    time_grain: ""
relations:
  - type: derived_from
    target: game.tactic.exchange
    description: 机械提取自 game.tactic.exchange 的 metrics 块（key=game_tactic_exchange_tactic_num_sum）
