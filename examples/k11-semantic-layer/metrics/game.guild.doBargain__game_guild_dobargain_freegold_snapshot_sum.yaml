kind: metric
name: game.guild.doBargain__game_guild_dobargain_freegold_snapshot_sum
description: 砍价发生时持有免费钻石(freegold)合计，参与者经济画像快照（货币余额，非收入）
domains:
  - 社交公会
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.coinList.freegold') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.coinList.freegold') AS BIGINT)
    source: game.guild.doBargain
    time_grain: ""
relations:
  - type: derived_from
    target: game.guild.doBargain
    description: 机械提取自 game.guild.doBargain 的 metrics 块（key=game_guild_dobargain_freegold_snapshot_sum）
