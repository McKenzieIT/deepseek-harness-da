kind: metric
name: game.rogue.summon__game_rogue_summon_role_uv
description: 发起招募的去重角色数
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.rogue.summon
    time_grain: ""
relations:
  - type: derived_from
    target: game.rogue.summon
    description: 机械提取自 game.rogue.summon 的 metrics 块（key=game_rogue_summon_role_uv）
