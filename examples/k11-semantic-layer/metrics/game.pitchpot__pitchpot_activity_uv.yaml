kind: metric
name: game.pitchpot__pitchpot_activity_uv
description: 触发的去重投壶活动数（按 pitchpotId）
domains:
  - 探索收集
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.pitchpotId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.pitchpotId')
    source: game.pitchpot
    time_grain: ""
relations:
  - type: derived_from
    target: game.pitchpot
    description: 机械提取自 game.pitchpot 的 metrics 块（key=pitchpot_activity_uv）
