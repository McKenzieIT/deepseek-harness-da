kind: metric
name: game.role.snapshot__game_role_snapshot_role_uv
description: 快照去重角色数（在线角色规模）
domains:
  - 用户生命周期
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.role.snapshot
    time_grain: ""
relations:
  - type: derived_from
    target: game.role.snapshot
    description: 机械提取自 game.role.snapshot 的 metrics 块（key=game_role_snapshot_role_uv）
