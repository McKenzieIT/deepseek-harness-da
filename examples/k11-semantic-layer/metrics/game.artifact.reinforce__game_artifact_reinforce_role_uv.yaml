kind: metric
name: game.artifact.reinforce__game_artifact_reinforce_role_uv
description: 参与兵符强化的去重角色数
domains:
  - 角色成长
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.artifact.reinforce
    time_grain: ""
relations:
  - type: derived_from
    target: game.artifact.reinforce
    description: 机械提取自 game.artifact.reinforce 的 metrics 块（key=game_artifact_reinforce_role_uv）
