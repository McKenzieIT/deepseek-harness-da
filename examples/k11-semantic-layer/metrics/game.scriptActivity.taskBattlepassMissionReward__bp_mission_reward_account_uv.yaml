kind: metric
name: game.scriptActivity.taskBattlepassMissionReward__bp_mission_reward_account_uv
description: 领取战令任务奖励的去重账号数
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT account_id)
  metadata:
    aggregation: count_distinct
    field: account_id
    source: game.scriptActivity.taskBattlepassMissionReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.scriptActivity.taskBattlepassMissionReward
    description: 机械提取自 game.scriptActivity.taskBattlepassMissionReward 的 metrics 块（key=bp_mission_reward_account_uv）
