kind: metric
name: game.scriptActivity.scriptMissionReward__script_mission_reward_server_uv
description: 领取奖励覆盖的去重游戏服数
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: game.scriptActivity.scriptMissionReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.scriptActivity.scriptMissionReward
    description: 机械提取自 game.scriptActivity.scriptMissionReward 的 metrics 块（key=script_mission_reward_server_uv）
