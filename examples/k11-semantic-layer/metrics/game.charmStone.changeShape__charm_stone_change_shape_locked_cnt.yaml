kind: metric
name: game.charmStone.changeShape__charm_stone_change_shape_locked_cnt
description: 变形时灵石处于上锁状态(isLock=true)的操作条数
domains:
  - 探索收集
computation:
  sql: SUM(CASE WHEN GET_JSON_OBJECT(params, '$.charmStone.isLock') = 'true' THEN 1 ELSE 0 END)
  metadata:
    aggregation: sum
    field: CASE WHEN GET_JSON_OBJECT(params, '$.charmStone.isLock') = 'true' THEN 1 ELSE 0 END
    source: game.charmStone.changeShape
    time_grain: ""
relations:
  - type: derived_from
    target: game.charmStone.changeShape
    description: 机械提取自 game.charmStone.changeShape 的 metrics 块（key=charm_stone_change_shape_locked_cnt）
