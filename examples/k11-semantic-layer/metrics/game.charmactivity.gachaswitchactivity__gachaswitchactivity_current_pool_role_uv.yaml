kind: metric
name: game.charmactivity.gachaswitchactivity__gachaswitchactivity_current_pool_role_uv
description: 切换到本期卡池的独立角色数(isCurrent=true 时去重 role_id)
domains:
  - 探索收集
computation:
  sql: COUNT(DISTINCT CASE WHEN GET_JSON_OBJECT(params, '$.isCurrent') = 'true' THEN role_id END)
  metadata:
    aggregation: count_distinct
    field: CASE WHEN GET_JSON_OBJECT(params, '$.isCurrent') = 'true' THEN role_id END
    source: game.charmactivity.gachaswitchactivity
    time_grain: ""
relations:
  - type: derived_from
    target: game.charmactivity.gachaswitchactivity
    description: 机械提取自 game.charmactivity.gachaswitchactivity 的 metrics 块（key=gachaswitchactivity_current_pool_role_uv）
