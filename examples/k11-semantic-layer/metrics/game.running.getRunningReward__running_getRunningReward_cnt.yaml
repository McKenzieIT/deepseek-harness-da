kind: metric
name: game.running.getRunningReward__running_getRunningReward_cnt
description: 赛马奖励领取事件总次数（每次领取计一次）
domains:
  - 自定义
computation:
  sql: COUNT(*)
  metadata:
    aggregation: count
    field: "*"
    source: game.running.getRunningReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.running.getRunningReward
    description: 机械提取自 game.running.getRunningReward 的 metrics 块（key=running_getRunningReward_cnt）
