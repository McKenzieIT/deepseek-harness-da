kind: metric
name: game.waractivity.burning__burning_candle_holder_sum
description: 点亮烛台总数（所有事件累加）
domains:
  - 自定义
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.caddleHolderNum') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.caddleHolderNum') AS BIGINT)
    source: game.waractivity.burning
    time_grain: ""
relations:
  - type: derived_from
    target: game.waractivity.burning
    description: 机械提取自 game.waractivity.burning 的 metrics 块（key=burning_candle_holder_sum）
