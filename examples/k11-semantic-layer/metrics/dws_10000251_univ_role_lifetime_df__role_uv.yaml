kind: metric
name: dws_10000251_univ_role_lifetime_df__role_uv
description: 角色累计去重数（日全量快照UV，分析主体=角色）
domains:
  - 用户生命周期
  - 付费经济
computation:
  sql: COUNT(DISTINCT user_id)
  metadata:
    aggregation: count_distinct
    field: user_id
    source: dws_10000251_univ_role_lifetime_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_univ_role_lifetime_df
    description: 机械提取自 dws_10000251_univ_role_lifetime_df 的 metrics 块（key=role_uv）
