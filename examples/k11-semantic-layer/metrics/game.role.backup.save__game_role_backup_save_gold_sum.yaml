kind: metric
name: game.role.backup.save__game_role_backup_save_gold_sum
description: 备份快照中充值钻石（gold）的累计值（角色级货币快照存量，非全周期支付额）
domains:
  - 用户生命周期
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.coinList.gold') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.coinList.gold') AS BIGINT)
    source: game.role.backup.save
    time_grain: ""
relations:
  - type: derived_from
    target: game.role.backup.save
    description: 机械提取自 game.role.backup.save 的 metrics 块（key=game_role_backup_save_gold_sum）
