kind: metric
name: game.knight.levelup__knight_levelup_level_sum
description: 升级后武将等级累计求和（可用于估算总升级量级）
domains:
  - 角色成长
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.knightLevel') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.knightLevel') AS BIGINT)
    source: game.knight.levelup
    time_grain: ""
relations:
  - type: derived_from
    target: game.knight.levelup
    description: 机械提取自 game.knight.levelup 的 metrics 块（key=knight_levelup_level_sum）
