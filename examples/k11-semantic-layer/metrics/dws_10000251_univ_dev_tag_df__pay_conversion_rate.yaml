kind: metric
name: dws_10000251_univ_dev_tag_df__pay_conversion_rate
description: 累计付费转化率(曾付费设备占比,0-1 比率)
domains:
  - 用户生命周期
  - 付费经济
  - 角色成长
computation:
  sql: AVG(CASE WHEN pay_amt_std > 0 THEN 1 ELSE 0 END)
  metadata:
    aggregation: avg
    field: CASE WHEN pay_amt_std > 0 THEN 1 ELSE 0 END
    source: dws_10000251_univ_dev_tag_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_univ_dev_tag_df
    description: 机械提取自 dws_10000251_univ_dev_tag_df 的 metrics 块（key=pay_conversion_rate）
