kind: metric
name: game.guild.submitMission__game_guild_submitMission_cnt
description: 公会提交任务事件总条数（零命中场景下实测返回 0）
domains:
  - 社交公会
computation:
  sql: COUNT(*)
  metadata:
    aggregation: count
    field: "*"
    source: game.guild.submitMission
    time_grain: ""
relations:
  - type: derived_from
    target: game.guild.submitMission
    description: 机械提取自 game.guild.submitMission 的 metrics 块（key=game_guild_submitMission_cnt）
