kind: metric
name: dws_10000251_univ_dev_whole_df__paid_dev_cnt
description: 累计曾付费设备数；统计截至快照日累计付费金额>0 的付费设备数量；作为付费率/ARPU 的分母
domains:
  - 用户生命周期
  - 付费经济
computation:
  sql: COUNT(DISTINCT CASE WHEN pay_amt_std > 0 THEN user_id ELSE NULL END)
  metadata:
    aggregation: count_distinct
    field: CASE WHEN pay_amt_std > 0 THEN user_id ELSE NULL END
    source: dws_10000251_univ_dev_whole_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_univ_dev_whole_df
    description: 机械提取自 dws_10000251_univ_dev_whole_df 的 metrics 块（key=paid_dev_cnt）
