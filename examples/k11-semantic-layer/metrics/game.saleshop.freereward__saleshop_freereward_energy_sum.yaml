kind: metric
name: game.saleshop.freereward__saleshop_freereward_energy_sum
description: 体力奖励发放总量
domains:
  - 付费经济
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.coinList.energy') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.coinList.energy') AS BIGINT)
    source: game.saleshop.freereward
    time_grain: ""
relations:
  - type: derived_from
    target: game.saleshop.freereward
    description: 机械提取自 game.saleshop.freereward 的 metrics 块（key=saleshop_freereward_energy_sum）
