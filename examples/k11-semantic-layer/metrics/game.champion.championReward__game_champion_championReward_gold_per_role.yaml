kind: metric
name: game.champion.championReward__game_champion_championReward_gold_per_role
description: 人均发放充值钻石量
domains:
  - 战斗关卡
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.coinList.gold') AS BIGINT)) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.coinList.gold') AS BIGINT)) / NULLIF(COUNT(DISTINCT role_id), 0
    source: game.champion.championReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.champion.championReward
    description: 机械提取自 game.champion.championReward 的 metrics 块（key=game_champion_championReward_gold_per_role）
