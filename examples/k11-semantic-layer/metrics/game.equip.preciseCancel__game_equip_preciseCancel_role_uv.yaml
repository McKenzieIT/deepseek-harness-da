kind: metric
name: game.equip.preciseCancel__game_equip_preciseCancel_role_uv
description: 触发过精准取消的去重角色数（分析主体=role_id）
domains:
  - 装备道具
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.equip.preciseCancel
    time_grain: ""
relations:
  - type: derived_from
    target: game.equip.preciseCancel
    description: 机械提取自 game.equip.preciseCancel 的 metrics 块（key=game_equip_preciseCancel_role_uv）
