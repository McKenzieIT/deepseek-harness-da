kind: metric
name: game.resort.new__resort_new_account_uv
description: 发起代打请求的去重账号数
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT account_id)
  metadata:
    aggregation: count_distinct
    field: account_id
    source: game.resort.new
    time_grain: ""
relations:
  - type: derived_from
    target: game.resort.new
    description: 机械提取自 game.resort.new 的 metrics 块（key=resort_new_account_uv）
