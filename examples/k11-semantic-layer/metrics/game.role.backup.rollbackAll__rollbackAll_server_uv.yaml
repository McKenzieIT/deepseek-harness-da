kind: metric
name: game.role.backup.rollbackAll__rollbackAll_server_uv
description: 触发全量回退的去重服务器数
domains:
  - 用户生命周期
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: game.role.backup.rollbackAll
    time_grain: ""
relations:
  - type: derived_from
    target: game.role.backup.rollbackAll
    description: 机械提取自 game.role.backup.rollbackAll 的 metrics 块（key=rollbackAll_server_uv）
