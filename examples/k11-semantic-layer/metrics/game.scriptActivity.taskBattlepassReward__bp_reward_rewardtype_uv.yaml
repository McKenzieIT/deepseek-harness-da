kind: metric
name: game.scriptActivity.taskBattlepassReward__bp_reward_rewardtype_uv
description: 涉及的去重奖励类型(rewardType)数
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params,'$.rewardType'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params,'$.rewardType')
    source: game.scriptActivity.taskBattlepassReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.scriptActivity.taskBattlepassReward
    description: 机械提取自 game.scriptActivity.taskBattlepassReward 的 metrics 块（key=bp_reward_rewardtype_uv）
