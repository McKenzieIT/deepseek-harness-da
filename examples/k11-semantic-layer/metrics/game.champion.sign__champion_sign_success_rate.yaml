kind: metric
name: game.champion.sign__champion_sign_success_rate
description: 报名成功率（成功次数 / 总次数）
domains:
  - 战斗关卡
computation:
  sql: SUM(CASE WHEN GET_JSON_OBJECT(params,'$.signResult') = '1' THEN 1 ELSE 0 END) / NULLIF(COUNT(*), 0)
  metadata:
    aggregation: sum
    field: CASE WHEN GET_JSON_OBJECT(params,'$.signResult') = '1' THEN 1 ELSE 0 END) / NULLIF(COUNT(*), 0
    source: game.champion.sign
    time_grain: ""
relations:
  - type: derived_from
    target: game.champion.sign
    description: 机械提取自 game.champion.sign 的 metrics 块（key=champion_sign_success_rate）
