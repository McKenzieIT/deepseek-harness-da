kind: metric
name: dws_10000251_role_progression_nday_df__retention_rate_day7
description: 7日留存率（7日留存角色数 / 首日活跃角色数，分母 NULLIF 防零除）
domains:
  - 角色成长
  - 用户生命周期
computation:
  sql: COUNT(DISTINCT CASE WHEN day7 IS NOT NULL THEN role_id END) / NULLIF(COUNT(DISTINCT CASE WHEN day1 IS NOT NULL THEN role_id END), 0)
  metadata:
    aggregation: count_distinct
    field: CASE WHEN day7 IS NOT NULL THEN role_id END) / NULLIF(COUNT(DISTINCT CASE WHEN day1 IS NOT NULL THEN role_id END), 0
    source: dws_10000251_role_progression_nday_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_role_progression_nday_df
    description: 机械提取自 dws_10000251_role_progression_nday_df 的 metrics 块（key=retention_rate_day7）
