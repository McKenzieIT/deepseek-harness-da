kind: metric
name: game.toy.resonanceLevelup__game_toy_resonance_levelup_per_role
description: 每个升级角色的平均升级次数
domains:
  - 探索收集
computation:
  sql: COUNT(*) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: count
    field: "*) / NULLIF(COUNT(DISTINCT role_id), 0"
    source: game.toy.resonanceLevelup
    time_grain: ""
relations:
  - type: derived_from
    target: game.toy.resonanceLevelup
    description: 机械提取自 game.toy.resonanceLevelup 的 metrics 块（key=game_toy_resonance_levelup_per_role）
