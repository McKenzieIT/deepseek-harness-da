kind: metric
name: game.battlepass.missionReward__battlepass_mission_reward_cnt
description: 战令任务领奖事件触发次数(每次完成战令任务并领取奖励记1条)
domains:
  - 角色成长
computation:
  sql: COUNT(*)
  metadata:
    aggregation: count
    field: "*"
    source: game.battlepass.missionReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.battlepass.missionReward
    description: 机械提取自 game.battlepass.missionReward 的 metrics 块（key=battlepass_mission_reward_cnt）
