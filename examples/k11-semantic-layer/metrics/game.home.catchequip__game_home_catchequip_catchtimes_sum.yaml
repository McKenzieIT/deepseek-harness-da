kind: metric
name: game.home.catchequip__game_home_catchequip_catchtimes_sum
description: 捕捞次数累计总和（来自 params.catchTimes）
domains:
  - 自定义
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params,'$.catchTimes') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params,'$.catchTimes') AS BIGINT)
    source: game.home.catchequip
    time_grain: ""
relations:
  - type: derived_from
    target: game.home.catchequip
    description: 机械提取自 game.home.catchequip 的 metrics 块（key=game_home_catchequip_catchtimes_sum）
