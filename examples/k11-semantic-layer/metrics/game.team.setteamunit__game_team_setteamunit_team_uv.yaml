kind: metric
name: game.team.setteamunit__game_team_setteamunit_team_uv
description: 被设置的去重队伍/槽位数（teamId）
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.teamId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.teamId')
    source: game.team.setteamunit
    time_grain: ""
relations:
  - type: derived_from
    target: game.team.setteamunit
    description: 机械提取自 game.team.setteamunit 的 metrics 块（key=game_team_setteamunit_team_uv）
