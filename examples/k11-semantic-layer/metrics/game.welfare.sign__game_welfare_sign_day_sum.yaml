kind: metric
name: game.welfare.sign__game_welfare_sign_day_sum
description: 累计签到天数（day 字段求和，仅用于总量参考）
domains:
  - 自定义
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.day') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.day') AS BIGINT)
    source: game.welfare.sign
    time_grain: ""
relations:
  - type: derived_from
    target: game.welfare.sign
    description: 机械提取自 game.welfare.sign 的 metrics 块（key=game_welfare_sign_day_sum）
