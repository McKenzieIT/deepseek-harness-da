kind: metric
name: dws_10000251_play_story_act_di__role_uv
description: 每日触达剧情的角色数（去重角色UV），粒度日增量。
domains:
  - 角色成长
  - 探索收集
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: dws_10000251_play_story_act_di
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_play_story_act_di
    description: 机械提取自 dws_10000251_play_story_act_di 的 metrics 块（key=role_uv）
