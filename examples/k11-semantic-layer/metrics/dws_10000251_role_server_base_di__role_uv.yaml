kind: metric
name: dws_10000251_role_server_base_di__role_uv
description: 当日活跃角色数（role_id 去重）
domains:
  - 用户生命周期
  - 付费经济
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: dws_10000251_role_server_base_di
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_role_server_base_di
    description: 机械提取自 dws_10000251_role_server_base_di 的 metrics 块（key=role_uv）
