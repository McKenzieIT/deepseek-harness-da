kind: metric
name: dws_10000251_biz_role_tag_df__tag_coverage_rate
description: 标签覆盖度=已标记角色数/总角色数
domains:
  - 角色成长
  - 系统监控
computation:
  sql: COUNT(DISTINCT CASE WHEN biz_name IS NOT NULL THEN user_id END) / NULLIF(COUNT(DISTINCT user_id), 0)
  metadata:
    aggregation: count_distinct
    field: CASE WHEN biz_name IS NOT NULL THEN user_id END) / NULLIF(COUNT(DISTINCT user_id), 0
    source: dws_10000251_biz_role_tag_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_biz_role_tag_df
    description: 机械提取自 dws_10000251_biz_role_tag_df 的 metrics 块（key=tag_coverage_rate）
