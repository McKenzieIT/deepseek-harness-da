kind: metric
name: game.gmActivityShop.RMBbuy__game_gmActivityShop_RMBbuy_buy_count_sum
description: GM活动商店人民币购买的商品总数量（buyCount累计）
domains:
  - 自定义
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params,'$.buyCount') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params,'$.buyCount') AS BIGINT)
    source: game.gmActivityShop.RMBbuy
    time_grain: ""
relations:
  - type: derived_from
    target: game.gmActivityShop.RMBbuy
    description: 机械提取自 game.gmActivityShop.RMBbuy 的 metrics 块（key=game_gmActivityShop_RMBbuy_buy_count_sum）
