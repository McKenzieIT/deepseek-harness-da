kind: metric
name: game.knight.setBondKnight__knight_set_bond_knight_avg_per_role
description: 平均每个角色的设置羁绊次数
domains:
  - 角色成长
computation:
  sql: COUNT(*) * 1.0 / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: count
    field: "*) * 1.0 / NULLIF(COUNT(DISTINCT role_id), 0"
    source: game.knight.setBondKnight
    time_grain: ""
relations:
  - type: derived_from
    target: game.knight.setBondKnight
    description: 机械提取自 game.knight.setBondKnight 的 metrics 块（key=knight_set_bond_knight_avg_per_role）
