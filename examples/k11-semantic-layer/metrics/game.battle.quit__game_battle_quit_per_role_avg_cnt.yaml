kind: metric
name: game.battle.quit__game_battle_quit_per_role_avg_cnt
description: 人均放弃战斗次数（总次数/角色UV）
domains:
  - 战斗关卡
computation:
  sql: COUNT(*)::FLOAT / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: count
    field: "*)::FLOAT / NULLIF(COUNT(DISTINCT role_id), 0"
    source: game.battle.quit
    time_grain: ""
relations:
  - type: derived_from
    target: game.battle.quit
    description: 机械提取自 game.battle.quit 的 metrics 块（key=game_battle_quit_per_role_avg_cnt）
