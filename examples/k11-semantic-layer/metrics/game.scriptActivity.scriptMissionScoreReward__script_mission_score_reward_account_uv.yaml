kind: metric
name: game.scriptActivity.scriptMissionScoreReward__script_mission_score_reward_account_uv
description: 领取过奖励的 distinct 账号数
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT account_id)
  metadata:
    aggregation: count_distinct
    field: account_id
    source: game.scriptActivity.scriptMissionScoreReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.scriptActivity.scriptMissionScoreReward
    description: 机械提取自 game.scriptActivity.scriptMissionScoreReward 的 metrics 块（key=script_mission_score_reward_account_uv）
