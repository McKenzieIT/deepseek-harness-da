kind: metric
name: game.yanwu.build__game_yanwu_build_per_role_actions
description: 人均设置队伍次数（总次数/去重角色数，NULLIF 防除零）
domains:
  - 战斗关卡
computation:
  sql: COUNT(*) * 1.0 / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: count
    field: "*) * 1.0 / NULLIF(COUNT(DISTINCT role_id), 0"
    source: game.yanwu.build
    time_grain: ""
relations:
  - type: derived_from
    target: game.yanwu.build
    description: 机械提取自 game.yanwu.build 的 metrics 块（key=game_yanwu_build_per_role_actions）
