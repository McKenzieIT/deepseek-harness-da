kind: metric
name: game.gacha.setWishId__game_gacha_setWishId_ids_filled_cnt
description: 携带心愿目标 ids 列表的事件数
domains:
  - 探索收集
computation:
  sql: SUM(CASE WHEN GET_JSON_OBJECT(params,'$.ids') IS NOT NULL THEN 1 ELSE 0 END)
  metadata:
    aggregation: sum
    field: CASE WHEN GET_JSON_OBJECT(params,'$.ids') IS NOT NULL THEN 1 ELSE 0 END
    source: game.gacha.setWishId
    time_grain: ""
relations:
  - type: derived_from
    target: game.gacha.setWishId
    description: 机械提取自 game.gacha.setWishId 的 metrics 块（key=game_gacha_setWishId_ids_filled_cnt）
