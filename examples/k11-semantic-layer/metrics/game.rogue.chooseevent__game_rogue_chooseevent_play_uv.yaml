kind: metric
name: game.rogue.chooseevent__game_rogue_chooseevent_play_uv
description: 涉及的古战场副本挑战场次去重数（playId）
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params,'$.playId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params,'$.playId')
    source: game.rogue.chooseevent
    time_grain: ""
relations:
  - type: derived_from
    target: game.rogue.chooseevent
    description: 机械提取自 game.rogue.chooseevent 的 metrics 块（key=game_rogue_chooseevent_play_uv）
