kind: metric
name: game.knightnatalchart.unlockattrslot__knightnatalchart_unlockattrslot_avg_per_role
description: 每个角色的平均属性槽位解锁次数（衡量深度养成）
domains:
  - 角色成长
computation:
  sql: COUNT(*) * 1.0 / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: count
    field: "*) * 1.0 / NULLIF(COUNT(DISTINCT role_id), 0"
    source: game.knightnatalchart.unlockattrslot
    time_grain: ""
relations:
  - type: derived_from
    target: game.knightnatalchart.unlockattrslot
    description: 机械提取自 game.knightnatalchart.unlockattrslot 的 metrics 块（key=knightnatalchart_unlockattrslot_avg_per_role）
