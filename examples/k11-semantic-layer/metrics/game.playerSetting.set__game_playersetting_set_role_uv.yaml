kind: metric
name: game.playerSetting.set__game_playersetting_set_role_uv
description: 触发系统设置变更的去重角色数
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.playerSetting.set
    time_grain: ""
relations:
  - type: derived_from
    target: game.playerSetting.set
    description: 机械提取自 game.playerSetting.set 的 metrics 块（key=game_playersetting_set_role_uv）
