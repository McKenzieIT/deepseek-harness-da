kind: metric
name: game.anniversary.dungeonReset__dungeonreset_server_dn
description: 触发周年庆关卡重置的去重游戏服数
domains:
  - 付费经济
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: game.anniversary.dungeonReset
    time_grain: ""
relations:
  - type: derived_from
    target: game.anniversary.dungeonReset
    description: 机械提取自 game.anniversary.dungeonReset 的 metrics 块（key=dungeonreset_server_dn）
