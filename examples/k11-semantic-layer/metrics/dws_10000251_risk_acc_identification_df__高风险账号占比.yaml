kind: metric
name: dws_10000251_risk_acc_identification_df__高风险账号占比
description: 高风险账号占全部被标识账号的比例，分母为0时返回NULL
domains:
  - 系统监控
computation:
  sql: COUNT(DISTINCT CASE WHEN risk_level >= 3 THEN user_id END) / NULLIF(COUNT(DISTINCT user_id), 0)
  metadata:
    aggregation: count_distinct
    field: CASE WHEN risk_level >= 3 THEN user_id END) / NULLIF(COUNT(DISTINCT user_id), 0
    source: dws_10000251_risk_acc_identification_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_risk_acc_identification_df
    description: 机械提取自 dws_10000251_risk_acc_identification_df 的 metrics 块（key=高风险账号占比）
