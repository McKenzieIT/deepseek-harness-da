kind: metric
name: game.knightnatalchart.unlockscheme__game_knightnatalchart_unlockscheme_scheme_uv
description: 解锁方案的去重方案类型数（取自 params.scheme）
domains:
  - 角色成长
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.scheme'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.scheme')
    source: game.knightnatalchart.unlockscheme
    time_grain: ""
relations:
  - type: derived_from
    target: game.knightnatalchart.unlockscheme
    description: 机械提取自 game.knightnatalchart.unlockscheme 的 metrics 块（key=game_knightnatalchart_unlockscheme_scheme_uv）
