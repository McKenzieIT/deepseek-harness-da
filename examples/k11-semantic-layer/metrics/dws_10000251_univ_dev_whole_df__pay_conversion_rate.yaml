kind: metric
name: dws_10000251_univ_dev_whole_df__pay_conversion_rate
description: 付费转化率；付费设备占全体设备的比例，用 AVG(CASE WHEN pay_amt_std>0 THEN 1 ELSE 0 END) 计算
domains:
  - 用户生命周期
  - 付费经济
computation:
  sql: AVG(CASE WHEN pay_amt_std > 0 THEN 1 ELSE 0 END)
  metadata:
    aggregation: avg
    field: CASE WHEN pay_amt_std > 0 THEN 1 ELSE 0 END
    source: dws_10000251_univ_dev_whole_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_univ_dev_whole_df
    description: 机械提取自 dws_10000251_univ_dev_whole_df 的 metrics 块（key=pay_conversion_rate）
