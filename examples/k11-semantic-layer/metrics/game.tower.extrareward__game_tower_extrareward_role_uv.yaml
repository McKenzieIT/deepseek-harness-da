kind: metric
name: game.tower.extrareward__game_tower_extrareward_role_uv
description: 领取额外奖励的去重角色数（分析主体粒度）
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.tower.extrareward
    time_grain: ""
relations:
  - type: derived_from
    target: game.tower.extrareward
    description: 机械提取自 game.tower.extrareward 的 metrics 块（key=game_tower_extrareward_role_uv）
