kind: metric
name: dws_10000251_role_summary_base_di__role_uv
description: 角色 UV，当日增量角色去重数（_di 每角色一行，COUNT(DISTINCT role_id) 即当日增量角色数）
domains:
  - 付费经济
  - 用户生命周期
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: dws_10000251_role_summary_base_di
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_role_summary_base_di
    description: 机械提取自 dws_10000251_role_summary_base_di 的 metrics 块（key=role_uv）
