kind: metric
name: game.toy.techonekeyactive__techonekeyactive_cnt
description: 一键科技激活事件总次数（每次点击一键激活记 1 次，对应一个 techIds 批量激活）
domains:
  - 探索收集
computation:
  sql: COUNT(*)
  metadata:
    aggregation: count
    field: "*"
    source: game.toy.techonekeyactive
    time_grain: ""
relations:
  - type: derived_from
    target: game.toy.techonekeyactive
    description: 机械提取自 game.toy.techonekeyactive 的 metrics 块（key=techonekeyactive_cnt）
