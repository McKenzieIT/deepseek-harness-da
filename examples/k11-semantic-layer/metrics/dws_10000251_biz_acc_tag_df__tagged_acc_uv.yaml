kind: metric
name: dws_10000251_biz_acc_tag_df__tagged_acc_uv
description: 已打标(biz_name IS NOT NULL)账号数（去重）
domains:
  - 用户生命周期
  - 系统监控
computation:
  sql: COUNT(DISTINCT CASE WHEN biz_name IS NOT NULL THEN user_id END)
  metadata:
    aggregation: count_distinct
    field: CASE WHEN biz_name IS NOT NULL THEN user_id END
    source: dws_10000251_biz_acc_tag_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_biz_acc_tag_df
    description: 机械提取自 dws_10000251_biz_acc_tag_df 的 metrics 块（key=tagged_acc_uv）
