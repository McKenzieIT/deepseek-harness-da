kind: metric
name: game.artifact.startup__game_artifact_startup_cnt
description: 神器升星启动事件触发总次数
domains:
  - 角色成长
computation:
  sql: COUNT(*)
  metadata:
    aggregation: count
    field: "*"
    source: game.artifact.startup
    time_grain: ""
relations:
  - type: derived_from
    target: game.artifact.startup
    description: 机械提取自 game.artifact.startup 的 metrics 块（key=game_artifact_startup_cnt）
