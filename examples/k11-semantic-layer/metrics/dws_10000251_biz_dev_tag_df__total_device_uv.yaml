kind: metric
name: dws_10000251_biz_dev_tag_df__total_device_uv
description: 当前快照下全量设备数(主体=user_id,user_type=1)。_df 全量快照口径。
domains:
  - 用户生命周期
  - 系统监控
computation:
  sql: COUNT(DISTINCT user_id)
  metadata:
    aggregation: count_distinct
    field: user_id
    source: dws_10000251_biz_dev_tag_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_biz_dev_tag_df
    description: 机械提取自 dws_10000251_biz_dev_tag_df 的 metrics 块（key=total_device_uv）
