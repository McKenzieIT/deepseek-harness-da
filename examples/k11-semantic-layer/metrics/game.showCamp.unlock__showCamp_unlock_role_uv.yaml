kind: metric
name: game.showCamp.unlock__showCamp_unlock_role_uv
description: 触发营地解锁的去重角色数
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.showCamp.unlock
    time_grain: ""
relations:
  - type: derived_from
    target: game.showCamp.unlock
    description: 机械提取自 game.showCamp.unlock 的 metrics 块（key=showCamp_unlock_role_uv）
