kind: metric
name: game.title.change__game_title_change_server_uv
description: 发生称号更换的去重游戏服数
domains:
  - 角色成长
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: game.title.change
    time_grain: ""
relations:
  - type: derived_from
    target: game.title.change
    description: 机械提取自 game.title.change 的 metrics 块（key=game_title_change_server_uv）
