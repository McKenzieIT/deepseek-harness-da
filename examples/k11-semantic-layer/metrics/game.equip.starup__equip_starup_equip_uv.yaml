kind: metric
name: game.equip.starup__equip_starup_equip_uv
description: 被升星的装备uid去重数
domains:
  - 装备道具
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.uid'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.uid')
    source: game.equip.starup
    time_grain: ""
relations:
  - type: derived_from
    target: game.equip.starup
    description: 机械提取自 game.equip.starup 的 metrics 块（key=equip_starup_equip_uv）
