kind: metric
name: game.dig.useDigItem__game_dig_useDigItem_role_uv
description: 使用挖掘道具的去重角色数
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.dig.useDigItem
    time_grain: ""
relations:
  - type: derived_from
    target: game.dig.useDigItem
    description: 机械提取自 game.dig.useDigItem 的 metrics 块（key=game_dig_useDigItem_role_uv）
