kind: metric
name: game.group.battle__group_battle_avg_role_num
description: 平均每场战斗参与队伍人数（roleNum 均值）
domains:
  - 社交公会
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.roleNum') AS BIGINT)) / NULLIF(COUNT(*), 0)
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.roleNum') AS BIGINT)) / NULLIF(COUNT(*), 0
    source: game.group.battle
    time_grain: ""
relations:
  - type: derived_from
    target: game.group.battle
    description: 机械提取自 game.group.battle 的 metrics 块（key=group_battle_avg_role_num）
