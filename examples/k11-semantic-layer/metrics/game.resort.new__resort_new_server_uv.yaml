kind: metric
name: game.resort.new__resort_new_server_uv
description: 命中代打请求的去重游戏服数
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: game.resort.new
    time_grain: ""
relations:
  - type: derived_from
    target: game.resort.new
    description: 机械提取自 game.resort.new 的 metrics 块（key=resort_new_server_uv）
