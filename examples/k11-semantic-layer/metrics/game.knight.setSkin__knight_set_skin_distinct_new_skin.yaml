kind: metric
name: game.knight.setSkin__knight_set_skin_distinct_new_skin
description: 被设置的新皮肤去重数(按newSkinId)
domains:
  - 角色成长
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.newSkinId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.newSkinId')
    source: game.knight.setSkin
    time_grain: ""
relations:
  - type: derived_from
    target: game.knight.setSkin
    description: 机械提取自 game.knight.setSkin 的 metrics 块（key=knight_set_skin_distinct_new_skin）
