kind: metric
name: game.maze.exit__game_maze_exit_role_uv
description: 退出迷宫的去重角色数（分析主体粒度）
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.maze.exit
    time_grain: ""
relations:
  - type: derived_from
    target: game.maze.exit
    description: 机械提取自 game.maze.exit 的 metrics 块（key=game_maze_exit_role_uv）
