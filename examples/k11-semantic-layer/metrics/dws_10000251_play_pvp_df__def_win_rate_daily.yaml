kind: metric
name: dws_10000251_play_pvp_df__def_win_rate_daily
description: 当日防守胜率（仅异步PVP有效，胜利次数/防守次数，0-1）
domains:
  - 战斗关卡
computation:
  sql: SUM(def_win_pv_daily) / NULLIF(SUM(def_pv_daily), 0)
  metadata:
    aggregation: sum
    field: def_win_pv_daily) / NULLIF(SUM(def_pv_daily), 0
    source: dws_10000251_play_pvp_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_play_pvp_df
    description: 机械提取自 dws_10000251_play_pvp_df 的 metrics 块（key=def_win_rate_daily）
