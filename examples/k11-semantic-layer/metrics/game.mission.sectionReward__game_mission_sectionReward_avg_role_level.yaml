kind: metric
name: game.mission.sectionReward__game_mission_sectionReward_avg_role_level
description: 领奖时角色平均等级（实测 ds=20260729≈30.36）
domains:
  - 角色成长
computation:
  sql: AVG(CAST(GET_JSON_OBJECT(params, '$.roleLevel') AS BIGINT))
  metadata:
    aggregation: avg
    field: CAST(GET_JSON_OBJECT(params, '$.roleLevel') AS BIGINT)
    source: game.mission.sectionReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.mission.sectionReward
    description: 机械提取自 game.mission.sectionReward 的 metrics 块（key=game_mission_sectionReward_avg_role_level）
