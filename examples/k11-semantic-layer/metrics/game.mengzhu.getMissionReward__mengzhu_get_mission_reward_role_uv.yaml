kind: metric
name: game.mengzhu.getMissionReward__mengzhu_get_mission_reward_role_uv
description: 领取奖励的去重角色数(分析主体为 role_id)
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.mengzhu.getMissionReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.mengzhu.getMissionReward
    description: 机械提取自 game.mengzhu.getMissionReward 的 metrics 块（key=mengzhu_get_mission_reward_role_uv）
