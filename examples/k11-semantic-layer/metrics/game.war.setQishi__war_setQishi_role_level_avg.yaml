kind: metric
name: game.war.setQishi__war_setQishi_role_level_avg
description: 触发事件角色平均等级（按行级算术平均，含 NULLIF 兜底）
domains:
  - 战斗关卡
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params,'$.roleLevel') AS BIGINT)) / NULLIF(COUNT(CAST(GET_JSON_OBJECT(params,'$.roleLevel') AS BIGINT)),0)
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params,'$.roleLevel') AS BIGINT)) / NULLIF(COUNT(CAST(GET_JSON_OBJECT(params,'$.roleLevel') AS BIGINT)),0
    source: game.war.setQishi
    time_grain: ""
relations:
  - type: derived_from
    target: game.war.setQishi
    description: 机械提取自 game.war.setQishi 的 metrics 块（key=war_setQishi_role_level_avg）
