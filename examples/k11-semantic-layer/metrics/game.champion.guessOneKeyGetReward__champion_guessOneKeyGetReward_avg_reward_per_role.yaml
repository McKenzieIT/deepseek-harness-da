kind: metric
name: game.champion.guessOneKeyGetReward__champion_guessOneKeyGetReward_avg_reward_per_role
description: 人均领取竞猜奖励数额（rewardSize 总和 / 去重角色数）
domains:
  - 战斗关卡
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.rewardSize') AS BIGINT)) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.rewardSize') AS BIGINT)) / NULLIF(COUNT(DISTINCT role_id), 0
    source: game.champion.guessOneKeyGetReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.champion.guessOneKeyGetReward
    description: 机械提取自 game.champion.guessOneKeyGetReward 的 metrics 块（key=champion_guessOneKeyGetReward_avg_reward_per_role）
