kind: metric
name: game.gve2.sweep__gve2_sweep_damage_sum
description: GVE2扫荡累计造成伤害（参数 damage 求和，单日全量非空）
domains:
  - 战斗关卡
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.damage') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.damage') AS BIGINT)
    source: game.gve2.sweep
    time_grain: ""
relations:
  - type: derived_from
    target: game.gve2.sweep
    description: 机械提取自 game.gve2.sweep 的 metrics 块（key=gve2_sweep_damage_sum）
