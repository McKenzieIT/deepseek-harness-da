kind: metric
name: game.guild.battle.result__game_guild_battle_result_account_uv
description: 公会战结算涉及的去重账号数（单日）
domains:
  - 社交公会
computation:
  sql: COUNT(DISTINCT account_id)
  metadata:
    aggregation: count_distinct
    field: account_id
    source: game.guild.battle.result
    time_grain: ""
relations:
  - type: derived_from
    target: game.guild.battle.result
    description: 机械提取自 game.guild.battle.result 的 metrics 块（key=game_guild_battle_result_account_uv）
