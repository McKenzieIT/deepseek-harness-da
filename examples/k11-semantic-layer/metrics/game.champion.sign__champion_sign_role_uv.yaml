kind: metric
name: game.champion.sign__champion_sign_role_uv
description: 报名角色去重数（分析主体粒度 role_id）
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.champion.sign
    time_grain: ""
relations:
  - type: derived_from
    target: game.champion.sign
    description: 机械提取自 game.champion.sign 的 metrics 块（key=champion_sign_role_uv）
