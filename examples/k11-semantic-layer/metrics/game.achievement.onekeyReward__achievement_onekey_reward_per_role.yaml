kind: metric
name: game.achievement.onekeyReward__achievement_onekey_reward_per_role
description: 人均领取次数（事件命中/去重角色，分母为0返回NULL）
domains:
  - 角色成长
computation:
  sql: CAST(COUNT(*) AS DECIMAL) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: ""
    field: ""
    source: game.achievement.onekeyReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.achievement.onekeyReward
    description: 机械提取自 game.achievement.onekeyReward 的 metrics 块（key=achievement_onekey_reward_per_role）
