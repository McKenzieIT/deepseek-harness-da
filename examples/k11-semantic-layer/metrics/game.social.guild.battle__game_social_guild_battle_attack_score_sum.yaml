kind: metric
name: game.social.guild.battle__game_social_guild_battle_attack_score_sum
description: 公会战挑战累计获得的进攻积分总和
domains:
  - 社交公会
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.attackScore') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.attackScore') AS BIGINT)
    source: game.social.guild.battle
    time_grain: ""
relations:
  - type: derived_from
    target: game.social.guild.battle
    description: 机械提取自 game.social.guild.battle 的 metrics 块（key=game_social_guild_battle_attack_score_sum）
