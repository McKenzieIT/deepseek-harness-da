kind: metric
name: dws_10000251_biz_pay_buy_df__arppu
description: 平均每付费账号付费金额
domains:
  - 付费经济
computation:
  sql: SUM(pay_amt) / NULLIF(COUNT(DISTINCT account_id), 0)
  metadata:
    aggregation: sum
    field: pay_amt) / NULLIF(COUNT(DISTINCT account_id), 0
    source: dws_10000251_biz_pay_buy_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_biz_pay_buy_df
    description: 机械提取自 dws_10000251_biz_pay_buy_df 的 metrics 块（key=arppu）
