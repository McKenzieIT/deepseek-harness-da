kind: metric
name: game.rogue.selectNode__rogue_selectNode_event_type_uv
description: 触发的不同 eventType 数量（节点选择场景类型广度）
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.eventType'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.eventType')
    source: game.rogue.selectNode
    time_grain: ""
relations:
  - type: derived_from
    target: game.rogue.selectNode
    description: 机械提取自 game.rogue.selectNode 的 metrics 块（key=rogue_selectNode_event_type_uv）
