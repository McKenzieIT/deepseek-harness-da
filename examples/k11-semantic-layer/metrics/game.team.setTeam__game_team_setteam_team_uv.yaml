kind: metric
name: game.team.setTeam__game_team_setteam_team_uv
description: 设置阵容涉及的去重阵容ID数
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.teamId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.teamId')
    source: game.team.setTeam
    time_grain: ""
relations:
  - type: derived_from
    target: game.team.setTeam
    description: 机械提取自 game.team.setTeam 的 metrics 块（key=game_team_setteam_team_uv）
