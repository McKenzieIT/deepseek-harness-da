kind: metric
name: game.battlepassDungeon.reward__battlepass_dungeon_reward_diamond_sum
description: 发放紫金总量(coinList.diamond 求和，当日可能为 NULL/0)
domains:
  - 角色成长
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.coinList.diamond') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.coinList.diamond') AS BIGINT)
    source: game.battlepassDungeon.reward
    time_grain: ""
relations:
  - type: derived_from
    target: game.battlepassDungeon.reward
    description: 机械提取自 game.battlepassDungeon.reward 的 metrics 块（key=battlepass_dungeon_reward_diamond_sum）
