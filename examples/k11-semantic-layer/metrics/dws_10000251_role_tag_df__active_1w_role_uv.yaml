kind: metric
name: dws_10000251_role_tag_df__active_1w_role_uv
description: 近7日活跃角色数（act_days_1w>0）
domains:
  - 角色成长
  - 付费经济
computation:
  sql: COUNT(DISTINCT CASE WHEN act_days_1w > 0 THEN role_id END)
  metadata:
    aggregation: count_distinct
    field: CASE WHEN act_days_1w > 0 THEN role_id END
    source: dws_10000251_role_tag_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_role_tag_df
    description: 机械提取自 dws_10000251_role_tag_df 的 metrics 块（key=active_1w_role_uv）
