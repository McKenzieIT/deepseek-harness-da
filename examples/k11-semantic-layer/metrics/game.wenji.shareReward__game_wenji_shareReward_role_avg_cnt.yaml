kind: metric
name: game.wenji.shareReward__game_wenji_shareReward_role_avg_cnt
description: 文姬分享奖励人均触发次数（总次数/角色去重）
domains:
  - 自定义
computation:
  sql: COUNT(*) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: count
    field: "*) / NULLIF(COUNT(DISTINCT role_id), 0"
    source: game.wenji.shareReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.wenji.shareReward
    description: 机械提取自 game.wenji.shareReward 的 metrics 块（key=game_wenji_shareReward_role_avg_cnt）
