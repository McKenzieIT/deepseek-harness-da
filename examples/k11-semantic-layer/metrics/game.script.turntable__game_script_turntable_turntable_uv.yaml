kind: metric
name: game.script.turntable__game_script_turntable_turntable_uv
description: 命中的去重转盘(turntableId)数
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params,'$.turntableId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params,'$.turntableId')
    source: game.script.turntable
    time_grain: ""
relations:
  - type: derived_from
    target: game.script.turntable
    description: 机械提取自 game.script.turntable 的 metrics 块（key=game_script_turntable_turntable_uv）
