kind: metric
name: game.rogue.skipBattle__game_rogue_skipBattle_role_uv
description: 跳过战斗的角色数（分析主体 role_id）
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.rogue.skipBattle
    time_grain: ""
relations:
  - type: derived_from
    target: game.rogue.skipBattle
    description: 机械提取自 game.rogue.skipBattle 的 metrics 块（key=game_rogue_skipBattle_role_uv）
