kind: metric
name: game.scriptActivity.getPreheatSignReward__getpreheatsignreward_role_uv
description: 领取签到奖励的去重角色数（分析主语）
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.scriptActivity.getPreheatSignReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.scriptActivity.getPreheatSignReward
    description: 机械提取自 game.scriptActivity.getPreheatSignReward 的 metrics 块（key=getpreheatsignreward_role_uv）
