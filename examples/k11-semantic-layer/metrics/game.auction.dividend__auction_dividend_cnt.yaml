kind: metric
name: game.auction.dividend__auction_dividend_cnt
description: 拍卖行分红事件总触发次数
domains:
  - 付费经济
computation:
  sql: COUNT(*)
  metadata:
    aggregation: count
    field: "*"
    source: game.auction.dividend
    time_grain: ""
relations:
  - type: derived_from
    target: game.auction.dividend
    description: 机械提取自 game.auction.dividend 的 metrics 块（key=auction_dividend_cnt）
