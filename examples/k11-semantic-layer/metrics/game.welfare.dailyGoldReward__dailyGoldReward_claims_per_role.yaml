kind: metric
name: game.welfare.dailyGoldReward__dailyGoldReward_claims_per_role
description: 每个领取角色的平均领取次数（>1 表示同一角色当日多次领取）
domains:
  - 资源产销
computation:
  sql: CAST(COUNT(*) AS DOUBLE) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: ""
    field: ""
    source: game.welfare.dailyGoldReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.welfare.dailyGoldReward
    description: 机械提取自 game.welfare.dailyGoldReward 的 metrics 块（key=dailyGoldReward_claims_per_role）
