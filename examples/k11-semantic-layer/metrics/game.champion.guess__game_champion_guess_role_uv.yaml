kind: metric
name: game.champion.guess__game_champion_guess_role_uv
description: 参与争霸赛竞猜的去重角色数(分析主体粒度)
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.champion.guess
    time_grain: ""
relations:
  - type: derived_from
    target: game.champion.guess
    description: 机械提取自 game.champion.guess 的 metrics 块（key=game_champion_guess_role_uv）
