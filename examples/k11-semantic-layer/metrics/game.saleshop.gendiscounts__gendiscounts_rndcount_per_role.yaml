kind: metric
name: game.saleshop.gendiscounts__gendiscounts_rndcount_per_role
description: 触发角色的平均折扣随机次数(总随机次数/去重角色数)
domains:
  - 付费经济
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.rndCount') AS BIGINT)) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.rndCount') AS BIGINT)) / NULLIF(COUNT(DISTINCT role_id), 0
    source: game.saleshop.gendiscounts
    time_grain: ""
relations:
  - type: derived_from
    target: game.saleshop.gendiscounts
    description: 机械提取自 game.saleshop.gendiscounts 的 metrics 块（key=gendiscounts_rndcount_per_role）
