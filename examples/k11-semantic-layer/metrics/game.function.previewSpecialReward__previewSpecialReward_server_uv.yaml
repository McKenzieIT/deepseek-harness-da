kind: metric
name: game.function.previewSpecialReward__previewSpecialReward_server_uv
description: 触发特殊奖励预览领取的去重游戏服数
domains:
  - 系统监控
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: game.function.previewSpecialReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.function.previewSpecialReward
    description: 机械提取自 game.function.previewSpecialReward 的 metrics 块（key=previewSpecialReward_server_uv）
