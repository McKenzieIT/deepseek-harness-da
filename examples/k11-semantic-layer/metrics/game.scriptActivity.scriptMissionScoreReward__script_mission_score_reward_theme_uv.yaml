kind: metric
name: game.scriptActivity.scriptMissionScoreReward__script_mission_score_reward_theme_uv
description: 涉及的 distinct 剧本活动主题数（按 themeId 去重）
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.themeId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.themeId')
    source: game.scriptActivity.scriptMissionScoreReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.scriptActivity.scriptMissionScoreReward
    description: 机械提取自 game.scriptActivity.scriptMissionScoreReward 的 metrics 块（key=script_mission_score_reward_theme_uv）
