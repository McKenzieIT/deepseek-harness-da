kind: metric
name: dws_10000251_role_tag_df__paying_role_uv
description: 快照累计付费角色数（pay_amt_std>0）
domains:
  - 角色成长
  - 付费经济
computation:
  sql: COUNT(DISTINCT CASE WHEN pay_amt_std > 0 THEN role_id END)
  metadata:
    aggregation: count_distinct
    field: CASE WHEN pay_amt_std > 0 THEN role_id END
    source: dws_10000251_role_tag_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_role_tag_df
    description: 机械提取自 dws_10000251_role_tag_df 的 metrics 块（key=paying_role_uv）
