kind: metric
name: game.rogue.train__game_rogue_train_server_uv
description: 涉及的去重游戏服数
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: game.rogue.train
    time_grain: ""
relations:
  - type: derived_from
    target: game.rogue.train
    description: 机械提取自 game.rogue.train 的 metrics 块（key=game_rogue_train_server_uv）
