kind: metric
name: game.equip.lock__game_equip_lock_unlock_cnt
description: 装备解锁操作次数(lock=0)
domains:
  - 装备道具
computation:
  sql: SUM(CASE WHEN CAST(GET_JSON_OBJECT(params,'$.lock') AS BIGINT)=0 THEN 1 ELSE 0 END)
  metadata:
    aggregation: sum
    field: CASE WHEN CAST(GET_JSON_OBJECT(params,'$.lock') AS BIGINT)=0 THEN 1 ELSE 0 END
    source: game.equip.lock
    time_grain: ""
relations:
  - type: derived_from
    target: game.equip.lock
    description: 机械提取自 game.equip.lock 的 metrics 块（key=game_equip_lock_unlock_cnt）
