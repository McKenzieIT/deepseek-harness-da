kind: metric
name: dws_10000251_risk_dev_identification_df__high_risk_ratio
description: 高风险主体占比（high_risk_uv / total_uv）
domains:
  - 系统监控
  - 自定义
computation:
  sql: COUNT(DISTINCT CASE WHEN risk_level>=3 THEN user_id END) * 1.0 / NULLIF(COUNT(DISTINCT user_id), 0)
  metadata:
    aggregation: count_distinct
    field: CASE WHEN risk_level>=3 THEN user_id END) * 1.0 / NULLIF(COUNT(DISTINCT user_id), 0
    source: dws_10000251_risk_dev_identification_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_risk_dev_identification_df
    description: 机械提取自 dws_10000251_risk_dev_identification_df 的 metrics 块（key=high_risk_ratio）
