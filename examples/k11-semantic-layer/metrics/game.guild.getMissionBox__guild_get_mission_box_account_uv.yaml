kind: metric
name: game.guild.getMissionBox__guild_get_mission_box_account_uv
description: 领取公会积分宝箱的去重账号数
domains:
  - 社交公会
computation:
  sql: COUNT(DISTINCT account_id)
  metadata:
    aggregation: count_distinct
    field: account_id
    source: game.guild.getMissionBox
    time_grain: ""
relations:
  - type: derived_from
    target: game.guild.getMissionBox
    description: 机械提取自 game.guild.getMissionBox 的 metrics 块（key=guild_get_mission_box_account_uv）
