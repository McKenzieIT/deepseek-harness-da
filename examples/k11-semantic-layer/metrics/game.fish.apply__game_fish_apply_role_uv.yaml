kind: metric
name: game.fish.apply__game_fish_apply_role_uv
description: 发起钓鱼申请的去重角色数
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.fish.apply
    time_grain: ""
relations:
  - type: derived_from
    target: game.fish.apply
    description: 机械提取自 game.fish.apply 的 metrics 块（key=game_fish_apply_role_uv）
