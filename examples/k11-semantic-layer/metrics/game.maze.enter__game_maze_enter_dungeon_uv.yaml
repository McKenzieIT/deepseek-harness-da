kind: metric
name: game.maze.enter__game_maze_enter_dungeon_uv
description: 进入过的不同迷宫副本id数
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.dungeonId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.dungeonId')
    source: game.maze.enter
    time_grain: ""
relations:
  - type: derived_from
    target: game.maze.enter
    description: 机械提取自 game.maze.enter 的 metrics 块（key=game_maze_enter_dungeon_uv）
