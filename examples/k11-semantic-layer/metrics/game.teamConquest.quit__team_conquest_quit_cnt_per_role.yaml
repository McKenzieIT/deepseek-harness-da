kind: metric
name: game.teamConquest.quit__team_conquest_quit_cnt_per_role
description: 人均退出组队讨伐次数（总次数/去重角色数）
domains:
  - 自定义
computation:
  sql: CAST(COUNT(*) AS DOUBLE) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: ""
    field: ""
    source: game.teamConquest.quit
    time_grain: ""
relations:
  - type: derived_from
    target: game.teamConquest.quit
    description: 机械提取自 game.teamConquest.quit 的 metrics 块（key=team_conquest_quit_cnt_per_role）
