kind: metric
name: game.social.friendact__friendact_target_role_uv
description: 被作用（目标）的去重角色数
domains:
  - 社交公会
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params,'$.relRoleId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params,'$.relRoleId')
    source: game.social.friendact
    time_grain: ""
relations:
  - type: derived_from
    target: game.social.friendact
    description: 机械提取自 game.social.friendact 的 metrics 块（key=friendact_target_role_uv）
