kind: metric
name: game.role.create__game_role_create_role_uv
description: 创角角色去重数（默认 subject 粒度，一个新角色计一次）
domains:
  - 用户生命周期
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.role.create
    time_grain: ""
relations:
  - type: derived_from
    target: game.role.create
    description: 机械提取自 game.role.create 的 metrics 块（key=game_role_create_role_uv）
