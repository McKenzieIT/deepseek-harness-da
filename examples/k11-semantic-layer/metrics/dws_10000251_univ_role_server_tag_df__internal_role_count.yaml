kind: metric
name: dws_10000251_univ_role_server_tag_df__internal_role_count
description: 内部角色数（fellow_flag=1，用于剔除内部用户前的盘点）
domains:
  - 用户生命周期
  - 付费经济
  - 角色成长
computation:
  sql: COUNT(DISTINCT CASE WHEN fellow_flag=1 THEN user_id END)
  metadata:
    aggregation: count_distinct
    field: CASE WHEN fellow_flag=1 THEN user_id END
    source: dws_10000251_univ_role_server_tag_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_univ_role_server_tag_df
    description: 机械提取自 dws_10000251_univ_role_server_tag_df 的 metrics 块（key=internal_role_count）
