kind: metric
name: game.rogue.shopExit__game_rogue_shopExit_gold_balance_sum
description: 退出商店时刻角色充值钻石余额合计
domains:
  - 战斗关卡
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.coinList.gold') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.coinList.gold') AS BIGINT)
    source: game.rogue.shopExit
    time_grain: ""
relations:
  - type: derived_from
    target: game.rogue.shopExit
    description: 机械提取自 game.rogue.shopExit 的 metrics 块（key=game_rogue_shopExit_gold_balance_sum）
