kind: metric
name: game.rogue.finishTask__rogue_finishTask_task_uv
description: 被完成的不同 taskId 数量（图鉴任务覆盖广度）
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.taskId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.taskId')
    source: game.rogue.finishTask
    time_grain: ""
relations:
  - type: derived_from
    target: game.rogue.finishTask
    description: 机械提取自 game.rogue.finishTask 的 metrics 块（key=rogue_finishTask_task_uv）
