kind: metric
name: dws_10000251_battle_stage_df__role_uv
description: 快照角色UV（当日快照内不重复角色数）
domains:
  - 战斗关卡
  - 角色成长
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: dws_10000251_battle_stage_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_battle_stage_df
    description: 机械提取自 dws_10000251_battle_stage_df 的 metrics 块（key=role_uv）
