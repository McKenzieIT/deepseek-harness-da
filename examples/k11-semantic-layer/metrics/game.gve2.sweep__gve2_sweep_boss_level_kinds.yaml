kind: metric
name: game.gve2.sweep__gve2_sweep_boss_level_kinds
description: 被扫荡的 bossLevel 关卡种类数
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.bossLevel'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.bossLevel')
    source: game.gve2.sweep
    time_grain: ""
relations:
  - type: derived_from
    target: game.gve2.sweep
    description: 机械提取自 game.gve2.sweep 的 metrics 块（key=gve2_sweep_boss_level_kinds）
