kind: metric
name: game.charmStone.onekeySynthesis__game_charmstone_onekeysynthesis_group_sum
description: 当期一键合成累计组数（num 求和，合成体量）
domains:
  - 探索收集
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params,'$.num') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params,'$.num') AS BIGINT)
    source: game.charmStone.onekeySynthesis
    time_grain: ""
relations:
  - type: derived_from
    target: game.charmStone.onekeySynthesis
    description: 机械提取自 game.charmStone.onekeySynthesis 的 metrics 块（key=game_charmstone_onekeysynthesis_group_sum）
