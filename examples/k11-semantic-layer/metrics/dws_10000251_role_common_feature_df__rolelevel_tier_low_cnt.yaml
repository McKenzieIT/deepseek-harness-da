kind: metric
name: dws_10000251_role_common_feature_df__rolelevel_tier_low_cnt
description: 低等级(1-30)角色数,用于等级分层分布
domains:
  - 角色成长
  - 付费经济
  - 社交公会
computation:
  sql: COUNT(DISTINCT CASE WHEN CAST(rolelevel AS BIGINT) BETWEEN 1 AND 30 THEN role_id END)
  metadata:
    aggregation: count_distinct
    field: CASE WHEN CAST(rolelevel AS BIGINT) BETWEEN 1 AND 30 THEN role_id END
    source: dws_10000251_role_common_feature_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_role_common_feature_df
    description: 机械提取自 dws_10000251_role_common_feature_df 的 metrics 块（key=rolelevel_tier_low_cnt）
