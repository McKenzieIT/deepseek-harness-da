kind: metric
name: game.rogue.skipBattle__game_rogue_skipBattle_avg_role_level
description: 跳过战斗角色的平均等级
domains:
  - 战斗关卡
computation:
  sql: AVG(CAST(GET_JSON_OBJECT(params, '$.roleLevel') AS BIGINT))
  metadata:
    aggregation: avg
    field: CAST(GET_JSON_OBJECT(params, '$.roleLevel') AS BIGINT)
    source: game.rogue.skipBattle
    time_grain: ""
relations:
  - type: derived_from
    target: game.rogue.skipBattle
    description: 机械提取自 game.rogue.skipBattle 的 metrics 块（key=game_rogue_skipBattle_avg_role_level）
