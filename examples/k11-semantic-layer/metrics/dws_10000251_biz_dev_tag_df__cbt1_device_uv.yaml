kind: metric
name: dws_10000251_biz_dev_tag_df__cbt1_device_uv
description: 参与内测批次 CBT1 的设备数。
domains:
  - 用户生命周期
  - 系统监控
computation:
  sql: COUNT(DISTINCT CASE WHEN biz_name = 'CBT1' THEN user_id END)
  metadata:
    aggregation: count_distinct
    field: CASE WHEN biz_name = 'CBT1' THEN user_id END
    source: dws_10000251_biz_dev_tag_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_biz_dev_tag_df
    description: 机械提取自 dws_10000251_biz_dev_tag_df 的 metrics 块（key=cbt1_device_uv）
