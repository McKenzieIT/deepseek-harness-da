kind: metric
name: game.rogue.train__game_rogue_train_role_uv
description: 触发激活天赋的去重角色数（分析主体为 role_id）
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.rogue.train
    time_grain: ""
relations:
  - type: derived_from
    target: game.rogue.train
    description: 机械提取自 game.rogue.train 的 metrics 块（key=game_rogue_train_role_uv）
