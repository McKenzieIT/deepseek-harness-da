kind: metric
name: game.pitchpot__pitchpot_floor_clear_role_uv
description: 完成通层的去重角色数（isFloorClear=true）
domains:
  - 探索收集
computation:
  sql: COUNT(DISTINCT CASE WHEN GET_JSON_OBJECT(params, '$.isFloorClear') = 'true' THEN role_id END)
  metadata:
    aggregation: count_distinct
    field: CASE WHEN GET_JSON_OBJECT(params, '$.isFloorClear') = 'true' THEN role_id END
    source: game.pitchpot
    time_grain: ""
relations:
  - type: derived_from
    target: game.pitchpot
    description: 机械提取自 game.pitchpot 的 metrics 块（key=pitchpot_floor_clear_role_uv）
