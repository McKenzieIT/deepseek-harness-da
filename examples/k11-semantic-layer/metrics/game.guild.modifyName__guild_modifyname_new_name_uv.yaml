kind: metric
name: game.guild.modifyName__guild_modifyname_new_name_uv
description: 改名选用的去重新公会名数量
domains:
  - 社交公会
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.name'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.name')
    source: game.guild.modifyName
    time_grain: ""
relations:
  - type: derived_from
    target: game.guild.modifyName
    description: 机械提取自 game.guild.modifyName 的 metrics 块（key=guild_modifyname_new_name_uv）
