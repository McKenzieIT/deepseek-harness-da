kind: metric
name: game.battlepass.buyExp__game_battlepass_buyexp_gain_sum
description: 战令购买经验总增量（exp 字段求和）
domains:
  - 角色成长
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.exp') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.exp') AS BIGINT)
    source: game.battlepass.buyExp
    time_grain: ""
relations:
  - type: derived_from
    target: game.battlepass.buyExp
    description: 机械提取自 game.battlepass.buyExp 的 metrics 块（key=game_battlepass_buyexp_gain_sum）
