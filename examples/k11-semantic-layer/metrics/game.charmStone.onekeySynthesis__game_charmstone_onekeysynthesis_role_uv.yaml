kind: metric
name: game.charmStone.onekeySynthesis__game_charmstone_onekeysynthesis_role_uv
description: 发起一键合成的角色去重数（分析主体粒度 role_id）
domains:
  - 探索收集
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.charmStone.onekeySynthesis
    time_grain: ""
relations:
  - type: derived_from
    target: game.charmStone.onekeySynthesis
    description: 机械提取自 game.charmStone.onekeySynthesis 的 metrics 块（key=game_charmstone_onekeysynthesis_role_uv）
