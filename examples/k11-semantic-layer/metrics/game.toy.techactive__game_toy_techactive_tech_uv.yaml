kind: metric
name: game.toy.techactive__game_toy_techactive_tech_uv
description: 被激活的去重科技种类数（techId），反映科技树覆盖广度
domains:
  - 探索收集
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params,'$.techId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params,'$.techId')
    source: game.toy.techactive
    time_grain: ""
relations:
  - type: derived_from
    target: game.toy.techactive
    description: 机械提取自 game.toy.techactive 的 metrics 块（key=game_toy_techactive_tech_uv）
