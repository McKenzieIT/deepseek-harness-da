kind: metric
name: game.rogue.pointLvlup__game_rogue_pointLvlup_role_uv
description: 触发军功升级的去重角色数（分析主体）
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.rogue.pointLvlup
    time_grain: ""
relations:
  - type: derived_from
    target: game.rogue.pointLvlup
    description: 机械提取自 game.rogue.pointLvlup 的 metrics 块（key=game_rogue_pointLvlup_role_uv）
