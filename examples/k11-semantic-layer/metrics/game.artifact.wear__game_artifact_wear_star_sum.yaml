kind: metric
name: game.artifact.wear__game_artifact_wear_star_sum
description: 被穿戴兵符星级求和
domains:
  - 角色成长
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.artifact.star') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.artifact.star') AS BIGINT)
    source: game.artifact.wear
    time_grain: ""
relations:
  - type: derived_from
    target: game.artifact.wear
    description: 机械提取自 game.artifact.wear 的 metrics 块（key=game_artifact_wear_star_sum）
