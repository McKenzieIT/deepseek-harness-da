kind: metric
name: game.scriptActivity.taskBattlepassMissionReward__bp_mission_reward_mission_uv
description: 涉及的去重任务ID(missionId)数
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params,'$.missionId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params,'$.missionId')
    source: game.scriptActivity.taskBattlepassMissionReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.scriptActivity.taskBattlepassMissionReward
    description: 机械提取自 game.scriptActivity.taskBattlepassMissionReward 的 metrics 块（key=bp_mission_reward_mission_uv）
