kind: metric
name: game.train.charm__train_charm_starup_ratio
description: 灵器升星事件在总养成中的占比（衡量升星渗透率）
domains:
  - 角色成长
computation:
  sql: SUM(CASE WHEN GET_JSON_OBJECT(params,'$.action') = '1' THEN 1 ELSE 0 END) / NULLIF(COUNT(*), 0)
  metadata:
    aggregation: sum
    field: CASE WHEN GET_JSON_OBJECT(params,'$.action') = '1' THEN 1 ELSE 0 END) / NULLIF(COUNT(*), 0
    source: game.train.charm
    time_grain: ""
relations:
  - type: derived_from
    target: game.train.charm
    description: 机械提取自 game.train.charm 的 metrics 块（key=train_charm_starup_ratio）
