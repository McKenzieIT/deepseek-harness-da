kind: metric
name: dws_10000251_univ_role_server_whole_df__active_role_cnt
description: 累计活跃角色数(截至快照日出现于该快照的角色去重数;为累计口径,非当日活跃角色数)。
domains:
  - 用户生命周期
  - 付费经济
  - 角色成长
computation:
  sql: COUNT(DISTINCT user_id)
  metadata:
    aggregation: count_distinct
    field: user_id
    source: dws_10000251_univ_role_server_whole_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_univ_role_server_whole_df
    description: 机械提取自 dws_10000251_univ_role_server_whole_df 的 metrics 块（key=active_role_cnt）
