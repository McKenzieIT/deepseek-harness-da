kind: metric
name: game.knight.clearRemakeCd__knight_clear_remake_cd_account_uv
description: 触发清除CD的去重账号数
domains:
  - 角色成长
computation:
  sql: COUNT(DISTINCT account_id)
  metadata:
    aggregation: count_distinct
    field: account_id
    source: game.knight.clearRemakeCd
    time_grain: ""
relations:
  - type: derived_from
    target: game.knight.clearRemakeCd
    description: 机械提取自 game.knight.clearRemakeCd 的 metrics 块（key=knight_clear_remake_cd_account_uv）
