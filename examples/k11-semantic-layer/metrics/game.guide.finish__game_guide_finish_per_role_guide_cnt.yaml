kind: metric
name: game.guide.finish__game_guide_finish_per_role_guide_cnt
description: 人均完成引导步骤数（日志条数 / 去重角色数，NULLIF 防除零）
domains:
  - 系统监控
computation:
  sql: COUNT(*) * 1.0 / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: count
    field: "*) * 1.0 / NULLIF(COUNT(DISTINCT role_id), 0"
    source: game.guide.finish
    time_grain: ""
relations:
  - type: derived_from
    target: game.guide.finish
    description: 机械提取自 game.guide.finish 的 metrics 块（key=game_guide_finish_per_role_guide_cnt）
