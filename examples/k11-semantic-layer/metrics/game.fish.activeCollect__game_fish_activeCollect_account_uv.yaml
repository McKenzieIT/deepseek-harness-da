kind: metric
name: game.fish.activeCollect__game_fish_activeCollect_account_uv
description: 触发钓鱼图鉴激活的去重账号数
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT account_id)
  metadata:
    aggregation: count_distinct
    field: account_id
    source: game.fish.activeCollect
    time_grain: ""
relations:
  - type: derived_from
    target: game.fish.activeCollect
    description: 机械提取自 game.fish.activeCollect 的 metrics 块（key=game_fish_activeCollect_account_uv）
