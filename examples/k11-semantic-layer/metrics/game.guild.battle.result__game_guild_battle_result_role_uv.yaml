kind: metric
name: game.guild.battle.result__game_guild_battle_result_role_uv
description: 公会战结算涉及的去重角色数（subject 粒度，单日）
domains:
  - 社交公会
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.guild.battle.result
    time_grain: ""
relations:
  - type: derived_from
    target: game.guild.battle.result
    description: 机械提取自 game.guild.battle.result 的 metrics 块（key=game_guild_battle_result_role_uv）
