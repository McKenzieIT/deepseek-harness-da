kind: metric
name: game.koiDraw.changeFloor__koi_draw_change_floor_account_uv
description: 触发切换层的去重账号数
domains:
  - 探索收集
computation:
  sql: COUNT(DISTINCT account_id)
  metadata:
    aggregation: count_distinct
    field: account_id
    source: game.koiDraw.changeFloor
    time_grain: ""
relations:
  - type: derived_from
    target: game.koiDraw.changeFloor
    description: 机械提取自 game.koiDraw.changeFloor 的 metrics 块（key=koi_draw_change_floor_account_uv）
