kind: metric
name: dws_10000251_univ_acc_tag_df__ltv_f3d
description: 新增首3日 LTV(新增前3日人均累计付费金额,元;需配合 WHERE 过滤到对应新增队列)
domains:
  - 用户生命周期
  - 付费经济
computation:
  sql: SUM(pay_amt_f3d)*1.0/NULLIF(COUNT(DISTINCT user_id),0)
  metadata:
    aggregation: sum
    field: pay_amt_f3d)*1.0/NULLIF(COUNT(DISTINCT user_id),0
    source: dws_10000251_univ_acc_tag_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_univ_acc_tag_df
    description: 机械提取自 dws_10000251_univ_acc_tag_df 的 metrics 块（key=ltv_f3d）
