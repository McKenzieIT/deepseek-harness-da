kind: metric
name: game.script.unlockcity__unlockcity_city_uv
description: 被解锁的去重城池数(cityId)
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.cityId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.cityId')
    source: game.script.unlockcity
    time_grain: ""
relations:
  - type: derived_from
    target: game.script.unlockcity
    description: 机械提取自 game.script.unlockcity 的 metrics 块（key=unlockcity_city_uv）
