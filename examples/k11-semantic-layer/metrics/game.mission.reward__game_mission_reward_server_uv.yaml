kind: metric
name: game.mission.reward__game_mission_reward_server_uv
description: 领奖覆盖游戏服数
domains:
  - 角色成长
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: game.mission.reward
    time_grain: ""
relations:
  - type: derived_from
    target: game.mission.reward
    description: 机械提取自 game.mission.reward 的 metrics 块（key=game_mission_reward_server_uv）
