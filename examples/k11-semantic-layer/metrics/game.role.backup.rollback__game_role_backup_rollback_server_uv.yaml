kind: metric
name: game.role.backup.rollback__game_role_backup_rollback_server_uv
description: 涉及回退的去重游戏服数
domains:
  - 用户生命周期
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: game.role.backup.rollback
    time_grain: ""
relations:
  - type: derived_from
    target: game.role.backup.rollback
    description: 机械提取自 game.role.backup.rollback 的 metrics 块（key=game_role_backup_rollback_server_uv）
