kind: metric
name: game.guild.create__game_guild_create_role_uv
description: 创建公会的去重角色数（分析主体默认 role_id）
domains:
  - 社交公会
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.guild.create
    time_grain: ""
relations:
  - type: derived_from
    target: game.guild.create
    description: 机械提取自 game.guild.create 的 metrics 块（key=game_guild_create_role_uv）
