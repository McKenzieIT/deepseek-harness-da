kind: metric
name: dws_10000251_pve_card_statistics_di__key_card_role_uv
description: 日使用主力卡牌(is_key=true)的活跃角色数
domains:
  - 战斗关卡
  - 角色成长
computation:
  sql: COUNT(DISTINCT CASE WHEN is_key = 'true' THEN role_id END)
  metadata:
    aggregation: count_distinct
    field: CASE WHEN is_key = 'true' THEN role_id END
    source: dws_10000251_pve_card_statistics_di
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_pve_card_statistics_di
    description: 机械提取自 dws_10000251_pve_card_statistics_di 的 metrics 块（key=key_card_role_uv）
