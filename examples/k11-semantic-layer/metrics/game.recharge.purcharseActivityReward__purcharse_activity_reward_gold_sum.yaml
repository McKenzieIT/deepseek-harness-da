kind: metric
name: game.recharge.purcharseActivityReward__purcharse_activity_reward_gold_sum
description: 通过该活动下发的充值钻石(coinList.gold)总量
domains:
  - 付费经济
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.coinList.gold') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.coinList.gold') AS BIGINT)
    source: game.recharge.purcharseActivityReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.recharge.purcharseActivityReward
    description: 机械提取自 game.recharge.purcharseActivityReward 的 metrics 块（key=purcharse_activity_reward_gold_sum）
