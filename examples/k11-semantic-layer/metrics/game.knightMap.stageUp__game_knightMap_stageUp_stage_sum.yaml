kind: metric
name: game.knightMap.stageUp__game_knightMap_stageUp_stage_sum
description: 武将云图升阶段累计提升的 stage 值总和
domains:
  - 自定义
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params,'$.stage') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params,'$.stage') AS BIGINT)
    source: game.knightMap.stageUp
    time_grain: ""
relations:
  - type: derived_from
    target: game.knightMap.stageUp
    description: 机械提取自 game.knightMap.stageUp 的 metrics 块（key=game_knightMap_stageUp_stage_sum）
