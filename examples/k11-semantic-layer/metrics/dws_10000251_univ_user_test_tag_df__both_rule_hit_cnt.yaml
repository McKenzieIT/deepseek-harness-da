kind: metric
name: dws_10000251_univ_user_test_tag_df__both_rule_hit_cnt
description: 同时命中原生与扩展规则的主体数（跨全部 user_type）
domains:
  - 用户生命周期
  - 自定义
computation:
  sql: SUM(CASE WHEN rule1 = 1 AND rule2 = 1 THEN 1 ELSE 0 END)
  metadata:
    aggregation: sum
    field: CASE WHEN rule1 = 1 AND rule2 = 1 THEN 1 ELSE 0 END
    source: dws_10000251_univ_user_test_tag_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_univ_user_test_tag_df
    description: 机械提取自 dws_10000251_univ_user_test_tag_df 的 metrics 块（key=both_rule_hit_cnt）
