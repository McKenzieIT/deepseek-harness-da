kind: metric
name: game.battlepassDungeon.reward__battlepass_dungeon_reward_order_uv
description: 命中的战令order种类数(去重 order)
domains:
  - 角色成长
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.order'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.order')
    source: game.battlepassDungeon.reward
    time_grain: ""
relations:
  - type: derived_from
    target: game.battlepassDungeon.reward
    description: 机械提取自 game.battlepassDungeon.reward 的 metrics 块（key=battlepass_dungeon_reward_order_uv）
