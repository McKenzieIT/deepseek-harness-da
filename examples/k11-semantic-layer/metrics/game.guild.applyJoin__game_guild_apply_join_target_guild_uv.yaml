kind: metric
name: game.guild.applyJoin__game_guild_apply_join_target_guild_uv
description: 被申请的去重目标公会数（取自 params.targetGuildId）
domains:
  - 社交公会
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.targetGuildId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.targetGuildId')
    source: game.guild.applyJoin
    time_grain: ""
relations:
  - type: derived_from
    target: game.guild.applyJoin
    description: 机械提取自 game.guild.applyJoin 的 metrics 块（key=game_guild_apply_join_target_guild_uv）
