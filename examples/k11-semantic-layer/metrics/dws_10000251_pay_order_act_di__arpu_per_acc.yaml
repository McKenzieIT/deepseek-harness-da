kind: metric
name: dws_10000251_pay_order_act_di__arpu_per_acc
description: 账号 ARPU(元/账号，金额合计/支付账号数)
domains:
  - 付费经济
  - 用户生命周期
computation:
  sql: SUM(pay_amt) / NULLIF(COUNT(DISTINCT account_id), 0)
  metadata:
    aggregation: sum
    field: pay_amt) / NULLIF(COUNT(DISTINCT account_id), 0
    source: dws_10000251_pay_order_act_di
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_pay_order_act_di
    description: 机械提取自 dws_10000251_pay_order_act_di 的 metrics 块（key=arpu_per_acc）
