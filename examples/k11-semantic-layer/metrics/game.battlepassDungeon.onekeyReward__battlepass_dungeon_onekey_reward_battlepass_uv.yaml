kind: metric
name: game.battlepassDungeon.onekeyReward__battlepass_dungeon_onekey_reward_battlepass_uv
description: 命中的战令id种类数(去重 battlepassId，反映一键领取跨越的战令档位范围)
domains:
  - 角色成长
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.battlepassId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.battlepassId')
    source: game.battlepassDungeon.onekeyReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.battlepassDungeon.onekeyReward
    description: 机械提取自 game.battlepassDungeon.onekeyReward 的 metrics 块（key=battlepass_dungeon_onekey_reward_battlepass_uv）
