kind: metric
name: game.welfare.sign__game_welfare_sign_role_uv
description: 签到角色去重数（分析主体 role_id）
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.welfare.sign
    time_grain: ""
relations:
  - type: derived_from
    target: game.welfare.sign
    description: 机械提取自 game.welfare.sign 的 metrics 块（key=game_welfare_sign_role_uv）
