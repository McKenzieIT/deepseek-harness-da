kind: metric
name: game.customLevel.cancelMatch__game_customLevel_cancelMatch_role_uv
description: 演武场取消匹配的去重角色数，主体粒度为 role_id
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.customLevel.cancelMatch
    time_grain: ""
relations:
  - type: derived_from
    target: game.customLevel.cancelMatch
    description: 机械提取自 game.customLevel.cancelMatch 的 metrics 块（key=game_customLevel_cancelMatch_role_uv）
