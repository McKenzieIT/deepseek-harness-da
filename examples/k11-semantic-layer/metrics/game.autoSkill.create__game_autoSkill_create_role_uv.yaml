kind: metric
name: game.autoSkill.create__game_autoSkill_create_role_uv
description: 创建武将方案的去重角色数（主体粒度 role_id）
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.autoSkill.create
    time_grain: ""
relations:
  - type: derived_from
    target: game.autoSkill.create
    description: 机械提取自 game.autoSkill.create 的 metrics 块（key=game_autoSkill_create_role_uv）
