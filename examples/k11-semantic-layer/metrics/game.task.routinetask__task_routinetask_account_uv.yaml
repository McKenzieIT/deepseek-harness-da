kind: metric
name: game.task.routinetask__task_routinetask_account_uv
description: 触发日常任务的去重账号数
domains:
  - 角色成长
computation:
  sql: COUNT(DISTINCT account_id)
  metadata:
    aggregation: count_distinct
    field: account_id
    source: game.task.routinetask
    time_grain: ""
relations:
  - type: derived_from
    target: game.task.routinetask
    description: 机械提取自 game.task.routinetask 的 metrics 块（key=task_routinetask_account_uv）
