kind: metric
name: game.duel.start__game_duel_start_per_role_attempts
description: 平均每个参与角色的切磋发起次数(人次/角色UV)
domains:
  - 战斗关卡
computation:
  sql: COUNT(*) * 1.0 / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: count
    field: "*) * 1.0 / NULLIF(COUNT(DISTINCT role_id), 0"
    source: game.duel.start
    time_grain: ""
relations:
  - type: derived_from
    target: game.duel.start
    description: 机械提取自 game.duel.start 的 metrics 块（key=game_duel_start_per_role_attempts）
