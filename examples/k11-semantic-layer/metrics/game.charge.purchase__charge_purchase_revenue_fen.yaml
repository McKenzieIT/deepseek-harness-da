kind: metric
name: game.charge.purchase__charge_purchase_revenue_fen
description: 充值营收合计(单位分，除以100转元)；本事件全部为付费购买，无现金券分档
domains:
  - 付费经济
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.money') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.money') AS BIGINT)
    source: game.charge.purchase
    time_grain: ""
relations:
  - type: derived_from
    target: game.charge.purchase
    description: 机械提取自 game.charge.purchase 的 metrics 块（key=charge_purchase_revenue_fen）
