kind: metric
name: dws_10000251_univ_role_server_tag_df__paid_role_share
description: 付费角色占比（付费角色数/角色总数，付费渗透率，NULLIF 防除零）
domains:
  - 用户生命周期
  - 付费经济
  - 角色成长
computation:
  sql: COUNT(DISTINCT CASE WHEN pay_amt_std>0 THEN user_id END)*1.0/NULLIF(COUNT(DISTINCT user_id),0)
  metadata:
    aggregation: count_distinct
    field: CASE WHEN pay_amt_std>0 THEN user_id END)*1.0/NULLIF(COUNT(DISTINCT user_id),0
    source: dws_10000251_univ_role_server_tag_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_univ_role_server_tag_df
    description: 机械提取自 dws_10000251_univ_role_server_tag_df 的 metrics 块（key=paid_role_share）
