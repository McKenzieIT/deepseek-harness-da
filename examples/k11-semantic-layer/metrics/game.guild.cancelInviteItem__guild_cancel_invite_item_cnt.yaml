kind: metric
name: game.guild.cancelInviteItem__guild_cancel_invite_item_cnt
description: 取消公会邀请条目事件触发次数(每次取消邀请条目记1条)
domains:
  - 社交公会
computation:
  sql: COUNT(*)
  metadata:
    aggregation: count
    field: "*"
    source: game.guild.cancelInviteItem
    time_grain: ""
relations:
  - type: derived_from
    target: game.guild.cancelInviteItem
    description: 机械提取自 game.guild.cancelInviteItem 的 metrics 块（key=guild_cancel_invite_item_cnt）
