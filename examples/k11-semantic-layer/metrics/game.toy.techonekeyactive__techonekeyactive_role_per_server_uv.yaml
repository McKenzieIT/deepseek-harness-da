kind: metric
name: game.toy.techonekeyactive__techonekeyactive_role_per_server_uv
description: 平均每服参与一键科技激活的角色数（用于评估功能在不同服务器的渗透，分母用 NULLIF 防零）
domains:
  - 探索收集
computation:
  sql: ROUND(COUNT(DISTINCT role_id) * 1.0 / NULLIF(COUNT(DISTINCT server_id), 0), 2)
  metadata:
    aggregation: ""
    field: ""
    source: game.toy.techonekeyactive
    time_grain: ""
relations:
  - type: derived_from
    target: game.toy.techonekeyactive
    description: 机械提取自 game.toy.techonekeyactive 的 metrics 块（key=techonekeyactive_role_per_server_uv）
