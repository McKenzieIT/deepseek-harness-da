kind: metric
name: game.scriptActivity.taskBattlepassMissionReward__bp_mission_reward_theme_uv
description: 涉及的去重战令活动ID(themeId)数
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params,'$.themeId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params,'$.themeId')
    source: game.scriptActivity.taskBattlepassMissionReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.scriptActivity.taskBattlepassMissionReward
    description: 机械提取自 game.scriptActivity.taskBattlepassMissionReward 的 metrics 块（key=bp_mission_reward_theme_uv）
