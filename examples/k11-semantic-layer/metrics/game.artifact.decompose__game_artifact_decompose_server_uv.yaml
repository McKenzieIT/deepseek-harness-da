kind: metric
name: game.artifact.decompose__game_artifact_decompose_server_uv
description: 发生兵符分解的去重游戏服数
domains:
  - 角色成长
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: game.artifact.decompose
    time_grain: ""
relations:
  - type: derived_from
    target: game.artifact.decompose
    description: 机械提取自 game.artifact.decompose 的 metrics 块（key=game_artifact_decompose_server_uv）
