kind: metric
name: game.achievement.onekeyReward__achievement_onekey_reward_account_uv
description: 领取成就奖励的去重账号数
domains:
  - 角色成长
computation:
  sql: COUNT(DISTINCT account_id)
  metadata:
    aggregation: count_distinct
    field: account_id
    source: game.achievement.onekeyReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.achievement.onekeyReward
    description: 机械提取自 game.achievement.onekeyReward 的 metrics 块（key=achievement_onekey_reward_account_uv）
