kind: metric
name: game.fish.compose__game_fish_compose_count_sum
description: 钓鱼合成产出数量总和(按 params.count 字段聚合)
domains:
  - 自定义
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params,'$.count') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params,'$.count') AS BIGINT)
    source: game.fish.compose
    time_grain: ""
relations:
  - type: derived_from
    target: game.fish.compose
    description: 机械提取自 game.fish.compose 的 metrics 块（key=game_fish_compose_count_sum）
