kind: metric
name: dws_10000251_univ_role_churn_di__churn_pay_role_uv
description: 流失角色中的付费角色数。实测 ds=20260730 得 972。
domains:
  - 用户生命周期
  - 付费经济
computation:
  sql: COUNT(DISTINCT CASE WHEN pay_amt_std > 0 THEN user_id END)
  metadata:
    aggregation: count_distinct
    field: CASE WHEN pay_amt_std > 0 THEN user_id END
    source: dws_10000251_univ_role_churn_di
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_univ_role_churn_di
    description: 机械提取自 dws_10000251_univ_role_churn_di 的 metrics 块（key=churn_pay_role_uv）
