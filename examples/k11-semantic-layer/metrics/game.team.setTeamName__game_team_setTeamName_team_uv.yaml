kind: metric
name: game.team.setTeamName__game_team_setTeamName_team_uv
description: 被命名的去重阵容数（按 teamId）
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.teamId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.teamId')
    source: game.team.setTeamName
    time_grain: ""
relations:
  - type: derived_from
    target: game.team.setTeamName
    description: 机械提取自 game.team.setTeamName 的 metrics 块（key=game_team_setTeamName_team_uv）
