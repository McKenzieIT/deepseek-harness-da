kind: metric
name: game.script.fetchhangup__script_fetchhangup_script_uv
description: 领取挂机奖励的去重剧本(scriptId)数
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params,'$.scriptId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params,'$.scriptId')
    source: game.script.fetchhangup
    time_grain: ""
relations:
  - type: derived_from
    target: game.script.fetchhangup
    description: 机械提取自 game.script.fetchhangup 的 metrics 块（key=script_fetchhangup_script_uv）
