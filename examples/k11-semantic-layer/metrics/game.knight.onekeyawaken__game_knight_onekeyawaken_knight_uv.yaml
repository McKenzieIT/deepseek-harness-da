kind: metric
name: game.knight.onekeyawaken__game_knight_onekeyawaken_knight_uv
description: 被一键觉醒的去重武将数(按 params.knightId)
domains:
  - 角色成长
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.knightId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.knightId')
    source: game.knight.onekeyawaken
    time_grain: ""
relations:
  - type: derived_from
    target: game.knight.onekeyawaken
    description: 机械提取自 game.knight.onekeyawaken 的 metrics 块（key=game_knight_onekeyawaken_knight_uv）
