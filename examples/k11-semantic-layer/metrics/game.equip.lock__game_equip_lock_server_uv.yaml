kind: metric
name: game.equip.lock__game_equip_lock_server_uv
description: 触发装备锁定/解锁操作的去重游戏服数
domains:
  - 装备道具
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: game.equip.lock
    time_grain: ""
relations:
  - type: derived_from
    target: game.equip.lock
    description: 机械提取自 game.equip.lock 的 metrics 块（key=game_equip_lock_server_uv）
