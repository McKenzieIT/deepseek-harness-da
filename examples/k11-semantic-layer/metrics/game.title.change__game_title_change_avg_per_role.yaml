kind: metric
name: game.title.change__game_title_change_avg_per_role
description: 人均更换次数 = 总次数 / 去重角色数
domains:
  - 角色成长
computation:
  sql: ROUND(COUNT(*) / NULLIF(COUNT(DISTINCT role_id), 0), 2)
  metadata:
    aggregation: ""
    field: ""
    source: game.title.change
    time_grain: ""
relations:
  - type: derived_from
    target: game.title.change
    description: 机械提取自 game.title.change 的 metrics 块（key=game_title_change_avg_per_role）
