kind: metric
name: dws_10000251_finance_pay_order_di__first_order_tm
description: 最早成功支付时间,可用于计算账号/角色首单时间(按主体分组后取 MIN)
domains:
  - 付费经济
  - 用户生命周期
computation:
  sql: MIN(CASE WHEN status=1 THEN success_tm END)
  metadata:
    aggregation: min
    field: CASE WHEN status=1 THEN success_tm END
    source: dws_10000251_finance_pay_order_di
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_finance_pay_order_di
    description: 机械提取自 dws_10000251_finance_pay_order_di 的 metrics 块（key=first_order_tm）
