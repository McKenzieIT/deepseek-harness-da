kind: metric
name: game.knightnatalchart.unlockscheme__game_knightnatalchart_unlockscheme_force_sum
description: 参与解锁方案角色的战力合计（取自 params.roleForce，用于衡量解锁群体的战力规模）
domains:
  - 角色成长
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.roleForce') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.roleForce') AS BIGINT)
    source: game.knightnatalchart.unlockscheme
    time_grain: ""
relations:
  - type: derived_from
    target: game.knightnatalchart.unlockscheme
    description: 机械提取自 game.knightnatalchart.unlockscheme 的 metrics 块（key=game_knightnatalchart_unlockscheme_force_sum）
