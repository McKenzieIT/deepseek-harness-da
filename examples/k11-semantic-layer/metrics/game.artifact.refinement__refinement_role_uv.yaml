kind: metric
name: game.artifact.refinement__refinement_role_uv
description: 参与精炼的去重角色数（分析主体）
domains:
  - 角色成长
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.artifact.refinement
    time_grain: ""
relations:
  - type: derived_from
    target: game.artifact.refinement
    description: 机械提取自 game.artifact.refinement 的 metrics 块（key=refinement_role_uv）
