kind: metric
name: game.gmActivity.foreverTestMission__game_gmActivity_foreverTestMission_mission_uv
description: 触发的去重 missionId 任务数
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params,'$.missionId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params,'$.missionId')
    source: game.gmActivity.foreverTestMission
    time_grain: ""
relations:
  - type: derived_from
    target: game.gmActivity.foreverTestMission
    description: 机械提取自 game.gmActivity.foreverTestMission 的 metrics 块（key=game_gmActivity_foreverTestMission_mission_uv）
