kind: metric
name: game.rogue.exit__rogue_exit_server_uv
description: 古战场退出覆盖的去重游戏服数
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: game.rogue.exit
    time_grain: ""
relations:
  - type: derived_from
    target: game.rogue.exit
    description: 机械提取自 game.rogue.exit 的 metrics 块（key=rogue_exit_server_uv）
