kind: metric
name: game.war.activityEnterPerformDungeon__game_war_activity_enter_perform_dungeon_cnt
description: 进入剧情表演副本事件总记录数（进入次数）
domains:
  - 战斗关卡
computation:
  sql: COUNT(*)
  metadata:
    aggregation: count
    field: "*"
    source: game.war.activityEnterPerformDungeon
    time_grain: ""
relations:
  - type: derived_from
    target: game.war.activityEnterPerformDungeon
    description: 机械提取自 game.war.activityEnterPerformDungeon 的 metrics 块（key=game_war_activity_enter_perform_dungeon_cnt）
