kind: metric
name: game.scriptActivity.taskBattlepassReward__bp_reward_cnt
description: 战令奖励一键领取总次数
domains:
  - 自定义
computation:
  sql: COUNT(*)
  metadata:
    aggregation: count
    field: "*"
    source: game.scriptActivity.taskBattlepassReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.scriptActivity.taskBattlepassReward
    description: 机械提取自 game.scriptActivity.taskBattlepassReward 的 metrics 块（key=bp_reward_cnt）
