kind: metric
name: game.guildLuckyBag.send__guild_lucky_bag_send_server_uv
description: 发送福袋的去重服务器数
domains:
  - 社交公会
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: game.guildLuckyBag.send
    time_grain: ""
relations:
  - type: derived_from
    target: game.guildLuckyBag.send
    description: 机械提取自 game.guildLuckyBag.send 的 metrics 块（key=guild_lucky_bag_send_server_uv）
