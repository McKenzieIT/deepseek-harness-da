kind: metric
name: game.anniversary.fetchBlankFloorReward__fetchBlankFloorReward_energy_sum
description: 发放的体力(energy)总量
domains:
  - 付费经济
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params,'$.coinList.energy') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params,'$.coinList.energy') AS BIGINT)
    source: game.anniversary.fetchBlankFloorReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.anniversary.fetchBlankFloorReward
    description: 机械提取自 game.anniversary.fetchBlankFloorReward 的 metrics 块（key=fetchBlankFloorReward_energy_sum）
