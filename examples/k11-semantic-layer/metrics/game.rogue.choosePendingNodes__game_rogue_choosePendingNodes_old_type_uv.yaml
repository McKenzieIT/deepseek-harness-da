kind: metric
name: game.rogue.choosePendingNodes__game_rogue_choosePendingNodes_old_type_uv
description: 替换前节点类型(oldType)的去重种类数
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.oldType'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.oldType')
    source: game.rogue.choosePendingNodes
    time_grain: ""
relations:
  - type: derived_from
    target: game.rogue.choosePendingNodes
    description: 机械提取自 game.rogue.choosePendingNodes 的 metrics 块（key=game_rogue_choosePendingNodes_old_type_uv）
