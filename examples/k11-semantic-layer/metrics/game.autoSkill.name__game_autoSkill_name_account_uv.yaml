kind: metric
name: game.autoSkill.name__game_autoSkill_name_account_uv
description: 触发方案重命名的去重账号数（ds=20260729 实测 24）
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT account_id)
  metadata:
    aggregation: count_distinct
    field: account_id
    source: game.autoSkill.name
    time_grain: ""
relations:
  - type: derived_from
    target: game.autoSkill.name
    description: 机械提取自 game.autoSkill.name 的 metrics 块（key=game_autoSkill_name_account_uv）
