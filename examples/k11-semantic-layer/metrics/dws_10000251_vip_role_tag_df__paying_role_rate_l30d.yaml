kind: metric
name: dws_10000251_vip_role_tag_df__paying_role_rate_l30d
description: 近30天付费角色占比，L30D付费渗透率
domains:
  - 付费经济
  - 角色成长
computation:
  sql: SUM(CASE WHEN pay_amt_l30d > 0 THEN 1 ELSE 0 END) / NULLIF(COUNT(*), 0)
  metadata:
    aggregation: sum
    field: CASE WHEN pay_amt_l30d > 0 THEN 1 ELSE 0 END) / NULLIF(COUNT(*), 0
    source: dws_10000251_vip_role_tag_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_vip_role_tag_df
    description: 机械提取自 dws_10000251_vip_role_tag_df 的 metrics 块（key=paying_role_rate_l30d）
