kind: metric
name: game.social.guild.boss__game_social_guild_boss_role_per_account
description: 平均每账号挑战角色数(独立角色/独立账号；衡量多角色账号占比)
domains:
  - 社交公会
computation:
  sql: COUNT(DISTINCT role_id) / NULLIF(COUNT(DISTINCT account_id), 0)
  metadata:
    aggregation: count_distinct
    field: role_id) / NULLIF(COUNT(DISTINCT account_id), 0
    source: game.social.guild.boss
    time_grain: ""
relations:
  - type: derived_from
    target: game.social.guild.boss
    description: 机械提取自 game.social.guild.boss 的 metrics 块（key=game_social_guild_boss_role_per_account）
