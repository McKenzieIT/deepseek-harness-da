kind: metric
name: dws_10000251_play_story_act_di__skip_rate
description: 每日剧情跳过率 = 跳过次数/访问次数（分母0返回NULL）。
domains:
  - 角色成长
  - 探索收集
computation:
  sql: SUM(skip_pv) * 1.0 / NULLIF(SUM(visit_pv), 0)
  metadata:
    aggregation: sum
    field: skip_pv) * 1.0 / NULLIF(SUM(visit_pv), 0
    source: dws_10000251_play_story_act_di
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_play_story_act_di
    description: 机械提取自 dws_10000251_play_story_act_di 的 metrics 块（key=skip_rate）
