kind: metric
name: game.role.snapshot__game_role_snapshot_cnt
description: 在线角色快照总记录数
domains:
  - 用户生命周期
computation:
  sql: COUNT(*)
  metadata:
    aggregation: count
    field: "*"
    source: game.role.snapshot
    time_grain: ""
relations:
  - type: derived_from
    target: game.role.snapshot
    description: 机械提取自 game.role.snapshot 的 metrics 块（key=game_role_snapshot_cnt）
