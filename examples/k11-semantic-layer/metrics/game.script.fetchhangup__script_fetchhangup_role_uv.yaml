kind: metric
name: game.script.fetchhangup__script_fetchhangup_role_uv
description: 领取挂机奖励的去重角色数（分析主体）
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.script.fetchhangup
    time_grain: ""
relations:
  - type: derived_from
    target: game.script.fetchhangup
    description: 机械提取自 game.script.fetchhangup 的 metrics 块（key=script_fetchhangup_role_uv）
