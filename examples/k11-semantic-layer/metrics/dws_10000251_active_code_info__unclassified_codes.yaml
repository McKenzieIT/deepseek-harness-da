kind: metric
name: dws_10000251_active_code_info__unclassified_codes
description: 未分类（type 为空）的激活码数，数据质量监控
domains:
  - 资源产销
computation:
  sql: SUM(CASE WHEN type IS NULL THEN 1 ELSE 0 END)
  metadata:
    aggregation: sum
    field: CASE WHEN type IS NULL THEN 1 ELSE 0 END
    source: dws_10000251_active_code_info
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_active_code_info
    description: 机械提取自 dws_10000251_active_code_info 的 metrics 块（key=unclassified_codes）
