kind: metric
name: game.scriptActivity.taskBattlepassReward__bp_reward_account_uv
description: 领取战令奖励的去重账号数
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT account_id)
  metadata:
    aggregation: count_distinct
    field: account_id
    source: game.scriptActivity.taskBattlepassReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.scriptActivity.taskBattlepassReward
    description: 机械提取自 game.scriptActivity.taskBattlepassReward 的 metrics 块（key=bp_reward_account_uv）
