kind: metric
name: game.gacha.switchSpecial__game_gacha_switchSpecial_account_uv
description: 切换新手集市卡池的账号去重数
domains:
  - 探索收集
computation:
  sql: COUNT(DISTINCT account_id)
  metadata:
    aggregation: count_distinct
    field: account_id
    source: game.gacha.switchSpecial
    time_grain: ""
relations:
  - type: derived_from
    target: game.gacha.switchSpecial
    description: 机械提取自 game.gacha.switchSpecial 的 metrics 块（key=game_gacha_switchSpecial_account_uv）
