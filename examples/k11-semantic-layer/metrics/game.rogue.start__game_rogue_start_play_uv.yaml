kind: metric
name: game.rogue.start__game_rogue_start_play_uv
description: 古战场挑战对局去重数（playId 标识一次副本挑战）
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params,'$.playId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params,'$.playId')
    source: game.rogue.start
    time_grain: ""
relations:
  - type: derived_from
    target: game.rogue.start
    description: 机械提取自 game.rogue.start 的 metrics 块（key=game_rogue_start_play_uv）
