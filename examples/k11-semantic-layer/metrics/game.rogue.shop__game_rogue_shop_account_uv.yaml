kind: metric
name: game.rogue.shop__game_rogue_shop_account_uv
description: 触发商店操作的去重账号数
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT account_id)
  metadata:
    aggregation: count_distinct
    field: account_id
    source: game.rogue.shop
    time_grain: ""
relations:
  - type: derived_from
    target: game.rogue.shop
    description: 机械提取自 game.rogue.shop 的 metrics 块（key=game_rogue_shop_account_uv）
