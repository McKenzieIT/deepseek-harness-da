kind: metric
name: game.guild.resourcechange__game_guild_resourcechange_guild_uv
description: 发生资源变更的去重公会数
domains:
  - 社交公会
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params,'$.guildId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params,'$.guildId')
    source: game.guild.resourcechange
    time_grain: ""
relations:
  - type: derived_from
    target: game.guild.resourcechange
    description: 机械提取自 game.guild.resourcechange 的 metrics 块（key=game_guild_resourcechange_guild_uv）
