kind: metric
name: game.social.guild.getmissionbox__game_social_guild_getmissionbox_reward_id_uv
description: 涉及的宝箱奖励类型数（去重 rewardId；实测 rewardId 100%非空、ds=20260729 共5档；注：埋点声明为 boxId 但日志实际为 rewardId）
domains:
  - 社交公会
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.rewardId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.rewardId')
    source: game.social.guild.getmissionbox
    time_grain: ""
relations:
  - type: derived_from
    target: game.social.guild.getmissionbox
    description: 机械提取自 game.social.guild.getmissionbox 的 metrics 块（key=game_social_guild_getmissionbox_reward_id_uv）
