kind: metric
name: game.battlepass.onekeyMissionReward__onekey_mission_reward_bp_exp_sum
description: 一次领奖获得的战令经验总量
domains:
  - 角色成长
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params,'$.battlePassExp') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params,'$.battlePassExp') AS BIGINT)
    source: game.battlepass.onekeyMissionReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.battlepass.onekeyMissionReward
    description: 机械提取自 game.battlepass.onekeyMissionReward 的 metrics 块（key=onekey_mission_reward_bp_exp_sum）
