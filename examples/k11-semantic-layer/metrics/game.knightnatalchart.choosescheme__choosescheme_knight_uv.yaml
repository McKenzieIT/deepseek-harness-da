kind: metric
name: game.knightnatalchart.choosescheme__choosescheme_knight_uv
description: 被选择过命盘方案的武将种类数（去重 params.knightId）
domains:
  - 角色成长
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.knightId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.knightId')
    source: game.knightnatalchart.choosescheme
    time_grain: ""
relations:
  - type: derived_from
    target: game.knightnatalchart.choosescheme
    description: 机械提取自 game.knightnatalchart.choosescheme 的 metrics 块（key=choosescheme_knight_uv）
