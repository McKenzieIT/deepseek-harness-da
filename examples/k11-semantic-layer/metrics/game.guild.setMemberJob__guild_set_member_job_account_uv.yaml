kind: metric
name: game.guild.setMemberJob__guild_set_member_job_account_uv
description: 操作者（任命方）去重账号数
domains:
  - 社交公会
computation:
  sql: COUNT(DISTINCT account_id)
  metadata:
    aggregation: count_distinct
    field: account_id
    source: game.guild.setMemberJob
    time_grain: ""
relations:
  - type: derived_from
    target: game.guild.setMemberJob
    description: 机械提取自 game.guild.setMemberJob 的 metrics 块（key=guild_set_member_job_account_uv）
