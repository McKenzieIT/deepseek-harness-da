kind: metric
name: game.clientValue.set__game_client_value_set_value_max
description: 当日设置的最大 value（进度计数器场景下达到的最高进度）
domains:
  - 系统监控
computation:
  sql: MAX(CAST(GET_JSON_OBJECT(params, '$.value') AS BIGINT))
  metadata:
    aggregation: max
    field: CAST(GET_JSON_OBJECT(params, '$.value') AS BIGINT)
    source: game.clientValue.set
    time_grain: ""
relations:
  - type: derived_from
    target: game.clientValue.set
    description: 机械提取自 game.clientValue.set 的 metrics 块（key=game_client_value_set_value_max）
