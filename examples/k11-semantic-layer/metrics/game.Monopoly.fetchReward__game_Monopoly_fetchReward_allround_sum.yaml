kind: metric
name: game.Monopoly.fetchReward__game_Monopoly_fetchReward_allround_sum
description: 大富翁玩法领取排行榜总计奖励累计量
domains:
  - 自定义
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params,'$.allRound') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params,'$.allRound') AS BIGINT)
    source: game.Monopoly.fetchReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.Monopoly.fetchReward
    description: 机械提取自 game.Monopoly.fetchReward 的 metrics 块（key=game_Monopoly_fetchReward_allround_sum）
