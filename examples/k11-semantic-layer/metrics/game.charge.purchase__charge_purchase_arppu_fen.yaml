kind: metric
name: game.charge.purchase__charge_purchase_arppu_fen
description: ARPPU(分/角色，营收/独立角色去重数；单位分，除以100转元)
domains:
  - 付费经济
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.money') AS BIGINT)) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.money') AS BIGINT)) / NULLIF(COUNT(DISTINCT role_id), 0
    source: game.charge.purchase
    time_grain: ""
relations:
  - type: derived_from
    target: game.charge.purchase
    description: 机械提取自 game.charge.purchase 的 metrics 块（key=charge_purchase_arppu_fen）
