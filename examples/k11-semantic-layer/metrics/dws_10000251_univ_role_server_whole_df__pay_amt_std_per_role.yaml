kind: metric
name: dws_10000251_univ_role_server_whole_df__pay_amt_std_per_role
description: 角色人均累计付费(元)=角色累计付费总额/角色去重数,除零保护。
domains:
  - 用户生命周期
  - 付费经济
  - 角色成长
computation:
  sql: SUM(pay_amt_std)*1.0/NULLIF(COUNT(DISTINCT user_id),0)
  metadata:
    aggregation: sum
    field: pay_amt_std)*1.0/NULLIF(COUNT(DISTINCT user_id),0
    source: dws_10000251_univ_role_server_whole_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_univ_role_server_whole_df
    description: 机械提取自 dws_10000251_univ_role_server_whole_df 的 metrics 块（key=pay_amt_std_per_role）
