kind: metric
name: game.team.share__team_share_world_uv
description: 分享到世界频道的去重角色数
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT CASE WHEN GET_JSON_OBJECT(params,'$.shareWorld')='true' THEN role_id END)
  metadata:
    aggregation: count_distinct
    field: CASE WHEN GET_JSON_OBJECT(params,'$.shareWorld')='true' THEN role_id END
    source: game.team.share
    time_grain: ""
relations:
  - type: derived_from
    target: game.team.share
    description: 机械提取自 game.team.share 的 metrics 块（key=team_share_world_uv）
