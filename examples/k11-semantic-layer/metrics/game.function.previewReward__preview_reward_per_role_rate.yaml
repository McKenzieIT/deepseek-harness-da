kind: metric
name: game.function.previewReward__preview_reward_per_role_rate
description: 人均领取次数（总次数 / 去重角色数）
domains:
  - 系统监控
computation:
  sql: COUNT(*) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: count
    field: "*) / NULLIF(COUNT(DISTINCT role_id), 0"
    source: game.function.previewReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.function.previewReward
    description: 机械提取自 game.function.previewReward 的 metrics 块（key=preview_reward_per_role_rate）
