kind: metric
name: dws_10000251_univ_role_summary_di__active_rate
description: 活跃角色占比，当日活跃角色 UV / 角色 UV，衡量角色活跃度
domains:
  - 用户生命周期
computation:
  sql: COUNT(DISTINCT CASE WHEN act=1 THEN user_id END) / NULLIF(COUNT(DISTINCT user_id),0)
  metadata:
    aggregation: count_distinct
    field: CASE WHEN act=1 THEN user_id END) / NULLIF(COUNT(DISTINCT user_id),0
    source: dws_10000251_univ_role_summary_di
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_univ_role_summary_di
    description: 机械提取自 dws_10000251_univ_role_summary_di 的 metrics 块（key=active_rate）
