kind: metric
name: game.task.routinetask__task_routinetask_avg_per_role
description: 人均任务日志条数(总行数/去重角色数，NULLIF 防除零)
domains:
  - 角色成长
computation:
  sql: ROUND(COUNT(*) * 1.0 / NULLIF(COUNT(DISTINCT role_id), 0), 3)
  metadata:
    aggregation: ""
    field: ""
    source: game.task.routinetask
    time_grain: ""
relations:
  - type: derived_from
    target: game.task.routinetask
    description: 机械提取自 game.task.routinetask 的 metrics 块（key=task_routinetask_avg_per_role）
