kind: metric
name: game.equip.lock__game_equip_lock_lock_role_uv
description: 执行过锁定的去重角色数
domains:
  - 装备道具
computation:
  sql: COUNT(DISTINCT CASE WHEN CAST(GET_JSON_OBJECT(params,'$.lock') AS BIGINT)=1 THEN role_id END)
  metadata:
    aggregation: count_distinct
    field: CASE WHEN CAST(GET_JSON_OBJECT(params,'$.lock') AS BIGINT)=1 THEN role_id END
    source: game.equip.lock
    time_grain: ""
relations:
  - type: derived_from
    target: game.equip.lock
    description: 机械提取自 game.equip.lock 的 metrics 块（key=game_equip_lock_lock_role_uv）
