kind: metric
name: dws_10000251_selfhelp_new_df__daily_pay_amt_total
description: 当日新用户付费总额（元），从 act_vals kv 解析 pay_amt 求和
domains:
  - 用户生命周期
  - 付费经济
computation:
  sql: SUM(CAST(SPLIT_PART(SPLIT_PART(act_vals, 'pay_amt=', 2), '`', 1) AS DOUBLE))
  metadata:
    aggregation: sum
    field: CAST(SPLIT_PART(SPLIT_PART(act_vals, 'pay_amt=', 2), '`', 1) AS DOUBLE)
    source: dws_10000251_selfhelp_new_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_selfhelp_new_df
    description: 机械提取自 dws_10000251_selfhelp_new_df 的 metrics 块（key=daily_pay_amt_total）
