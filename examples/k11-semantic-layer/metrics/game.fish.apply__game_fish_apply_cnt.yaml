kind: metric
name: game.fish.apply__game_fish_apply_cnt
description: 钓鱼申请总次数（去重前）
domains:
  - 自定义
computation:
  sql: COUNT(*)
  metadata:
    aggregation: count
    field: "*"
    source: game.fish.apply
    time_grain: ""
relations:
  - type: derived_from
    target: game.fish.apply
    description: 机械提取自 game.fish.apply 的 metrics 块（key=game_fish_apply_cnt）
