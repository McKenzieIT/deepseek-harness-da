kind: metric
name: dws_10000251_vip_acc_tag_df__paying_uv_l30d
description: 最近30天有付费金额的账号数（快照口径）
domains:
  - 付费经济
  - 用户生命周期
computation:
  sql: COUNT(DISTINCT CASE WHEN pay_amt_l30d > 0 THEN user_id END)
  metadata:
    aggregation: count_distinct
    field: CASE WHEN pay_amt_l30d > 0 THEN user_id END
    source: dws_10000251_vip_acc_tag_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_vip_acc_tag_df
    description: 机械提取自 dws_10000251_vip_acc_tag_df 的 metrics 块（key=paying_uv_l30d）
