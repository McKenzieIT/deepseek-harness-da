kind: metric
name: game.gmActivity.shopExchange__game_gmActivity_shopExchange_account_uv
description: 参与GM活动商店兑换的去重账号数
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT account_id)
  metadata:
    aggregation: count_distinct
    field: account_id
    source: game.gmActivity.shopExchange
    time_grain: ""
relations:
  - type: derived_from
    target: game.gmActivity.shopExchange
    description: 机械提取自 game.gmActivity.shopExchange 的 metrics 块（key=game_gmActivity_shopExchange_account_uv）
