kind: metric
name: game.pvp.end__game_pvp_end_boost_rate
description: 代打占比(isBoost=1),用于异常账户/代练检测
domains:
  - 战斗关卡
computation:
  sql: SUM(CASE WHEN CAST(GET_JSON_OBJECT(params, '$.isBoost') AS BIGINT) = 1 THEN 1 ELSE 0 END) / NULLIF(COUNT(*), 0)
  metadata:
    aggregation: sum
    field: CASE WHEN CAST(GET_JSON_OBJECT(params, '$.isBoost') AS BIGINT) = 1 THEN 1 ELSE 0 END) / NULLIF(COUNT(*), 0
    source: game.pvp.end
    time_grain: ""
relations:
  - type: derived_from
    target: game.pvp.end
    description: 机械提取自 game.pvp.end 的 metrics 块（key=game_pvp_end_boost_rate）
