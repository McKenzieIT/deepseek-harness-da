kind: metric
name: dws_10000251_play_unlock_df__unlock_server_cnt
description: 当日发生解锁行为的不重复区服数(覆盖区服数)
domains:
  - 探索收集
  - 角色成长
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: dws_10000251_play_unlock_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_play_unlock_df
    description: 机械提取自 dws_10000251_play_unlock_df 的 metrics 块（key=unlock_server_cnt）
