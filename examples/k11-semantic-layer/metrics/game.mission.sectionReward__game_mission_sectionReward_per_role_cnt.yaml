kind: metric
name: game.mission.sectionReward__game_mission_sectionReward_per_role_cnt
description: 单角色平均领奖次数（实测 ds=20260729≈2.30）
domains:
  - 角色成长
computation:
  sql: COUNT(*) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: count
    field: "*) / NULLIF(COUNT(DISTINCT role_id), 0"
    source: game.mission.sectionReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.mission.sectionReward
    description: 机械提取自 game.mission.sectionReward 的 metrics 块（key=game_mission_sectionReward_per_role_cnt）
