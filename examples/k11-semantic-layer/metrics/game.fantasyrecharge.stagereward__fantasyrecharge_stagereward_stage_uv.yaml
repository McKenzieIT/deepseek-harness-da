kind: metric
name: game.fantasyrecharge.stagereward__fantasyrecharge_stagereward_stage_uv
description: 涉及的阶段id数(去重 stage；反映已被领取的阶段档位覆盖)
domains:
  - 付费经济
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.stage'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.stage')
    source: game.fantasyrecharge.stagereward
    time_grain: ""
relations:
  - type: derived_from
    target: game.fantasyrecharge.stagereward
    description: 机械提取自 game.fantasyrecharge.stagereward 的 metrics 块（key=fantasyrecharge_stagereward_stage_uv）
