kind: metric
name: dws_10000251_role_common_feature_df__vip_high_role_cnt
description: 高VIP(>=10)角色数,高净值付费用户规模
domains:
  - 角色成长
  - 付费经济
  - 社交公会
computation:
  sql: COUNT(DISTINCT CASE WHEN CAST(viplevel AS BIGINT) >= 10 THEN role_id END)
  metadata:
    aggregation: count_distinct
    field: CASE WHEN CAST(viplevel AS BIGINT) >= 10 THEN role_id END
    source: dws_10000251_role_common_feature_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_role_common_feature_df
    description: 机械提取自 dws_10000251_role_common_feature_df 的 metrics 块（key=vip_high_role_cnt）
