kind: metric
name: game.rogue.start__game_rogue_start_server_uv
description: 涉及的游戏服去重数
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: game.rogue.start
    time_grain: ""
relations:
  - type: derived_from
    target: game.rogue.start
    description: 机械提取自 game.rogue.start 的 metrics 块（key=game_rogue_start_server_uv）
