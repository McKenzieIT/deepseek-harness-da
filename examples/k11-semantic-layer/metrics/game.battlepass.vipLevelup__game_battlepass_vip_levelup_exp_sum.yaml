kind: metric
name: game.battlepass.vipLevelup__game_battlepass_vip_levelup_exp_sum
description: 战令VIP升级获得的总战令经验
domains:
  - 角色成长
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.battlePassExp') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.battlePassExp') AS BIGINT)
    source: game.battlepass.vipLevelup
    time_grain: ""
relations:
  - type: derived_from
    target: game.battlepass.vipLevelup
    description: 机械提取自 game.battlepass.vipLevelup 的 metrics 块（key=game_battlepass_vip_levelup_exp_sum）
