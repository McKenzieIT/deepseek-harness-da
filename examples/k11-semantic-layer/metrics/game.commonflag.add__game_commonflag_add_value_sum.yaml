kind: metric
name: game.commonflag.add__game_commonflag_add_value_sum
description: 标记值(value)累计求和(flag 为状态量，sum 用于数据校验/完整性核验)
domains:
  - 系统监控
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.value') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.value') AS BIGINT)
    source: game.commonflag.add
    time_grain: ""
relations:
  - type: derived_from
    target: game.commonflag.add
    description: 机械提取自 game.commonflag.add 的 metrics 块（key=game_commonflag_add_value_sum）
