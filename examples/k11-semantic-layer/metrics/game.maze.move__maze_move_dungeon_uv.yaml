kind: metric
name: game.maze.move__maze_move_dungeon_uv
description: 涉及的去重迷宫id数
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.dungeonId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.dungeonId')
    source: game.maze.move
    time_grain: ""
relations:
  - type: derived_from
    target: game.maze.move
    description: 机械提取自 game.maze.move 的 metrics 块（key=maze_move_dungeon_uv）
