kind: metric
name: game.koiDraw.changeFloor__koi_draw_change_floor_actual_change_role_uv
description: 实际发生层数变化(newFloor<>oldFloor)的去重角色数，剔除同层重报
domains:
  - 探索收集
computation:
  sql: COUNT(DISTINCT CASE WHEN GET_JSON_OBJECT(params, '$.newFloor') <> GET_JSON_OBJECT(params, '$.oldFloor') THEN role_id END)
  metadata:
    aggregation: count_distinct
    field: CASE WHEN GET_JSON_OBJECT(params, '$.newFloor') <> GET_JSON_OBJECT(params, '$.oldFloor') THEN role_id END
    source: game.koiDraw.changeFloor
    time_grain: ""
relations:
  - type: derived_from
    target: game.koiDraw.changeFloor
    description: 机械提取自 game.koiDraw.changeFloor 的 metrics 块（key=koi_draw_change_floor_actual_change_role_uv）
