kind: metric
name: game.tower.extrareward__game_tower_extrareward_avg_per_role
description: 角色人均额外奖励领取次数（总次数 / 去重角色数，NULLIF 防零除）
domains:
  - 战斗关卡
computation:
  sql: COUNT(*) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: count
    field: "*) / NULLIF(COUNT(DISTINCT role_id), 0"
    source: game.tower.extrareward
    time_grain: ""
relations:
  - type: derived_from
    target: game.tower.extrareward
    description: 机械提取自 game.tower.extrareward 的 metrics 块（key=game_tower_extrareward_avg_per_role）
