kind: metric
name: game.rogue.summon__game_rogue_summon_server_uv
description: 出现招募行为的去重游戏服数
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: game.rogue.summon
    time_grain: ""
relations:
  - type: derived_from
    target: game.rogue.summon
    description: 机械提取自 game.rogue.summon 的 metrics 块（key=game_rogue_summon_server_uv）
