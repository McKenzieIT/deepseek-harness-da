kind: metric
name: dws_10000251_univ_acc_server_tag_df__churn_risk_acc_cnt
description: 流失风险账号数(最近两次活跃间隔>=30 天);可调整阈值 7/14/30 用于流失/回流分级
domains:
  - 用户生命周期
  - 付费经济
  - 系统监控
computation:
  sql: COUNT(DISTINCT CASE WHEN act_ymd_diff >= 30 THEN user_id END)
  metadata:
    aggregation: count_distinct
    field: CASE WHEN act_ymd_diff >= 30 THEN user_id END
    source: dws_10000251_univ_acc_server_tag_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_univ_acc_server_tag_df
    description: 机械提取自 dws_10000251_univ_acc_server_tag_df 的 metrics 块（key=churn_risk_acc_cnt）
