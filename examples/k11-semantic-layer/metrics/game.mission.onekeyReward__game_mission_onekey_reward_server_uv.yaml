kind: metric
name: game.mission.onekeyReward__game_mission_onekey_reward_server_uv
description: 触发一键领奖的去重游戏服数
domains:
  - 角色成长
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: game.mission.onekeyReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.mission.onekeyReward
    description: 机械提取自 game.mission.onekeyReward 的 metrics 块（key=game_mission_onekey_reward_server_uv）
