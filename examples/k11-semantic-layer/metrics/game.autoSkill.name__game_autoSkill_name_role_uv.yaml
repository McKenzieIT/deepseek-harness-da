kind: metric
name: game.autoSkill.name__game_autoSkill_name_role_uv
description: 触发方案重命名的去重角色数（ds=20260729 实测 29）
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.autoSkill.name
    time_grain: ""
relations:
  - type: derived_from
    target: game.autoSkill.name
    description: 机械提取自 game.autoSkill.name 的 metrics 块（key=game_autoSkill_name_role_uv）
