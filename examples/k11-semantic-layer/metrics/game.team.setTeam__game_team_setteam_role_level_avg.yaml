kind: metric
name: game.team.setTeam__game_team_setteam_role_level_avg
description: 设置阵容时角色平均等级（按事件次数加权）
domains:
  - 自定义
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.roleLevel') AS BIGINT)) / NULLIF(COUNT(*), 0)
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.roleLevel') AS BIGINT)) / NULLIF(COUNT(*), 0
    source: game.team.setTeam
    time_grain: ""
relations:
  - type: derived_from
    target: game.team.setTeam
    description: 机械提取自 game.team.setTeam 的 metrics 块（key=game_team_setteam_role_level_avg）
