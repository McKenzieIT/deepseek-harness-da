kind: metric
name: game.resort.accept__resort_accept_resort_uv
description: 去重请求方玩家数（被接受的代打请求发起方 resortUid）
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.resortUid'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.resortUid')
    source: game.resort.accept
    time_grain: ""
relations:
  - type: derived_from
    target: game.resort.accept
    description: 机械提取自 game.resort.accept 的 metrics 块（key=resort_accept_resort_uv）
