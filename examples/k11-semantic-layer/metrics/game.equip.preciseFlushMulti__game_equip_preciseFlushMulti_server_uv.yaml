kind: metric
name: game.equip.preciseFlushMulti__game_equip_preciseFlushMulti_server_uv
description: 覆盖游戏服去重数
domains:
  - 装备道具
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: game.equip.preciseFlushMulti
    time_grain: ""
relations:
  - type: derived_from
    target: game.equip.preciseFlushMulti
    description: 机械提取自 game.equip.preciseFlushMulti 的 metrics 块（key=game_equip_preciseFlushMulti_server_uv）
