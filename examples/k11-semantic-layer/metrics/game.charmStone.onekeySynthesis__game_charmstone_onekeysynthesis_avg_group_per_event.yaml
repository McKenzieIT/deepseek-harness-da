kind: metric
name: game.charmStone.onekeySynthesis__game_charmstone_onekeysynthesis_avg_group_per_event
description: 单次一键合成平均组数（num 均值，批量规模）
domains:
  - 探索收集
computation:
  sql: AVG(CAST(GET_JSON_OBJECT(params,'$.num') AS DOUBLE))
  metadata:
    aggregation: avg
    field: CAST(GET_JSON_OBJECT(params,'$.num') AS DOUBLE)
    source: game.charmStone.onekeySynthesis
    time_grain: ""
relations:
  - type: derived_from
    target: game.charmStone.onekeySynthesis
    description: 机械提取自 game.charmStone.onekeySynthesis 的 metrics 块（key=game_charmstone_onekeysynthesis_avg_group_per_event）
