kind: metric
name: game.koiDraw.gacha__koiDraw_gacha_guarantee_trigger_cnt
description: 触发保底的锦鲤抽奖事件数(guaranteeTriggered>0；1=核心反保底/2=高级正保底/3=高级反保底)
domains:
  - 探索收集
computation:
  sql: SUM(CASE WHEN CAST(GET_JSON_OBJECT(params, '$.guaranteeTriggered') AS BIGINT) > 0 THEN 1 ELSE 0 END)
  metadata:
    aggregation: sum
    field: CASE WHEN CAST(GET_JSON_OBJECT(params, '$.guaranteeTriggered') AS BIGINT) > 0 THEN 1 ELSE 0 END
    source: game.koiDraw.gacha
    time_grain: ""
relations:
  - type: derived_from
    target: game.koiDraw.gacha
    description: 机械提取自 game.koiDraw.gacha 的 metrics 块（key=koiDraw_gacha_guarantee_trigger_cnt）
