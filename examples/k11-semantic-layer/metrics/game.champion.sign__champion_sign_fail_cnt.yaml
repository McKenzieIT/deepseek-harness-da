kind: metric
name: game.champion.sign__champion_sign_fail_cnt
description: 报名失败次数（signResult=0）
domains:
  - 战斗关卡
computation:
  sql: SUM(CASE WHEN GET_JSON_OBJECT(params,'$.signResult') = '0' THEN 1 ELSE 0 END)
  metadata:
    aggregation: sum
    field: CASE WHEN GET_JSON_OBJECT(params,'$.signResult') = '0' THEN 1 ELSE 0 END
    source: game.champion.sign
    time_grain: ""
relations:
  - type: derived_from
    target: game.champion.sign
    description: 机械提取自 game.champion.sign 的 metrics 块（key=champion_sign_fail_cnt）
