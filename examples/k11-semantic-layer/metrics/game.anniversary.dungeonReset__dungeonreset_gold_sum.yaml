kind: metric
name: game.anniversary.dungeonReset__dungeonreset_gold_sum
description: 周年庆关卡重置事件携带的充值钻石余额快照总量(coinList.gold 求和,反映参与角色的充值钻石余额规模,非关卡重置交易金额)
domains:
  - 付费经济
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.coinList.gold') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.coinList.gold') AS BIGINT)
    source: game.anniversary.dungeonReset
    time_grain: ""
relations:
  - type: derived_from
    target: game.anniversary.dungeonReset
    description: 机械提取自 game.anniversary.dungeonReset 的 metrics 块（key=dungeonreset_gold_sum）
