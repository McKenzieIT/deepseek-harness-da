kind: metric
name: game.artifact.gacha__game_artifact_gacha_avg_event_per_role
description: 人均兵符抽卡事件次数（事件次数 / 去重角色数）
domains:
  - 角色成长
computation:
  sql: COUNT(*) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: count
    field: "*) / NULLIF(COUNT(DISTINCT role_id), 0"
    source: game.artifact.gacha
    time_grain: ""
relations:
  - type: derived_from
    target: game.artifact.gacha
    description: 机械提取自 game.artifact.gacha 的 metrics 块（key=game_artifact_gacha_avg_event_per_role）
