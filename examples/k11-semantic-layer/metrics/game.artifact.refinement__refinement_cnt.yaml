kind: metric
name: game.artifact.refinement__refinement_cnt
description: 兵符精炼事件总次数
domains:
  - 角色成长
computation:
  sql: COUNT(*)
  metadata:
    aggregation: count
    field: "*"
    source: game.artifact.refinement
    time_grain: ""
relations:
  - type: derived_from
    target: game.artifact.refinement
    description: 机械提取自 game.artifact.refinement 的 metrics 块（key=refinement_cnt）
