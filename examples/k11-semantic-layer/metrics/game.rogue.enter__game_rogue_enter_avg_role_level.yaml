kind: metric
name: game.rogue.enter__game_rogue_enter_avg_role_level
description: 进入古战场角色的平均角色等级
domains:
  - 战斗关卡
computation:
  sql: AVG(CAST(GET_JSON_OBJECT(params, '$.roleLevel') AS BIGINT))
  metadata:
    aggregation: avg
    field: CAST(GET_JSON_OBJECT(params, '$.roleLevel') AS BIGINT)
    source: game.rogue.enter
    time_grain: ""
relations:
  - type: derived_from
    target: game.rogue.enter
    description: 机械提取自 game.rogue.enter 的 metrics 块（key=game_rogue_enter_avg_role_level）
