kind: metric
name: game.anniversary.dungeonReset__dungeonreset_role_uv
description: 触发周年庆关卡重置的去重角色数(去重基础视图 role_id,即 params.roleId)
domains:
  - 付费经济
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.anniversary.dungeonReset
    time_grain: ""
relations:
  - type: derived_from
    target: game.anniversary.dungeonReset
    description: 机械提取自 game.anniversary.dungeonReset 的 metrics 块（key=dungeonreset_role_uv）
