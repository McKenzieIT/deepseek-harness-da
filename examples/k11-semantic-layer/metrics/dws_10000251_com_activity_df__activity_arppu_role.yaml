kind: metric
name: dws_10000251_com_activity_df__activity_arppu_role
description: 活动付费角色 ARPPU（人均付费金额/分=总金额/去重角色数）
domains:
  - 付费经济
computation:
  sql: SUM(pay_amt) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: sum
    field: pay_amt) / NULLIF(COUNT(DISTINCT role_id), 0
    source: dws_10000251_com_activity_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_com_activity_df
    description: 机械提取自 dws_10000251_com_activity_df 的 metrics 块（key=activity_arppu_role）
