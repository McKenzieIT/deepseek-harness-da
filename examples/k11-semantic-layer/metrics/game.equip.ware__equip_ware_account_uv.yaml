kind: metric
name: game.equip.ware__equip_ware_account_uv
description: 触发装备穿戴的去重账号数
domains:
  - 装备道具
computation:
  sql: COUNT(DISTINCT account_id)
  metadata:
    aggregation: count_distinct
    field: account_id
    source: game.equip.ware
    time_grain: ""
relations:
  - type: derived_from
    target: game.equip.ware
    description: 机械提取自 game.equip.ware 的 metrics 块（key=equip_ware_account_uv）
