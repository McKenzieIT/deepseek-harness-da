kind: metric
name: game.fantasyrecharge.stagereward__fantasyrecharge_stagereward_role_per_account
description: 平均每账号领取角色数(独立角色/独立账号；衡量多角色账号占比)
domains:
  - 付费经济
computation:
  sql: COUNT(DISTINCT role_id) / NULLIF(COUNT(DISTINCT account_id), 0)
  metadata:
    aggregation: count_distinct
    field: role_id) / NULLIF(COUNT(DISTINCT account_id), 0
    source: game.fantasyrecharge.stagereward
    time_grain: ""
relations:
  - type: derived_from
    target: game.fantasyrecharge.stagereward
    description: 机械提取自 game.fantasyrecharge.stagereward 的 metrics 块（key=fantasyrecharge_stagereward_role_per_account）
