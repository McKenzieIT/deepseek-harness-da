kind: metric
name: dws_10000251_item_circle_di__item_flow_pv
description: 当日物品获得+使用行为总次数
domains:
  - 资源产销
  - 装备道具
computation:
  sql: SUM(get_pv) + SUM(use_pv)
  metadata:
    aggregation: sum
    field: get_pv) + SUM(use_pv
    source: dws_10000251_item_circle_di
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_item_circle_di
    description: 机械提取自 dws_10000251_item_circle_di 的 metrics 块（key=item_flow_pv）
