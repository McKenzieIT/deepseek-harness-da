kind: metric
name: game.guild.getMissionBox__guild_get_mission_box_avg_per_role
description: 人均领取宝箱次数（总次数/去重角色数）
domains:
  - 社交公会
computation:
  sql: ROUND(COUNT(*) * 1.0 / NULLIF(COUNT(DISTINCT role_id), 0), 2)
  metadata:
    aggregation: ""
    field: ""
    source: game.guild.getMissionBox
    time_grain: ""
relations:
  - type: derived_from
    target: game.guild.getMissionBox
    description: 机械提取自 game.guild.getMissionBox 的 metrics 块（key=guild_get_mission_box_avg_per_role）
