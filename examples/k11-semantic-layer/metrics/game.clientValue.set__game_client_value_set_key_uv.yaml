kind: metric
name: game.clientValue.set__game_client_value_set_key_uv
description: 当日被设置的客户端 key 种类数（去重）
domains:
  - 系统监控
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.key'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.key')
    source: game.clientValue.set
    time_grain: ""
relations:
  - type: derived_from
    target: game.clientValue.set
    description: 机械提取自 game.clientValue.set 的 metrics 块（key=game_client_value_set_key_uv）
