kind: metric
name: dws_10000251_biz_acc_tag_df__cbt2_acc_uv
description: CBT2 批次账号数（去重）
domains:
  - 用户生命周期
  - 系统监控
computation:
  sql: COUNT(DISTINCT CASE WHEN biz_name = 'CBT2' THEN user_id END)
  metadata:
    aggregation: count_distinct
    field: CASE WHEN biz_name = 'CBT2' THEN user_id END
    source: dws_10000251_biz_acc_tag_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_biz_acc_tag_df
    description: 机械提取自 dws_10000251_biz_acc_tag_df 的 metrics 块（key=cbt2_acc_uv）
