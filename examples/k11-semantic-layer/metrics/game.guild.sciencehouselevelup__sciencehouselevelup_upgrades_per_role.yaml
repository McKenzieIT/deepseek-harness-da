kind: metric
name: game.guild.sciencehouselevelup__sciencehouselevelup_upgrades_per_role
description: 人均军机处升级次数（总次数/去重角色数）
domains:
  - 社交公会
computation:
  sql: COUNT(*) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: count
    field: "*) / NULLIF(COUNT(DISTINCT role_id), 0"
    source: game.guild.sciencehouselevelup
    time_grain: ""
relations:
  - type: derived_from
    target: game.guild.sciencehouselevelup
    description: 机械提取自 game.guild.sciencehouselevelup 的 metrics 块（key=sciencehouselevelup_upgrades_per_role）
