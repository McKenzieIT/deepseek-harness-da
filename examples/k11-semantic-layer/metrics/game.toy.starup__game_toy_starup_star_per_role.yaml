kind: metric
name: game.toy.starup__game_toy_starup_star_per_role
description: 平均每个触发角色的升星次数
domains:
  - 探索收集
computation:
  sql: COUNT(*) * 1.0 / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: count
    field: "*) * 1.0 / NULLIF(COUNT(DISTINCT role_id), 0"
    source: game.toy.starup
    time_grain: ""
relations:
  - type: derived_from
    target: game.toy.starup
    description: 机械提取自 game.toy.starup 的 metrics 块（key=game_toy_starup_star_per_role）
