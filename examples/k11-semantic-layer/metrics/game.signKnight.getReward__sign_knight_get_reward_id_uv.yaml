kind: metric
name: game.signKnight.getReward__sign_knight_get_reward_id_uv
description: 领取奖励覆盖的去重奖励 id 数
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params,'$.id'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params,'$.id')
    source: game.signKnight.getReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.signKnight.getReward
    description: 机械提取自 game.signKnight.getReward 的 metrics 块（key=sign_knight_get_reward_id_uv）
