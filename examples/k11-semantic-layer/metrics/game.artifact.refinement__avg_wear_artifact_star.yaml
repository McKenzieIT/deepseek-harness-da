kind: metric
name: game.artifact.refinement__avg_wear_artifact_star
description: 穿戴兵符的平均星级（精炼目标）
domains:
  - 角色成长
computation:
  sql: AVG(CAST(GET_JSON_OBJECT(params, '$.wearArtifact.star') AS BIGINT))
  metadata:
    aggregation: avg
    field: CAST(GET_JSON_OBJECT(params, '$.wearArtifact.star') AS BIGINT)
    source: game.artifact.refinement
    time_grain: ""
relations:
  - type: derived_from
    target: game.artifact.refinement
    description: 机械提取自 game.artifact.refinement 的 metrics 块（key=avg_wear_artifact_star）
