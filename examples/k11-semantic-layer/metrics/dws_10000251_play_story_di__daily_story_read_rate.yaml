kind: metric
name: dws_10000251_play_story_di__daily_story_read_rate
description: 当日剧情完整阅读率（阅读次数/访问次数），反映剧情吸引力
domains:
  - 战斗关卡
  - 探索收集
computation:
  sql: SUM(read_pv) / NULLIF(SUM(visit_pv), 0)
  metadata:
    aggregation: sum
    field: read_pv) / NULLIF(SUM(visit_pv), 0
    source: dws_10000251_play_story_di
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_play_story_di
    description: 机械提取自 dws_10000251_play_story_di 的 metrics 块（key=daily_story_read_rate）
