kind: metric
name: game.gmActivity.missionReward__game_gmActivity_missionReward_role_uv
description: 领取GM活动任务奖励的去重角色数
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.gmActivity.missionReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.gmActivity.missionReward
    description: 机械提取自 game.gmActivity.missionReward 的 metrics 块（key=game_gmActivity_missionReward_role_uv）
