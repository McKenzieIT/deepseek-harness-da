kind: metric
name: game.rogue.reborn__game_rogue_reborn_role_force_sum
description: 复活时角色战力(roleForce)累计值
domains:
  - 战斗关卡
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params,'$.roleForce') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params,'$.roleForce') AS BIGINT)
    source: game.rogue.reborn
    time_grain: ""
relations:
  - type: derived_from
    target: game.rogue.reborn
    description: 机械提取自 game.rogue.reborn 的 metrics 块（key=game_rogue_reborn_role_force_sum）
