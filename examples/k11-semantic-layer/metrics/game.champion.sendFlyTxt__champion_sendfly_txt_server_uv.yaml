kind: metric
name: game.champion.sendFlyTxt__champion_sendfly_txt_server_uv
description: 涉及的游戏服去重数
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: game.champion.sendFlyTxt
    time_grain: ""
relations:
  - type: derived_from
    target: game.champion.sendFlyTxt
    description: 机械提取自 game.champion.sendFlyTxt 的 metrics 块（key=champion_sendfly_txt_server_uv）
