kind: metric
name: game.knightnatalchart.refine__knightnatalchart_refine_per_role_cnt
description: 人均改命次数（总次数 / 参与角色数，分母 NULLIF 防零除）
domains:
  - 角色成长
computation:
  sql: ROUND(COUNT(*) * 1.0 / NULLIF(COUNT(DISTINCT role_id), 0), 2)
  metadata:
    aggregation: ""
    field: ""
    source: game.knightnatalchart.refine
    time_grain: ""
relations:
  - type: derived_from
    target: game.knightnatalchart.refine
    description: 机械提取自 game.knightnatalchart.refine 的 metrics 块（key=knightnatalchart_refine_per_role_cnt）
