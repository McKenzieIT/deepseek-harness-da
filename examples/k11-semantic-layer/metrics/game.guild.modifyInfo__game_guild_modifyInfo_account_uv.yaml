kind: metric
name: game.guild.modifyInfo__game_guild_modifyInfo_account_uv
description: 发起公会信息修改的去重账号数
domains:
  - 社交公会
computation:
  sql: COUNT(DISTINCT account_id)
  metadata:
    aggregation: count_distinct
    field: account_id
    source: game.guild.modifyInfo
    time_grain: ""
relations:
  - type: derived_from
    target: game.guild.modifyInfo
    description: 机械提取自 game.guild.modifyInfo 的 metrics 块（key=game_guild_modifyInfo_account_uv）
