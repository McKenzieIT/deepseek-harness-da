kind: metric
name: dws_10000251_univ_user_predown_tag_df__rule2_hit_cnt
description: 命中扩展预下载规则(rule2)的行数（0/1 标志位累加，行级计数，非去重用户数）
domains:
  - 用户生命周期
  - 资源产销
computation:
  sql: SUM(rule2)
  metadata:
    aggregation: sum
    field: rule2
    source: dws_10000251_univ_user_predown_tag_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_univ_user_predown_tag_df
    description: 机械提取自 dws_10000251_univ_user_predown_tag_df 的 metrics 块（key=rule2_hit_cnt）
