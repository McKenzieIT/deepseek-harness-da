kind: metric
name: game.social.guild.addpoints__guild_addpoints_add_points_sum
description: 当日新增公会活跃点数总和(来自 params.guildActiveAddPoints)
domains:
  - 社交公会
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params,'$.guildActiveAddPoints') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params,'$.guildActiveAddPoints') AS BIGINT)
    source: game.social.guild.addpoints
    time_grain: ""
relations:
  - type: derived_from
    target: game.social.guild.addpoints
    description: 机械提取自 game.social.guild.addpoints 的 metrics 块（key=guild_addpoints_add_points_sum）
