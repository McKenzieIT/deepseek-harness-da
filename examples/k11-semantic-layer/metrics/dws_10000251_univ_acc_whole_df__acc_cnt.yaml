kind: metric
name: dws_10000251_univ_acc_whole_df__acc_cnt
description: 截至快照日的累计账号数(user_id 去重;本表固定账号主体,无需 user_type 过滤)
domains:
  - 用户生命周期
  - 付费经济
computation:
  sql: COUNT(DISTINCT user_id)
  metadata:
    aggregation: count_distinct
    field: user_id
    source: dws_10000251_univ_acc_whole_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_univ_acc_whole_df
    description: 机械提取自 dws_10000251_univ_acc_whole_df 的 metrics 块（key=acc_cnt）
