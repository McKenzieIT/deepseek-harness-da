kind: metric
name: game.mission.reward__game_mission_reward_mission_cnt
description: 领奖涉及的任务id数
domains:
  - 角色成长
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.missionId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.missionId')
    source: game.mission.reward
    time_grain: ""
relations:
  - type: derived_from
    target: game.mission.reward
    description: 机械提取自 game.mission.reward 的 metrics 块（key=game_mission_reward_mission_cnt）
