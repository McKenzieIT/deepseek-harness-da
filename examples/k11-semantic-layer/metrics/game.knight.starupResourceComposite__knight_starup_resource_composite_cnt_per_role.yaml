kind: metric
name: game.knight.starupResourceComposite__knight_starup_resource_composite_cnt_per_role
description: 平均每角色升星合成次数
domains:
  - 角色成长
computation:
  sql: COUNT(*) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: count
    field: "*) / NULLIF(COUNT(DISTINCT role_id), 0"
    source: game.knight.starupResourceComposite
    time_grain: ""
relations:
  - type: derived_from
    target: game.knight.starupResourceComposite
    description: 机械提取自 game.knight.starupResourceComposite 的 metrics 块（key=knight_starup_resource_composite_cnt_per_role）
