kind: metric
name: game.base.setImage__game_base_setImage_server_uv
description: 发生看板娘形象设置事件的独立游戏服数
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: game.base.setImage
    time_grain: ""
relations:
  - type: derived_from
    target: game.base.setImage
    description: 机械提取自 game.base.setImage 的 metrics 块（key=game_base_setImage_server_uv）
