kind: metric
name: game.equip.preciseChooseMulti__game_equip_preciseChooseMulti_per_acc_cnt
description: 单账号平均多选精选择操作次数
domains:
  - 装备道具
computation:
  sql: COUNT(*) / NULLIF(COUNT(DISTINCT account_id), 0)
  metadata:
    aggregation: count
    field: "*) / NULLIF(COUNT(DISTINCT account_id), 0"
    source: game.equip.preciseChooseMulti
    time_grain: ""
relations:
  - type: derived_from
    target: game.equip.preciseChooseMulti
    description: 机械提取自 game.equip.preciseChooseMulti 的 metrics 块（key=game_equip_preciseChooseMulti_per_acc_cnt）
