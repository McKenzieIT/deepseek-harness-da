kind: metric
name: game.experienceMission.bonusReward__game_experience_mission_bonus_reward_account_uv
description: 领取最终大奖的去重账号数（账号粒度去重，跨多角色场景）
domains:
  - 角色成长
computation:
  sql: COUNT(DISTINCT account_id)
  metadata:
    aggregation: count_distinct
    field: account_id
    source: game.experienceMission.bonusReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.experienceMission.bonusReward
    description: 机械提取自 game.experienceMission.bonusReward 的 metrics 块（key=game_experience_mission_bonus_reward_account_uv）
