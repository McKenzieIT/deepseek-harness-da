kind: metric
name: game.customLevel.match__game_customLevel_match_account_uv
description: 触发匹配事件的去重账号数(account_id)
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT account_id)
  metadata:
    aggregation: count_distinct
    field: account_id
    source: game.customLevel.match
    time_grain: ""
relations:
  - type: derived_from
    target: game.customLevel.match
    description: 机械提取自 game.customLevel.match 的 metrics 块（key=game_customLevel_match_account_uv）
