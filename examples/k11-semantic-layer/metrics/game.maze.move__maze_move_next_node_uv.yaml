kind: metric
name: game.maze.move__maze_move_next_node_uv
description: 移动到的去重下一节点id数
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.nextId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.nextId')
    source: game.maze.move
    time_grain: ""
relations:
  - type: derived_from
    target: game.maze.move
    description: 机械提取自 game.maze.move 的 metrics 块（key=maze_move_next_node_uv）
