kind: metric
name: game.toy.techonekeyactive__techonekeyactive_avg_activations_per_role
description: 平均每个参与角色的激活次数（衡量一键激活功能的重复使用强度，分母用 NULLIF 防零）
domains:
  - 探索收集
computation:
  sql: ROUND(COUNT(*) * 1.0 / NULLIF(COUNT(DISTINCT role_id), 0), 2)
  metadata:
    aggregation: ""
    field: ""
    source: game.toy.techonekeyactive
    time_grain: ""
relations:
  - type: derived_from
    target: game.toy.techonekeyactive
    description: 机械提取自 game.toy.techonekeyactive 的 metrics 块（key=techonekeyactive_avg_activations_per_role）
