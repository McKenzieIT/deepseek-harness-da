kind: metric
name: game.war.getMissionReward__game_war_get_mission_reward_avg_role_level
description: 领取奖励角色的平均等级
domains:
  - 战斗关卡
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.roleLevel') AS BIGINT)) / NULLIF(COUNT(*), 0)
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.roleLevel') AS BIGINT)) / NULLIF(COUNT(*), 0
    source: game.war.getMissionReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.war.getMissionReward
    description: 机械提取自 game.war.getMissionReward 的 metrics 块（key=game_war_get_mission_reward_avg_role_level）
