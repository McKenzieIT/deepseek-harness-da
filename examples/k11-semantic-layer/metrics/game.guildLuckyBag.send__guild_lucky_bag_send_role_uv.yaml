kind: metric
name: game.guildLuckyBag.send__guild_lucky_bag_send_role_uv
description: 发送福袋的去重角色数
domains:
  - 社交公会
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.guildLuckyBag.send
    time_grain: ""
relations:
  - type: derived_from
    target: game.guildLuckyBag.send
    description: 机械提取自 game.guildLuckyBag.send 的 metrics 块（key=guild_lucky_bag_send_role_uv）
