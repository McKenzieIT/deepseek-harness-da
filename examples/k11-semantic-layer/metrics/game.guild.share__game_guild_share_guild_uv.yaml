kind: metric
name: game.guild.share__game_guild_share_guild_uv
description: 被分享的去重公会数（params.id）
domains:
  - 社交公会
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.id'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.id')
    source: game.guild.share
    time_grain: ""
relations:
  - type: derived_from
    target: game.guild.share
    description: 机械提取自 game.guild.share 的 metrics 块（key=game_guild_share_guild_uv）
