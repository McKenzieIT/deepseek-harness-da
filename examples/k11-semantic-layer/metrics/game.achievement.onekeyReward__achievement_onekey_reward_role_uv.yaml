kind: metric
name: game.achievement.onekeyReward__achievement_onekey_reward_role_uv
description: 领取成就奖励的去重角色数（分析主体 role_id）
domains:
  - 角色成长
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.achievement.onekeyReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.achievement.onekeyReward
    description: 机械提取自 game.achievement.onekeyReward 的 metrics 块（key=achievement_onekey_reward_role_uv）
