kind: metric
name: game.champion.sendFlyTxt__champion_sendfly_txt_success_rate
description: 弹幕发送成功率（成功次数 / 总次数）
domains:
  - 战斗关卡
computation:
  sql: SUM(CASE WHEN GET_JSON_OBJECT(params,'$.sendResult') = '1' THEN 1 ELSE 0 END) / NULLIF(COUNT(*), 0)
  metadata:
    aggregation: sum
    field: CASE WHEN GET_JSON_OBJECT(params,'$.sendResult') = '1' THEN 1 ELSE 0 END) / NULLIF(COUNT(*), 0
    source: game.champion.sendFlyTxt
    time_grain: ""
relations:
  - type: derived_from
    target: game.champion.sendFlyTxt
    description: 机械提取自 game.champion.sendFlyTxt 的 metrics 块（key=champion_sendfly_txt_success_rate）
