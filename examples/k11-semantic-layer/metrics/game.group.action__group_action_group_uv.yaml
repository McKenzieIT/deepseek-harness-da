kind: metric
name: game.group.action__group_action_group_uv
description: 涉及的去重队伍数（params.groupId）
domains:
  - 社交公会
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.groupId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.groupId')
    source: game.group.action
    time_grain: ""
relations:
  - type: derived_from
    target: game.group.action
    description: 机械提取自 game.group.action 的 metrics 块（key=group_action_group_uv）
