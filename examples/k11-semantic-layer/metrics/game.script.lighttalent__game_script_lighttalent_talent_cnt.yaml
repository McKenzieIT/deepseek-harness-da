kind: metric
name: game.script.lighttalent__game_script_lighttalent_talent_cnt
description: 涉及的去重天赋(talentId)数量
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.talentId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.talentId')
    source: game.script.lighttalent
    time_grain: ""
relations:
  - type: derived_from
    target: game.script.lighttalent
    description: 机械提取自 game.script.lighttalent 的 metrics 块（key=game_script_lighttalent_talent_cnt）
