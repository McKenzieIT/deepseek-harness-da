kind: metric
name: game.champion.getDailyCoin__game_champion_getDailyCoin_gold_sum
description: 每日竞猜币发放充值钻石(gold)总量
domains:
  - 战斗关卡
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.coinList.gold') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.coinList.gold') AS BIGINT)
    source: game.champion.getDailyCoin
    time_grain: ""
relations:
  - type: derived_from
    target: game.champion.getDailyCoin
    description: 机械提取自 game.champion.getDailyCoin 的 metrics 块（key=game_champion_getDailyCoin_gold_sum）
