kind: metric
name: game.scriptActivity.taskBattlepassMissionOnekeyReward__bp_onekey_reward_addexp_sum
description: 单日战令任务一键领取经验总量（addExp 求和，反映奖励经验发放规模）
domains:
  - 自定义
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.addExp') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.addExp') AS BIGINT)
    source: game.scriptActivity.taskBattlepassMissionOnekeyReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.scriptActivity.taskBattlepassMissionOnekeyReward
    description: 机械提取自 game.scriptActivity.taskBattlepassMissionOnekeyReward 的 metrics 块（key=bp_onekey_reward_addexp_sum）
