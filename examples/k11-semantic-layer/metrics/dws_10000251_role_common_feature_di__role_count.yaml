kind: metric
name: dws_10000251_role_common_feature_di__role_count
description: 角色数,统计当日有记录(增量快照)的不同角色数量,受 di 增量口径限制非全量角色
domains:
  - 角色成长
  - 社交公会
  - 付费经济
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: dws_10000251_role_common_feature_di
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_role_common_feature_di
    description: 机械提取自 dws_10000251_role_common_feature_di 的 metrics 块（key=role_count）
