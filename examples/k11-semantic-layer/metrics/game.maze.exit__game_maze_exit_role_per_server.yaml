kind: metric
name: game.maze.exit__game_maze_exit_role_per_server
description: 平均每个游戏服的退出角色数
domains:
  - 战斗关卡
computation:
  sql: CAST(COUNT(DISTINCT role_id) AS DOUBLE) / NULLIF(COUNT(DISTINCT server_id), 0)
  metadata:
    aggregation: ""
    field: ""
    source: game.maze.exit
    time_grain: ""
relations:
  - type: derived_from
    target: game.maze.exit
    description: 机械提取自 game.maze.exit 的 metrics 块（key=game_maze_exit_role_per_server）
