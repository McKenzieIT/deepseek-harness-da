kind: metric
name: game.dreamDungeon.talkEffect__game_dreamDungeon_talkEffect_dungeon_uv
description: 被触发对话的去重迷宫id数量
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params,'$.dungeonId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params,'$.dungeonId')
    source: game.dreamDungeon.talkEffect
    time_grain: ""
relations:
  - type: derived_from
    target: game.dreamDungeon.talkEffect
    description: 机械提取自 game.dreamDungeon.talkEffect 的 metrics 块（key=game_dreamDungeon_talkEffect_dungeon_uv）
