kind: metric
name: game.guild.cancelInviteItem__guild_cancel_invite_item_role_uv
description: 发起取消邀请的独立角色数(去重 role_id；主体粒度为角色)
domains:
  - 社交公会
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.guild.cancelInviteItem
    time_grain: ""
relations:
  - type: derived_from
    target: game.guild.cancelInviteItem
    description: 机械提取自 game.guild.cancelInviteItem 的 metrics 块（key=guild_cancel_invite_item_role_uv）
