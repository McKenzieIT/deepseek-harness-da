kind: metric
name: game.showCamp.onSwap__showcamp_onswap_swap_per_role
description: 人均交换次数（总次数 / 去重角色数）
domains:
  - 自定义
computation:
  sql: COUNT(*) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: count
    field: "*) / NULLIF(COUNT(DISTINCT role_id), 0"
    source: game.showCamp.onSwap
    time_grain: ""
relations:
  - type: derived_from
    target: game.showCamp.onSwap
    description: 机械提取自 game.showCamp.onSwap 的 metrics 块（key=showcamp_onswap_swap_per_role）
