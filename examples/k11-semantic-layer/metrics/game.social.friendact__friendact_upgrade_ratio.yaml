kind: metric
name: game.social.friendact__friendact_upgrade_ratio
description: 好友关系升级动作占变更总次数的比例（NULLIF 防零除）
domains:
  - 社交公会
computation:
  sql: ROUND(SUM(CASE WHEN GET_JSON_OBJECT(params,'$.action')='2' THEN 1 ELSE 0 END) / NULLIF(COUNT(*),0),4)
  metadata:
    aggregation: ""
    field: ""
    source: game.social.friendact
    time_grain: ""
relations:
  - type: derived_from
    target: game.social.friendact
    description: 机械提取自 game.social.friendact 的 metrics 块（key=friendact_upgrade_ratio）
