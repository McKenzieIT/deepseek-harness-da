kind: metric
name: game.fantasyrecharge.stagereward__fantasyrecharge_stagereward_gold_sum
description: 充值钻石发放总额(coinList.gold 求和；ds=20260729 scout 实测 sum=8，阶段奖励以小额钻石为主)
domains:
  - 付费经济
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.coinList.gold') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.coinList.gold') AS BIGINT)
    source: game.fantasyrecharge.stagereward
    time_grain: ""
relations:
  - type: derived_from
    target: game.fantasyrecharge.stagereward
    description: 机械提取自 game.fantasyrecharge.stagereward 的 metrics 块（key=fantasyrecharge_stagereward_gold_sum）
