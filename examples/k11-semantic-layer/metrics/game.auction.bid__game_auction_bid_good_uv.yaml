kind: metric
name: game.auction.bid__game_auction_bid_good_uv
description: 被竞价商品的去重数量（goodUid）
domains:
  - 付费经济
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params,'$.goodUid'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params,'$.goodUid')
    source: game.auction.bid
    time_grain: ""
relations:
  - type: derived_from
    target: game.auction.bid
    description: 机械提取自 game.auction.bid 的 metrics 块（key=game_auction_bid_good_uv）
