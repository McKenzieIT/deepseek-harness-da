kind: metric
name: game.guild.donate__game_guild_donate_guild_uv
description: 收到捐献的去重公会数
domains:
  - 社交公会
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.guild'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.guild')
    source: game.guild.donate
    time_grain: ""
relations:
  - type: derived_from
    target: game.guild.donate
    description: 机械提取自 game.guild.donate 的 metrics 块（key=game_guild_donate_guild_uv）
