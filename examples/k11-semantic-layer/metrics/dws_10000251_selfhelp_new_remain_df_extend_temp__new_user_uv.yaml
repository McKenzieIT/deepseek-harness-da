kind: metric
name: dws_10000251_selfhelp_new_remain_df_extend_temp__new_user_uv
description: 新增用户数（账户维度全量快照 UV）
domains:
  - 用户生命周期
computation:
  sql: COUNT(DISTINCT user_id)
  metadata:
    aggregation: count_distinct
    field: user_id
    source: dws_10000251_selfhelp_new_remain_df_extend_temp
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_selfhelp_new_remain_df_extend_temp
    description: 机械提取自 dws_10000251_selfhelp_new_remain_df_extend_temp 的 metrics 块（key=new_user_uv）
