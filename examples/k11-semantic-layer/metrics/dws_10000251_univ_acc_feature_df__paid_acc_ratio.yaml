kind: metric
name: dws_10000251_univ_acc_feature_df__paid_acc_ratio
description: 买量账号占比（ua_flag=1 账号数 / 全量账号UV，NULLIF 防除零）
domains:
  - 用户生命周期
  - 角色成长
computation:
  sql: COUNT(DISTINCT CASE WHEN ua_flag = 1 THEN user_id END) / NULLIF(COUNT(DISTINCT user_id), 0)
  metadata:
    aggregation: count_distinct
    field: CASE WHEN ua_flag = 1 THEN user_id END) / NULLIF(COUNT(DISTINCT user_id), 0
    source: dws_10000251_univ_acc_feature_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_univ_acc_feature_df
    description: 机械提取自 dws_10000251_univ_acc_feature_df 的 metrics 块（key=paid_acc_ratio）
