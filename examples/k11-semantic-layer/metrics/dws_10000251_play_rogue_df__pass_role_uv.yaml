kind: metric
name: dws_10000251_play_rogue_df__pass_role_uv
description: 至少通关一个关卡的角色数(pass_status=1)
domains:
  - 战斗关卡
  - 角色成长
computation:
  sql: COUNT(DISTINCT CASE WHEN pass_status = 1 THEN role_id END)
  metadata:
    aggregation: count_distinct
    field: CASE WHEN pass_status = 1 THEN role_id END
    source: dws_10000251_play_rogue_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_play_rogue_df
    description: 机械提取自 dws_10000251_play_rogue_df 的 metrics 块（key=pass_role_uv）
