kind: metric
name: game.scriptActivity.scriptMissionReward__script_mission_reward_role_uv
description: 领取奖励的去重角色数（分析主体 role_id）
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.scriptActivity.scriptMissionReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.scriptActivity.scriptMissionReward
    description: 机械提取自 game.scriptActivity.scriptMissionReward 的 metrics 块（key=script_mission_reward_role_uv）
