kind: metric
name: game.role.create__game_role_create_account_uv
description: 创角账号去重数（同一账号多角色计一次）
domains:
  - 用户生命周期
computation:
  sql: COUNT(DISTINCT account_id)
  metadata:
    aggregation: count_distinct
    field: account_id
    source: game.role.create
    time_grain: ""
relations:
  - type: derived_from
    target: game.role.create
    description: 机械提取自 game.role.create 的 metrics 块（key=game_role_create_account_uv）
