kind: metric
name: game.equip.onekeyUnwear__equip_onekey_unwear_cnt
description: 一键脱下事件总次数（按 ds 切片为单日触发量）
domains:
  - 装备道具
computation:
  sql: COUNT(*)
  metadata:
    aggregation: count
    field: "*"
    source: game.equip.onekeyUnwear
    time_grain: ""
relations:
  - type: derived_from
    target: game.equip.onekeyUnwear
    description: 机械提取自 game.equip.onekeyUnwear 的 metrics 块（key=equip_onekey_unwear_cnt）
