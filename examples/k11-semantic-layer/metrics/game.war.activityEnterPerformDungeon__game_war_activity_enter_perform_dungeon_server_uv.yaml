kind: metric
name: game.war.activityEnterPerformDungeon__game_war_activity_enter_perform_dungeon_server_uv
description: 进入剧情表演副本涉及的游戏服数（去重）
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: game.war.activityEnterPerformDungeon
    time_grain: ""
relations:
  - type: derived_from
    target: game.war.activityEnterPerformDungeon
    description: 机械提取自 game.war.activityEnterPerformDungeon 的 metrics 块（key=game_war_activity_enter_perform_dungeon_server_uv）
