kind: metric
name: dws_10000251_univ_dev_server_tag_df__pay_rate
description: 付费率(累计口径:累计付费设备数/累计活跃设备数,非当日付费率),用 AVG(CASE WHEN) 计算避免除零;反映历史付费转化比例
domains:
  - 用户生命周期
  - 付费经济
  - 角色成长
  - 系统监控
computation:
  sql: AVG(CASE WHEN pay_amt_std > 0 THEN 1 ELSE 0 END)
  metadata:
    aggregation: avg
    field: CASE WHEN pay_amt_std > 0 THEN 1 ELSE 0 END
    source: dws_10000251_univ_dev_server_tag_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_univ_dev_server_tag_df
    description: 机械提取自 dws_10000251_univ_dev_server_tag_df 的 metrics 块（key=pay_rate）
