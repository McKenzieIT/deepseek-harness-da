kind: metric
name: game.social.guild.operate__game_social_guild_operate_action_uv
description: 公会操作类型去重数（params.action 取值种类）
domains:
  - 社交公会
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.action'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.action')
    source: game.social.guild.operate
    time_grain: ""
relations:
  - type: derived_from
    target: game.social.guild.operate
    description: 机械提取自 game.social.guild.operate 的 metrics 块（key=game_social_guild_operate_action_uv）
