kind: metric
name: game.toy.sign__game_toy_sign_avg_role_force
description: 签到角色平均战力（roleForce 均值，战力分布画像）
domains:
  - 探索收集
computation:
  sql: AVG(CAST(GET_JSON_OBJECT(params, '$.roleForce') AS BIGINT))
  metadata:
    aggregation: avg
    field: CAST(GET_JSON_OBJECT(params, '$.roleForce') AS BIGINT)
    source: game.toy.sign
    time_grain: ""
relations:
  - type: derived_from
    target: game.toy.sign
    description: 机械提取自 game.toy.sign 的 metrics 块（key=game_toy_sign_avg_role_force）
