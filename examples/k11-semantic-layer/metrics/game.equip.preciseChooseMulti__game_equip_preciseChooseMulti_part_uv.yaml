kind: metric
name: game.equip.preciseChooseMulti__game_equip_preciseChooseMulti_part_uv
description: 涉及的装备部位(partId)去重数
domains:
  - 装备道具
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.partId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.partId')
    source: game.equip.preciseChooseMulti
    time_grain: ""
relations:
  - type: derived_from
    target: game.equip.preciseChooseMulti
    description: 机械提取自 game.equip.preciseChooseMulti 的 metrics 块（key=game_equip_preciseChooseMulti_part_uv）
