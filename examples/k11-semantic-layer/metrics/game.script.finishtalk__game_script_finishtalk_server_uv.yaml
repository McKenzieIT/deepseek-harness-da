kind: metric
name: game.script.finishtalk__game_script_finishtalk_server_uv
description: 完成剧本对话涉及的去重游戏服数
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: game.script.finishtalk
    time_grain: ""
relations:
  - type: derived_from
    target: game.script.finishtalk
    description: 机械提取自 game.script.finishtalk 的 metrics 块（key=game_script_finishtalk_server_uv）
