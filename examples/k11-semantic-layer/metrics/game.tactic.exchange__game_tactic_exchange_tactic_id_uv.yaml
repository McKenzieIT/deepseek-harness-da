kind: metric
name: game.tactic.exchange__game_tactic_exchange_tactic_id_uv
description: 被兑换的去重战法id数（战法覆盖广度）
domains:
  - 角色成长
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.tacticId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.tacticId')
    source: game.tactic.exchange
    time_grain: ""
relations:
  - type: derived_from
    target: game.tactic.exchange
    description: 机械提取自 game.tactic.exchange 的 metrics 块（key=game_tactic_exchange_tactic_id_uv）
