kind: metric
name: game.toy.techonekeyactive__techonekeyactive_account_uv
description: 触发一键科技激活的去重账号数
domains:
  - 探索收集
computation:
  sql: COUNT(DISTINCT account_id)
  metadata:
    aggregation: count_distinct
    field: account_id
    source: game.toy.techonekeyactive
    time_grain: ""
relations:
  - type: derived_from
    target: game.toy.techonekeyactive
    description: 机械提取自 game.toy.techonekeyactive 的 metrics 块（key=techonekeyactive_account_uv）
