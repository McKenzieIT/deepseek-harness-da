kind: metric
name: game.toy.techactive__game_toy_techactive_avg_act_per_role
description: 人均科技激活次数（按 role_id 去重）= 事件总次数/去重角色数，衡量科技推进深度
domains:
  - 探索收集
computation:
  sql: COUNT(*) * 1.0 / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: count
    field: "*) * 1.0 / NULLIF(COUNT(DISTINCT role_id), 0"
    source: game.toy.techactive
    time_grain: ""
relations:
  - type: derived_from
    target: game.toy.techactive
    description: 机械提取自 game.toy.techactive 的 metrics 块（key=game_toy_techactive_avg_act_per_role）
