kind: metric
name: game.pitchpot__pitchpot_floor_count_sum
description: 本层投壶次数之和（按 count，反映单层投壶密度）
domains:
  - 探索收集
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.count') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.count') AS BIGINT)
    source: game.pitchpot
    time_grain: ""
relations:
  - type: derived_from
    target: game.pitchpot
    description: 机械提取自 game.pitchpot 的 metrics 块（key=pitchpot_floor_count_sum）
