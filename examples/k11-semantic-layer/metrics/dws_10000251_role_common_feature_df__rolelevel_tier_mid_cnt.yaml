kind: metric
name: dws_10000251_role_common_feature_df__rolelevel_tier_mid_cnt
description: 中等等级(31-60)角色数
domains:
  - 角色成长
  - 付费经济
  - 社交公会
computation:
  sql: COUNT(DISTINCT CASE WHEN CAST(rolelevel AS BIGINT) BETWEEN 31 AND 60 THEN role_id END)
  metadata:
    aggregation: count_distinct
    field: CASE WHEN CAST(rolelevel AS BIGINT) BETWEEN 31 AND 60 THEN role_id END
    source: dws_10000251_role_common_feature_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_role_common_feature_df
    description: 机械提取自 dws_10000251_role_common_feature_df 的 metrics 块（key=rolelevel_tier_mid_cnt）
