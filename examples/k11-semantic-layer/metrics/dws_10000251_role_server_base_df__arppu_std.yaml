kind: metric
name: dws_10000251_role_server_base_df__arppu_std
description: 累计付费角色ARPPU（元/付费角色）：累计付费总金额 / 累计付费角色数
domains:
  - 用户生命周期
  - 付费经济
computation:
  sql: SUM(pay_amt_std) / NULLIF(COUNT(DISTINCT CASE WHEN pay_amt_std > 0 THEN role_id END), 0)
  metadata:
    aggregation: sum
    field: pay_amt_std) / NULLIF(COUNT(DISTINCT CASE WHEN pay_amt_std > 0 THEN role_id END), 0
    source: dws_10000251_role_server_base_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_role_server_base_df
    description: 机械提取自 dws_10000251_role_server_base_df 的 metrics 块（key=arppu_std）
