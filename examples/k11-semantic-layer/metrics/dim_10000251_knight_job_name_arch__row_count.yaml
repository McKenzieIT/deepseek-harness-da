kind: metric
name: dim_10000251_knight_job_name_arch__row_count
description: 维度归档表总行数（含所有 ds 快照分区）
domains:
  - 角色成长
computation:
  sql: COUNT(*)
  metadata:
    aggregation: count
    field: "*"
    source: dim_10000251_knight_job_name_arch
    time_grain: ""
relations:
  - type: derived_from
    target: dim_10000251_knight_job_name_arch
    description: 机械提取自 dim_10000251_knight_job_name_arch 的 metrics 块（key=row_count）
