kind: metric
name: game.func.getReward__game_func_get_reward_cnt_per_role
description: 人均领取功能奖励次数（总次数/去重角色数）
domains:
  - 系统监控
computation:
  sql: COUNT(*) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: count
    field: "*) / NULLIF(COUNT(DISTINCT role_id), 0"
    source: game.func.getReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.func.getReward
    description: 机械提取自 game.func.getReward 的 metrics 块（key=game_func_get_reward_cnt_per_role）
