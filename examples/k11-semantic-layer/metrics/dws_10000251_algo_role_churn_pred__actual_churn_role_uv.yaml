kind: metric
name: dws_10000251_algo_role_churn_pred__actual_churn_role_uv
description: 实际流失角色数（label=1，需等待标签回流后才有值）
domains:
  - 用户生命周期
  - 系统监控
computation:
  sql: COUNT(DISTINCT CASE WHEN label=1 THEN role_id END)
  metadata:
    aggregation: count_distinct
    field: CASE WHEN label=1 THEN role_id END
    source: dws_10000251_algo_role_churn_pred
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_algo_role_churn_pred
    description: 机械提取自 dws_10000251_algo_role_churn_pred 的 metrics 块（key=actual_churn_role_uv）
