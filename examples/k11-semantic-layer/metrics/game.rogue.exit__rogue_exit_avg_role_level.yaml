kind: metric
name: game.rogue.exit__rogue_exit_avg_role_level
description: 退出角色的平均等级
domains:
  - 战斗关卡
computation:
  sql: AVG(CAST(GET_JSON_OBJECT(params,'$.roleLevel') AS BIGINT))
  metadata:
    aggregation: avg
    field: CAST(GET_JSON_OBJECT(params,'$.roleLevel') AS BIGINT)
    source: game.rogue.exit
    time_grain: ""
relations:
  - type: derived_from
    target: game.rogue.exit
    description: 机械提取自 game.rogue.exit 的 metrics 块（key=rogue_exit_avg_role_level）
