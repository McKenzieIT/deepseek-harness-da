kind: metric
name: game.equip.preciseChoose__game_equip_preciseChoose_per_role_cnt
description: 单角色平均精选择操作次数
domains:
  - 装备道具
computation:
  sql: COUNT(*) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: count
    field: "*) / NULLIF(COUNT(DISTINCT role_id), 0"
    source: game.equip.preciseChoose
    time_grain: ""
relations:
  - type: derived_from
    target: game.equip.preciseChoose
    description: 机械提取自 game.equip.preciseChoose 的 metrics 块（key=game_equip_preciseChoose_per_role_cnt）
