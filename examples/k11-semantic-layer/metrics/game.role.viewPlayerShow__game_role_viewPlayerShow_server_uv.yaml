kind: metric
name: game.role.viewPlayerShow__game_role_viewPlayerShow_server_uv
description: 触发查看展示事件的去重游戏服数
domains:
  - 用户生命周期
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: game.role.viewPlayerShow
    time_grain: ""
relations:
  - type: derived_from
    target: game.role.viewPlayerShow
    description: 机械提取自 game.role.viewPlayerShow 的 metrics 块（key=game_role_viewPlayerShow_server_uv）
