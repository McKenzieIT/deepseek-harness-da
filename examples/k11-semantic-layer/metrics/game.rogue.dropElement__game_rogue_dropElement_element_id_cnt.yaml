kind: metric
name: game.rogue.dropElement__game_rogue_dropElement_element_id_cnt
description: 去重元素id数
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.elementId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.elementId')
    source: game.rogue.dropElement
    time_grain: ""
relations:
  - type: derived_from
    target: game.rogue.dropElement
    description: 机械提取自 game.rogue.dropElement 的 metrics 块（key=game_rogue_dropElement_element_id_cnt）
