kind: metric
name: game.pve.rank__pve_rank_progress_reward_cnt
description: 领取进度奖励(progressOrder)的记录数(progressOrder 约41%非空，仅领取进度奖励的记录计入)
domains:
  - 战斗关卡
computation:
  sql: SUM(CASE WHEN GET_JSON_OBJECT(params, '$.progressOrder') IS NOT NULL THEN 1 ELSE 0 END)
  metadata:
    aggregation: sum
    field: CASE WHEN GET_JSON_OBJECT(params, '$.progressOrder') IS NOT NULL THEN 1 ELSE 0 END
    source: game.pve.rank
    time_grain: ""
relations:
  - type: derived_from
    target: game.pve.rank
    description: 机械提取自 game.pve.rank 的 metrics 块（key=pve_rank_progress_reward_cnt）
