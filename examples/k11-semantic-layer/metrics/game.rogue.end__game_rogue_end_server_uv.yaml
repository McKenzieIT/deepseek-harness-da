kind: metric
name: game.rogue.end__game_rogue_end_server_uv
description: 涉及的游戏服数量（去重）
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: game.rogue.end
    time_grain: ""
relations:
  - type: derived_from
    target: game.rogue.end
    description: 机械提取自 game.rogue.end 的 metrics 块（key=game_rogue_end_server_uv）
