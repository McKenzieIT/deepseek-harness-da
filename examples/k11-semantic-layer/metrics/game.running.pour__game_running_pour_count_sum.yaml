kind: metric
name: game.running.pour__game_running_pour_count_sum
description: 赛马下注总注数（count 字段累计，反映下注规模）
domains:
  - 自定义
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.count') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.count') AS BIGINT)
    source: game.running.pour
    time_grain: ""
relations:
  - type: derived_from
    target: game.running.pour
    description: 机械提取自 game.running.pour 的 metrics 块（key=game_running_pour_count_sum）
