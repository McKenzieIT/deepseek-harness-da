kind: metric
name: dws_10000251_univ_role_server_tag_df__retention_rate_d3
description: 3日留存近似率（基于累计活跃天数>=3 的近似口径，非逐日精确留存；本表全为角色行，分母为角色总数）
domains:
  - 用户生命周期
  - 付费经济
  - 角色成长
computation:
  sql: SUM(CASE WHEN act_days_std>=3 THEN 1 ELSE 0 END)*1.0/NULLIF(COUNT(*),0)
  metadata:
    aggregation: sum
    field: CASE WHEN act_days_std>=3 THEN 1 ELSE 0 END)*1.0/NULLIF(COUNT(*),0
    source: dws_10000251_univ_role_server_tag_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_univ_role_server_tag_df
    description: 机械提取自 dws_10000251_univ_role_server_tag_df 的 metrics 块（key=retention_rate_d3）
