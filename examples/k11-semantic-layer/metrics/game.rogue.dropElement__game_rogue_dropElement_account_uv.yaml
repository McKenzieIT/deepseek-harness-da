kind: metric
name: game.rogue.dropElement__game_rogue_dropElement_account_uv
description: 去重丢弃元素账号数
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT account_id)
  metadata:
    aggregation: count_distinct
    field: account_id
    source: game.rogue.dropElement
    time_grain: ""
relations:
  - type: derived_from
    target: game.rogue.dropElement
    description: 机械提取自 game.rogue.dropElement 的 metrics 块（key=game_rogue_dropElement_account_uv）
