kind: metric
name: game.pvp.rank__pvp_rank_id_uv
description: 去重排行榜 ID 数（真正可度量主体维度，实测 37）
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.rankId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.rankId')
    source: game.pvp.rank
    time_grain: ""
relations:
  - type: derived_from
    target: game.pvp.rank
    description: 机械提取自 game.pvp.rank 的 metrics 块（key=pvp_rank_id_uv）
