kind: metric
name: game.artifact.attuned__artifact_attuned_role_uv
description: 鉴定兵符的去重角色数（分析主体）
domains:
  - 角色成长
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.artifact.attuned
    time_grain: ""
relations:
  - type: derived_from
    target: game.artifact.attuned
    description: 机械提取自 game.artifact.attuned 的 metrics 块（key=artifact_attuned_role_uv）
