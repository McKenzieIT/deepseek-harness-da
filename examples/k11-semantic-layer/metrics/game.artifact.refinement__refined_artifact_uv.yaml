kind: metric
name: game.artifact.refinement__refined_artifact_uv
description: 被精炼的去重兵符 uid 数（要使用的兵符 uid）
domains:
  - 角色成长
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.uid'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.uid')
    source: game.artifact.refinement
    time_grain: ""
relations:
  - type: derived_from
    target: game.artifact.refinement
    description: 机械提取自 game.artifact.refinement 的 metrics 块（key=refined_artifact_uv）
