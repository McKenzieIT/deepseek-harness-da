kind: metric
name: game.home.practice__game_home_practice_account_uv
description: 武将特训去重账号数（账号口径参考）
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT account_id)
  metadata:
    aggregation: count_distinct
    field: account_id
    source: game.home.practice
    time_grain: ""
relations:
  - type: derived_from
    target: game.home.practice
    description: 机械提取自 game.home.practice 的 metrics 块（key=game_home_practice_account_uv）
