kind: metric
name: dws_10000251_pay_order_di__pay_arpu
description: 账号口径 ARPU（付费总额/付费账号数）
domains:
  - 付费经济
  - 用户生命周期
computation:
  sql: SUM(pay_amt) / NULLIF(COUNT(DISTINCT account_id), 0)
  metadata:
    aggregation: sum
    field: pay_amt) / NULLIF(COUNT(DISTINCT account_id), 0
    source: dws_10000251_pay_order_di
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_pay_order_di
    description: 机械提取自 dws_10000251_pay_order_di 的 metrics 块（key=pay_arpu）
