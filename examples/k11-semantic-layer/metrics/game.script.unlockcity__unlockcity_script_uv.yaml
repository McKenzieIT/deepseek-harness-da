kind: metric
name: game.script.unlockcity__unlockcity_script_uv
description: 涉及的去重剧本数(scriptId)
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.scriptId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.scriptId')
    source: game.script.unlockcity
    time_grain: ""
relations:
  - type: derived_from
    target: game.script.unlockcity
    description: 机械提取自 game.script.unlockcity 的 metrics 块（key=unlockcity_script_uv）
