kind: metric
name: game.toy.gachaBuyItem__game_toy_gachaBuyItem_pull_per_role
description: 人均购买次数 = 购买次数合计 / 角色去重数（NULLIF 防零除）
domains:
  - 探索收集
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params,'$.count') AS BIGINT)) / NULLIF(COUNT(DISTINCT role_id),0)
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params,'$.count') AS BIGINT)) / NULLIF(COUNT(DISTINCT role_id),0
    source: game.toy.gachaBuyItem
    time_grain: ""
relations:
  - type: derived_from
    target: game.toy.gachaBuyItem
    description: 机械提取自 game.toy.gachaBuyItem 的 metrics 块（key=game_toy_gachaBuyItem_pull_per_role）
