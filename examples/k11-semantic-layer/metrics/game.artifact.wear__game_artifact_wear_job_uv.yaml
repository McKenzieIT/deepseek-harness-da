kind: metric
name: game.artifact.wear__game_artifact_wear_job_uv
description: 穿戴部位（job）的去重数
domains:
  - 角色成长
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.job'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.job')
    source: game.artifact.wear
    time_grain: ""
relations:
  - type: derived_from
    target: game.artifact.wear
    description: 机械提取自 game.artifact.wear 的 metrics 块（key=game_artifact_wear_job_uv）
