kind: metric
name: game.guild.sciencehouselevelup__sciencehouselevelup_level_max
description: 当日观测到的军机处最高升级等级
domains:
  - 社交公会
computation:
  sql: MAX(CAST(GET_JSON_OBJECT(params, '$.level') AS BIGINT))
  metadata:
    aggregation: max
    field: CAST(GET_JSON_OBJECT(params, '$.level') AS BIGINT)
    source: game.guild.sciencehouselevelup
    time_grain: ""
relations:
  - type: derived_from
    target: game.guild.sciencehouselevelup
    description: 机械提取自 game.guild.sciencehouselevelup 的 metrics 块（key=sciencehouselevelup_level_max）
