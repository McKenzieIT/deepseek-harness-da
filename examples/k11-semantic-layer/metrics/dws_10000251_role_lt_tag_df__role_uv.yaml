kind: metric
name: dws_10000251_role_lt_tag_df__role_uv
description: 角色UV：日全量快照下去重角色数（user_type=3主体指标；ds=20251228实测=1559829）
domains:
  - 角色成长
  - 装备道具
computation:
  sql: COUNT(DISTINCT user_id)
  metadata:
    aggregation: count_distinct
    field: user_id
    source: dws_10000251_role_lt_tag_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_role_lt_tag_df
    description: 机械提取自 dws_10000251_role_lt_tag_df 的 metrics 块（key=role_uv）
