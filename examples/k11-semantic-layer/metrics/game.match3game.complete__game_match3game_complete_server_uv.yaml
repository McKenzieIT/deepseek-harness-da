kind: metric
name: game.match3game.complete__game_match3game_complete_server_uv
description: 涉及的游戏服数量（去重）
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: game.match3game.complete
    time_grain: ""
relations:
  - type: derived_from
    target: game.match3game.complete
    description: 机械提取自 game.match3game.complete 的 metrics 块（key=game_match3game_complete_server_uv）
