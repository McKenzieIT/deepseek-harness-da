kind: metric
name: game.battlepass.missionReward__battlepass_mission_reward_bp_exp_sum
description: 领奖发放的战令经验总量(battlePassExp 求和)
domains:
  - 角色成长
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.battlePassExp') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.battlePassExp') AS BIGINT)
    source: game.battlepass.missionReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.battlepass.missionReward
    description: 机械提取自 game.battlepass.missionReward 的 metrics 块（key=battlepass_mission_reward_bp_exp_sum）
