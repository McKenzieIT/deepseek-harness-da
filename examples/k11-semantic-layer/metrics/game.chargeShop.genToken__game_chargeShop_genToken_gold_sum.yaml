kind: metric
name: game.chargeShop.genToken__game_chargeShop_genToken_gold_sum
description: 申请充值时角色"充值钻石"余额快照总量（coinList.gold，非实际入账金额，仅为发起时刻的货币快照）
domains:
  - 付费经济
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.coinList.gold') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.coinList.gold') AS BIGINT)
    source: game.chargeShop.genToken
    time_grain: ""
relations:
  - type: derived_from
    target: game.chargeShop.genToken
    description: 机械提取自 game.chargeShop.genToken 的 metrics 块（key=game_chargeShop_genToken_gold_sum）
