kind: metric
name: game.rogue.skipBattle__game_rogue_skipBattle_per_role_avg_cnt
description: 人均跳过战斗次数（总次数/角色UV）
domains:
  - 战斗关卡
computation:
  sql: COUNT(*)::FLOAT / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: count
    field: "*)::FLOAT / NULLIF(COUNT(DISTINCT role_id), 0"
    source: game.rogue.skipBattle
    time_grain: ""
relations:
  - type: derived_from
    target: game.rogue.skipBattle
    description: 机械提取自 game.rogue.skipBattle 的 metrics 块（key=game_rogue_skipBattle_per_role_avg_cnt）
