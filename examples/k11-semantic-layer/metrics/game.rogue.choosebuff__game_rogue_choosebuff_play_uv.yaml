kind: metric
name: game.rogue.choosebuff__game_rogue_choosebuff_play_uv
description: 涉及的古战场副本挑战次数（playId去重）
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.playId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.playId')
    source: game.rogue.choosebuff
    time_grain: ""
relations:
  - type: derived_from
    target: game.rogue.choosebuff
    description: 机械提取自 game.rogue.choosebuff 的 metrics 块（key=game_rogue_choosebuff_play_uv）
