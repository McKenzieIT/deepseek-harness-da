kind: metric
name: game.artifact.attuned__artifact_attuned_cnt
description: 兵符鉴定事件总次数（含同一角色多次鉴定）
domains:
  - 角色成长
computation:
  sql: COUNT(*)
  metadata:
    aggregation: count
    field: "*"
    source: game.artifact.attuned
    time_grain: ""
relations:
  - type: derived_from
    target: game.artifact.attuned
    description: 机械提取自 game.artifact.attuned 的 metrics 块（key=artifact_attuned_cnt）
