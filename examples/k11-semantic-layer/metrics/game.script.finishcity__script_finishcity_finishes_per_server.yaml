kind: metric
name: game.script.finishcity__script_finishcity_finishes_per_server
description: 每个游戏服的平均完成城池次数（衡量各服剧本推进热度，NULLIF 防除零）
domains:
  - 自定义
computation:
  sql: CAST(COUNT(*) AS DOUBLE) / NULLIF(COUNT(DISTINCT server_id), 0)
  metadata:
    aggregation: ""
    field: ""
    source: game.script.finishcity
    time_grain: ""
relations:
  - type: derived_from
    target: game.script.finishcity
    description: 机械提取自 game.script.finishcity 的 metrics 块（key=script_finishcity_finishes_per_server）
