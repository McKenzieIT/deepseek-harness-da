kind: metric
name: dws_10000251_resource_item_di__uv_role
description: 涉产角色UV（当日有道具增/减记录的去重角色数）
domains:
  - 资源产销
  - 装备道具
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: dws_10000251_resource_item_di
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_resource_item_di
    description: 机械提取自 dws_10000251_resource_item_di 的 metrics 块（key=uv_role）
