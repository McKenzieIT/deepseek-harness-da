kind: metric
name: game.guild.cancelInviteItem__guild_cancel_invite_item_per_role_avg_cnt
description: 人均取消邀请条目次数(总次数/独立角色数；实测 ≈2.33，3个角色触发7次取消)
domains:
  - 社交公会
computation:
  sql: COUNT(*) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: count
    field: "*) / NULLIF(COUNT(DISTINCT role_id), 0"
    source: game.guild.cancelInviteItem
    time_grain: ""
relations:
  - type: derived_from
    target: game.guild.cancelInviteItem
    description: 机械提取自 game.guild.cancelInviteItem 的 metrics 块（key=guild_cancel_invite_item_per_role_avg_cnt）
