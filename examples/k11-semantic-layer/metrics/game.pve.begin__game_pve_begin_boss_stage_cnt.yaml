kind: metric
name: game.pve.begin__game_pve_begin_boss_stage_cnt
description: 头目关卡(stageType=2)进入次数
domains:
  - 战斗关卡
computation:
  sql: SUM(CASE WHEN GET_JSON_OBJECT(params, '$.stageType') = '2' THEN 1 ELSE 0 END)
  metadata:
    aggregation: sum
    field: CASE WHEN GET_JSON_OBJECT(params, '$.stageType') = '2' THEN 1 ELSE 0 END
    source: game.pve.begin
    time_grain: ""
relations:
  - type: derived_from
    target: game.pve.begin
    description: 机械提取自 game.pve.begin 的 metrics 块（key=game_pve_begin_boss_stage_cnt）
