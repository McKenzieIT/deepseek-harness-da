kind: metric
name: game.Monopoly.completeTalk__game_Monopoly_completeTalk_talk_index_sum
description: 大富翁对话talkIndex累计值(对话序号求和)
domains:
  - 自定义
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params,'$.talkIndex') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params,'$.talkIndex') AS BIGINT)
    source: game.Monopoly.completeTalk
    time_grain: ""
relations:
  - type: derived_from
    target: game.Monopoly.completeTalk
    description: 机械提取自 game.Monopoly.completeTalk 的 metrics 块（key=game_Monopoly_completeTalk_talk_index_sum）
