kind: metric
name: dws_10000251_algo_role_churn_train__train_sample_role_uv
description: 训练样本角色去重数(UV)，按 role_id 去重
domains:
  - 用户生命周期
  - 角色成长
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: dws_10000251_algo_role_churn_train
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_algo_role_churn_train
    description: 机械提取自 dws_10000251_algo_role_churn_train 的 metrics 块（key=train_sample_role_uv）
