kind: metric
name: game.champion.guessGetReward__game_champion_guessGetReward_gold_sum
description: 领奖后充值钻石余额总量
domains:
  - 战斗关卡
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.coinList.gold') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.coinList.gold') AS BIGINT)
    source: game.champion.guessGetReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.champion.guessGetReward
    description: 机械提取自 game.champion.guessGetReward 的 metrics 块（key=game_champion_guessGetReward_gold_sum）
