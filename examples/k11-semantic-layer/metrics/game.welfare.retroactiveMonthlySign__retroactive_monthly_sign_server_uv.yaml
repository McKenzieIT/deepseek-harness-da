kind: metric
name: game.welfare.retroactiveMonthlySign__retroactive_monthly_sign_server_uv
description: 补签覆盖游戏服数
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: game.welfare.retroactiveMonthlySign
    time_grain: ""
relations:
  - type: derived_from
    target: game.welfare.retroactiveMonthlySign
    description: 机械提取自 game.welfare.retroactiveMonthlySign 的 metrics 块（key=retroactive_monthly_sign_server_uv）
