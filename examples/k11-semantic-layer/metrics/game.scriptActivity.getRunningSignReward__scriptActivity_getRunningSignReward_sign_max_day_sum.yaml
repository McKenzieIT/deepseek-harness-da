kind: metric
name: game.scriptActivity.getRunningSignReward__scriptActivity_getRunningSignReward_sign_max_day_sum
description: 领取到的签到位置(signRewardMaxDay)累计值
domains:
  - 自定义
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params,'$.signRewardMaxDay') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params,'$.signRewardMaxDay') AS BIGINT)
    source: game.scriptActivity.getRunningSignReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.scriptActivity.getRunningSignReward
    description: 机械提取自 game.scriptActivity.getRunningSignReward 的 metrics 块（key=scriptActivity_getRunningSignReward_sign_max_day_sum）
