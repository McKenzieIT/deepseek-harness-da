kind: metric
name: game.toy.techactive__game_toy_techactive_exp_sum
description: 激活时刻角色经验(exp)累计（瞬时快照求和，非增量）
domains:
  - 探索收集
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params,'$.exp') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params,'$.exp') AS BIGINT)
    source: game.toy.techactive
    time_grain: ""
relations:
  - type: derived_from
    target: game.toy.techactive
    description: 机械提取自 game.toy.techactive 的 metrics 块（key=game_toy_techactive_exp_sum）
