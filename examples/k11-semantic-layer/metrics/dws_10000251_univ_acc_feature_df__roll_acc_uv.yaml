kind: metric
name: dws_10000251_univ_acc_feature_df__roll_acc_uv
description: 滚服账号数（roll_flag=1，账号维度滚服标识）
domains:
  - 用户生命周期
  - 角色成长
computation:
  sql: COUNT(DISTINCT CASE WHEN roll_flag = 1 THEN user_id END)
  metadata:
    aggregation: count_distinct
    field: CASE WHEN roll_flag = 1 THEN user_id END
    source: dws_10000251_univ_acc_feature_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_univ_acc_feature_df
    description: 机械提取自 dws_10000251_univ_acc_feature_df 的 metrics 块（key=roll_acc_uv）
