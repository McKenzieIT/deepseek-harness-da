kind: metric
name: game.resort.accept__resort_accept_server_uv
description: 覆盖的去重游戏服数
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: game.resort.accept
    time_grain: ""
relations:
  - type: derived_from
    target: game.resort.accept
    description: 机械提取自 game.resort.accept 的 metrics 块（key=resort_accept_server_uv）
