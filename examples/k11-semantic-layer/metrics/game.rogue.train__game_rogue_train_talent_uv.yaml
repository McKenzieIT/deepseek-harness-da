kind: metric
name: game.rogue.train__game_rogue_train_talent_uv
description: 被激活的去重天赋id数量
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.talentId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.talentId')
    source: game.rogue.train
    time_grain: ""
relations:
  - type: derived_from
    target: game.rogue.train
    description: 机械提取自 game.rogue.train 的 metrics 块（key=game_rogue_train_talent_uv）
