kind: metric
name: game.tactic.share__game_tactic_share_tactic_uv
description: 被分享的去重战法id数（tacticId）
domains:
  - 角色成长
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.tacticId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.tacticId')
    source: game.tactic.share
    time_grain: ""
relations:
  - type: derived_from
    target: game.tactic.share
    description: 机械提取自 game.tactic.share 的 metrics 块（key=game_tactic_share_tactic_uv）
