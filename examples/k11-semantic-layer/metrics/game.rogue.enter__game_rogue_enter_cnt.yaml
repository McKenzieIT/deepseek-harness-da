kind: metric
name: game.rogue.enter__game_rogue_enter_cnt
description: 古战场进入事件总次数
domains:
  - 战斗关卡
computation:
  sql: COUNT(*)
  metadata:
    aggregation: count
    field: "*"
    source: game.rogue.enter
    time_grain: ""
relations:
  - type: derived_from
    target: game.rogue.enter
    description: 机械提取自 game.rogue.enter 的 metrics 块（key=game_rogue_enter_cnt）
