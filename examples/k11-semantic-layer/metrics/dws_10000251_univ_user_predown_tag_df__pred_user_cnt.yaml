kind: metric
name: dws_10000251_univ_user_predown_tag_df__pred_user_cnt
description: 预下载用户去重数（按 (user_type, user_id) 组合去重，避免跨主体 ID 空间重叠失真）
domains:
  - 用户生命周期
  - 资源产销
computation:
  sql: COUNT(DISTINCT CONCAT(CAST(user_type AS string), '_', user_id))
  metadata:
    aggregation: count_distinct
    field: CONCAT(CAST(user_type AS string), '_', user_id)
    source: dws_10000251_univ_user_predown_tag_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_univ_user_predown_tag_df
    description: 机械提取自 dws_10000251_univ_user_predown_tag_df 的 metrics 块（key=pred_user_cnt）
