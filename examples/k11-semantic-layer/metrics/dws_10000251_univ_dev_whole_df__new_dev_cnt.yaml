kind: metric
name: dws_10000251_univ_dev_whole_df__new_dev_cnt
description: 新增设备数；配合 WHERE act_tm_fst 当日过滤，统计当日(或区间)新增的设备数量
domains:
  - 用户生命周期
  - 付费经济
computation:
  sql: COUNT(DISTINCT user_id)
  metadata:
    aggregation: count_distinct
    field: user_id
    source: dws_10000251_univ_dev_whole_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_univ_dev_whole_df
    description: 机械提取自 dws_10000251_univ_dev_whole_df 的 metrics 块（key=new_dev_cnt）
