kind: metric
name: dws_10000251_vip_contact_info_df__wx_contact_rate
description: 企微建联率（企微已建联账号数/总账号数）
domains:
  - 用户生命周期
computation:
  sql: SUM(CASE WHEN is_wx_contact=1 THEN 1 ELSE 0 END) / NULLIF(COUNT(*),0)
  metadata:
    aggregation: sum
    field: CASE WHEN is_wx_contact=1 THEN 1 ELSE 0 END) / NULLIF(COUNT(*),0
    source: dws_10000251_vip_contact_info_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_vip_contact_info_df
    description: 机械提取自 dws_10000251_vip_contact_info_df 的 metrics 块（key=wx_contact_rate）
