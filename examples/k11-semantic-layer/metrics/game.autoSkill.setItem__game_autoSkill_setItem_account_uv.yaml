kind: metric
name: game.autoSkill.setItem__game_autoSkill_setItem_account_uv
description: 触发自动战斗方案设置的去重账号数
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT account_id)
  metadata:
    aggregation: count_distinct
    field: account_id
    source: game.autoSkill.setItem
    time_grain: ""
relations:
  - type: derived_from
    target: game.autoSkill.setItem
    description: 机械提取自 game.autoSkill.setItem 的 metrics 块（key=game_autoSkill_setItem_account_uv）
