kind: metric
name: game.equip.autoBreak__equip_autoBreak_role_per_server_ratio
description: 平均每区服突破角色数(角色UV/区服UV；衡量区服集中度)
domains:
  - 装备道具
computation:
  sql: ROUND(CAST(COUNT(DISTINCT role_id) AS DOUBLE) / NULLIF(COUNT(DISTINCT server_id), 0), 2)
  metadata:
    aggregation: ""
    field: ""
    source: game.equip.autoBreak
    time_grain: ""
relations:
  - type: derived_from
    target: game.equip.autoBreak
    description: 机械提取自 game.equip.autoBreak 的 metrics 块（key=equip_autoBreak_role_per_server_ratio）
