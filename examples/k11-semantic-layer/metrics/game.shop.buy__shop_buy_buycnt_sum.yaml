kind: metric
name: game.shop.buy__shop_buy_buycnt_sum
description: 商店购买数量合计（buyCnt 求和）
domains:
  - 付费经济
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params,'$.buyCnt') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params,'$.buyCnt') AS BIGINT)
    source: game.shop.buy
    time_grain: ""
relations:
  - type: derived_from
    target: game.shop.buy
    description: 机械提取自 game.shop.buy 的 metrics 块（key=shop_buy_buycnt_sum）
