kind: metric
name: game.script.finishtalk__game_script_finishtalk_script_uv
description: 完成对话涉及的去重剧本数(scriptId)
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.scriptId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.scriptId')
    source: game.script.finishtalk
    time_grain: ""
relations:
  - type: derived_from
    target: game.script.finishtalk
    description: 机械提取自 game.script.finishtalk 的 metrics 块（key=game_script_finishtalk_script_uv）
