kind: metric
name: game.battlepass.levelup__battlepass_levelup_max_order
description: 当日触发的最高战令档位等级（order 最大值）
domains:
  - 角色成长
computation:
  sql: MAX(CAST(GET_JSON_OBJECT(params, '$.order') AS BIGINT))
  metadata:
    aggregation: max
    field: CAST(GET_JSON_OBJECT(params, '$.order') AS BIGINT)
    source: game.battlepass.levelup
    time_grain: ""
relations:
  - type: derived_from
    target: game.battlepass.levelup
    description: 机械提取自 game.battlepass.levelup 的 metrics 块（key=battlepass_levelup_max_order）
