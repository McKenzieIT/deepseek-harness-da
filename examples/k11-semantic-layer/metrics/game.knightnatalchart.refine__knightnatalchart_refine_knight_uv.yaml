kind: metric
name: game.knightnatalchart.refine__knightnatalchart_refine_knight_uv
description: 被改命的武将种类数（按 knightId 去重）
domains:
  - 角色成长
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.knightId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.knightId')
    source: game.knightnatalchart.refine
    time_grain: ""
relations:
  - type: derived_from
    target: game.knightnatalchart.refine
    description: 机械提取自 game.knightnatalchart.refine 的 metrics 块（key=knightnatalchart_refine_knight_uv）
