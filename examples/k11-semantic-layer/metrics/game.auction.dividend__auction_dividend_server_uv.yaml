kind: metric
name: game.auction.dividend__auction_dividend_server_uv
description: 触发拍卖行分红的服务器去重数
domains:
  - 付费经济
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: game.auction.dividend
    time_grain: ""
relations:
  - type: derived_from
    target: game.auction.dividend
    description: 机械提取自 game.auction.dividend 的 metrics 块（key=auction_dividend_server_uv）
