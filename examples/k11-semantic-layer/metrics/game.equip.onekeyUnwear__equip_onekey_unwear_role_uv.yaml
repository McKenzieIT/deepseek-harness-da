kind: metric
name: game.equip.onekeyUnwear__equip_onekey_unwear_role_uv
description: 触发一键脱下的去重角色数（分析主体粒度：role_id）
domains:
  - 装备道具
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.equip.onekeyUnwear
    time_grain: ""
relations:
  - type: derived_from
    target: game.equip.onekeyUnwear
    description: 机械提取自 game.equip.onekeyUnwear 的 metrics 块（key=equip_onekey_unwear_role_uv）
