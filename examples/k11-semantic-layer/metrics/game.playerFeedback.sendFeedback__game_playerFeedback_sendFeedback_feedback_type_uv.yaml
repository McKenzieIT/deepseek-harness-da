kind: metric
name: game.playerFeedback.sendFeedback__game_playerFeedback_sendFeedback_feedback_type_uv
description: 反馈类型去重数（1游戏问题/2建议）
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.type'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.type')
    source: game.playerFeedback.sendFeedback
    time_grain: ""
relations:
  - type: derived_from
    target: game.playerFeedback.sendFeedback
    description: 机械提取自 game.playerFeedback.sendFeedback 的 metrics 块（key=game_playerFeedback_sendFeedback_feedback_type_uv）
