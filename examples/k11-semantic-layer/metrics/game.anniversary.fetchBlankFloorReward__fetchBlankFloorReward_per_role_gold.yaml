kind: metric
name: game.anniversary.fetchBlankFloorReward__fetchBlankFloorReward_per_role_gold
description: 人均充值钻石奖励（gold 总量 / 去重角色数）
domains:
  - 付费经济
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params,'$.coinList.gold') AS BIGINT)) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params,'$.coinList.gold') AS BIGINT)) / NULLIF(COUNT(DISTINCT role_id), 0
    source: game.anniversary.fetchBlankFloorReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.anniversary.fetchBlankFloorReward
    description: 机械提取自 game.anniversary.fetchBlankFloorReward 的 metrics 块（key=fetchBlankFloorReward_per_role_gold）
