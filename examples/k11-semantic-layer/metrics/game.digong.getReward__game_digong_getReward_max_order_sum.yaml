kind: metric
name: game.digong.getReward__game_digong_getReward_max_order_sum
description: 地宫领奖最高领取位置累计（SUM maxOrder，反映领奖进度总量）
domains:
  - 自定义
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params,'$.maxOrder') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params,'$.maxOrder') AS BIGINT)
    source: game.digong.getReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.digong.getReward
    description: 机械提取自 game.digong.getReward 的 metrics 块（key=game_digong_getReward_max_order_sum）
