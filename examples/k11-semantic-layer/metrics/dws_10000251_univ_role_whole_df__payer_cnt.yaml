kind: metric
name: dws_10000251_univ_role_whole_df__payer_cnt
description: 付费角色数(累计付费金额大于 0 的去重角色数)
domains:
  - 用户生命周期
  - 付费经济
computation:
  sql: COUNT(DISTINCT CASE WHEN pay_amt_std > 0 THEN user_id END)
  metadata:
    aggregation: count_distinct
    field: CASE WHEN pay_amt_std > 0 THEN user_id END
    source: dws_10000251_univ_role_whole_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_univ_role_whole_df
    description: 机械提取自 dws_10000251_univ_role_whole_df 的 metrics 块（key=payer_cnt）
