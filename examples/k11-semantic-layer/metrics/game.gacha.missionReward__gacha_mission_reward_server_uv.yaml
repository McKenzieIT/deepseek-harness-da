kind: metric
name: game.gacha.missionReward__gacha_mission_reward_server_uv
description: 领取抽卡任务奖励的去重游戏服数
domains:
  - 探索收集
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: game.gacha.missionReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.gacha.missionReward
    description: 机械提取自 game.gacha.missionReward 的 metrics 块（key=gacha_mission_reward_server_uv）
