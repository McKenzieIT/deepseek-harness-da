kind: metric
name: game.achievement.reward__achievement_reward_star_sum
description: 成就星数累计（成就完成深度）
domains:
  - 角色成长
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params,'$.order') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params,'$.order') AS BIGINT)
    source: game.achievement.reward
    time_grain: ""
relations:
  - type: derived_from
    target: game.achievement.reward
    description: 机械提取自 game.achievement.reward 的 metrics 块（key=achievement_reward_star_sum）
