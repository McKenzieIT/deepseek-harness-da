kind: metric
name: game.toy.gachaMissionOnekey__gacha_mission_onekey_server_uv
description: 触发抽卡任务一键领取的去重服务器数
domains:
  - 探索收集
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: game.toy.gachaMissionOnekey
    time_grain: ""
relations:
  - type: derived_from
    target: game.toy.gachaMissionOnekey
    description: 机械提取自 game.toy.gachaMissionOnekey 的 metrics 块（key=gacha_mission_onekey_server_uv）
