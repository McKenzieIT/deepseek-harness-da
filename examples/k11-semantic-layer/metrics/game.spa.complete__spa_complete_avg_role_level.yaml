kind: metric
name: game.spa.complete__spa_complete_avg_role_level
description: 触发事件角色的平均等级（等级累计 / 事件数，NULLIF 防 0 除）
domains:
  - 自定义
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.roleLevel') AS BIGINT)) / NULLIF(COUNT(*), 0)
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.roleLevel') AS BIGINT)) / NULLIF(COUNT(*), 0
    source: game.spa.complete
    time_grain: ""
relations:
  - type: derived_from
    target: game.spa.complete
    description: 机械提取自 game.spa.complete 的 metrics 块（key=spa_complete_avg_role_level）
