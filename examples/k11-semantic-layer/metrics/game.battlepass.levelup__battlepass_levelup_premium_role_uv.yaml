kind: metric
name: game.battlepass.levelup__battlepass_levelup_premium_role_uv
description: 触发高级/珍藏版战令升级的去重角色数
domains:
  - 角色成长
computation:
  sql: COUNT(DISTINCT CASE WHEN CAST(GET_JSON_OBJECT(params, '$.enabledStatus') AS BIGINT) >= 1 THEN role_id END)
  metadata:
    aggregation: count_distinct
    field: CASE WHEN CAST(GET_JSON_OBJECT(params, '$.enabledStatus') AS BIGINT) >= 1 THEN role_id END
    source: game.battlepass.levelup
    time_grain: ""
relations:
  - type: derived_from
    target: game.battlepass.levelup
    description: 机械提取自 game.battlepass.levelup 的 metrics 块（key=battlepass_levelup_premium_role_uv）
