kind: metric
name: dws_10000251_selfhelp_new_remain_df__d7_retention_rate
description: 7日留存率(D7留存账号/新增账号，user_type=2)
domains:
  - 用户生命周期
  - 系统监控
computation:
  sql: COUNT(DISTINCT CASE WHEN lt=7 AND user_type=2 THEN user_id END) / NULLIF(COUNT(DISTINCT CASE WHEN lt=1 AND user_type=2 THEN user_id END), 0)
  metadata:
    aggregation: count_distinct
    field: CASE WHEN lt=7 AND user_type=2 THEN user_id END) / NULLIF(COUNT(DISTINCT CASE WHEN lt=1 AND user_type=2 THEN user_id END), 0
    source: dws_10000251_selfhelp_new_remain_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_selfhelp_new_remain_df
    description: 机械提取自 dws_10000251_selfhelp_new_remain_df 的 metrics 块（key=d7_retention_rate）
