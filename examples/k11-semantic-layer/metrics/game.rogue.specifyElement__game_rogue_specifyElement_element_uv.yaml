kind: metric
name: game.rogue.specifyElement__game_rogue_specifyElement_element_uv
description: 被指定的元素id去重数（element）
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params,'$.element'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params,'$.element')
    source: game.rogue.specifyElement
    time_grain: ""
relations:
  - type: derived_from
    target: game.rogue.specifyElement
    description: 机械提取自 game.rogue.specifyElement 的 metrics 块（key=game_rogue_specifyElement_element_uv）
