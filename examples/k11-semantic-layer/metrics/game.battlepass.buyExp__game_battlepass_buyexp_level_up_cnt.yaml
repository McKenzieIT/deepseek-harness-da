kind: metric
name: game.battlepass.buyExp__game_battlepass_buyexp_level_up_cnt
description: 因购买经验导致角色等级提升的事件次数
domains:
  - 角色成长
computation:
  sql: SUM(CASE WHEN CAST(GET_JSON_OBJECT(params, '$.newLevel') AS BIGINT) > CAST(GET_JSON_OBJECT(params, '$.oldLevel') AS BIGINT) THEN 1 ELSE 0 END)
  metadata:
    aggregation: sum
    field: CASE WHEN CAST(GET_JSON_OBJECT(params, '$.newLevel') AS BIGINT) > CAST(GET_JSON_OBJECT(params, '$.oldLevel') AS BIGINT) THEN 1 ELSE 0 END
    source: game.battlepass.buyExp
    time_grain: ""
relations:
  - type: derived_from
    target: game.battlepass.buyExp
    description: 机械提取自 game.battlepass.buyExp 的 metrics 块（key=game_battlepass_buyexp_level_up_cnt）
