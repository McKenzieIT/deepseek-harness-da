kind: metric
name: game.scriptActivity.taskBattlepassMissionOnekeyReward__bp_onekey_reward_role_uv
description: 触发战令一键领取的 distinct 角色数（分析主体为 role_id）
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.scriptActivity.taskBattlepassMissionOnekeyReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.scriptActivity.taskBattlepassMissionOnekeyReward
    description: 机械提取自 game.scriptActivity.taskBattlepassMissionOnekeyReward 的 metrics 块（key=bp_onekey_reward_role_uv）
