kind: metric
name: game.digong.getReward__game_digong_getReward_role_uv
description: 地宫领奖触达角色数（COUNT DISTINCT role_id）
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.digong.getReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.digong.getReward
    description: 机械提取自 game.digong.getReward 的 metrics 块（key=game_digong_getReward_role_uv）
