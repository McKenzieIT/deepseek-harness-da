kind: metric
name: game.fantasyrecharge.finalreward__fantasyrecharge_finalreward_diamond_sum
description: 领取最终奖励涉及紫金(diamond)总量
domains:
  - 付费经济
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.coinList.diamond') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.coinList.diamond') AS BIGINT)
    source: game.fantasyrecharge.finalreward
    time_grain: ""
relations:
  - type: derived_from
    target: game.fantasyrecharge.finalreward
    description: 机械提取自 game.fantasyrecharge.finalreward 的 metrics 块（key=fantasyrecharge_finalreward_diamond_sum）
