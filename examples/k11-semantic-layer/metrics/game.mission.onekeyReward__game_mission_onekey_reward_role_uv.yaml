kind: metric
name: game.mission.onekeyReward__game_mission_onekey_reward_role_uv
description: 领取奖励的去重角色数（分析主体粒度）
domains:
  - 角色成长
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.mission.onekeyReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.mission.onekeyReward
    description: 机械提取自 game.mission.onekeyReward 的 metrics 块（key=game_mission_onekey_reward_role_uv）
