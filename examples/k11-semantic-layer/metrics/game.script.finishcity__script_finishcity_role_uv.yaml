kind: metric
name: game.script.finishcity__script_finishcity_role_uv
description: 去重完成方角色数（完成城池的角色，分析默认主体）
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.script.finishcity
    time_grain: ""
relations:
  - type: derived_from
    target: game.script.finishcity
    description: 机械提取自 game.script.finishcity 的 metrics 块（key=script_finishcity_role_uv）
