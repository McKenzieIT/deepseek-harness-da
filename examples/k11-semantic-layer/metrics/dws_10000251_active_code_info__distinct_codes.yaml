kind: metric
name: dws_10000251_active_code_info__distinct_codes
description: 去重激活码数
domains:
  - 资源产销
computation:
  sql: COUNT(DISTINCT code)
  metadata:
    aggregation: count_distinct
    field: code
    source: dws_10000251_active_code_info
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_active_code_info
    description: 机械提取自 dws_10000251_active_code_info 的 metrics 块（key=distinct_codes）
