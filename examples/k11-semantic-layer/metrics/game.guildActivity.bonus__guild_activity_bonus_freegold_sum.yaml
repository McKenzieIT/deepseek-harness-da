kind: metric
name: game.guildActivity.bonus__guild_activity_bonus_freegold_sum
description: 分红发放的免费钻石总量(coinList.freegold 求和；实测 ds=20260729 合计 812975)
domains:
  - 社交公会
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.coinList.freegold') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.coinList.freegold') AS BIGINT)
    source: game.guildActivity.bonus
    time_grain: ""
relations:
  - type: derived_from
    target: game.guildActivity.bonus
    description: 机械提取自 game.guildActivity.bonus 的 metrics 块（key=guild_activity_bonus_freegold_sum）
