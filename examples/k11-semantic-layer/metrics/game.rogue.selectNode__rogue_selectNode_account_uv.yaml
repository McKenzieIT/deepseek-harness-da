kind: metric
name: game.rogue.selectNode__rogue_selectNode_account_uv
description: 触发选择节点事件的去重账号数
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT account_id)
  metadata:
    aggregation: count_distinct
    field: account_id
    source: game.rogue.selectNode
    time_grain: ""
relations:
  - type: derived_from
    target: game.rogue.selectNode
    description: 机械提取自 game.rogue.selectNode 的 metrics 块（key=rogue_selectNode_account_uv）
