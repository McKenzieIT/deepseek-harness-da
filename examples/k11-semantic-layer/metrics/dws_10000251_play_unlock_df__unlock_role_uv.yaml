kind: metric
name: dws_10000251_play_unlock_df__unlock_role_uv
description: 当日解锁任意玩法功能的不重复角色数(解锁 UV)
domains:
  - 探索收集
  - 角色成长
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: dws_10000251_play_unlock_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_play_unlock_df
    description: 机械提取自 dws_10000251_play_unlock_df 的 metrics 块（key=unlock_role_uv）
