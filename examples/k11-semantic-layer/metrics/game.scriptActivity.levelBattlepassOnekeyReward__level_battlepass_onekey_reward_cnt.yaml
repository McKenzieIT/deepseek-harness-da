kind: metric
name: game.scriptActivity.levelBattlepassOnekeyReward__level_battlepass_onekey_reward_cnt
description: 等级战令一键领取事件触发总次数
domains:
  - 自定义
computation:
  sql: COUNT(*)
  metadata:
    aggregation: count
    field: "*"
    source: game.scriptActivity.levelBattlepassOnekeyReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.scriptActivity.levelBattlepassOnekeyReward
    description: 机械提取自 game.scriptActivity.levelBattlepassOnekeyReward 的 metrics 块（key=level_battlepass_onekey_reward_cnt）
