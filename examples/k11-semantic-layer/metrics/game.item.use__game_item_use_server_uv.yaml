kind: metric
name: game.item.use__game_item_use_server_uv
description: 涉及道具使用的去重游戏服数
domains:
  - 装备道具
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: game.item.use
    time_grain: ""
relations:
  - type: derived_from
    target: game.item.use
    description: 机械提取自 game.item.use 的 metrics 块（key=game_item_use_server_uv）
