kind: metric
name: game.social.guild.getmissionbox__game_social_guild_getmissionbox_guild_id_uv
description: 涉及的去重公会数（实测 guildId 100%非空、ds=20260729 共219个公会）
domains:
  - 社交公会
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.guildId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.guildId')
    source: game.social.guild.getmissionbox
    time_grain: ""
relations:
  - type: derived_from
    target: game.social.guild.getmissionbox
    description: 机械提取自 game.social.guild.getmissionbox 的 metrics 块（key=game_social_guild_getmissionbox_guild_id_uv）
