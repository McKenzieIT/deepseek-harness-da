kind: metric
name: game.gacha.missionReward__gacha_mission_reward_role_uv
description: 领取抽卡任务奖励的去重角色数
domains:
  - 探索收集
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.gacha.missionReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.gacha.missionReward
    description: 机械提取自 game.gacha.missionReward 的 metrics 块（key=gacha_mission_reward_role_uv）
