kind: metric
name: game.rogue.choosesoul__game_rogue_choosesoul_choices_per_role
description: 角色平均将魂选择次数（人均深度，反映古战场将魂选择节点的触发频度）
domains:
  - 战斗关卡
computation:
  sql: COUNT(*) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: count
    field: "*) / NULLIF(COUNT(DISTINCT role_id), 0"
    source: game.rogue.choosesoul
    time_grain: ""
relations:
  - type: derived_from
    target: game.rogue.choosesoul
    description: 机械提取自 game.rogue.choosesoul 的 metrics 块（key=game_rogue_choosesoul_choices_per_role）
