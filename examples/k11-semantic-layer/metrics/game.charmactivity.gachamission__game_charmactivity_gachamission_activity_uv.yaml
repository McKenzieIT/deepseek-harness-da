kind: metric
name: game.charmactivity.gachamission__game_charmactivity_gachamission_activity_uv
description: 涉及的灵器抽卡活动数量（activityId 去重）
domains:
  - 探索收集
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.activityId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.activityId')
    source: game.charmactivity.gachamission
    time_grain: ""
relations:
  - type: derived_from
    target: game.charmactivity.gachamission
    description: 机械提取自 game.charmactivity.gachamission 的 metrics 块（key=game_charmactivity_gachamission_activity_uv）
