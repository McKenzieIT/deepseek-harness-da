kind: metric
name: dws_10000251_biz_acc_tag_df__tag_rate
description: 账号打标渗透率 = 已打标账号数 / 累计账号数
domains:
  - 用户生命周期
  - 系统监控
computation:
  sql: COUNT(DISTINCT CASE WHEN biz_name IS NOT NULL THEN user_id END) * 1.0 / NULLIF(COUNT(DISTINCT user_id), 0)
  metadata:
    aggregation: count_distinct
    field: CASE WHEN biz_name IS NOT NULL THEN user_id END) * 1.0 / NULLIF(COUNT(DISTINCT user_id), 0
    source: dws_10000251_biz_acc_tag_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_biz_acc_tag_df
    description: 机械提取自 dws_10000251_biz_acc_tag_df 的 metrics 块（key=tag_rate）
