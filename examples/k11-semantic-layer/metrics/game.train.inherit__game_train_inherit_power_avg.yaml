kind: metric
name: game.train.inherit__game_train_inherit_power_avg
description: 继承后新武将平均战力
domains:
  - 角色成长
computation:
  sql: AVG(CAST(GET_JSON_OBJECT(params, '$.power') AS BIGINT))
  metadata:
    aggregation: avg
    field: CAST(GET_JSON_OBJECT(params, '$.power') AS BIGINT)
    source: game.train.inherit
    time_grain: ""
relations:
  - type: derived_from
    target: game.train.inherit
    description: 机械提取自 game.train.inherit 的 metrics 块（key=game_train_inherit_power_avg）
