kind: metric
name: game.recover.recover__recover_account_uv
description: 触发恢复的 distinct 账号数
domains:
  - 资源产销
computation:
  sql: COUNT(DISTINCT account_id)
  metadata:
    aggregation: count_distinct
    field: account_id
    source: game.recover.recover
    time_grain: ""
relations:
  - type: derived_from
    target: game.recover.recover
    description: 机械提取自 game.recover.recover 的 metrics 块（key=recover_account_uv）
