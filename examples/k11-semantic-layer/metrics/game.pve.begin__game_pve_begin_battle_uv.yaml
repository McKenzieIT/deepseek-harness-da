kind: metric
name: game.pve.begin__game_pve_begin_battle_uv
description: 去重战斗会话数（battleId 唯一标识一次战斗）
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.battleId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.battleId')
    source: game.pve.begin
    time_grain: ""
relations:
  - type: derived_from
    target: game.pve.begin
    description: 机械提取自 game.pve.begin 的 metrics 块（key=game_pve_begin_battle_uv）
