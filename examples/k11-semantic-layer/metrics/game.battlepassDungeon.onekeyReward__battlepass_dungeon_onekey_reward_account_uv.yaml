kind: metric
name: game.battlepassDungeon.onekeyReward__battlepass_dungeon_onekey_reward_account_uv
description: 一键领奖独立账号数(去重 account_id)
domains:
  - 角色成长
computation:
  sql: COUNT(DISTINCT account_id)
  metadata:
    aggregation: count_distinct
    field: account_id
    source: game.battlepassDungeon.onekeyReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.battlepassDungeon.onekeyReward
    description: 机械提取自 game.battlepassDungeon.onekeyReward 的 metrics 块（key=battlepass_dungeon_onekey_reward_account_uv）
