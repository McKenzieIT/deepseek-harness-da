kind: metric
name: game.running.getCoupon__running_getCoupon_role_force_avg
description: 领取角色的平均战力
domains:
  - 自定义
computation:
  sql: AVG(CAST(GET_JSON_OBJECT(params, '$.roleForce') AS BIGINT))
  metadata:
    aggregation: avg
    field: CAST(GET_JSON_OBJECT(params, '$.roleForce') AS BIGINT)
    source: game.running.getCoupon
    time_grain: ""
relations:
  - type: derived_from
    target: game.running.getCoupon
    description: 机械提取自 game.running.getCoupon 的 metrics 块（key=running_getCoupon_role_force_avg）
