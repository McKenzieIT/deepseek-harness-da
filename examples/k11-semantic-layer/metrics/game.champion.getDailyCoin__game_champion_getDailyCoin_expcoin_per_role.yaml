kind: metric
name: game.champion.getDailyCoin__game_champion_getDailyCoin_expcoin_per_role
description: 人均经验卷轴领取量
domains:
  - 战斗关卡
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.coinList.expCoin') AS BIGINT)) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.coinList.expCoin') AS BIGINT)) / NULLIF(COUNT(DISTINCT role_id), 0
    source: game.champion.getDailyCoin
    time_grain: ""
relations:
  - type: derived_from
    target: game.champion.getDailyCoin
    description: 机械提取自 game.champion.getDailyCoin 的 metrics 块（key=game_champion_getDailyCoin_expcoin_per_role）
