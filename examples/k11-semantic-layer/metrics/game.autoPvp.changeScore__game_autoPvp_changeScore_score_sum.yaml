kind: metric
name: game.autoPvp.changeScore__game_autoPvp_changeScore_score_sum
description: 自动PVP事件中角色当前score累计总量（按事件汇总）
domains:
  - 自定义
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params,'$.score') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params,'$.score') AS BIGINT)
    source: game.autoPvp.changeScore
    time_grain: ""
relations:
  - type: derived_from
    target: game.autoPvp.changeScore
    description: 机械提取自 game.autoPvp.changeScore 的 metrics 块（key=game_autoPvp_changeScore_score_sum）
