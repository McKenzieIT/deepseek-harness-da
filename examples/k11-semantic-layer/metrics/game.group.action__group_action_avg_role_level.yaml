kind: metric
name: game.group.action__group_action_avg_role_level
description: 组队角色平均等级（params.roleLevel 均值）
domains:
  - 社交公会
computation:
  sql: AVG(CAST(GET_JSON_OBJECT(params, '$.roleLevel') AS DOUBLE))
  metadata:
    aggregation: avg
    field: CAST(GET_JSON_OBJECT(params, '$.roleLevel') AS DOUBLE)
    source: game.group.action
    time_grain: ""
relations:
  - type: derived_from
    target: game.group.action
    description: 机械提取自 game.group.action 的 metrics 块（key=group_action_avg_role_level）
