kind: metric
name: game.battlepass.onekeyMissionReward__onekey_mission_reward_leveled_up_cnt
description: 领奖后战令等级提升的事件次数
domains:
  - 角色成长
computation:
  sql: SUM(CASE WHEN GET_JSON_OBJECT(params,'$.newLevel') IS NOT NULL AND GET_JSON_OBJECT(params,'$.oldLevel') IS NOT NULL AND CAST(GET_JSON_OBJECT(params,'$.newLevel') AS BIGINT) > CAST(GET_JSON_OBJECT(params,'$.oldLevel') AS BIGINT) THEN 1 ELSE 0 END)
  metadata:
    aggregation: sum
    field: CASE WHEN GET_JSON_OBJECT(params,'$.newLevel') IS NOT NULL AND GET_JSON_OBJECT(params,'$.oldLevel') IS NOT NULL AND CAST(GET_JSON_OBJECT(params,'$.newLevel') AS BIGINT) > CAST(GET_JSON_OBJECT(params,'$.oldLevel') AS BIGINT) THEN 1 ELSE 0 END
    source: game.battlepass.onekeyMissionReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.battlepass.onekeyMissionReward
    description: 机械提取自 game.battlepass.onekeyMissionReward 的 metrics 块（key=onekey_mission_reward_leveled_up_cnt）
