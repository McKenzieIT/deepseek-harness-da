kind: metric
name: dws_10000251_progression_card_snapshot_df__avg_card_force
description: 平均单卡战力(衡量卡牌培养强度)
domains:
  - 角色成长
  - 探索收集
computation:
  sql: SUM(CAST(card_force AS BIGINT)) / NULLIF(COUNT(*), 0)
  metadata:
    aggregation: sum
    field: CAST(card_force AS BIGINT)) / NULLIF(COUNT(*), 0
    source: dws_10000251_progression_card_snapshot_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_progression_card_snapshot_df
    description: 机械提取自 dws_10000251_progression_card_snapshot_df 的 metrics 块（key=avg_card_force）
