kind: metric
name: game.guide.finish__game_guide_finish_avg_role_level
description: 完成引导时角色的平均等级快照
domains:
  - 系统监控
computation:
  sql: AVG(CAST(GET_JSON_OBJECT(params, '$.roleLevel') AS BIGINT))
  metadata:
    aggregation: avg
    field: CAST(GET_JSON_OBJECT(params, '$.roleLevel') AS BIGINT)
    source: game.guide.finish
    time_grain: ""
relations:
  - type: derived_from
    target: game.guide.finish
    description: 机械提取自 game.guide.finish 的 metrics 块（key=game_guide_finish_avg_role_level）
