kind: metric
name: game.artifact.decompose__game_artifact_decompose_avg_per_role
description: 人均兵符分解次数（事件次数 / 去重角色数）
domains:
  - 角色成长
computation:
  sql: COUNT(*) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: count
    field: "*) / NULLIF(COUNT(DISTINCT role_id), 0"
    source: game.artifact.decompose
    time_grain: ""
relations:
  - type: derived_from
    target: game.artifact.decompose
    description: 机械提取自 game.artifact.decompose 的 metrics 块（key=game_artifact_decompose_avg_per_role）
