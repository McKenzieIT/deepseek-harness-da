kind: metric
name: game.script.fetchhangup__script_fetchhangup_avg_expdelta
description: 单次领取挂机奖励的平均获得资源量（事件级加权平均）
domains:
  - 自定义
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params,'$.expDelta') AS BIGINT)) / NULLIF(COUNT(*),0)
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params,'$.expDelta') AS BIGINT)) / NULLIF(COUNT(*),0
    source: game.script.fetchhangup
    time_grain: ""
relations:
  - type: derived_from
    target: game.script.fetchhangup
    description: 机械提取自 game.script.fetchhangup 的 metrics 块（key=script_fetchhangup_avg_expdelta）
