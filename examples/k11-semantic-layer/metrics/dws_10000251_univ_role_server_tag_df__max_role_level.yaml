kind: metric
name: dws_10000251_univ_role_server_tag_df__max_role_level
description: 角色最高等级（截至 ds）
domains:
  - 用户生命周期
  - 付费经济
  - 角色成长
computation:
  sql: MAX(role_level)
  metadata:
    aggregation: max
    field: role_level
    source: dws_10000251_univ_role_server_tag_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_univ_role_server_tag_df
    description: 机械提取自 dws_10000251_univ_role_server_tag_df 的 metrics 块（key=max_role_level）
