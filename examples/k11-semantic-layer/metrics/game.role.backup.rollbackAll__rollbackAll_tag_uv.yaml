kind: metric
name: game.role.backup.rollbackAll__rollbackAll_tag_uv
description: 被回退到的去重 tagId 存档点数
domains:
  - 用户生命周期
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params,'$.tagId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params,'$.tagId')
    source: game.role.backup.rollbackAll
    time_grain: ""
relations:
  - type: derived_from
    target: game.role.backup.rollbackAll
    description: 机械提取自 game.role.backup.rollbackAll 的 metrics 块（key=rollbackAll_tag_uv）
