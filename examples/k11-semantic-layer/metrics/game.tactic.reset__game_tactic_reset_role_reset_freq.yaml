kind: metric
name: game.tactic.reset__game_tactic_reset_role_reset_freq
description: 人均战法重置频次(总次数/去重角色数，分母为0返回NULL)
domains:
  - 角色成长
computation:
  sql: COUNT(*) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: count
    field: "*) / NULLIF(COUNT(DISTINCT role_id), 0"
    source: game.tactic.reset
    time_grain: ""
relations:
  - type: derived_from
    target: game.tactic.reset
    description: 机械提取自 game.tactic.reset 的 metrics 块（key=game_tactic_reset_role_reset_freq）
