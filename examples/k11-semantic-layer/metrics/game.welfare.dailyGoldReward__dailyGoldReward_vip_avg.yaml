kind: metric
name: game.welfare.dailyGoldReward__dailyGoldReward_vip_avg
description: 领取角色的平均VIP等级
domains:
  - 资源产销
computation:
  sql: AVG(CAST(GET_JSON_OBJECT(params, '$.vipLevel') AS BIGINT))
  metadata:
    aggregation: avg
    field: CAST(GET_JSON_OBJECT(params, '$.vipLevel') AS BIGINT)
    source: game.welfare.dailyGoldReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.welfare.dailyGoldReward
    description: 机械提取自 game.welfare.dailyGoldReward 的 metrics 块（key=dailyGoldReward_vip_avg）
