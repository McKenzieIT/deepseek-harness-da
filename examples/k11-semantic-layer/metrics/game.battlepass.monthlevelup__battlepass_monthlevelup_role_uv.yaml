kind: metric
name: game.battlepass.monthlevelup__battlepass_monthlevelup_role_uv
description: 触发等级提升的去重角色数
domains:
  - 角色成长
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.battlepass.monthlevelup
    time_grain: ""
relations:
  - type: derived_from
    target: game.battlepass.monthlevelup
    description: 机械提取自 game.battlepass.monthlevelup 的 metrics 块（key=battlepass_monthlevelup_role_uv）
