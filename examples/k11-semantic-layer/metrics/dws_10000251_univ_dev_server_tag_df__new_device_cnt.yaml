kind: metric
name: dws_10000251_univ_dev_server_tag_df__new_device_cnt
description: 当日新增设备数,累计活跃天数等于1的设备去重计数(快照口径近似新增);依赖 act_days_std=1 判定新增
domains:
  - 用户生命周期
  - 付费经济
  - 角色成长
  - 系统监控
computation:
  sql: COUNT(DISTINCT CASE WHEN act_days_std = 1 THEN user_id END)
  metadata:
    aggregation: count_distinct
    field: CASE WHEN act_days_std = 1 THEN user_id END
    source: dws_10000251_univ_dev_server_tag_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_univ_dev_server_tag_df
    description: 机械提取自 dws_10000251_univ_dev_server_tag_df 的 metrics 块（key=new_device_cnt）
