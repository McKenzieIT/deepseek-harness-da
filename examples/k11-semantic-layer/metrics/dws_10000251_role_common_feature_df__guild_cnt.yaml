kind: metric
name: dws_10000251_role_common_feature_df__guild_cnt
description: 公会数(去重 guildid,排除空字符串/NULL的无公会标识,避免虚高)
domains:
  - 角色成长
  - 付费经济
  - 社交公会
computation:
  sql: COUNT(DISTINCT CASE WHEN guildid IS NOT NULL AND guildid <> '' THEN guildid END)
  metadata:
    aggregation: count_distinct
    field: CASE WHEN guildid IS NOT NULL AND guildid <> '' THEN guildid END
    source: dws_10000251_role_common_feature_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_role_common_feature_df
    description: 机械提取自 dws_10000251_role_common_feature_df 的 metrics 块（key=guild_cnt）
