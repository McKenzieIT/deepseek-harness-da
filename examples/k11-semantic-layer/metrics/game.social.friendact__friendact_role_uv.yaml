kind: metric
name: game.social.friendact__friendact_role_uv
description: 发起好友关系变更的去重角色数（发起方）
domains:
  - 社交公会
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.social.friendact
    time_grain: ""
relations:
  - type: derived_from
    target: game.social.friendact
    description: 机械提取自 game.social.friendact 的 metrics 块（key=friendact_role_uv）
