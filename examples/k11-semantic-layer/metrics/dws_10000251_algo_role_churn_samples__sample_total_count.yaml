kind: metric
name: dws_10000251_algo_role_churn_samples__sample_total_count
description: 样本总数（按分区 ds 统计的角色-样本行数）
domains:
  - 用户生命周期
  - 系统监控
computation:
  sql: COUNT(*)
  metadata:
    aggregation: count
    field: "*"
    source: dws_10000251_algo_role_churn_samples
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_algo_role_churn_samples
    description: 机械提取自 dws_10000251_algo_role_churn_samples 的 metrics 块（key=sample_total_count）
