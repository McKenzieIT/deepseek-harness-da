kind: metric
name: game.rogue.chooseevent__game_rogue_chooseevent_coin_avg
description: 事件选择发生时剩余战场币（coin）的平均数量
domains:
  - 战斗关卡
computation:
  sql: AVG(CAST(GET_JSON_OBJECT(params,'$.coin') AS BIGINT))
  metadata:
    aggregation: avg
    field: CAST(GET_JSON_OBJECT(params,'$.coin') AS BIGINT)
    source: game.rogue.chooseevent
    time_grain: ""
relations:
  - type: derived_from
    target: game.rogue.chooseevent
    description: 机械提取自 game.rogue.chooseevent 的 metrics 块（key=game_rogue_chooseevent_coin_avg）
