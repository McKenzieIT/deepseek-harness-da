kind: metric
name: dws_10000251_univ_role_act_mi__role_new_uv
description: 角色月新增 UV（act_fst=1，当月首次活跃的新增角色数）
domains:
  - 用户生命周期
  - 付费经济
computation:
  sql: SUM(CASE WHEN act_fst = 1 THEN 1 ELSE 0 END)
  metadata:
    aggregation: sum
    field: CASE WHEN act_fst = 1 THEN 1 ELSE 0 END
    source: dws_10000251_univ_role_act_mi
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_univ_role_act_mi
    description: 机械提取自 dws_10000251_univ_role_act_mi 的 metrics 块（key=role_new_uv）
