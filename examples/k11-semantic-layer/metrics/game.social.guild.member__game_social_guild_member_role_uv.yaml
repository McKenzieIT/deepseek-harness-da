kind: metric
name: game.social.guild.member__game_social_guild_member_role_uv
description: 去重角色数（公会粒度事件基表 role_id 为空串，该指标退化为常数1，仅作结构占位，非分析主体）
domains:
  - 社交公会
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.social.guild.member
    time_grain: ""
relations:
  - type: derived_from
    target: game.social.guild.member
    description: 机械提取自 game.social.guild.member 的 metrics 块（key=game_social_guild_member_role_uv）
