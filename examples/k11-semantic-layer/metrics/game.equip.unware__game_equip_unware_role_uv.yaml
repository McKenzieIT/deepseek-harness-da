kind: metric
name: game.equip.unware__game_equip_unware_role_uv
description: 脱下装备的去重角色数（分析主体：role_id）
domains:
  - 装备道具
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.equip.unware
    time_grain: ""
relations:
  - type: derived_from
    target: game.equip.unware
    description: 机械提取自 game.equip.unware 的 metrics 块（key=game_equip_unware_role_uv）
