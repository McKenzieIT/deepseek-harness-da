kind: metric
name: game.pve.daily__game_pve_daily_per_role_cnt
description: 人均完成次数（总次数/去重角色数）
domains:
  - 战斗关卡
computation:
  sql: COUNT(*) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: count
    field: "*) / NULLIF(COUNT(DISTINCT role_id), 0"
    source: game.pve.daily
    time_grain: ""
relations:
  - type: derived_from
    target: game.pve.daily
    description: 机械提取自 game.pve.daily 的 metrics 块（key=game_pve_daily_per_role_cnt）
