kind: metric
name: game.home.practice__game_home_practice_piece_sum
description: 武将特训涉及碎片（pieceAmount）累计总量（产出/消耗语义需结合业务确认）
domains:
  - 自定义
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.pieceAmount') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.pieceAmount') AS BIGINT)
    source: game.home.practice
    time_grain: ""
relations:
  - type: derived_from
    target: game.home.practice
    description: 机械提取自 game.home.practice 的 metrics 块（key=game_home_practice_piece_sum）
