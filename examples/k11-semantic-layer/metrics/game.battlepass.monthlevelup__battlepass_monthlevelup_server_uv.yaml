kind: metric
name: game.battlepass.monthlevelup__battlepass_monthlevelup_server_uv
description: 触发等级提升的去重游戏服数
domains:
  - 角色成长
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: game.battlepass.monthlevelup
    time_grain: ""
relations:
  - type: derived_from
    target: game.battlepass.monthlevelup
    description: 机械提取自 game.battlepass.monthlevelup 的 metrics 块（key=battlepass_monthlevelup_server_uv）
