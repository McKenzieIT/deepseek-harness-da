kind: metric
name: game.mission.beginnerFinalReward__beginner_final_reward_server_uv
description: 领取养成任务组最终奖励的去重游戏服数
domains:
  - 角色成长
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: game.mission.beginnerFinalReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.mission.beginnerFinalReward
    description: 机械提取自 game.mission.beginnerFinalReward 的 metrics 块（key=beginner_final_reward_server_uv）
