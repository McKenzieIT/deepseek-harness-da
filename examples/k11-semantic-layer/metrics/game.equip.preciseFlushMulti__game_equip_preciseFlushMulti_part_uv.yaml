kind: metric
name: game.equip.preciseFlushMulti__game_equip_preciseFlushMulti_part_uv
description: 被洗练的装备部位去重数（partId 取值 1/2/3/4）
domains:
  - 装备道具
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.partId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.partId')
    source: game.equip.preciseFlushMulti
    time_grain: ""
relations:
  - type: derived_from
    target: game.equip.preciseFlushMulti
    description: 机械提取自 game.equip.preciseFlushMulti 的 metrics 块（key=game_equip_preciseFlushMulti_part_uv）
