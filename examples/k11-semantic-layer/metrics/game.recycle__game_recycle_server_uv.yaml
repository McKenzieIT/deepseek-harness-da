kind: metric
name: game.recycle__game_recycle_server_uv
description: 涉及资源回收的去重游戏服数
domains:
  - 资源产销
  - 装备道具
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: game.recycle
    time_grain: ""
relations:
  - type: derived_from
    target: game.recycle
    description: 机械提取自 game.recycle 的 metrics 块（key=game_recycle_server_uv）
