kind: metric
name: dws_10000251_univ_dev_server_whole_df__pay_user_cnt
description: 有付费记录的去重设备数。本表未付费设备的 pay_tm_fst 为 1970-01-01 哨兵值而非 NULL，故付费判定统一用 pay_amt_std>0。
domains:
  - 付费经济
  - 用户生命周期
computation:
  sql: COUNT(DISTINCT CASE WHEN pay_amt_std > 0 THEN user_id END)
  metadata:
    aggregation: count_distinct
    field: CASE WHEN pay_amt_std > 0 THEN user_id END
    source: dws_10000251_univ_dev_server_whole_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_univ_dev_server_whole_df
    description: 机械提取自 dws_10000251_univ_dev_server_whole_df 的 metrics 块（key=pay_user_cnt）
