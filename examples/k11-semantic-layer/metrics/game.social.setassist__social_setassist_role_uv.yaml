kind: metric
name: game.social.setassist__social_setassist_role_uv
description: 设置助战的去重角色数（分析主体）
domains:
  - 社交公会
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.social.setassist
    time_grain: ""
relations:
  - type: derived_from
    target: game.social.setassist
    description: 机械提取自 game.social.setassist 的 metrics 块（key=social_setassist_role_uv）
