kind: metric
name: game.group.action__group_action_events_per_role
description: 人均组队操作次数（总次数/角色uv，分母用 NULLIF 防零）
domains:
  - 社交公会
computation:
  sql: COUNT(*) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: count
    field: "*) / NULLIF(COUNT(DISTINCT role_id), 0"
    source: game.group.action
    time_grain: ""
relations:
  - type: derived_from
    target: game.group.action
    description: 机械提取自 game.group.action 的 metrics 块（key=group_action_events_per_role）
