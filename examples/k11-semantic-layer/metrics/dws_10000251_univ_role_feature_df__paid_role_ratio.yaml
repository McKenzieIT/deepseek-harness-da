kind: metric
name: dws_10000251_univ_role_feature_df__paid_role_ratio
description: 买量角色占比（ua_flag=1 / 全量角色）
domains:
  - 用户生命周期
  - 角色成长
computation:
  sql: SUM(CASE WHEN ua_flag = 1 THEN 1 ELSE 0 END) / NULLIF(COUNT(*), 0)
  metadata:
    aggregation: sum
    field: CASE WHEN ua_flag = 1 THEN 1 ELSE 0 END) / NULLIF(COUNT(*), 0
    source: dws_10000251_univ_role_feature_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_univ_role_feature_df
    description: 机械提取自 dws_10000251_univ_role_feature_df 的 metrics 块（key=paid_role_ratio）
