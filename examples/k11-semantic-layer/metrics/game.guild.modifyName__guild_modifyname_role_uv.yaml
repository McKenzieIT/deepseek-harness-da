kind: metric
name: game.guild.modifyName__guild_modifyname_role_uv
description: 改名的去重角色数（分析主体 role_id）
domains:
  - 社交公会
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.guild.modifyName
    time_grain: ""
relations:
  - type: derived_from
    target: game.guild.modifyName
    description: 机械提取自 game.guild.modifyName 的 metrics 块（key=guild_modifyname_role_uv）
