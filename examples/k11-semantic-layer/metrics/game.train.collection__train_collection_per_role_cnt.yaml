kind: metric
name: game.train.collection__train_collection_per_role_cnt
description: 人均藏品养成触发次数（总次数/角色uv）
domains:
  - 角色成长
computation:
  sql: COUNT(*) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: count
    field: "*) / NULLIF(COUNT(DISTINCT role_id), 0"
    source: game.train.collection
    time_grain: ""
relations:
  - type: derived_from
    target: game.train.collection
    description: 机械提取自 game.train.collection 的 metrics 块（key=train_collection_per_role_cnt）
