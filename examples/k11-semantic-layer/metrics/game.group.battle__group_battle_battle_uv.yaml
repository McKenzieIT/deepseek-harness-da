kind: metric
name: game.group.battle__group_battle_battle_uv
description: 独立战斗场次数（battleId 去重）
domains:
  - 社交公会
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.battleId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.battleId')
    source: game.group.battle
    time_grain: ""
relations:
  - type: derived_from
    target: game.group.battle
    description: 机械提取自 game.group.battle 的 metrics 块（key=group_battle_battle_uv）
