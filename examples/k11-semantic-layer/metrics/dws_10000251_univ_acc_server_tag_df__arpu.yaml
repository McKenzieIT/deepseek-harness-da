kind: metric
name: dws_10000251_univ_acc_server_tag_df__arpu
description: 活跃账号 ARPU(总累计付费/活跃账号数),单位:元;pay_amt_std 存在 bigint/double 重名列,生产 SQL 须先按数仓确认口径并对列消歧后再引用
domains:
  - 用户生命周期
  - 付费经济
  - 系统监控
computation:
  sql: SUM(pay_amt_std) * 1.0 / NULLIF(COUNT(DISTINCT user_id), 0)
  metadata:
    aggregation: sum
    field: pay_amt_std) * 1.0 / NULLIF(COUNT(DISTINCT user_id), 0
    source: dws_10000251_univ_acc_server_tag_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_univ_acc_server_tag_df
    description: 机械提取自 dws_10000251_univ_acc_server_tag_df 的 metrics 块（key=arpu）
