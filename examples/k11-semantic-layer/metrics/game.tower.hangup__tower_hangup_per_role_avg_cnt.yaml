kind: metric
name: game.tower.hangup__tower_hangup_per_role_avg_cnt
description: 人均烽火台挂机奖励触发次数（总次数/去重角色数，NULLIF 防零除）
domains:
  - 战斗关卡
computation:
  sql: CAST(COUNT_IF(event = 'game.tower.hangup') AS DOUBLE) / NULLIF(COUNT(DISTINCT CASE WHEN event = 'game.tower.hangup' THEN role_id END), 0)
  metadata:
    aggregation: ""
    field: ""
    source: game.tower.hangup
    time_grain: ""
relations:
  - type: derived_from
    target: game.tower.hangup
    description: 机械提取自 game.tower.hangup 的 metrics 块（key=tower_hangup_per_role_avg_cnt）
