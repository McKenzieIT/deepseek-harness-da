kind: metric
name: game.item.change__game_item_change_server_uv
description: 触发道具变化的游戏服数
domains:
  - 装备道具
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: game.item.change
    time_grain: ""
relations:
  - type: derived_from
    target: game.item.change
    description: 机械提取自 game.item.change 的 metrics 块（key=game_item_change_server_uv）
