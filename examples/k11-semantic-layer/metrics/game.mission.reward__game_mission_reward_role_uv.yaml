kind: metric
name: game.mission.reward__game_mission_reward_role_uv
description: 领奖去重角色数（分析主体 role_id）
domains:
  - 角色成长
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.mission.reward
    time_grain: ""
relations:
  - type: derived_from
    target: game.mission.reward
    description: 机械提取自 game.mission.reward 的 metrics 块（key=game_mission_reward_role_uv）
