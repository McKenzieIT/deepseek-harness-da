kind: metric
name: dws_10000251_acc_summary_df__acc_uv
description: 账号数：当日快照下去重账号数（账号粒度主体指标）
domains:
  - 用户生命周期
  - 付费经济
computation:
  sql: COUNT(DISTINCT account_id)
  metadata:
    aggregation: count_distinct
    field: account_id
    source: dws_10000251_acc_summary_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_acc_summary_df
    description: 机械提取自 dws_10000251_acc_summary_df 的 metrics 块（key=acc_uv）
