kind: metric
name: dws_10000251_role_tag_pbt2_df__role_uv
description: 角色UV（去重角色数），_df日全量快照口径
domains:
  - 角色成长
  - 付费经济
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: dws_10000251_role_tag_pbt2_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_role_tag_pbt2_df
    description: 机械提取自 dws_10000251_role_tag_pbt2_df 的 metrics 块（key=role_uv）
