kind: metric
name: game.role.backup.rollbackAll__rollbackAll_avg_per_role
description: 人均全量回退次数（总次数/去重角色数）
domains:
  - 用户生命周期
computation:
  sql: ROUND(COUNT(*) * 1.0 / NULLIF(COUNT(DISTINCT role_id),0), 3)
  metadata:
    aggregation: ""
    field: ""
    source: game.role.backup.rollbackAll
    time_grain: ""
relations:
  - type: derived_from
    target: game.role.backup.rollbackAll
    description: 机械提取自 game.role.backup.rollbackAll 的 metrics 块（key=rollbackAll_avg_per_role）
