kind: metric
name: game.war.activityEnterPerformDungeon__game_war_activity_enter_perform_dungeon_enters_per_role
description: 人均进入剧情表演副本次数（总次数/角色数）
domains:
  - 战斗关卡
computation:
  sql: CAST(COUNT(*) AS DOUBLE) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: ""
    field: ""
    source: game.war.activityEnterPerformDungeon
    time_grain: ""
relations:
  - type: derived_from
    target: game.war.activityEnterPerformDungeon
    description: 机械提取自 game.war.activityEnterPerformDungeon 的 metrics 块（key=game_war_activity_enter_perform_dungeon_enters_per_role）
