kind: metric
name: game.guild.cancelInviteItem__guild_cancel_invite_item_server_uv
description: 涉及的区服数(去重 server_id，反映取消邀请行为覆盖面)
domains:
  - 社交公会
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: game.guild.cancelInviteItem
    time_grain: ""
relations:
  - type: derived_from
    target: game.guild.cancelInviteItem
    description: 机械提取自 game.guild.cancelInviteItem 的 metrics 块（key=guild_cancel_invite_item_server_uv）
