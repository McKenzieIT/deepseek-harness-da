kind: metric
name: game.scriptActivity.levelBattlepassOnekeyReward__level_battlepass_onekey_reward_per_role
description: 人均一键领取次数（总次数 / 角色去重数）
domains:
  - 自定义
computation:
  sql: ROUND(COUNT(*) / NULLIF(COUNT(DISTINCT role_id), 0), 2)
  metadata:
    aggregation: ""
    field: ""
    source: game.scriptActivity.levelBattlepassOnekeyReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.scriptActivity.levelBattlepassOnekeyReward
    description: 机械提取自 game.scriptActivity.levelBattlepassOnekeyReward 的 metrics 块（key=level_battlepass_onekey_reward_per_role）
