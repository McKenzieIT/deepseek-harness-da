kind: metric
name: game.guild.modifyInfo__game_guild_modifyInfo_role_uv
description: 发起公会信息修改的去重角色数
domains:
  - 社交公会
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.guild.modifyInfo
    time_grain: ""
relations:
  - type: derived_from
    target: game.guild.modifyInfo
    description: 机械提取自 game.guild.modifyInfo 的 metrics 块（key=game_guild_modifyInfo_role_uv）
