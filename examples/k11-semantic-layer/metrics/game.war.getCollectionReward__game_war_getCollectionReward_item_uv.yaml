kind: metric
name: game.war.getCollectionReward__game_war_getCollectionReward_item_uv
description: 领取的去重收集品id数（基于 params.id）
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.id'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.id')
    source: game.war.getCollectionReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.war.getCollectionReward
    description: 机械提取自 game.war.getCollectionReward 的 metrics 块（key=game_war_getCollectionReward_item_uv）
