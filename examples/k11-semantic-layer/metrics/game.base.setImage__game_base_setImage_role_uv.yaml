kind: metric
name: game.base.setImage__game_base_setImage_role_uv
description: 设置过看板娘形象的独立角色数
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.base.setImage
    time_grain: ""
relations:
  - type: derived_from
    target: game.base.setImage
    description: 机械提取自 game.base.setImage 的 metrics 块（key=game_base_setImage_role_uv）
