kind: metric
name: game.battlepass.vipLevelup__game_battlepass_vip_levelup_real_levelup_cnt
description: 实际发生VIP等级提升(newVipLevel>oldVipLevel)的事件次数
domains:
  - 角色成长
computation:
  sql: SUM(CASE WHEN CAST(GET_JSON_OBJECT(params, '$.newVipLevel') AS INT) > CAST(GET_JSON_OBJECT(params, '$.oldVipLevel') AS INT) THEN 1 ELSE 0 END)
  metadata:
    aggregation: sum
    field: CASE WHEN CAST(GET_JSON_OBJECT(params, '$.newVipLevel') AS INT) > CAST(GET_JSON_OBJECT(params, '$.oldVipLevel') AS INT) THEN 1 ELSE 0 END
    source: game.battlepass.vipLevelup
    time_grain: ""
relations:
  - type: derived_from
    target: game.battlepass.vipLevelup
    description: 机械提取自 game.battlepass.vipLevelup 的 metrics 块（key=game_battlepass_vip_levelup_real_levelup_cnt）
