kind: metric
name: game.knightarchive.buy__knightarchive_buy_archive_uv
description: 当日被购买的不同典藏条目种类数
domains:
  - 付费经济
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.archive'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.archive')
    source: game.knightarchive.buy
    time_grain: ""
relations:
  - type: derived_from
    target: game.knightarchive.buy
    description: 机械提取自 game.knightarchive.buy 的 metrics 块（key=knightarchive_buy_archive_uv）
