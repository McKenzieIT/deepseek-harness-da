kind: metric
name: game.social.guild.battle__game_social_guild_battle_avg_attack_score_per_role
description: 参与角色的平均进攻积分（NULLIF 防零除）
domains:
  - 社交公会
computation:
  sql: CAST(SUM(CAST(GET_JSON_OBJECT(params, '$.attackScore') AS BIGINT)) AS DOUBLE) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: ""
    field: ""
    source: game.social.guild.battle
    time_grain: ""
relations:
  - type: derived_from
    target: game.social.guild.battle
    description: 机械提取自 game.social.guild.battle 的 metrics 块（key=game_social_guild_battle_avg_attack_score_per_role）
