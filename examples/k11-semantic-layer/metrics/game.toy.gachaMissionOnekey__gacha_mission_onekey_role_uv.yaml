kind: metric
name: game.toy.gachaMissionOnekey__gacha_mission_onekey_role_uv
description: 触发抽卡任务一键领取的去重角色数（分析主体粒度）
domains:
  - 探索收集
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.toy.gachaMissionOnekey
    time_grain: ""
relations:
  - type: derived_from
    target: game.toy.gachaMissionOnekey
    description: 机械提取自 game.toy.gachaMissionOnekey 的 metrics 块（key=gacha_mission_onekey_role_uv）
