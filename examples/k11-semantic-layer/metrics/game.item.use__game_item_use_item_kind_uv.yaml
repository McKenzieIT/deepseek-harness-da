kind: metric
name: game.item.use__game_item_use_item_kind_uv
description: 当日被使用的去重道具种类数（按 itemId 去重）
domains:
  - 装备道具
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.itemId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.itemId')
    source: game.item.use
    time_grain: ""
relations:
  - type: derived_from
    target: game.item.use
    description: 机械提取自 game.item.use 的 metrics 块（key=game_item_use_item_kind_uv）
