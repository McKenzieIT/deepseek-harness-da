kind: metric
name: game.gacha.missionReward__gacha_mission_reward_cnt_per_role
description: 人均触发次数（事件总数/角色UV）
domains:
  - 探索收集
computation:
  sql: ROUND(COUNT(*) / NULLIF(COUNT(DISTINCT role_id), 0), 4)
  metadata:
    aggregation: ""
    field: ""
    source: game.gacha.missionReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.gacha.missionReward
    description: 机械提取自 game.gacha.missionReward 的 metrics 块（key=gacha_mission_reward_cnt_per_role）
