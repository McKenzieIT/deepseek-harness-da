kind: metric
name: game.realm.levelUp__game_realm_levelUp_server_uv
description: 触发境界升级的去重游戏服数
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: game.realm.levelUp
    time_grain: ""
relations:
  - type: derived_from
    target: game.realm.levelUp
    description: 机械提取自 game.realm.levelUp 的 metrics 块（key=game_realm_levelUp_server_uv）
