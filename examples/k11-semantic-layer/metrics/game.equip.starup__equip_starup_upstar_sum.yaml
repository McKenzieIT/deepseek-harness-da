kind: metric
name: game.equip.starup__equip_starup_upstar_sum
description: 升星总星数（单次提升星数累加）
domains:
  - 装备道具
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.upstar') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.upstar') AS BIGINT)
    source: game.equip.starup
    time_grain: ""
relations:
  - type: derived_from
    target: game.equip.starup
    description: 机械提取自 game.equip.starup 的 metrics 块（key=equip_starup_upstar_sum）
