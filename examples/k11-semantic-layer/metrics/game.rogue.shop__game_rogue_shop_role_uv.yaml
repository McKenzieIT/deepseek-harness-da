kind: metric
name: game.rogue.shop__game_rogue_shop_role_uv
description: 触发商店操作的去重角色数
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.rogue.shop
    time_grain: ""
relations:
  - type: derived_from
    target: game.rogue.shop
    description: 机械提取自 game.rogue.shop 的 metrics 块（key=game_rogue_shop_role_uv）
