kind: metric
name: dws_10000251_univ_dev_server_whole_df__pay_amt_total
description: 区服维度的累计付费总额（元）。pay_amt_std 为历史累计口径，跨多日分区聚合前需先按设备取最新分区值，避免重复累加；单分区(单日快照)内可直接 SUM。
domains:
  - 付费经济
  - 用户生命周期
computation:
  sql: SUM(pay_amt_std)
  metadata:
    aggregation: sum
    field: pay_amt_std
    source: dws_10000251_univ_dev_server_whole_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_univ_dev_server_whole_df
    description: 机械提取自 dws_10000251_univ_dev_server_whole_df 的 metrics 块（key=pay_amt_total）
