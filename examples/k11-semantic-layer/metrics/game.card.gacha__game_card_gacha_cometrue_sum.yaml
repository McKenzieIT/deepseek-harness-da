kind: metric
name: game.card.gacha__game_card_gacha_cometrue_sum
description: 心愿达成次数合计（cometrue 求和）
domains:
  - 探索收集
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params,'$.cometrue') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params,'$.cometrue') AS BIGINT)
    source: game.card.gacha
    time_grain: ""
relations:
  - type: derived_from
    target: game.card.gacha
    description: 机械提取自 game.card.gacha 的 metrics 块（key=game_card_gacha_cometrue_sum）
