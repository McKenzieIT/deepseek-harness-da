kind: metric
name: game.artifact.attuned__artifact_attuned_avg_role_level
description: 鉴定时刻角色平均等级（基于 roleLevel 快照）
domains:
  - 角色成长
computation:
  sql: ROUND(AVG(CAST(GET_JSON_OBJECT(params, '$.roleLevel') AS BIGINT)), 2)
  metadata:
    aggregation: ""
    field: ""
    source: game.artifact.attuned
    time_grain: ""
relations:
  - type: derived_from
    target: game.artifact.attuned
    description: 机械提取自 game.artifact.attuned 的 metrics 块（key=artifact_attuned_avg_role_level）
