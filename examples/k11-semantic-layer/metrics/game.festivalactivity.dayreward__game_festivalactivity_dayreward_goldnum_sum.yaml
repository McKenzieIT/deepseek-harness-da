kind: metric
name: game.festivalactivity.dayreward__game_festivalactivity_dayreward_goldnum_sum
description: 通过节日活动日奖发放的三山壁（goldNum）总数量
domains:
  - 自定义
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.goldNum') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.goldNum') AS BIGINT)
    source: game.festivalactivity.dayreward
    time_grain: ""
relations:
  - type: derived_from
    target: game.festivalactivity.dayreward
    description: 机械提取自 game.festivalactivity.dayreward 的 metrics 块（key=game_festivalactivity_dayreward_goldnum_sum）
