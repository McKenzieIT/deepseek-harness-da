kind: metric
name: game.running.pour__game_running_pour_horse_uv
description: 被投注马匹的去重数（反映投注标的集中度）
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.horseId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.horseId')
    source: game.running.pour
    time_grain: ""
relations:
  - type: derived_from
    target: game.running.pour
    description: 机械提取自 game.running.pour 的 metrics 块（key=game_running_pour_horse_uv）
