kind: metric
name: game.charmactivity.gachaswitchactivity__gachaswitchactivity_activity_uv
description: 涉及的灵器活动数(去重 activityId)
domains:
  - 探索收集
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.activityId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.activityId')
    source: game.charmactivity.gachaswitchactivity
    time_grain: ""
relations:
  - type: derived_from
    target: game.charmactivity.gachaswitchactivity
    description: 机械提取自 game.charmactivity.gachaswitchactivity 的 metrics 块（key=gachaswitchactivity_activity_uv）
