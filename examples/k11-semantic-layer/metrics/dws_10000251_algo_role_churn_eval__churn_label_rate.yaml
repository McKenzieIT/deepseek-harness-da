kind: metric
name: dws_10000251_algo_role_churn_eval__churn_label_rate
description: 实际流失率=实际流失角色数/评估样本角色数
domains:
  - 用户生命周期
  - 系统监控
computation:
  sql: SUM(label) / NULLIF(COUNT(*), 0)
  metadata:
    aggregation: sum
    field: label) / NULLIF(COUNT(*), 0
    source: dws_10000251_algo_role_churn_eval
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_algo_role_churn_eval
    description: 机械提取自 dws_10000251_algo_role_churn_eval 的 metrics 块（key=churn_label_rate）
