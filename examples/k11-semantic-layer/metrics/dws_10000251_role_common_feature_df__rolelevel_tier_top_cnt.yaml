kind: metric
name: dws_10000251_role_common_feature_df__rolelevel_tier_top_cnt
description: 顶级(91+)角色数
domains:
  - 角色成长
  - 付费经济
  - 社交公会
computation:
  sql: COUNT(DISTINCT CASE WHEN CAST(rolelevel AS BIGINT) >= 91 THEN role_id END)
  metadata:
    aggregation: count_distinct
    field: CASE WHEN CAST(rolelevel AS BIGINT) >= 91 THEN role_id END
    source: dws_10000251_role_common_feature_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_role_common_feature_df
    description: 机械提取自 dws_10000251_role_common_feature_df 的 metrics 块（key=rolelevel_tier_top_cnt）
