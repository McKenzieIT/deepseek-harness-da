kind: metric
name: game.autoSkill.name__game_autoSkill_name_scheme_uv
description: 被重命名的去重方案数（方案id字段 id）
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.id'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.id')
    source: game.autoSkill.name
    time_grain: ""
relations:
  - type: derived_from
    target: game.autoSkill.name
    description: 机械提取自 game.autoSkill.name 的 metrics 块（key=game_autoSkill_name_scheme_uv）
