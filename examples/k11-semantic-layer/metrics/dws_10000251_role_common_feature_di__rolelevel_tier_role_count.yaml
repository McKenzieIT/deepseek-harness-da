kind: metric
name: dws_10000251_role_common_feature_di__rolelevel_tier_role_count
description: 高等级角色数(等级>=60,阈值可按业务调整),用于等级分层
domains:
  - 角色成长
  - 社交公会
  - 付费经济
computation:
  sql: COUNT(DISTINCT CASE WHEN CAST(rolelevel AS DOUBLE) >= 60 THEN role_id END)
  metadata:
    aggregation: count_distinct
    field: CASE WHEN CAST(rolelevel AS DOUBLE) >= 60 THEN role_id END
    source: dws_10000251_role_common_feature_di
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_role_common_feature_di
    description: 机械提取自 dws_10000251_role_common_feature_di 的 metrics 块（key=rolelevel_tier_role_count）
