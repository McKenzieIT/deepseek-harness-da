kind: metric
name: game.charmactivity.buyitem__game_charmactivity_buyitem_per_role_qty
description: 人均购买道具数量（count 求和/角色 UV，NULLIF 防除零）
domains:
  - 探索收集
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.count') AS BIGINT)) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.count') AS BIGINT)) / NULLIF(COUNT(DISTINCT role_id), 0
    source: game.charmactivity.buyitem
    time_grain: ""
relations:
  - type: derived_from
    target: game.charmactivity.buyitem
    description: 机械提取自 game.charmactivity.buyitem 的 metrics 块（key=game_charmactivity_buyitem_per_role_qty）
