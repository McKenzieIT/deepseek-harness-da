kind: metric
name: game.champion.guess__game_champion_guess_def_sum
description: gssCountDef 字段累计值(防守相关计数)
domains:
  - 战斗关卡
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.gssCountDef') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.gssCountDef') AS BIGINT)
    source: game.champion.guess
    time_grain: ""
relations:
  - type: derived_from
    target: game.champion.guess
    description: 机械提取自 game.champion.guess 的 metrics 块（key=game_champion_guess_def_sum）
