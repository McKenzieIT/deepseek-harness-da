kind: metric
name: game.guild.levelReward__game_guild_levelReward_expcoin_sum
description: 经验卷轴发放总量
domains:
  - 社交公会
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.coinList.expCoin') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.coinList.expCoin') AS BIGINT)
    source: game.guild.levelReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.guild.levelReward
    description: 机械提取自 game.guild.levelReward 的 metrics 块（key=game_guild_levelReward_expcoin_sum）
