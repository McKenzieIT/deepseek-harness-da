kind: metric
name: game.tactic.unwear__game_tactic_unwear_knight_uv
description: 被卸下战法的目标武将去重数（knightId）
domains:
  - 角色成长
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.knightId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.knightId')
    source: game.tactic.unwear
    time_grain: ""
relations:
  - type: derived_from
    target: game.tactic.unwear
    description: 机械提取自 game.tactic.unwear 的 metrics 块（key=game_tactic_unwear_knight_uv）
