kind: metric
name: game.gmActivity.missionReward__game_gmActivity_missionReward_account_uv
description: 领取GM活动任务奖励的去重账号数
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT account_id)
  metadata:
    aggregation: count_distinct
    field: account_id
    source: game.gmActivity.missionReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.gmActivity.missionReward
    description: 机械提取自 game.gmActivity.missionReward 的 metrics 块（key=game_gmActivity_missionReward_account_uv）
