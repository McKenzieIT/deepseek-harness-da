kind: metric
name: game.chargeShop.buy__game_chargeShop_buy_money_per_role
description: 人均消耗凤翎（money_sum / role_uv，分母为0时返回NULL）
domains:
  - 付费经济
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.money') AS BIGINT)) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.money') AS BIGINT)) / NULLIF(COUNT(DISTINCT role_id), 0
    source: game.chargeShop.buy
    time_grain: ""
relations:
  - type: derived_from
    target: game.chargeShop.buy
    description: 机械提取自 game.chargeShop.buy 的 metrics 块（key=game_chargeShop_buy_money_per_role）
