kind: metric
name: dws_10000251_play_story_df__read_complete_cnt
description: 完整阅读剧情的条目数（role×story 记录中 read_status=1 的计数）
domains:
  - 探索收集
  - 角色成长
computation:
  sql: SUM(CASE WHEN read_status=1 THEN 1 ELSE 0 END)
  metadata:
    aggregation: sum
    field: CASE WHEN read_status=1 THEN 1 ELSE 0 END
    source: dws_10000251_play_story_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_play_story_df
    description: 机械提取自 dws_10000251_play_story_df 的 metrics 块（key=read_complete_cnt）
