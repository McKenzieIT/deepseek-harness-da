kind: metric
name: game.task.act__game_task_act_server_uv
description: 涉及的区服数(去重 server_id，反映任务覆盖面)
domains:
  - 角色成长
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: game.task.act
    time_grain: ""
relations:
  - type: derived_from
    target: game.task.act
    description: 机械提取自 game.task.act 的 metrics 块（key=game_task_act_server_uv）
