kind: metric
name: game.role.offline__game_role_offline_duration_sum
description: 下线事件累计在线时长（秒），来自 params.duration
domains:
  - 用户生命周期
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.duration') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.duration') AS BIGINT)
    source: game.role.offline
    time_grain: ""
relations:
  - type: derived_from
    target: game.role.offline
    description: 机械提取自 game.role.offline 的 metrics 块（key=game_role_offline_duration_sum）
