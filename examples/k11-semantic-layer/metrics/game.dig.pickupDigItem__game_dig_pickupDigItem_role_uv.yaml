kind: metric
name: game.dig.pickupDigItem__game_dig_pickupDigItem_role_uv
description: 触发挖宝拾取的去重角色数
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.dig.pickupDigItem
    time_grain: ""
relations:
  - type: derived_from
    target: game.dig.pickupDigItem
    description: 机械提取自 game.dig.pickupDigItem 的 metrics 块（key=game_dig_pickupDigItem_role_uv）
