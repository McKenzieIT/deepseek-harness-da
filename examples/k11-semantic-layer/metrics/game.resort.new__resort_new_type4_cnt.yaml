kind: metric
name: game.resort.new__resort_new_type4_cnt
description: type=4 代打请求次数（实测最大占比类型）
domains:
  - 自定义
computation:
  sql: SUM(CASE WHEN CAST(GET_JSON_OBJECT(params, '$.type') AS BIGINT) = 4 THEN 1 ELSE 0 END)
  metadata:
    aggregation: sum
    field: CASE WHEN CAST(GET_JSON_OBJECT(params, '$.type') AS BIGINT) = 4 THEN 1 ELSE 0 END
    source: game.resort.new
    time_grain: ""
relations:
  - type: derived_from
    target: game.resort.new
    description: 机械提取自 game.resort.new 的 metrics 块（key=resort_new_type4_cnt）
