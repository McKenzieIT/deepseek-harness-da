kind: metric
name: game.customLevel.segReward__game_customLevel_segReward_role_uv
description: 领取段位奖励的去重角色数
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.customLevel.segReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.customLevel.segReward
    description: 机械提取自 game.customLevel.segReward 的 metrics 块（key=game_customLevel_segReward_role_uv）
