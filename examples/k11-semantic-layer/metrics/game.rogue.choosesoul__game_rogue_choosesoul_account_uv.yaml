kind: metric
name: game.rogue.choosesoul__game_rogue_choosesoul_account_uv
description: 触发将魂选择的账号数（去重）
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT account_id)
  metadata:
    aggregation: count_distinct
    field: account_id
    source: game.rogue.choosesoul
    time_grain: ""
relations:
  - type: derived_from
    target: game.rogue.choosesoul
    description: 机械提取自 game.rogue.choosesoul 的 metrics 块（key=game_rogue_choosesoul_account_uv）
