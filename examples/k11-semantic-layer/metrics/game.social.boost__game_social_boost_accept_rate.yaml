kind: metric
name: game.social.boost__game_social_boost_accept_rate
description: 接取动作占整体动作比例（基于 NULLIF 防除零）
domains:
  - 社交公会
computation:
  sql: COUNT(CASE WHEN GET_JSON_OBJECT(params, '$.action') = '3' THEN 1 END) / NULLIF(COUNT(*), 0)
  metadata:
    aggregation: count
    field: CASE WHEN GET_JSON_OBJECT(params, '$.action') = '3' THEN 1 END) / NULLIF(COUNT(*), 0
    source: game.social.boost
    time_grain: ""
relations:
  - type: derived_from
    target: game.social.boost
    description: 机械提取自 game.social.boost 的 metrics 块（key=game_social_boost_accept_rate）
