kind: metric
name: dws_10000251_selfhelp_tag_df__user_uv
description: 当日快照用户数(UV)，需配合 user_type 过滤区分主体(1设备/2账号/3角色)
domains:
  - 用户生命周期
  - 系统监控
computation:
  sql: COUNT(DISTINCT user_id)
  metadata:
    aggregation: count_distinct
    field: user_id
    source: dws_10000251_selfhelp_tag_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_selfhelp_tag_df
    description: 机械提取自 dws_10000251_selfhelp_tag_df 的 metrics 块（key=user_uv）
