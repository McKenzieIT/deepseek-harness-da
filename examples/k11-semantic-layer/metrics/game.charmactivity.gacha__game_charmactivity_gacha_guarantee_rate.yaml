kind: metric
name: game.charmactivity.gacha__game_charmactivity_gacha_guarantee_rate
description: 保底触发率 = 保底次数 / 抽卡日志条数（NULLIF 防零除）
domains:
  - 探索收集
computation:
  sql: SUM(CASE WHEN GET_JSON_OBJECT(params,'$.guarantee')='true' THEN 1 ELSE 0 END) / NULLIF(COUNT(*),0)
  metadata:
    aggregation: sum
    field: CASE WHEN GET_JSON_OBJECT(params,'$.guarantee')='true' THEN 1 ELSE 0 END) / NULLIF(COUNT(*),0
    source: game.charmactivity.gacha
    time_grain: ""
relations:
  - type: derived_from
    target: game.charmactivity.gacha
    description: 机械提取自 game.charmactivity.gacha 的 metrics 块（key=game_charmactivity_gacha_guarantee_rate）
