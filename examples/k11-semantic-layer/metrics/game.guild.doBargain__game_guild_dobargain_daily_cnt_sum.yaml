kind: metric
name: game.guild.doBargain__game_guild_dobargain_daily_cnt_sum
description: 当日累计砍价次数之和（基于 dailyBargainCnt，ds=20260729 每人 1 次）
domains:
  - 社交公会
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.dailyBargainCnt') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.dailyBargainCnt') AS BIGINT)
    source: game.guild.doBargain
    time_grain: ""
relations:
  - type: derived_from
    target: game.guild.doBargain
    description: 机械提取自 game.guild.doBargain 的 metrics 块（key=game_guild_dobargain_daily_cnt_sum）
