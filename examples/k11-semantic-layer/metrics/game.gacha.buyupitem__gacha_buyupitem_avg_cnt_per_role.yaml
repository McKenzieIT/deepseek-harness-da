kind: metric
name: game.gacha.buyupitem__gacha_buyupitem_avg_cnt_per_role
description: 人均购买up道具数量(总数量除以去重角色数)
domains:
  - 探索收集
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params,'$.count') AS BIGINT)) / NULLIF(COUNT(DISTINCT role_id),0)
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params,'$.count') AS BIGINT)) / NULLIF(COUNT(DISTINCT role_id),0
    source: game.gacha.buyupitem
    time_grain: ""
relations:
  - type: derived_from
    target: game.gacha.buyupitem
    description: 机械提取自 game.gacha.buyupitem 的 metrics 块（key=gacha_buyupitem_avg_cnt_per_role）
