kind: metric
name: game.rogue.enter__game_rogue_enter_theme_uv
description: 进入过的不同古战场主题数
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.rogueTheme'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.rogueTheme')
    source: game.rogue.enter
    time_grain: ""
relations:
  - type: derived_from
    target: game.rogue.enter
    description: 机械提取自 game.rogue.enter 的 metrics 块（key=game_rogue_enter_theme_uv）
