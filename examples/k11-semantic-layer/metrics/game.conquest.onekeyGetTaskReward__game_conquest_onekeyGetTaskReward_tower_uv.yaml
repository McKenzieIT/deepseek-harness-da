kind: metric
name: game.conquest.onekeyGetTaskReward__game_conquest_onekeyGetTaskReward_tower_uv
description: 去重讨伐塔ID数（玩法覆盖广度）
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.towerId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.towerId')
    source: game.conquest.onekeyGetTaskReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.conquest.onekeyGetTaskReward
    description: 机械提取自 game.conquest.onekeyGetTaskReward 的 metrics 块（key=game_conquest_onekeyGetTaskReward_tower_uv）
