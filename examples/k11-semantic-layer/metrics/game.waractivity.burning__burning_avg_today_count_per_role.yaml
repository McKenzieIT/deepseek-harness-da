kind: metric
name: game.waractivity.burning__burning_avg_today_count_per_role
description: 人均今日燃灵次数（对照每日5次上限）
domains:
  - 自定义
computation:
  sql: CAST(SUM(CAST(GET_JSON_OBJECT(params, '$.todayBurningCount') AS BIGINT)) AS DOUBLE) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: ""
    field: ""
    source: game.waractivity.burning
    time_grain: ""
relations:
  - type: derived_from
    target: game.waractivity.burning
    description: 机械提取自 game.waractivity.burning 的 metrics 块（key=burning_avg_today_count_per_role）
