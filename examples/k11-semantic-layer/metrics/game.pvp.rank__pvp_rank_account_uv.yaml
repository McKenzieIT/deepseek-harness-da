kind: metric
name: game.pvp.rank__pvp_rank_account_uv
description: 命中账号数（实测退化为单一值，仅供口径记录）
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT account_id)
  metadata:
    aggregation: count_distinct
    field: account_id
    source: game.pvp.rank
    time_grain: ""
relations:
  - type: derived_from
    target: game.pvp.rank
    description: 机械提取自 game.pvp.rank 的 metrics 块（key=pvp_rank_account_uv）
