kind: metric
name: dws_10000251_pve_progress_di__win_rate
description: 当日PVE胜率（战果为胜的记录占比）
domains:
  - 战斗关卡
  - 角色成长
computation:
  sql: SUM(CASE WHEN result = 1 THEN 1 ELSE 0 END) / NULLIF(COUNT(*), 0)
  metadata:
    aggregation: sum
    field: CASE WHEN result = 1 THEN 1 ELSE 0 END) / NULLIF(COUNT(*), 0
    source: dws_10000251_pve_progress_di
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_pve_progress_di
    description: 机械提取自 dws_10000251_pve_progress_di 的 metrics 块（key=win_rate）
