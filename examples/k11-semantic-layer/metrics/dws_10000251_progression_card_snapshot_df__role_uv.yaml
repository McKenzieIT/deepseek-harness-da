kind: metric
name: dws_10000251_progression_card_snapshot_df__role_uv
description: 持有卡牌的角色(role)数(卡牌覆盖角色UV)
domains:
  - 角色成长
  - 探索收集
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: dws_10000251_progression_card_snapshot_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_progression_card_snapshot_df
    description: 机械提取自 dws_10000251_progression_card_snapshot_df 的 metrics 块（key=role_uv）
