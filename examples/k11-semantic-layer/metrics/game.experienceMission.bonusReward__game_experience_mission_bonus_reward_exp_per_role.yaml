kind: metric
name: game.experienceMission.bonusReward__game_experience_mission_bonus_reward_exp_per_role
description: 人均获得经验（exp_sum / role_uv，NULLIF 防零除）
domains:
  - 角色成长
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.exp') AS BIGINT)) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.exp') AS BIGINT)) / NULLIF(COUNT(DISTINCT role_id), 0
    source: game.experienceMission.bonusReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.experienceMission.bonusReward
    description: 机械提取自 game.experienceMission.bonusReward 的 metrics 块（key=game_experience_mission_bonus_reward_exp_per_role）
