kind: metric
name: game.knight.starupResourceComposite__knight_starup_resource_composite_starup_type_uv
description: 命中的去重升星类型数（starupId）
domains:
  - 角色成长
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params,'$.starupId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params,'$.starupId')
    source: game.knight.starupResourceComposite
    time_grain: ""
relations:
  - type: derived_from
    target: game.knight.starupResourceComposite
    description: 机械提取自 game.knight.starupResourceComposite 的 metrics 块（key=knight_starup_resource_composite_starup_type_uv）
