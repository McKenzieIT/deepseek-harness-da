kind: metric
name: game.tactic.wear__tactic_wear_avg_per_role
description: 角色平均战法穿戴次数（总次数/去重角色数，分母为0返回NULL）
domains:
  - 角色成长
computation:
  sql: ROUND(CAST(COUNT(*) AS DOUBLE) / NULLIF(COUNT(DISTINCT role_id), 0), 2)
  metadata:
    aggregation: ""
    field: ""
    source: game.tactic.wear
    time_grain: ""
relations:
  - type: derived_from
    target: game.tactic.wear
    description: 机械提取自 game.tactic.wear 的 metrics 块（key=tactic_wear_avg_per_role）
