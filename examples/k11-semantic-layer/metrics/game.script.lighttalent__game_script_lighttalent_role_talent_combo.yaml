kind: metric
name: game.script.lighttalent__game_script_lighttalent_role_talent_combo
description: 角色 × 天赋去重组合数（衡量天赋点亮覆盖广度）
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT CONCAT(role_id, '#', GET_JSON_OBJECT(params, '$.talentId')))
  metadata:
    aggregation: count_distinct
    field: CONCAT(role_id, '#', GET_JSON_OBJECT(params, '$.talentId'))
    source: game.script.lighttalent
    time_grain: ""
relations:
  - type: derived_from
    target: game.script.lighttalent
    description: 机械提取自 game.script.lighttalent 的 metrics 块（key=game_script_lighttalent_role_talent_combo）
