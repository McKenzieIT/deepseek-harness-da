kind: metric
name: game.item.change__game_item_change_amount_per_role
description: 人均道具变化数量（总变化量/活跃角色数）
domains:
  - 装备道具
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.amount') AS BIGINT)) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.amount') AS BIGINT)) / NULLIF(COUNT(DISTINCT role_id), 0
    source: game.item.change
    time_grain: ""
relations:
  - type: derived_from
    target: game.item.change
    description: 机械提取自 game.item.change 的 metrics 块（key=game_item_change_amount_per_role）
