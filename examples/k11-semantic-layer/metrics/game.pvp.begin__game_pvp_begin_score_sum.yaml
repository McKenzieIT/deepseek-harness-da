kind: metric
name: game.pvp.begin__game_pvp_begin_score_sum
description: 己方积分累计和
domains:
  - 战斗关卡
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.score') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.score') AS BIGINT)
    source: game.pvp.begin
    time_grain: ""
relations:
  - type: derived_from
    target: game.pvp.begin
    description: 机械提取自 game.pvp.begin 的 metrics 块（key=game_pvp_begin_score_sum）
