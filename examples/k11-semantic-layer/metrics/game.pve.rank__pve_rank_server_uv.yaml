kind: metric
name: game.pve.rank__pve_rank_server_uv
description: 涉及的区服数(去重 server_id，反映活动覆盖面)
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: game.pve.rank
    time_grain: ""
relations:
  - type: derived_from
    target: game.pve.rank
    description: 机械提取自 game.pve.rank 的 metrics 块（key=pve_rank_server_uv）
