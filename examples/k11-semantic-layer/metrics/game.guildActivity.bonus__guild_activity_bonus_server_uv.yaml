kind: metric
name: game.guildActivity.bonus__guild_activity_bonus_server_uv
description: 涉及的区服数(去重 server_id，反映公会活跃分红活动覆盖面)
domains:
  - 社交公会
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: game.guildActivity.bonus
    time_grain: ""
relations:
  - type: derived_from
    target: game.guildActivity.bonus
    description: 机械提取自 game.guildActivity.bonus 的 metrics 块（key=guild_activity_bonus_server_uv）
