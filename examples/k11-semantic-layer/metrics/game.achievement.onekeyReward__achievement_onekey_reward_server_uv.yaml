kind: metric
name: game.achievement.onekeyReward__achievement_onekey_reward_server_uv
description: 涉及区服数
domains:
  - 角色成长
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: game.achievement.onekeyReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.achievement.onekeyReward
    description: 机械提取自 game.achievement.onekeyReward 的 metrics 块（key=achievement_onekey_reward_server_uv）
