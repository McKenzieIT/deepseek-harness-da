kind: metric
name: game.autoSkill.delete__game_autoSkill_delete_scheme_uv
description: 被删除的去重武将方案id数（params.id）
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params,'$.id'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params,'$.id')
    source: game.autoSkill.delete
    time_grain: ""
relations:
  - type: derived_from
    target: game.autoSkill.delete
    description: 机械提取自 game.autoSkill.delete 的 metrics 块（key=game_autoSkill_delete_scheme_uv）
