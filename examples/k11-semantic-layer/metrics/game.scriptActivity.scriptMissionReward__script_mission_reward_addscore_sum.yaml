kind: metric
name: game.scriptActivity.scriptMissionReward__script_mission_reward_addscore_sum
description: 领取奖励累计增加的活动积分总和
domains:
  - 自定义
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params,'$.addScore') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params,'$.addScore') AS BIGINT)
    source: game.scriptActivity.scriptMissionReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.scriptActivity.scriptMissionReward
    description: 机械提取自 game.scriptActivity.scriptMissionReward 的 metrics 块（key=script_mission_reward_addscore_sum）
