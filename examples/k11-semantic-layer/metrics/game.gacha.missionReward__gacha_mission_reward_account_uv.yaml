kind: metric
name: game.gacha.missionReward__gacha_mission_reward_account_uv
description: 领取抽卡任务奖励的去重账号数
domains:
  - 探索收集
computation:
  sql: COUNT(DISTINCT account_id)
  metadata:
    aggregation: count_distinct
    field: account_id
    source: game.gacha.missionReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.gacha.missionReward
    description: 机械提取自 game.gacha.missionReward 的 metrics 块（key=gacha_mission_reward_account_uv）
