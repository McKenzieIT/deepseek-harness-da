kind: metric
name: game.guild.levelReward__game_guild_levelReward_role_uv
description: 领取公会等级奖励的去重角色数（主体粒度 role_id）
domains:
  - 社交公会
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.guild.levelReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.guild.levelReward
    description: 机械提取自 game.guild.levelReward 的 metrics 块（key=game_guild_levelReward_role_uv）
