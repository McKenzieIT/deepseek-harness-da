kind: metric
name: game.conquest.setThemeBuff__game_conquest_setThemeBuff_buffgroup_uv
description: 去重被选择加成buff阵营(buffGroup)数量
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.buffGroup'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.buffGroup')
    source: game.conquest.setThemeBuff
    time_grain: ""
relations:
  - type: derived_from
    target: game.conquest.setThemeBuff
    description: 机械提取自 game.conquest.setThemeBuff 的 metrics 块（key=game_conquest_setThemeBuff_buffgroup_uv）
