kind: metric
name: game.anniversary.fetchBlankFloorReward__fetchBlankFloorReward_gold_sum
description: 发放的充值钻石(gold)总量
domains:
  - 付费经济
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params,'$.coinList.gold') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params,'$.coinList.gold') AS BIGINT)
    source: game.anniversary.fetchBlankFloorReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.anniversary.fetchBlankFloorReward
    description: 机械提取自 game.anniversary.fetchBlankFloorReward 的 metrics 块（key=fetchBlankFloorReward_gold_sum）
