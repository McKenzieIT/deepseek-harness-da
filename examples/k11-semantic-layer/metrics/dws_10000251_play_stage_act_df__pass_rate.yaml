kind: metric
name: dws_10000251_play_stage_act_df__pass_rate
description: 通关率(通关UV/参与UV)
domains:
  - 战斗关卡
  - 角色成长
computation:
  sql: COUNT(DISTINCT CASE WHEN pass_status = 1 THEN role_id END) * 1.0 / NULLIF(COUNT(DISTINCT CASE WHEN part_pv > 0 THEN role_id END), 0)
  metadata:
    aggregation: count_distinct
    field: CASE WHEN pass_status = 1 THEN role_id END) * 1.0 / NULLIF(COUNT(DISTINCT CASE WHEN part_pv > 0 THEN role_id END), 0
    source: dws_10000251_play_stage_act_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_play_stage_act_df
    description: 机械提取自 dws_10000251_play_stage_act_df 的 metrics 块（key=pass_rate）
