kind: metric
name: game.guild.gvgExchangeScoreReward__guild_gvgExchangeScoreReward_count_sum
description: 兑换扣除次数总和（count 字段求和）
domains:
  - 社交公会
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.count') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.count') AS BIGINT)
    source: game.guild.gvgExchangeScoreReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.guild.gvgExchangeScoreReward
    description: 机械提取自 game.guild.gvgExchangeScoreReward 的 metrics 块（key=guild_gvgExchangeScoreReward_count_sum）
