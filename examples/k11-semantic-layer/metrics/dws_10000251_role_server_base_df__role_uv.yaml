kind: metric
name: dws_10000251_role_server_base_df__role_uv
description: 角色数UV：当日快照下去重角色数（角色-区服粒度主体指标）
domains:
  - 用户生命周期
  - 付费经济
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: dws_10000251_role_server_base_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_role_server_base_df
    description: 机械提取自 dws_10000251_role_server_base_df 的 metrics 块（key=role_uv）
