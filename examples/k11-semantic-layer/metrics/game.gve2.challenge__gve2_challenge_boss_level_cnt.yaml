kind: metric
name: game.gve2.challenge__gve2_challenge_boss_level_cnt
description: GVE2挑战触及的boss层数去重数量
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params,'$.bossLevel'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params,'$.bossLevel')
    source: game.gve2.challenge
    time_grain: ""
relations:
  - type: derived_from
    target: game.gve2.challenge
    description: 机械提取自 game.gve2.challenge 的 metrics 块（key=gve2_challenge_boss_level_cnt）
