kind: metric
name: game.toy.techonekeyactive__techonekeyactive_server_uv
description: 触发一键科技激活的去重服务器数
domains:
  - 探索收集
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: game.toy.techonekeyactive
    time_grain: ""
relations:
  - type: derived_from
    target: game.toy.techonekeyactive
    description: 机械提取自 game.toy.techonekeyactive 的 metrics 块（key=techonekeyactive_server_uv）
