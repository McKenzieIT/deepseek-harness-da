kind: metric
name: game.role.backup.rollbackAll__rollbackAll_cnt
description: 玩家全量数据回退总次数
domains:
  - 用户生命周期
computation:
  sql: COUNT(*)
  metadata:
    aggregation: count
    field: "*"
    source: game.role.backup.rollbackAll
    time_grain: ""
relations:
  - type: derived_from
    target: game.role.backup.rollbackAll
    description: 机械提取自 game.role.backup.rollbackAll 的 metrics 块（key=rollbackAll_cnt）
