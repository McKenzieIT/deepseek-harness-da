kind: metric
name: game.toy.gacha__game_toy_gacha_pulls_per_role
description: 人均抽卡次数 = 累计抽卡次数 / 角色去重数（NULLIF 防零除）
domains:
  - 探索收集
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params,'$.count') AS BIGINT)) / NULLIF(COUNT(DISTINCT role_id),0)
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params,'$.count') AS BIGINT)) / NULLIF(COUNT(DISTINCT role_id),0
    source: game.toy.gacha
    time_grain: ""
relations:
  - type: derived_from
    target: game.toy.gacha
    description: 机械提取自 game.toy.gacha 的 metrics 块（key=game_toy_gacha_pulls_per_role）
