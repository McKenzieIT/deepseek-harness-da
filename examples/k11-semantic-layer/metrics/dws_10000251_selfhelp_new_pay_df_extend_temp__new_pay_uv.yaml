kind: metric
name: dws_10000251_selfhelp_new_pay_df_extend_temp__new_pay_uv
description: 新增付费用户数（去重user_id），_df 日全量快照；按 user_type 区分设备(1)/账号(2)/角色(3) 三种主体。
domains:
  - 付费经济
  - 用户生命周期
computation:
  sql: COUNT(DISTINCT user_id)
  metadata:
    aggregation: count_distinct
    field: user_id
    source: dws_10000251_selfhelp_new_pay_df_extend_temp
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_selfhelp_new_pay_df_extend_temp
    description: 机械提取自 dws_10000251_selfhelp_new_pay_df_extend_temp 的 metrics 块（key=new_pay_uv）
