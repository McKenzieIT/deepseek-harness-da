kind: metric
name: game.guild.kickout__guild_kickout_target_per_actor
description: 平均每个操作者踢出的去重目标数(被踢目标/操作者；NULLIF 防零除)
domains:
  - 社交公会
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.targetUid')) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.targetUid')) / NULLIF(COUNT(DISTINCT role_id), 0
    source: game.guild.kickout
    time_grain: ""
relations:
  - type: derived_from
    target: game.guild.kickout
    description: 机械提取自 game.guild.kickout 的 metrics 块（key=guild_kickout_target_per_actor）
