kind: metric
name: game.mission.reward__game_mission_reward_gold_sum
description: 领奖产出充值钻石总量
domains:
  - 角色成长
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.coinList.gold') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.coinList.gold') AS BIGINT)
    source: game.mission.reward
    time_grain: ""
relations:
  - type: derived_from
    target: game.mission.reward
    description: 机械提取自 game.mission.reward 的 metrics 块（key=game_mission_reward_gold_sum）
