kind: metric
name: dws_10000251_lt_role_tag_df__avg_basic_role_level
description: basic标签下角色等级的平均值（NULLIF语义由CASE保证，仅角色等级键参与）
domains:
  - 角色成长
  - 用户生命周期
computation:
  sql: AVG(CASE WHEN tag_key = '角色等级' THEN tag_value END)
  metadata:
    aggregation: avg
    field: CASE WHEN tag_key = '角色等级' THEN tag_value END
    source: dws_10000251_lt_role_tag_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_lt_role_tag_df
    description: 机械提取自 dws_10000251_lt_role_tag_df 的 metrics 块（key=avg_basic_role_level）
