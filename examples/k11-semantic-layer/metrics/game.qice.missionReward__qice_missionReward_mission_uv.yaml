kind: metric
name: game.qice.missionReward__qice_missionReward_mission_uv
description: 当日被领取奖励的去重任务数（反映任务参与广度）
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.missionId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.missionId')
    source: game.qice.missionReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.qice.missionReward
    description: 机械提取自 game.qice.missionReward 的 metrics 块（key=qice_missionReward_mission_uv）
