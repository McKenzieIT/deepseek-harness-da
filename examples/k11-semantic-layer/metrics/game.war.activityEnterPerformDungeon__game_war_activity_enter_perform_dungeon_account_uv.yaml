kind: metric
name: game.war.activityEnterPerformDungeon__game_war_activity_enter_perform_dungeon_account_uv
description: 进入剧情表演副本的账号数（去重）
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT account_id)
  metadata:
    aggregation: count_distinct
    field: account_id
    source: game.war.activityEnterPerformDungeon
    time_grain: ""
relations:
  - type: derived_from
    target: game.war.activityEnterPerformDungeon
    description: 机械提取自 game.war.activityEnterPerformDungeon 的 metrics 块（key=game_war_activity_enter_perform_dungeon_account_uv）
