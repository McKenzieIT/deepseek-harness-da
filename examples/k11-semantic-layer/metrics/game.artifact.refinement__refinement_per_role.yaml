kind: metric
name: game.artifact.refinement__refinement_per_role
description: 人均精炼次数（总次数 / 去重角色数）
domains:
  - 角色成长
computation:
  sql: COUNT(*) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: count
    field: "*) / NULLIF(COUNT(DISTINCT role_id), 0"
    source: game.artifact.refinement
    time_grain: ""
relations:
  - type: derived_from
    target: game.artifact.refinement
    description: 机械提取自 game.artifact.refinement 的 metrics 块（key=refinement_per_role）
