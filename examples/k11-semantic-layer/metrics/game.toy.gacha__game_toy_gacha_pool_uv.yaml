kind: metric
name: game.toy.gacha__game_toy_gacha_pool_uv
description: 当期涉及卡池去重数（gachaId 去重）
domains:
  - 探索收集
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params,'$.gachaId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params,'$.gachaId')
    source: game.toy.gacha
    time_grain: ""
relations:
  - type: derived_from
    target: game.toy.gacha
    description: 机械提取自 game.toy.gacha 的 metrics 块（key=game_toy_gacha_pool_uv）
