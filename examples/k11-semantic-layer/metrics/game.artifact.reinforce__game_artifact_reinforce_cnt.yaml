kind: metric
name: game.artifact.reinforce__game_artifact_reinforce_cnt
description: 兵符强化事件总数（记录条数）
domains:
  - 角色成长
computation:
  sql: COUNT(*)
  metadata:
    aggregation: count
    field: "*"
    source: game.artifact.reinforce
    time_grain: ""
relations:
  - type: derived_from
    target: game.artifact.reinforce
    description: 机械提取自 game.artifact.reinforce 的 metrics 块（key=game_artifact_reinforce_cnt）
