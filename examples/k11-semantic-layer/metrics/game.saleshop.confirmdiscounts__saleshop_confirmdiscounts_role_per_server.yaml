kind: metric
name: game.saleshop.confirmdiscounts__saleshop_confirmdiscounts_role_per_server
description: 每服平均触发确认折扣的角色数
domains:
  - 付费经济
computation:
  sql: COUNT(DISTINCT role_id) / NULLIF(COUNT(DISTINCT server_id), 0)
  metadata:
    aggregation: count_distinct
    field: role_id) / NULLIF(COUNT(DISTINCT server_id), 0
    source: game.saleshop.confirmdiscounts
    time_grain: ""
relations:
  - type: derived_from
    target: game.saleshop.confirmdiscounts
    description: 机械提取自 game.saleshop.confirmdiscounts 的 metrics 块（key=saleshop_confirmdiscounts_role_per_server）
