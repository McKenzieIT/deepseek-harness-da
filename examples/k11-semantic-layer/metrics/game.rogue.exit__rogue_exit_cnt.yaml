kind: metric
name: game.rogue.exit__rogue_exit_cnt
description: 古战场退出事件总次数
domains:
  - 战斗关卡
computation:
  sql: COUNT(*)
  metadata:
    aggregation: count
    field: "*"
    source: game.rogue.exit
    time_grain: ""
relations:
  - type: derived_from
    target: game.rogue.exit
    description: 机械提取自 game.rogue.exit 的 metrics 块（key=rogue_exit_cnt）
