kind: metric
name: game.experienceMission.bonusReward__game_experience_mission_bonus_reward_exp_sum
description: 通过最终大奖发放的经验总量（params.exp 字段累加）
domains:
  - 角色成长
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.exp') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.exp') AS BIGINT)
    source: game.experienceMission.bonusReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.experienceMission.bonusReward
    description: 机械提取自 game.experienceMission.bonusReward 的 metrics 块（key=game_experience_mission_bonus_reward_exp_sum）
