kind: metric
name: dws_10000251_risk_dev_identification_df__device_uv
description: 设备主体(user_type=1)风险标识UV
domains:
  - 系统监控
  - 自定义
computation:
  sql: COUNT(DISTINCT CASE WHEN user_type=1 THEN user_id END)
  metadata:
    aggregation: count_distinct
    field: CASE WHEN user_type=1 THEN user_id END
    source: dws_10000251_risk_dev_identification_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_risk_dev_identification_df
    description: 机械提取自 dws_10000251_risk_dev_identification_df 的 metrics 块（key=device_uv）
