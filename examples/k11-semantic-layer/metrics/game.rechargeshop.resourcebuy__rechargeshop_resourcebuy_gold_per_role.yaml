kind: metric
name: game.rechargeshop.resourcebuy__rechargeshop_resourcebuy_gold_per_role
description: 人均消耗充值钻石（gold_sum / role_uv，分母为0时返回NULL）
domains:
  - 付费经济
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.coinList.gold') AS BIGINT)) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.coinList.gold') AS BIGINT)) / NULLIF(COUNT(DISTINCT role_id), 0
    source: game.rechargeshop.resourcebuy
    time_grain: ""
relations:
  - type: derived_from
    target: game.rechargeshop.resourcebuy
    description: 机械提取自 game.rechargeshop.resourcebuy 的 metrics 块（key=rechargeshop_resourcebuy_gold_per_role）
