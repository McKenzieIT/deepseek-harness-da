kind: metric
name: game.social.friendgift__friendgift_send_cnt
description: 单次赠礼（action=1）事件次数
domains:
  - 社交公会
computation:
  sql: SUM(CASE WHEN GET_JSON_OBJECT(params,'$.action')='1' THEN 1 ELSE 0 END)
  metadata:
    aggregation: sum
    field: CASE WHEN GET_JSON_OBJECT(params,'$.action')='1' THEN 1 ELSE 0 END
    source: game.social.friendgift
    time_grain: ""
relations:
  - type: derived_from
    target: game.social.friendgift
    description: 机械提取自 game.social.friendgift 的 metrics 块（key=friendgift_send_cnt）
