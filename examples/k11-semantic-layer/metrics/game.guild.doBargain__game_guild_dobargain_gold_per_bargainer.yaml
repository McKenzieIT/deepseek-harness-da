kind: metric
name: game.guild.doBargain__game_guild_dobargain_gold_per_bargainer
description: 砍价参与者人均持有充值钻石(gold)，砍价时经济画像（货币快照非收入）
domains:
  - 社交公会
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.coinList.gold') AS BIGINT)) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.coinList.gold') AS BIGINT)) / NULLIF(COUNT(DISTINCT role_id), 0
    source: game.guild.doBargain
    time_grain: ""
relations:
  - type: derived_from
    target: game.guild.doBargain
    description: 机械提取自 game.guild.doBargain 的 metrics 块（key=game_guild_dobargain_gold_per_bargainer）
