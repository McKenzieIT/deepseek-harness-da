kind: metric
name: game.task.routinetask__task_routinetask_daily_task_role_uv
description: 每日任务(taskType=1)参与去重角色数
domains:
  - 角色成长
computation:
  sql: COUNT(DISTINCT CASE WHEN GET_JSON_OBJECT(params,'$.taskType')='1' THEN role_id END)
  metadata:
    aggregation: count_distinct
    field: CASE WHEN GET_JSON_OBJECT(params,'$.taskType')='1' THEN role_id END
    source: game.task.routinetask
    time_grain: ""
relations:
  - type: derived_from
    target: game.task.routinetask
    description: 机械提取自 game.task.routinetask 的 metrics 块（key=task_routinetask_daily_task_role_uv）
