kind: metric
name: game.battlepass.onekeyMissionReward__onekey_mission_reward_role_uv
description: 触发一键领奖的去重角色数
domains:
  - 角色成长
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.battlepass.onekeyMissionReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.battlepass.onekeyMissionReward
    description: 机械提取自 game.battlepass.onekeyMissionReward 的 metrics 块（key=onekey_mission_reward_role_uv）
