kind: metric
name: game.fish.compose__game_fish_compose_role_uv
description: 触发钓鱼合成的去重角色数
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.fish.compose
    time_grain: ""
relations:
  - type: derived_from
    target: game.fish.compose
    description: 机械提取自 game.fish.compose 的 metrics 块（key=game_fish_compose_role_uv）
