kind: metric
name: game.charmStone.setLock__game_charmStone_setLock_lock_rate
description: 上锁操作占总操作的比例
domains:
  - 探索收集
computation:
  sql: SUM(CASE WHEN GET_JSON_OBJECT(params,'$.charmStone.isLock')='true' THEN 1 ELSE 0 END) / NULLIF(COUNT(*), 0)
  metadata:
    aggregation: sum
    field: CASE WHEN GET_JSON_OBJECT(params,'$.charmStone.isLock')='true' THEN 1 ELSE 0 END) / NULLIF(COUNT(*), 0
    source: game.charmStone.setLock
    time_grain: ""
relations:
  - type: derived_from
    target: game.charmStone.setLock
    description: 机械提取自 game.charmStone.setLock 的 metrics 块（key=game_charmStone_setLock_lock_rate）
