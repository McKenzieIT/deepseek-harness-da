kind: metric
name: dws_10000251_univ_user_predown_tag_df__rule_both_hit_cnt
description: 同时命中原生与扩展两条规则的行数，用于识别规则重叠人群（行级计数，非去重用户数）
domains:
  - 用户生命周期
  - 资源产销
computation:
  sql: SUM(CASE WHEN rule1 = 1 AND rule2 = 1 THEN 1 ELSE 0 END)
  metadata:
    aggregation: sum
    field: CASE WHEN rule1 = 1 AND rule2 = 1 THEN 1 ELSE 0 END
    source: dws_10000251_univ_user_predown_tag_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_univ_user_predown_tag_df
    description: 机械提取自 dws_10000251_univ_user_predown_tag_df 的 metrics 块（key=rule_both_hit_cnt）
