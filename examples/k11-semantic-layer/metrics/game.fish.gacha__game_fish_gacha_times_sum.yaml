kind: metric
name: game.fish.gacha__game_fish_gacha_times_sum
description: 钓鱼抽卡累计次数总和（params.times 字段）
domains:
  - 自定义
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params,'$.times') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params,'$.times') AS BIGINT)
    source: game.fish.gacha
    time_grain: ""
relations:
  - type: derived_from
    target: game.fish.gacha
    description: 机械提取自 game.fish.gacha 的 metrics 块（key=game_fish_gacha_times_sum）
