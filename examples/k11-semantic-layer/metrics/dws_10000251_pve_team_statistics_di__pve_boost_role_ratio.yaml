kind: metric
name: dws_10000251_pve_team_statistics_di__pve_boost_role_ratio
description: 当日代打角色占比(代打角色去重数/参与角色去重数)，衡量代打( boosting )使用情况
domains:
  - 战斗关卡
  - 角色成长
computation:
  sql: COUNT(DISTINCT CASE WHEN is_boost = 1 THEN role_id END) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: count_distinct
    field: CASE WHEN is_boost = 1 THEN role_id END) / NULLIF(COUNT(DISTINCT role_id), 0
    source: dws_10000251_pve_team_statistics_di
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_pve_team_statistics_di
    description: 机械提取自 dws_10000251_pve_team_statistics_di 的 metrics 块（key=pve_boost_role_ratio）
