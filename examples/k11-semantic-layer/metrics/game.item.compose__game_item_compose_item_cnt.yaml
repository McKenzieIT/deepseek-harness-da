kind: metric
name: game.item.compose__game_item_compose_item_cnt
description: 合成道具总数量(按 params.count 求和)
domains:
  - 装备道具
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.count') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.count') AS BIGINT)
    source: game.item.compose
    time_grain: ""
relations:
  - type: derived_from
    target: game.item.compose
    description: 机械提取自 game.item.compose 的 metrics 块（key=game_item_compose_item_cnt）
