kind: metric
name: dws_10000251_univ_acc_act_mi__pay_rate
description: 月付费率=付费账号UV/月活账号UV，分母为0时返回NULL
domains:
  - 用户生命周期
  - 付费经济
computation:
  sql: SUM(pay) * 1.0 / NULLIF(COUNT(DISTINCT user_id), 0)
  metadata:
    aggregation: sum
    field: pay) * 1.0 / NULLIF(COUNT(DISTINCT user_id), 0
    source: dws_10000251_univ_acc_act_mi
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_univ_acc_act_mi
    description: 机械提取自 dws_10000251_univ_acc_act_mi 的 metrics 块（key=pay_rate）
