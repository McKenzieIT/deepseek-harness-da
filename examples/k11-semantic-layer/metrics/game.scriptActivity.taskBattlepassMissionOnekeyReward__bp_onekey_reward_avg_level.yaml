kind: metric
name: game.scriptActivity.taskBattlepassMissionOnekeyReward__bp_onekey_reward_avg_level
description: 触发一键领取角色的平均等级（领取时刻角色快照等级）
domains:
  - 自定义
computation:
  sql: AVG(CAST(GET_JSON_OBJECT(params, '$.roleLevel') AS DOUBLE))
  metadata:
    aggregation: avg
    field: CAST(GET_JSON_OBJECT(params, '$.roleLevel') AS DOUBLE)
    source: game.scriptActivity.taskBattlepassMissionOnekeyReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.scriptActivity.taskBattlepassMissionOnekeyReward
    description: 机械提取自 game.scriptActivity.taskBattlepassMissionOnekeyReward 的 metrics 块（key=bp_onekey_reward_avg_level）
