kind: metric
name: game.knight.levelup__knight_levelup_knight_uv
description: 被升级的去重武将数（按 knightId）
domains:
  - 角色成长
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.knightId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.knightId')
    source: game.knight.levelup
    time_grain: ""
relations:
  - type: derived_from
    target: game.knight.levelup
    description: 机械提取自 game.knight.levelup 的 metrics 块（key=knight_levelup_knight_uv）
