kind: metric
name: game.rogue.enter__game_rogue_enter_avg_per_role
description: 人均古战场进入次数(总次数/去重角色数)
domains:
  - 战斗关卡
computation:
  sql: CAST(COUNT(*) AS DOUBLE) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: ""
    field: ""
    source: game.rogue.enter
    time_grain: ""
relations:
  - type: derived_from
    target: game.rogue.enter
    description: 机械提取自 game.rogue.enter 的 metrics 块（key=game_rogue_enter_avg_per_role）
