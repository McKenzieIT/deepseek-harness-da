kind: metric
name: dws_10000251_role_common_feature_di__vip_role_ratio
description: VIP 角色占比,VIP 等级>0 的角色比例,衡量付费特权渗透率
domains:
  - 角色成长
  - 社交公会
  - 付费经济
computation:
  sql: AVG(CASE WHEN CAST(viplevel AS DOUBLE) > 0 THEN 1 ELSE 0 END)
  metadata:
    aggregation: avg
    field: CASE WHEN CAST(viplevel AS DOUBLE) > 0 THEN 1 ELSE 0 END
    source: dws_10000251_role_common_feature_di
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_role_common_feature_di
    description: 机械提取自 dws_10000251_role_common_feature_di 的 metrics 块（key=vip_role_ratio）
