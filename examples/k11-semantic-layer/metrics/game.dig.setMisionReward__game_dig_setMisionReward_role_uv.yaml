kind: metric
name: game.dig.setMisionReward__game_dig_setMisionReward_role_uv
description: 触发挖宝设置任务奖励的去重角色数（主体粒度 role_id）
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.dig.setMisionReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.dig.setMisionReward
    description: 机械提取自 game.dig.setMisionReward 的 metrics 块（key=game_dig_setMisionReward_role_uv）
