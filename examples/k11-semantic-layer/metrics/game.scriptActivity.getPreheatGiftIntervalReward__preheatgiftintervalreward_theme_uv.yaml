kind: metric
name: game.scriptActivity.getPreheatGiftIntervalReward__preheatgiftintervalreward_theme_uv
description: 触发间隔领奖的去重活动主题数
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.themeId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.themeId')
    source: game.scriptActivity.getPreheatGiftIntervalReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.scriptActivity.getPreheatGiftIntervalReward
    description: 机械提取自 game.scriptActivity.getPreheatGiftIntervalReward 的 metrics 块（key=preheatgiftintervalreward_theme_uv）
