kind: metric
name: game.rogue.selectNode__rogue_selectNode_cnt
description: 古战场选择节点事件总次数（每条事件=选择一次 nodeId）
domains:
  - 战斗关卡
computation:
  sql: COUNT(*)
  metadata:
    aggregation: count
    field: "*"
    source: game.rogue.selectNode
    time_grain: ""
relations:
  - type: derived_from
    target: game.rogue.selectNode
    description: 机械提取自 game.rogue.selectNode 的 metrics 块（key=rogue_selectNode_cnt）
