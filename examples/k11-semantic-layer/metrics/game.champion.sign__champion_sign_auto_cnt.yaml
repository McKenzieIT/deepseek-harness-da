kind: metric
name: game.champion.sign__champion_sign_auto_cnt
description: 自动报名次数（signType=1）
domains:
  - 战斗关卡
computation:
  sql: SUM(CASE WHEN GET_JSON_OBJECT(params,'$.signType') = '1' THEN 1 ELSE 0 END)
  metadata:
    aggregation: sum
    field: CASE WHEN GET_JSON_OBJECT(params,'$.signType') = '1' THEN 1 ELSE 0 END
    source: game.champion.sign
    time_grain: ""
relations:
  - type: derived_from
    target: game.champion.sign
    description: 机械提取自 game.champion.sign 的 metrics 块（key=champion_sign_auto_cnt）
