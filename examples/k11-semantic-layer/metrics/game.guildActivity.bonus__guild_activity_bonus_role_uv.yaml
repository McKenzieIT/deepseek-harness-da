kind: metric
name: game.guildActivity.bonus__guild_activity_bonus_role_uv
description: 领取分红的独立角色数(去重 role_id；主体粒度为角色)
domains:
  - 社交公会
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.guildActivity.bonus
    time_grain: ""
relations:
  - type: derived_from
    target: game.guildActivity.bonus
    description: 机械提取自 game.guildActivity.bonus 的 metrics 块（key=guild_activity_bonus_role_uv）
