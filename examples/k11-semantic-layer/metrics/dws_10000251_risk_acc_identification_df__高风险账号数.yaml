kind: metric
name: dws_10000251_risk_acc_identification_df__高风险账号数
description: 高风险等级(risk_level>=3)账号数
domains:
  - 系统监控
computation:
  sql: COUNT(DISTINCT CASE WHEN risk_level >= 3 THEN user_id END)
  metadata:
    aggregation: count_distinct
    field: CASE WHEN risk_level >= 3 THEN user_id END
    source: dws_10000251_risk_acc_identification_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_risk_acc_identification_df
    description: 机械提取自 dws_10000251_risk_acc_identification_df 的 metrics 块（key=高风险账号数）
