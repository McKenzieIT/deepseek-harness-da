kind: metric
name: dws_10000251_univ_role_act_mi__role_pay_rate
description: 角色月付费率 = 月付费 UV / 月活 UV
domains:
  - 用户生命周期
  - 付费经济
computation:
  sql: SUM(CASE WHEN pay = 1 THEN 1 ELSE 0 END) / NULLIF(COUNT(DISTINCT user_id), 0)
  metadata:
    aggregation: sum
    field: CASE WHEN pay = 1 THEN 1 ELSE 0 END) / NULLIF(COUNT(DISTINCT user_id), 0
    source: dws_10000251_univ_role_act_mi
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_univ_role_act_mi
    description: 机械提取自 dws_10000251_univ_role_act_mi 的 metrics 块（key=role_pay_rate）
