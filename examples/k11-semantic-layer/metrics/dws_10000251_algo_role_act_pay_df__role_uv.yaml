kind: metric
name: dws_10000251_algo_role_act_pay_df__role_uv
description: 当日活跃角色数（DISTINCT role_id）
domains:
  - 付费经济
  - 用户生命周期
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: dws_10000251_algo_role_act_pay_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_algo_role_act_pay_df
    description: 机械提取自 dws_10000251_algo_role_act_pay_df 的 metrics 块（key=role_uv）
