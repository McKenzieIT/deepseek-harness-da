kind: metric
name: game.champion.resetGuess__champion_reset_guess_count_sum
description: count 字段累计值（取消竞猜相关计数值合计）
domains:
  - 战斗关卡
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params,'$.count') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params,'$.count') AS BIGINT)
    source: game.champion.resetGuess
    time_grain: ""
relations:
  - type: derived_from
    target: game.champion.resetGuess
    description: 机械提取自 game.champion.resetGuess 的 metrics 块（key=champion_reset_guess_count_sum）
