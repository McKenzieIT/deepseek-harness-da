kind: metric
name: game.rogue.finishTask__rogue_finishTask_per_role_avg
description: 人均完成图鉴任务次数（总次数/去重角色数，分母为0返回NULL）
domains:
  - 战斗关卡
computation:
  sql: CAST(COUNT(*) AS DOUBLE) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: ""
    field: ""
    source: game.rogue.finishTask
    time_grain: ""
relations:
  - type: derived_from
    target: game.rogue.finishTask
    description: 机械提取自 game.rogue.finishTask 的 metrics 块（key=rogue_finishTask_per_role_avg）
