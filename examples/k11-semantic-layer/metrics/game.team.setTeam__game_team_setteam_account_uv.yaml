kind: metric
name: game.team.setTeam__game_team_setteam_account_uv
description: 设置阵容的去重账号数
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT account_id)
  metadata:
    aggregation: count_distinct
    field: account_id
    source: game.team.setTeam
    time_grain: ""
relations:
  - type: derived_from
    target: game.team.setTeam
    description: 机械提取自 game.team.setTeam 的 metrics 块（key=game_team_setteam_account_uv）
