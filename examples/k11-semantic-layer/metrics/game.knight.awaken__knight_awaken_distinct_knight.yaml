kind: metric
name: game.knight.awaken__knight_awaken_distinct_knight
description: 觉醒涉及的武将去重数(按knightId)
domains:
  - 角色成长
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.knightId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.knightId')
    source: game.knight.awaken
    time_grain: ""
relations:
  - type: derived_from
    target: game.knight.awaken
    description: 机械提取自 game.knight.awaken 的 metrics 块（key=knight_awaken_distinct_knight）
