kind: metric
name: game.achievement.reward__achievement_reward_energy_sum
description: 体力奖励发放累计
domains:
  - 角色成长
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params,'$.coinList.energy') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params,'$.coinList.energy') AS BIGINT)
    source: game.achievement.reward
    time_grain: ""
relations:
  - type: derived_from
    target: game.achievement.reward
    description: 机械提取自 game.achievement.reward 的 metrics 块（key=achievement_reward_energy_sum）
