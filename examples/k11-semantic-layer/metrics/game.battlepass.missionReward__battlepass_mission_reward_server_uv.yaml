kind: metric
name: game.battlepass.missionReward__battlepass_mission_reward_server_uv
description: 涉及的区服数(去重 server_id，反映战令任务覆盖面)
domains:
  - 角色成长
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: game.battlepass.missionReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.battlepass.missionReward
    description: 机械提取自 game.battlepass.missionReward 的 metrics 块（key=battlepass_mission_reward_server_uv）
