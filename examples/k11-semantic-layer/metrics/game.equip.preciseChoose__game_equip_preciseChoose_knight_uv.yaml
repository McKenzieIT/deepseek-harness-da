kind: metric
name: game.equip.preciseChoose__game_equip_preciseChoose_knight_uv
description: 涉及的骑士(knightId)去重数
domains:
  - 装备道具
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.knightId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.knightId')
    source: game.equip.preciseChoose
    time_grain: ""
relations:
  - type: derived_from
    target: game.equip.preciseChoose
    description: 机械提取自 game.equip.preciseChoose 的 metrics 块（key=game_equip_preciseChoose_knight_uv）
