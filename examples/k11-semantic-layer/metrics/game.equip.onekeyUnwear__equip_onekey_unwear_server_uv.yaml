kind: metric
name: game.equip.onekeyUnwear__equip_onekey_unwear_server_uv
description: 触发一键脱下覆盖的去重游戏服数
domains:
  - 装备道具
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: game.equip.onekeyUnwear
    time_grain: ""
relations:
  - type: derived_from
    target: game.equip.onekeyUnwear
    description: 机械提取自 game.equip.onekeyUnwear 的 metrics 块（key=equip_onekey_unwear_server_uv）
