kind: metric
name: game.scriptActivity.getRunningSignReward__scriptActivity_getRunningSignReward_theme_uv
description: 领取签到奖励的去重活动主题(themeId)数
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params,'$.themeId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params,'$.themeId')
    source: game.scriptActivity.getRunningSignReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.scriptActivity.getRunningSignReward
    description: 机械提取自 game.scriptActivity.getRunningSignReward 的 metrics 块（key=scriptActivity_getRunningSignReward_theme_uv）
