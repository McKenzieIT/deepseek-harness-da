kind: metric
name: game.gacha.switchSpecial__game_gacha_switchSpecial_switch_count_sum
description: 切换后累计切换次数（switchCount）求和，用于衡量卡池切换强度
domains:
  - 探索收集
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params,'$.switchCount') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params,'$.switchCount') AS BIGINT)
    source: game.gacha.switchSpecial
    time_grain: ""
relations:
  - type: derived_from
    target: game.gacha.switchSpecial
    description: 机械提取自 game.gacha.switchSpecial 的 metrics 块（key=game_gacha_switchSpecial_switch_count_sum）
