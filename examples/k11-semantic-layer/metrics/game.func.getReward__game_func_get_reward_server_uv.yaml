kind: metric
name: game.func.getReward__game_func_get_reward_server_uv
description: 涉及的游戏服数量（去重）
domains:
  - 系统监控
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: game.func.getReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.func.getReward
    description: 机械提取自 game.func.getReward 的 metrics 块（key=game_func_get_reward_server_uv）
