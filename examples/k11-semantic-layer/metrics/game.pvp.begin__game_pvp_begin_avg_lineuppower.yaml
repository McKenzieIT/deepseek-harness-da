kind: metric
name: game.pvp.begin__game_pvp_begin_avg_lineuppower
description: 平均单场己方阵容战力
domains:
  - 战斗关卡
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.lineupPower') AS BIGINT)) / NULLIF(COUNT(*), 0)
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.lineupPower') AS BIGINT)) / NULLIF(COUNT(*), 0
    source: game.pvp.begin
    time_grain: ""
relations:
  - type: derived_from
    target: game.pvp.begin
    description: 机械提取自 game.pvp.begin 的 metrics 块（key=game_pvp_begin_avg_lineuppower）
