kind: metric
name: dws_10000251_univ_user_predown_tag_df__rule_any_hit_cnt
description: 命中任一预下载规则的行数（行级计数，每 user_type+user_id 一行计一次，如需用户级去重需另用 COUNT DISTINCT）
domains:
  - 用户生命周期
  - 资源产销
computation:
  sql: SUM(CASE WHEN rule1 = 1 OR rule2 = 1 THEN 1 ELSE 0 END)
  metadata:
    aggregation: sum
    field: CASE WHEN rule1 = 1 OR rule2 = 1 THEN 1 ELSE 0 END
    source: dws_10000251_univ_user_predown_tag_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_univ_user_predown_tag_df
    description: 机械提取自 dws_10000251_univ_user_predown_tag_df 的 metrics 块（key=rule_any_hit_cnt）
