kind: metric
name: game.script.lighttalent__game_script_lighttalent_avg_talent_per_role
description: 人均点亮天赋数 = 总点亮次数 / 去重角色数
domains:
  - 自定义
computation:
  sql: COUNT(*) * 1.0 / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: count
    field: "*) * 1.0 / NULLIF(COUNT(DISTINCT role_id), 0"
    source: game.script.lighttalent
    time_grain: ""
relations:
  - type: derived_from
    target: game.script.lighttalent
    description: 机械提取自 game.script.lighttalent 的 metrics 块（key=game_script_lighttalent_avg_talent_per_role）
