kind: metric
name: game.recharge.purcharseActivityReward__purcharse_activity_reward_reward_uv
description: 涉及的去重奖励档位数(基于 rewardId)
domains:
  - 付费经济
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.rewardId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.rewardId')
    source: game.recharge.purcharseActivityReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.recharge.purcharseActivityReward
    description: 机械提取自 game.recharge.purcharseActivityReward 的 metrics 块（key=purcharse_activity_reward_reward_uv）
