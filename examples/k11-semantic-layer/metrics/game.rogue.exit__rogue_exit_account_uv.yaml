kind: metric
name: game.rogue.exit__rogue_exit_account_uv
description: 古战场退出的去重账号数
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT account_id)
  metadata:
    aggregation: count_distinct
    field: account_id
    source: game.rogue.exit
    time_grain: ""
relations:
  - type: derived_from
    target: game.rogue.exit
    description: 机械提取自 game.rogue.exit 的 metrics 块（key=rogue_exit_account_uv）
