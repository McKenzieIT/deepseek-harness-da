kind: metric
name: dws_10000251_play_rogue_df__role_uv
description: 参与古战场玩法的角色数(UV)，去重角色主键
domains:
  - 战斗关卡
  - 角色成长
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: dws_10000251_play_rogue_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_play_rogue_df
    description: 机械提取自 dws_10000251_play_rogue_df 的 metrics 块（key=role_uv）
