kind: metric
name: game.guild.cancelInviteItem__guild_cancel_invite_item_account_uv
description: 发起取消邀请的独立账号数(去重 account_id)
domains:
  - 社交公会
computation:
  sql: COUNT(DISTINCT account_id)
  metadata:
    aggregation: count_distinct
    field: account_id
    source: game.guild.cancelInviteItem
    time_grain: ""
relations:
  - type: derived_from
    target: game.guild.cancelInviteItem
    description: 机械提取自 game.guild.cancelInviteItem 的 metrics 块（key=guild_cancel_invite_item_account_uv）
