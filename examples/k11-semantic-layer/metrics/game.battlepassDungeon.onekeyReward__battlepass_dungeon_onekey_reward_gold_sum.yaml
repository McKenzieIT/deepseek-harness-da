kind: metric
name: game.battlepassDungeon.onekeyReward__battlepass_dungeon_onekey_reward_gold_sum
description: 发放充值钻石总量(coinList.gold 求和)
domains:
  - 角色成长
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.coinList.gold') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.coinList.gold') AS BIGINT)
    source: game.battlepassDungeon.onekeyReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.battlepassDungeon.onekeyReward
    description: 机械提取自 game.battlepassDungeon.onekeyReward 的 metrics 块（key=battlepass_dungeon_onekey_reward_gold_sum）
