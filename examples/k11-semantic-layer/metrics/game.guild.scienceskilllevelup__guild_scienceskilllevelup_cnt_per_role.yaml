kind: metric
name: game.guild.scienceskilllevelup__guild_scienceskilllevelup_cnt_per_role
description: 人均公会科技升级次数（总次数/去重角色数，分母 NULLIF 防除零）
domains:
  - 社交公会
computation:
  sql: COUNT(*) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: count
    field: "*) / NULLIF(COUNT(DISTINCT role_id), 0"
    source: game.guild.scienceskilllevelup
    time_grain: ""
relations:
  - type: derived_from
    target: game.guild.scienceskilllevelup
    description: 机械提取自 game.guild.scienceskilllevelup 的 metrics 块（key=guild_scienceskilllevelup_cnt_per_role）
