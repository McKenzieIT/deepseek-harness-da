kind: metric
name: dws_10000251_univ_acc_tag_df__deep_churn_rate
description: 深度流失率(最近活跃间隔>=90天的账号占比,用于长期流失预警)
domains:
  - 用户生命周期
  - 付费经济
computation:
  sql: AVG(CASE WHEN act_ymd_diff>=90 THEN 1 ELSE 0 END)
  metadata:
    aggregation: avg
    field: CASE WHEN act_ymd_diff>=90 THEN 1 ELSE 0 END
    source: dws_10000251_univ_acc_tag_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_univ_acc_tag_df
    description: 机械提取自 dws_10000251_univ_acc_tag_df 的 metrics 块（key=deep_churn_rate）
