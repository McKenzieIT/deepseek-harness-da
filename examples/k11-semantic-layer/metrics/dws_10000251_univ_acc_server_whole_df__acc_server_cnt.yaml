kind: metric
name: dws_10000251_univ_acc_server_whole_df__acc_server_cnt
description: 账号-区服组合记录数；反映账号跨区服分布广度
domains:
  - 用户生命周期
  - 付费经济
computation:
  sql: COUNT(DISTINCT user_id || '_' || server_id)
  metadata:
    aggregation: count_distinct
    field: user_id || '_' || server_id
    source: dws_10000251_univ_acc_server_whole_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_univ_acc_server_whole_df
    description: 机械提取自 dws_10000251_univ_acc_server_whole_df 的 metrics 块（key=acc_server_cnt）
