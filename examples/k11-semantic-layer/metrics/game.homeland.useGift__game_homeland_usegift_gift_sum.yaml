kind: metric
name: game.homeland.useGift__game_homeland_usegift_gift_sum
description: 礼品消耗总数量(giftNum 求和)
domains:
  - 自定义
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.giftNum') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.giftNum') AS BIGINT)
    source: game.homeland.useGift
    time_grain: ""
relations:
  - type: derived_from
    target: game.homeland.useGift
    description: 机械提取自 game.homeland.useGift 的 metrics 块（key=game_homeland_usegift_gift_sum）
