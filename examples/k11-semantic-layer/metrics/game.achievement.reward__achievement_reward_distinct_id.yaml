kind: metric
name: game.achievement.reward__achievement_reward_distinct_id
description: 被领取的去重成就id数（成就完成广度）
domains:
  - 角色成长
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params,'$.id'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params,'$.id')
    source: game.achievement.reward
    time_grain: ""
relations:
  - type: derived_from
    target: game.achievement.reward
    description: 机械提取自 game.achievement.reward 的 metrics 块（key=achievement_reward_distinct_id）
