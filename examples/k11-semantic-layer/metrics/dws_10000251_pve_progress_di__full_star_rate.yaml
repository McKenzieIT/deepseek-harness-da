kind: metric
name: dws_10000251_pve_progress_di__full_star_rate
description: 当日满星通关率（full_star=1 记录占比，反映通关质量）
domains:
  - 战斗关卡
  - 角色成长
computation:
  sql: SUM(CASE WHEN full_star = 1 THEN 1 ELSE 0 END) / NULLIF(COUNT(*), 0)
  metadata:
    aggregation: sum
    field: CASE WHEN full_star = 1 THEN 1 ELSE 0 END) / NULLIF(COUNT(*), 0
    source: dws_10000251_pve_progress_di
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_pve_progress_di
    description: 机械提取自 dws_10000251_pve_progress_di 的 metrics 块（key=full_star_rate）
