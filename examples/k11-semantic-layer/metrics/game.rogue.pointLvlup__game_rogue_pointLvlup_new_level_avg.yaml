kind: metric
name: game.rogue.pointLvlup__game_rogue_pointLvlup_new_level_avg
description: 平均军功新等级（事件维度平均）
domains:
  - 战斗关卡
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.newLevel') AS BIGINT)) / NULLIF(COUNT(*), 0)
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.newLevel') AS BIGINT)) / NULLIF(COUNT(*), 0
    source: game.rogue.pointLvlup
    time_grain: ""
relations:
  - type: derived_from
    target: game.rogue.pointLvlup
    description: 机械提取自 game.rogue.pointLvlup 的 metrics 块（key=game_rogue_pointLvlup_new_level_avg）
