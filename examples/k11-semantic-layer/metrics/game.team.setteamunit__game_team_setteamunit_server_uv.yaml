kind: metric
name: game.team.setteamunit__game_team_setteamunit_server_uv
description: 涉及阵容设置的去重游戏服数
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT server_id)
  metadata:
    aggregation: count_distinct
    field: server_id
    source: game.team.setteamunit
    time_grain: ""
relations:
  - type: derived_from
    target: game.team.setteamunit
    description: 机械提取自 game.team.setteamunit 的 metrics 块（key=game_team_setteamunit_server_uv）
