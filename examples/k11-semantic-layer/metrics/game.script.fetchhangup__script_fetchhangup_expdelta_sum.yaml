kind: metric
name: game.script.fetchhangup__script_fetchhangup_expdelta_sum
description: 领取挂机奖励获得的资源(expDelta)累计值
domains:
  - 自定义
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params,'$.expDelta') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params,'$.expDelta') AS BIGINT)
    source: game.script.fetchhangup
    time_grain: ""
relations:
  - type: derived_from
    target: game.script.fetchhangup
    description: 机械提取自 game.script.fetchhangup 的 metrics 块（key=script_fetchhangup_expdelta_sum）
