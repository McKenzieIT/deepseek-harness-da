kind: metric
name: game.tower.hangup__tower_hangup_account_uv
description: 触发烽火台挂机奖励的去重账号数（account_id）
domains:
  - 战斗关卡
computation:
  sql: COUNT(DISTINCT CASE WHEN event = 'game.tower.hangup' THEN account_id END)
  metadata:
    aggregation: count_distinct
    field: CASE WHEN event = 'game.tower.hangup' THEN account_id END
    source: game.tower.hangup
    time_grain: ""
relations:
  - type: derived_from
    target: game.tower.hangup
    description: 机械提取自 game.tower.hangup 的 metrics 块（key=tower_hangup_account_uv）
