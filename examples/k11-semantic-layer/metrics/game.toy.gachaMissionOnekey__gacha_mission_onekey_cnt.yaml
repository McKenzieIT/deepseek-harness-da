kind: metric
name: game.toy.gachaMissionOnekey__gacha_mission_onekey_cnt
description: 抽卡任务一键领取事件总次数（所有角色所有服务器汇总）
domains:
  - 探索收集
computation:
  sql: COUNT(*)
  metadata:
    aggregation: count
    field: "*"
    source: game.toy.gachaMissionOnekey
    time_grain: ""
relations:
  - type: derived_from
    target: game.toy.gachaMissionOnekey
    description: 机械提取自 game.toy.gachaMissionOnekey 的 metrics 块（key=gacha_mission_onekey_cnt）
