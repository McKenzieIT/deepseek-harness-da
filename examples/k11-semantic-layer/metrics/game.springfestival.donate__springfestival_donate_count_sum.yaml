kind: metric
name: game.springfestival.donate__springfestival_donate_count_sum
description: 捐赠道具总数量（累加 params.count）
domains:
  - 自定义
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.count') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.count') AS BIGINT)
    source: game.springfestival.donate
    time_grain: ""
relations:
  - type: derived_from
    target: game.springfestival.donate
    description: 机械提取自 game.springfestival.donate 的 metrics 块（key=springfestival_donate_count_sum）
