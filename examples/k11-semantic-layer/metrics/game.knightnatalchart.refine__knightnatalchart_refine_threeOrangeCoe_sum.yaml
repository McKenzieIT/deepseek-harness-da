kind: metric
name: game.knightnatalchart.refine__knightnatalchart_refine_threeOrangeCoe_sum
description: 未触发三橙的累计次数之和（threeOrangeCoe 字段求和，反映改命过程中未命中三橙稀有度的总尝试量）
domains:
  - 角色成长
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.threeOrangeCoe') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.threeOrangeCoe') AS BIGINT)
    source: game.knightnatalchart.refine
    time_grain: ""
relations:
  - type: derived_from
    target: game.knightnatalchart.refine
    description: 机械提取自 game.knightnatalchart.refine 的 metrics 块（key=knightnatalchart_refine_threeOrangeCoe_sum）
