kind: metric
name: game.artifact.startup__game_artifact_startup_role_uv
description: 参与神器升星的角色数（去重）
domains:
  - 角色成长
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.artifact.startup
    time_grain: ""
relations:
  - type: derived_from
    target: game.artifact.startup
    description: 机械提取自 game.artifact.startup 的 metrics 块（key=game_artifact_startup_role_uv）
