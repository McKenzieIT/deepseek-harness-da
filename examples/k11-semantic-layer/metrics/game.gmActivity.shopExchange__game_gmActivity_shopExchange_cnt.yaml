kind: metric
name: game.gmActivity.shopExchange__game_gmActivity_shopExchange_cnt
description: 兑换事件总次数（含同一角色多次兑换），主体粒度role_id
domains:
  - 自定义
computation:
  sql: COUNT(*)
  metadata:
    aggregation: count
    field: "*"
    source: game.gmActivity.shopExchange
    time_grain: ""
relations:
  - type: derived_from
    target: game.gmActivity.shopExchange
    description: 机械提取自 game.gmActivity.shopExchange 的 metrics 块（key=game_gmActivity_shopExchange_cnt）
