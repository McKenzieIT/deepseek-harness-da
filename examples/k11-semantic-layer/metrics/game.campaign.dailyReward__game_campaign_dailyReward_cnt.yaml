kind: metric
name: game.campaign.dailyReward__game_campaign_dailyReward_cnt
description: 历战每日奖励触发总次数（领奖行为次数）
domains:
  - 自定义
computation:
  sql: COUNT(*)
  metadata:
    aggregation: count
    field: "*"
    source: game.campaign.dailyReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.campaign.dailyReward
    description: 机械提取自 game.campaign.dailyReward 的 metrics 块（key=game_campaign_dailyReward_cnt）
