kind: metric
name: game.battlepass.missionReward__battlepass_mission_reward_role_uv
description: 领取战令任务奖励的独立角色数(去重 role_id；主体粒度为角色)
domains:
  - 角色成长
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.battlepass.missionReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.battlepass.missionReward
    description: 机械提取自 game.battlepass.missionReward 的 metrics 块（key=battlepass_mission_reward_role_uv）
