kind: metric
name: game.knight.setSkin__knight_set_skin_distinct_knight
description: 设置皮肤涉及的武将去重数(按knightId)
domains:
  - 角色成长
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.knightId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.knightId')
    source: game.knight.setSkin
    time_grain: ""
relations:
  - type: derived_from
    target: game.knight.setSkin
    description: 机械提取自 game.knight.setSkin 的 metrics 块（key=knight_set_skin_distinct_knight）
