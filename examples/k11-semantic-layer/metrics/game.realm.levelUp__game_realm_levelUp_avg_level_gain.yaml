kind: metric
name: game.realm.levelUp__game_realm_levelUp_avg_level_gain
description: 平均每次境界升级等级提升数（currLevel-oldLevel）
domains:
  - 战斗关卡
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.currLevel') AS BIGINT) - CAST(GET_JSON_OBJECT(params, '$.oldLevel') AS BIGINT)) / NULLIF(COUNT(*), 0)
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.currLevel') AS BIGINT) - CAST(GET_JSON_OBJECT(params, '$.oldLevel') AS BIGINT)) / NULLIF(COUNT(*), 0
    source: game.realm.levelUp
    time_grain: ""
relations:
  - type: derived_from
    target: game.realm.levelUp
    description: 机械提取自 game.realm.levelUp 的 metrics 块（key=game_realm_levelUp_avg_level_gain）
