kind: metric
name: game.resort.share__game_resort_share_guild_uv
description: 分享至公会的角色去重数
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT CASE WHEN GET_JSON_OBJECT(params,'$.shareGuild') = 'true' THEN role_id END)
  metadata:
    aggregation: count_distinct
    field: CASE WHEN GET_JSON_OBJECT(params,'$.shareGuild') = 'true' THEN role_id END
    source: game.resort.share
    time_grain: ""
relations:
  - type: derived_from
    target: game.resort.share
    description: 机械提取自 game.resort.share 的 metrics 块（key=game_resort_share_guild_uv）
