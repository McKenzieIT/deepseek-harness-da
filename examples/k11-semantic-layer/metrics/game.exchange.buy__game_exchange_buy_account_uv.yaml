kind: metric
name: game.exchange.buy__game_exchange_buy_account_uv
description: 触发交易所购买的去重账号数。
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT account_id)
  metadata:
    aggregation: count_distinct
    field: account_id
    source: game.exchange.buy
    time_grain: ""
relations:
  - type: derived_from
    target: game.exchange.buy
    description: 机械提取自 game.exchange.buy 的 metrics 块（key=game_exchange_buy_account_uv）
