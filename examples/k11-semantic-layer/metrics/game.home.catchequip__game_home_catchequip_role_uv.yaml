kind: metric
name: game.home.catchequip__game_home_catchequip_role_uv
description: 触发捕捞装备的去重角色数
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.home.catchequip
    time_grain: ""
relations:
  - type: derived_from
    target: game.home.catchequip
    description: 机械提取自 game.home.catchequip 的 metrics 块（key=game_home_catchequip_role_uv）
