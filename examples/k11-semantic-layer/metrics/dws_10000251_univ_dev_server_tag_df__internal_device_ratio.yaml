kind: metric
name: dws_10000251_univ_dev_server_tag_df__internal_device_ratio
description: 内部设备占比,内部流量占活跃设备的比例,用于评估流量纯净度
domains:
  - 用户生命周期
  - 付费经济
  - 角色成长
  - 系统监控
computation:
  sql: AVG(CASE WHEN fellow_flag = 1 THEN 1 ELSE 0 END)
  metadata:
    aggregation: avg
    field: CASE WHEN fellow_flag = 1 THEN 1 ELSE 0 END
    source: dws_10000251_univ_dev_server_tag_df
    time_grain: ""
relations:
  - type: derived_from
    target: dws_10000251_univ_dev_server_tag_df
    description: 机械提取自 dws_10000251_univ_dev_server_tag_df 的 metrics 块（key=internal_device_ratio）
