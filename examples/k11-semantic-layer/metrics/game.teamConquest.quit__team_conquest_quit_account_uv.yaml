kind: metric
name: game.teamConquest.quit__team_conquest_quit_account_uv
description: 退出组队讨伐的去重账号数
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT account_id)
  metadata:
    aggregation: count_distinct
    field: account_id
    source: game.teamConquest.quit
    time_grain: ""
relations:
  - type: derived_from
    target: game.teamConquest.quit
    description: 机械提取自 game.teamConquest.quit 的 metrics 块（key=team_conquest_quit_account_uv）
