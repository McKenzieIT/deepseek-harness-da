kind: metric
name: game.social.guild.boss__game_social_guild_boss_id_uv
description: 涉及的BOSS种类数(去重 bossId；按 params_fields 推断)
domains:
  - 社交公会
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.bossId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.bossId')
    source: game.social.guild.boss
    time_grain: ""
relations:
  - type: derived_from
    target: game.social.guild.boss
    description: 机械提取自 game.social.guild.boss 的 metrics 块（key=game_social_guild_boss_id_uv）
