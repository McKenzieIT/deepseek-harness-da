kind: metric
name: game.team.setTeamDetail__team_setteamdetail_team_uv
description: 被编辑的去重队伍数（按 teamId 去重）
domains:
  - 自定义
computation:
  sql: COUNT(DISTINCT GET_JSON_OBJECT(params, '$.teamId'))
  metadata:
    aggregation: count_distinct
    field: GET_JSON_OBJECT(params, '$.teamId')
    source: game.team.setTeamDetail
    time_grain: ""
relations:
  - type: derived_from
    target: game.team.setTeamDetail
    description: 机械提取自 game.team.setTeamDetail 的 metrics 块（key=team_setteamdetail_team_uv）
