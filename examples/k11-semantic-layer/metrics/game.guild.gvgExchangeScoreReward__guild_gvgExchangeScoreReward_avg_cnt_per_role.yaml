kind: metric
name: game.guild.gvgExchangeScoreReward__guild_gvgExchangeScoreReward_avg_cnt_per_role
description: 人均兑换次数（事件次数 / 去重角色数，NULLIF 防除零）
domains:
  - 社交公会
computation:
  sql: COUNT(*) / NULLIF(COUNT(DISTINCT role_id), 0)
  metadata:
    aggregation: count
    field: "*) / NULLIF(COUNT(DISTINCT role_id), 0"
    source: game.guild.gvgExchangeScoreReward
    time_grain: ""
relations:
  - type: derived_from
    target: game.guild.gvgExchangeScoreReward
    description: 机械提取自 game.guild.gvgExchangeScoreReward 的 metrics 块（key=guild_gvgExchangeScoreReward_avg_cnt_per_role）
