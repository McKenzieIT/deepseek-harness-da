kind: metric
name: game.koiDraw.changeFloor__koi_draw_change_floor_role_uv
description: 触发切换层的去重角色数（分析主体粒度）
domains:
  - 探索收集
computation:
  sql: COUNT(DISTINCT role_id)
  metadata:
    aggregation: count_distinct
    field: role_id
    source: game.koiDraw.changeFloor
    time_grain: ""
relations:
  - type: derived_from
    target: game.koiDraw.changeFloor
    description: 机械提取自 game.koiDraw.changeFloor 的 metrics 块（key=koi_draw_change_floor_role_uv）
