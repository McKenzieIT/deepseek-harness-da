kind: metric
name: game.guild.donate__game_guild_donate_count_sum
description: 捐献次数 count 的累计值（捐献总量）
domains:
  - 社交公会
computation:
  sql: SUM(CAST(GET_JSON_OBJECT(params, '$.count') AS BIGINT))
  metadata:
    aggregation: sum
    field: CAST(GET_JSON_OBJECT(params, '$.count') AS BIGINT)
    source: game.guild.donate
    time_grain: ""
relations:
  - type: derived_from
    target: game.guild.donate
    description: 机械提取自 game.guild.donate 的 metrics 块（key=game_guild_donate_count_sum）
