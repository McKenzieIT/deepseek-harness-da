name: game.recycle
event_filter: event = 'game.recycle'
description: '资源回收事件：玩家通过回收/分解功能消耗装备与资源时上报，记录回收行为发生的服务器、角色及当时持有的货币与装备快照。分析主体为 role_id（角色级），事件按行为流水逐条记录。单日 ds=20260729 scout 实测 cnt=5180、role_uv=2057、acc_uv=1968、server_uv=273。params 共 28 个字段，含 resources（回收资源数组 type/value/size/uid）与 equips（装备 uid 数组）。'
domains:
- 资源产销
- 装备道具
domain: resource
metrics:
  game_recycle_cnt:
    expression: COUNT(*)
    description: 资源回收事件总条数（每次回收行为一条流水）
  game_recycle_role_uv:
    expression: COUNT(DISTINCT role_id)
    description: 触发资源回收的去重角色数（分析主体粒度 role_id）
  game_recycle_account_uv:
    expression: COUNT(DISTINCT account_id)
    description: 触发资源回收的去重账号数
  game_recycle_server_uv:
    expression: COUNT(DISTINCT server_id)
    description: 涉及资源回收的去重游戏服数
coverage:
  scenarios:
  - 通过 cnt/role_uv/account_uv 监控资源回收行为的日活跃规模与参与度
  - 通过 server_uv 与按服聚合定位高活跃回收服，支撑资源产销与装备道具域的消耗侧分析
  limitations:
  - 仅 ds=20260729 单日 scout 实测，未做全量多日复核；多日趋势需另行批量校验
  - 分析主体粒度默认 role_id（角色），未对 account_id 跨角色重复回收做去重校验
  - resources（回收资源数组）与 equips（装备 uid 数组）为 JSON 数组，单行抽样显示部分为空对象 {} 或 null，未对其 size/数量逐元素求和，如需按资源类型聚合需单独建模（避免脏数据放大）
params_fields:
  serverId:
    type: int
    description: 游戏服id
  serverName:
    type: string
    description: 游戏服名字
  accountId:
    type: string
    description: 账号id
  roleId:
    type: int
    description: 角色id
  roleName:
    type: string
    description: 角色名
  sex:
    type: int
    description: 性别
  createTime:
    type: int
    description: 创角时间
  onlineTime:
    type: int
    description: 上线时间
  roleLevel:
    type: int
    description: 角色等级
  vipLevel:
    type: int
    description: vip等级
  roleForce:
    type: int
    description: 角色战力
  exp:
    type: int
    description: 经验
  image:
    type: int
    description: 看板娘
  coinList:
    type: array
    description: 关键货币列表
  coinList.gold:
    type: int
    description: 充值钻石
  coinList.sendgold:
    type: int
    description: 充值赠送钻石
  coinList.freegold:
    type: int
    description: 免费钻石
  coinList.diamond:
    type: int
    description: 紫金
  coinList.expCoin:
    type: int
    description: 经验卷轴
  coinList.energy:
    type: int
    description: 体力
  sessionId:
    type: string
    description: 会话id
  logUUID:
    type: string
    description: logUUID（日志唯一id）
  equips:
    type: array
    description: 装备uid列表
  resources:
    type: array
    description: 回收的资源
  resources.type:
    type: int
    description: 类型
  resources.value:
    type: int
    description: 值
  resources.size:
    type: int
    description: 数量
  resources.uid:
    type: int
    description: 唯一id
confirmation:
  status: analyst_confirmed
  confirmed_by: scout-verification-2026-07-30
  confirmed_at: "2026-07-30"
  note: 'scout 实测 ds=20260729：cnt=5180, role_uv=2057, acc_uv=1968, server_uv=273；event+ds
    双过滤命中，数字量级合理，事件真实在线可查。resources 数组抽样部分为空，故未对回收数量求和。'
coverage_status: analyst_confirmed
