name: game.toy.gacha
event_filter: event = 'game.toy.gacha'
description: 遗物抽卡事件：角色在遗物卡池进行抽卡时上报，记录每次抽卡的卡池id、卡池类型、抽卡数量、消耗资源(cost)、实际给玩家添加的资源(rewards)、真实结果(gainRes)及抽卡任务(mission)进度。分析主体粒度为 role_id（角色），单次抽卡行为计为一条日志。ds=20260729 实测：cnt=18337，role_uv=2312，acc_uv=2229，server_uv=383；当日累计抽卡次数(count合计) 136102，涉及卡池去重 23 个、卡池类型 2 种。
domains:
  - 探索收集
domain: resource
params_fields:
  serverId:
    type: int
    description: 游戏服id
  serverName:
    type: string
    description: 游戏服名字
  accountId:
    type: string
    description: 账号id
  roleId:
    type: int
    description: 角色id
  roleName:
    type: string
    description: 角色名
  sex:
    type: int
    description: 性别
  createTime:
    type: int
    description: 创角时间
  onlineTime:
    type: int
    description: 上线时间
  roleLevel:
    type: int
    description: 角色等级
  vipLevel:
    type: int
    description: vip等级
  roleForce:
    type: int
    description: 角色战力
  exp:
    type: int
    description: 经验
  image:
    type: int
    description: 看板娘
  coinList:
    type: array
    description: 关键货币列表
  coinList.gold:
    type: int
    description: 充值钻石
  coinList.sendgold:
    type: int
    description: 充值赠送钻石
  coinList.freegold:
    type: int
    description: 免费钻石
  coinList.diamond:
    type: int
    description: 紫金
  coinList.expCoin:
    type: int
    description: 经验卷轴
  coinList.energy:
    type: int
    description: 体力
  sessionId:
    type: string
    description: 会话id
  logUUID:
    type: string
    description: logUUID（日志唯一id）
  gachaId:
    type: int
    description: 卡池id
  count:
    type: int
    description: 抽卡数量
  mission:
    type: json
    description: 抽卡任务
  mission.point:
    type: int
    description: mission.point
  mission.lastFetchPoint:
    type: int
    description: mission.lastFetchPoint
  mission.lastFetchId:
    type: int
    description: mission.lastFetchId
  type:
    type: int
    description: 卡池类型
  cost:
    type: array
    description: 抽卡消耗
  cost.type:
    type: int
    description: 类型
  cost.value:
    type: int
    description: 值
  cost.size:
    type: int
    description: 数量
  cost.uid:
    type: int
    description: 唯一id
  rewards:
    type: array
    description: 抽卡实际给玩家添加的资源
  rewards.type:
    type: int
    description: 类型
  rewards.value:
    type: int
    description: 值
  rewards.size:
    type: int
    description: 数量
  rewards.uid:
    type: int
    description: 唯一id
  gainRes:
    type: array
    description: 抽卡真实结果
  gainRes.type:
    type: int
    description: 类型
  gainRes.value:
    type: int
    description: 值
  gainRes.size:
    type: int
    description: 数量
  gainRes.uid:
    type: int
    description: 唯一id
metrics:
  game_toy_gacha_cnt:
    expression: COUNT(*)
    description: 抽卡日志条数（每次抽卡行为一条）
  game_toy_gacha_role_uv:
    expression: COUNT(DISTINCT role_id)
    description: 发起抽卡的角色去重数（分析主体粒度 role_id）
  game_toy_gacha_account_uv:
    expression: COUNT(DISTINCT account_id)
    description: 发起抽卡的账号去重数
  game_toy_gacha_server_uv:
    expression: COUNT(DISTINCT server_id)
    description: 涉及抽卡的游戏服去重数
  game_toy_gacha_pulls_sum:
    expression: SUM(CAST(GET_JSON_OBJECT(params,'$.count') AS BIGINT))
    description: 当期累计抽卡次数（count 求和）
  game_toy_gacha_pool_uv:
    expression: COUNT(DISTINCT GET_JSON_OBJECT(params,'$.gachaId'))
    description: 当期涉及卡池去重数（gachaId 去重）
  game_toy_gacha_type_uv:
    expression: COUNT(DISTINCT GET_JSON_OBJECT(params,'$.type'))
    description: 当期涉及卡池类型去重数（type 去重）
  game_toy_gacha_pulls_per_role:
    expression: SUM(CAST(GET_JSON_OBJECT(params,'$.count') AS BIGINT)) / NULLIF(COUNT(DISTINCT role_id),0)
    description: 人均抽卡次数 = 累计抽卡次数 / 角色去重数（NULLIF 防零除）
coverage:
  scenarios:
    - 通过 role_uv/account_uv 评估遗物抽卡玩法的角色/账号渗透规模
    - 通过 pulls_sum、pool_uv、type_uv 衡量抽卡产出与卡池分布，辅助卡池调优
    - 通过 pulls_per_role 评估人均抽卡深度，定位重度抽卡角色
    - 通过 server_uv 与按服维度聚合，定位高活跃抽卡服
  limitations:
    - 仅 ds=20260729 单日 scout 实测，未做全量多日复核；多日趋势需另行批量校验
    - 分析主体粒度默认 role_id（角色），未对 account_id 跨角色复抽做去重校验
    - cost（消耗资源）、rewards（添加资源）、gainRes（真实结果）为数组参数，未在本事件 metrics 内展开逐元素求和，如需按资源类型聚合需单独建模
    - mission（抽卡任务）为 json 参数，point/lastFetchPoint/lastFetchId 未在 metrics 内展开
confirmation:
  status: analyst_confirmed
  confirmed_by: scout-verification-2026-07-30
  confirmed_at: "2026-07-30"
  note: scout 实测 ds=20260729：cnt=18337, role_uv=2312, acc_uv=2229, server_uv=383; count合计=136102, gachaId去重=23, type去重=2, count非空行=18337。数值合理，event+ds 双过滤命中，确认在线可查。
coverage_status: analyst_confirmed
external_refs:
  - dim_table: dim_10000251_trans_gacha_mission_group_df_arch
    join_keys:
      - dws_column: type
        dim_column: type
    derivation: 确定性：事件字段 type 与 dim_10000251_trans_gacha_mission_group_df_arch 主键精确同名
  - dim_table: dim_10000251_trans_item_df
    join_keys:
      - dws_column: type
        dim_column: type
    derivation: 确定性：事件字段 type 与 dim_10000251_trans_item_df 主键精确同名
  - dim_table: dim_10000251_trans_item_df_arch
    join_keys:
      - dws_column: type
        dim_column: type
    derivation: 确定性：事件字段 type 与 dim_10000251_trans_item_df_arch 主键精确同名
  - dim_table: dim_10000251_trans_resource_df
    join_keys:
      - dws_column: type
        dim_column: type
    derivation: 确定性：事件字段 type 与 dim_10000251_trans_resource_df 主键精确同名
  - dim_table: dim_10000251_trans_resource_df_arch
    join_keys:
      - dws_column: type
        dim_column: type
    derivation: 确定性：事件字段 type 与 dim_10000251_trans_resource_df_arch 主键精确同名
  - dim_table: dim_10000251_trans_resource_item_df
    join_keys:
      - dws_column: type
        dim_column: type
    derivation: 确定性：事件字段 type 与 dim_10000251_trans_resource_item_df 主键精确同名
  - dim_table: dim_10000251_trans_resource_item_df_arch
    join_keys:
      - dws_column: type
        dim_column: type
    derivation: 确定性：事件字段 type 与 dim_10000251_trans_resource_item_df_arch 主键精确同名
