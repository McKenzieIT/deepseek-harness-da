name: game.showCamp.unlock
event_filter: event = 'game.showCamp.unlock'
description: 折扣服玩法：营地/阵营解锁事件。记录角色解锁营地（看板娘 slot）时刻，含角色等级、vip等级、战力、经验及关键货币余额快照（钻石/紫金/经验卷轴/体力）。分析主体为 role_id（角色级），ds=20260729 scout 实测 cnt=2342/role_uv=668/acc_uv=659/server_uv=189。
domains:
  - 自定义
domain: activity
metrics:
  showCamp_unlock_cnt:
    expression: COUNT(*)
    description: 营地解锁事件总次数
  showCamp_unlock_role_uv:
    expression: COUNT(DISTINCT role_id)
    description: 触发营地解锁的去重角色数
  showCamp_unlock_account_uv:
    expression: COUNT(DISTINCT account_id)
    description: 触发营地解锁的去重账号数
  showCamp_unlock_server_uv:
    expression: COUNT(DISTINCT server_id)
    description: 触发营地解锁的去重服务器数
coverage:
  scenarios:
    - 营地解锁参与度：每日解锁营地的角色/账号数与事件总量
    - 解锁时点角色画像：角色等级、vip等级、角色战力分布
    - 解锁时点货币余额：钻石/紫金/经验卷轴/体力快照
    - 服务器覆盖：解锁事件触发的去重服务器数
  limitations:
    - 仅 ds=20260729 单日 scout 实测，未做全量复核
    - 分析主体默认为 role_id（角色级），未单独核实 account_id 唯一性
    - coinList 为解锁时刻余额快照，非交易流水金额，不可作营收汇总
coverage_status: analyst_confirmed
params_fields:
  serverId:
    type: int
    description: 游戏服id
  serverName:
    type: string
    description: 游戏服名字
  accountId:
    type: string
    description: 账号id
  roleId:
    type: int
    description: 角色id
  roleName:
    type: string
    description: 角色名
  sex:
    type: int
    description: 性别
  createTime:
    type: int
    description: 创角时间
  onlineTime:
    type: int
    description: 上线时间
  roleLevel:
    type: int
    description: 角色等级
  vipLevel:
    type: int
    description: vip等级
  roleForce:
    type: int
    description: 角色战力
  exp:
    type: int
    description: 经验
  image:
    type: int
    description: 看板娘
  coinList:
    type: array
    description: 关键货币列表
  coinList.gold:
    type: int
    description: 充值钻石
  coinList.sendgold:
    type: int
    description: 充值赠送钻石
  coinList.freegold:
    type: int
    description: 免费钻石
  coinList.diamond:
    type: int
    description: 紫金
  coinList.expCoin:
    type: int
    description: 经验卷轴
  coinList.energy:
    type: int
    description: 体力
  sessionId:
    type: string
    description: 会话id
  logUUID:
    type: string
    description: logUUID（日志唯一id）
  type:
    type: string
    description: 类型
  slot:
    type: int
    description: 槽位
confirmation:
  status: analyst_confirmed
  confirmed_by: scout-verification-2026-07-30
  confirmed_at: "2026-07-30"
  note: "scout 实测 ds=20260729: cnt=2342, role_uv=668, acc_uv=659, server_uv=189；事件命中且量级合理"
external_refs:
  - dim_table: dim_10000251_trans_gacha_mission_group_df_arch
    join_keys:
      - dws_column: type
        dim_column: type
    derivation: 确定性：事件字段 type 与 dim_10000251_trans_gacha_mission_group_df_arch 主键精确同名
  - dim_table: dim_10000251_trans_item_df
    join_keys:
      - dws_column: type
        dim_column: type
    derivation: 确定性：事件字段 type 与 dim_10000251_trans_item_df 主键精确同名
  - dim_table: dim_10000251_trans_item_df_arch
    join_keys:
      - dws_column: type
        dim_column: type
    derivation: 确定性：事件字段 type 与 dim_10000251_trans_item_df_arch 主键精确同名
  - dim_table: dim_10000251_trans_resource_df
    join_keys:
      - dws_column: type
        dim_column: type
    derivation: 确定性：事件字段 type 与 dim_10000251_trans_resource_df 主键精确同名
  - dim_table: dim_10000251_trans_resource_df_arch
    join_keys:
      - dws_column: type
        dim_column: type
    derivation: 确定性：事件字段 type 与 dim_10000251_trans_resource_df_arch 主键精确同名
  - dim_table: dim_10000251_trans_resource_item_df
    join_keys:
      - dws_column: type
        dim_column: type
    derivation: 确定性：事件字段 type 与 dim_10000251_trans_resource_item_df 主键精确同名
  - dim_table: dim_10000251_trans_resource_item_df_arch
    join_keys:
      - dws_column: type
        dim_column: type
    derivation: 确定性：事件字段 type 与 dim_10000251_trans_resource_item_df_arch 主键精确同名
